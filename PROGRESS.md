# PROGRESS.md — the build's honest ledger (design revision v1.9)

*Repo root. Updated every working session — what shipped, what's red, what was deliberately deferred, and any decision proposals. This file is the month-one discipline instrument (MASTER §17.1): if it stops being truthful, the phase gates stop working.*

## Current state
- **Phase:** 2 — **COMPLETE and MERGED** (PR #1 `phase2-funnel-ledger` → `main`, 2026-07-18; checkpoints 2.1–2.12 done; **all 8 gate boxes green**; **539 tests**). **Docs at v1.9.34** (cohort maturation curve KPI, D88): findings through **216**, decisions through **D88**, proposals **P1–P13**. Phase 1 remains phase-complete: the live `--universe sp100 --years 20` backfill ran clean on 2026-07-15 (304 EODHD calls / 99.7% headroom; 101 members, 488,217 bars, GSPC proxy 5,029 bars over 20y) and the INTEGRATIONS §1 call-limits ⚠VERIFY is confirmed. **Next:** Phase 3 — the honest arena (metrics, MDE, the gate + allocator, the read-models incl. the additive `StrategyRow.seat` field (§23.6), and the Api command/read endpoints incl. FR-32's 409). `FR34_NoOverlappingWriters` is recorded PARTIAL (the launch-time guard ships in 2.12; the endpoint-level 409 lands with FR-32). Before Phase 3 opens: the operator takes the first off-machine backup (see below; M1+M2+M3 now **applied** — 2026-07-19) — **the finding-191/196 before-Phase-3 items are now CLEARED (191 → **D86**; 196 blessed, no D-number — v1.9.29; 190 & 195 were resolved in v1.9.25), so no Phase-2-review finding now blocks Phase 3's content.**
- **Operator migrations — DONE (2026-07-19):** M1 (`Phase2Ledger`, 8 ledger tables), M2 (`Phase2RegimeLabels`), and M3 (`RunsForwardUniqueIndex`, the `ux_runs_ok_forward` partial index) are **applied** to the live arena. Confirmed today: both `dotnet run --project tools/Backfill` and the `dotnet run --project src/AlphaLab.Worker` launch ran without the v1.9.17 finding-A pending-migration refusal — which only passes on a fully-migrated store, and since the backfill never migrates an *existing* store (rule 14), the three were applied beforehand via `pwsh tools/migrate.ps1 -Arena sp500`. `SchemaStartup` verifies the schema and never applies it.
- **CW benchmark proxy — INGESTED (2026-07-19):** the cap-weight ETF proxy is a wired backfill step (`BackfillCapWeightProxyStep`), and the live sp100 backfill has run it. **DB-confirmed:** `Benchmark.CapWeightProxySecurityId` = **103** (v1, append-only) → security `OEF`/`US` with **5,029 bars** (2006→2026-07-17) + **80 dividends**; the config→security JOIN resolves, so `ResolveUniverse(CapWeightProxy)` returns the proxy and the holds-cash branch is now unreachable for this arena. Forward operation is **not started yet** (`runs`/`accounts`/`strategies` empty), so the CW account holds nothing until the first pipeline day opens it — a Phase-3 step, deliberately deferred.
- **Blocking:** none. **Standing operator item — NOW DUE:** checkpoint 2.10 is the first committed Phase-2 write (`trades`/`decisions`/`equity_curve` — the lab's own output, which no provider returns). The Ops-log reasoning of 2026-07-15 (no off-machine backup because the store was re-fetchable) has **expired**. The per-launch LOCAL backup now ships (2.12 `LocalBackup`: `wal_checkpoint(TRUNCATE)` → dated copy under `{arena}\backups\` → prune > `Ops.BackupRetentionDays`), but it is a same-drive convenience snapshot — the **off-machine** leg is still a manual operator action, due at go-live once the live pipeline has written.
- **Phase-2 review open items (v1.9.23, findings 190–197):** **190 RESOLVED (D84, v1.9.25):** the funnel cash constraint — Stage 5 now sizes new opens against **available cash** (scale-to-fit), never total equity, so an account can no longer overspend into implicit margin and no held name is sold to fund an open (rule 7); `Sizing.Size` gains an `availableCash` ceiling and `FunnelRunner` passes cash for opens / equity for a whole-book rebalance (`D84_*` tests + the pipeline no-leverage guard) · **191 RESOLVED (D86, v1.9.29):** frozen/unpriced positions valued at cost basis — §13.6 (line 422) amended to cost-basis so the doc matches the code (`MarkToMarket` already conformed), closing the doc-vs-code conflict the `equity_curve` read-models would have inherited; terminal delisting stays last-print, untouched (**P9** ✔) · **192** `CorporateActionApplier` matches `AppliedOn == asOf` exactly, so an action effective on a non-session day never applies — **before the merger/delist feeds turn on (the widening)** · **193** `LastStoredDate` reads the full series per security per day (`GetSeries(id, "0001-01-01", …)` — the P1R-3 anti-pattern reintroduced; fix = a MAX(date) query) — **before the sp500 widening / any multi-day catch-up at scale** · **194** catch-up stamps recovered days with the session-derived `{as_of}T22:00:00Z` watermark, fabricating a PIT observation date (**P10**) — **before Phase 4** (replay reasons over `observed_at`) · **195 RESOLVED (v1.9.25):** basis math no longer routes ledger money through a `double` ratio — the sell-leg reduction is decimal end to end via `BasisMath.ReduceForSale` (D69), landed with 190's funnel work (`BasisMathTests`) · **196 RESOLVED (blessed, no D-number, v1.9.29):** rejected/clipped T+1 fills are dropped and re-opened by the strategy's next decision — shipped behaviour blessed and documented (§13.6 fills note); a bounded re-plan is rejected as a second decision authority (cf. D84) (**P11** ✔) · **197** forward membership refresh not wired — the pipeline reads the stored roster; nothing re-fetches OEF/Wikipedia, so the roster is frozen at the 2026-07-15 backfill and the FR-6 divergence alarm has no producer; deadline **upgraded** from finding EE's widening deferral to **before go-live of daily forward operation**, and the Data-health freshness cell must show membership's true fetch date, not the run date.
- **Last session:** 2026-07-20 — **Phase 3 checkpoint 3.1: honest-arena foundation (config options + EF tables + migration)** (**551 → 558 tests**; full solution green). Added the five Phase-3 option classes (`GateOptions`, `PopulationsOptions` (+ nested `FamilySeeds`), `AllocatorOptions`, `VerdictsOptions`, `KpiOptions`) in AlphaLab.Core/Config, defaults mirroring CONFIG_REFERENCE (guarded by `Phase3OptionsDefaultsTests`). Per finding F ("the consuming phase owns the bind"), the options are **not** DI-registered yet — the checkpoints that consume them (3.3+ Worker, 3.13 Api) will bind them; their C# defaults equal the CONFIG values until then. Added `EvaluationEntities.cs` (9 entities: `ControlPopulationRow`, `ControlEquityRow`, `TrialsRegistryRow`, `PowerReportRow`, `GoLiveLogRow`, `AllocationLogRow`, `OverfittingCheckRow`, `OverfittingStatusRow`, `JournalEntryRow`) + DbSets + `OnModelCreating` maps (decimal→TEXT on `control_equity.equity` (D69); composite PKs on `control_equity`/`overfitting_status`; covering index `ix_overfitting_checks_path`; CHECKs only on `overfitting_status.status` and `journal_entries.(kind,outcome)` per SCHEMA). New migration `Phase3HonestArena` — EF-generated then **hand-edited (rule 14)** to strip `Sqlite:Autoincrement` from the seven bare-INTEGER PKs (population_id, trial_id, report_id, event_id ×2, check_id, entry_id); the model snapshot keeps `ValueGeneratedOnAdd` (no `PendingModelChangesWarning`). `SchemaFidelityTests` updated (34-table set; +7 no-autoincrement tables; new Phase-3 CHECK + covering-index tests) and `SchemaStartupTests` bumped 25→34. **SCHEMA_v1.9.md** `overfitting_checks.signal` comment extended to `-- S1..S8; turnover_match (descriptive, excluded from status)` (D-B; the fidelity DDL check reads `sqlite_master`, which carries no comments, so this doc edit has zero fidelity impact). **Decisions D-A (equal sizing this phase, covariance seam only), D-B (turnover persisted as an `overfitting_checks` signal='turnover_match' row), D-C (only the honest-arena tables + `journal_entries` migrated now).** RF placeholder (user-confirmed): a named `Metrics.RiskFreePlaceholderAnnual` constant lands in 3.2 (no invented CONFIG key; surfaced via an `rf_placeholder` reason tag). **journal_entries** moved into Phase 3 (FR-28 needs it) — the old `SchemaFidelityTests` "Phase 7" note corrected. Not committed (awaiting git-workflow direction).
- **Prior session:** 2026-07-19 — **feat: cap-weight benchmark proxy ingestion** (STRATEGY_CATALOG §5.1 / D26/D27; **539 → 551 tests**; `ci.ps1` green; no migration, no new config key). Built the missing writer for the CW benchmark's ETF proxy. New `CapWeightProxyIngestion` (AlphaLab.Data) resolves the proxy's identity by current-symbol (asOf-independent, so a re-run with an earlier asOf never mints a duplicate) and writes the append-only `Benchmark.CapWeightProxySecurityId` config row on the `RegimeProxyIngestion` pattern; a new `BackfillCapWeightProxyStep` in `BackfillRunner` (wired into `RunAsync` after the regime-proxy step) reuses the existing `BackfillSecurityStep` for bars + dividends + splits — the proxy is a real ETF, so it needs the member path, not the bars-only regime path. Layering: AlphaLab.Data cannot reference `AlphaLab.Strategies.CapWeightProxy` (where the symbol map + key live), so the Backfill CLI composition root bridges it — `CapWeightProxyTarget.FromEodhdSymbol(CapWeightProxy.SymbolFor(Universe.Bootstrap.MembershipPrimary), …)` splits the fully-qualified `OEF.US` into the **bare** ticker + exchange the member fetch path expects (else the URL would be `OEF.US.US`) and passes it in as strings (tools/ is outside the D57 reference-graph guard). Null target ⇒ a logged no-op, so existing `RunAsync` tests stay green. Forward ingestion already works: once the row exists, the proxy joins the daily Stage-1 fetch set as a regular tradeable security. Committed `1c9ea1b` + pushed to `main`; the live sp100 backfill then ran and wrote the row — **DB-confirmed** (config `Benchmark.CapWeightProxySecurityId`=103 → OEF/US, 5,029 bars, 80 divs; the run also confirmed M1+M2+M3 are applied). Forward operation not yet started (runs/accounts/strategies empty), so no CW position exists to observe yet — starting it deferred by request. Follow-up **docs(v1.9.32)** (`9692d5a`, finding 214): recorded the ingestion as a named Phase-1 build step in `BUILD_AND_PROMPTS` — prompt item (i) parallel to the regime proxy's (h), plus the Phase-1 DoD row — closing the spec-vs-code asymmetry that let the write path go unbuilt through Phase 2; docs-only, tests stay 551, `ci.ps1` green.
- **Prior session:** 2026-07-18 — **fix(v1.9.25): funnel cash constraint (D84) + basis math** (findings 190 & 195 resolved; **528 → 539 tests**; `ci.ps1` green). Stage 5 now sizes new opens against **available cash** (scale-to-fit, D84) instead of total equity: `Sizing.Size` gains an `availableCash` ceiling and scales opens proportionally to fit (near-zero cash ⇒ opens nothing), `SizingResult` carries `SpendableCash` for an honest `UninvestedCash`, `FunnelRunner` passes cash for opens / equity for a whole-book rebalance (which self-funds), and `DailyPipeline` threads the account's real cash. No held position is ever sold to fund an open (rule 7); the residual overnight-gap/cost overshoot is bounded and self-correcting (finding 196). Finding 195: the sell-leg basis reduction moves to decimal `BasisMath` (D69). Docs: MASTER §2 (D84) + §6 Stage-5 + §0 provenance; STRATEGY_CATALOG §9; CHANGELOG v1.9.25. Proposal **P12** opened (the cash-shortfall allocation policy). Branch `fix/finding-190-cash-constraint`.
- **Prior session:** 2026-07-18 — **docs(v1.9.24): reconciliation pass** (docs only; tests stay **528**; `ci.ps1` green; no new decision). A staleness sweep completing finding 188: v1.9.23 removed the retired sentiment-score / blend-A/B framing from MASTER §7/§9, but the identical pre-D79–D82 framing survived in five further MASTER spots — Golden Rule 28, §23.1, the D46 row (+ rationale cell), §0 item 12, and the §3 mental-model diagram (the fused "Claude read" node) — all rewritten to the contestant / no-LLM-twin framing (rule 32); the stale "(pre-implementation)" title qualifier dropped (line 5 already said "live"); D73's already-resolved `⚠VERIFY` index-EOD marker swept (finding 165 had fixed only BUILD); cosmetic — D49 added to the §2 provenance list, §10 attribution gains the D83 dual-role pointer; and the same retired-A/B construct fixed in DESIGN_IMPROVEMENTS §4.1 (finding 206 — finding 188's DI §6 sweep had missed §4.1). Findings **200–206**; MANIFEST + MASTER banner → v1.9.24.
- **Earlier session:** 2026-07-18 — **docs(v1.9.23): reconciliation pass** (docs only; tests stay **528**; `ci.ps1` green). The retroactive v1.9.22 CHANGELOG section (findings 174–177 — four commits had shipped catalog §6.5–6.7, UX-14 + the replay recolour, the TEST_PLAN §6 AI-seat fixtures, and the DI §4 three-seat rewrite without rows); BUILD reconciled to MASTER §23 (finding 178: FR-23 → the D82 resolution; the retired sentiment-score and blend-A/B framings removed from Phase 5/6; §23.6 items phased into Phases 3/5/6; the two finding-173 KPIs phased — allocator value-add → Phase 4 DoD, researcher yield → Phase 5); the three new strategies propagated (179: BUILD Phase 6, MASTER §9 roster, DI §3.1 LW consumers); **D83** (180: factor returns graduate to §6.5's availability-lagged signal input, amending D41, with the deterministic MKT-fallback lag-hole rule); catalog completions (181–186: TSMOM `skip = 21`, `SignFlip`, `AllPositive`, the seeded tie-break, `RandomPop-Event`, the §6.7 covariance seam + shrinkage-aware tolerance); the §12 trials arithmetic (187); housekeeping (188: MANIFEST/CLAUDE.md ranges → D1–D83, UX-13/14 italics, stragglers); self-improvement clarifications (189: days-to-kill derivation; recipe_version ∈ frozen config_json) + proposal **P8**; the eight Phase-2-review code findings recorded (190–197) + proposals **P9/P10/P11**; the LocalBackup busy-checkpoint test-flake fix recorded (198, commit `61bcd38`); OVERFITTING_MONITOR AI-seat handling §3½ (199). The SP1500 widening-target proposal gains the Grinold-overclaim and compute-scale caveats.
- **Earlier still:** 2026-07-18 — **PR #1 review fixes (v1.9.20 findings LL–NN)** + merge to `main`: **LL** — `Benchmark.CapWeightProxySecurityId` documented in CONFIG_REFERENCE. **MM** — `LocalBackup` fails CLOSED on a busy checkpoint (rule 10; bounded retry then abort, no file). **NN** — the Stage-2 heartbeat stamp's comments rewritten honest. 527 → **528** tests; `ci.ps1` green. Earlier that day the v1.9.22 doc commits landed (recorded retroactively as findings 174–177).
- **Earlier still:** 2026-07-17 — **Phase 2 checkpoint 2.12 (the last — Phase 2 COMPLETE)**: the **D72 launch order + backup**. `OnDemandRunner` now drives `StaleRunRecovery → CatchupRunner → JobDrainer → LocalBackup → exit`. **StaleRunRecovery** clears an orphaned `run_in_progress` from a prior crash (marks its run 'failed', clears the flag) so the crashed day becomes an ordinary missed session — and a FRESH heartbeat instead fails the launch closed (`OverlappingWriterException`, D59 sole writer) = the PARTIAL `FR34_NoOverlappingWriters` guard. **HeartbeatService** (own scope + connection) beats every 30s while a run is in progress; the pipeline also stamps the heartbeat at Stage-2 start + warns on a slow txn. **JobDrainer** (behind `Worker.DrainQueuedJobsOnLaunch`) drains queued jobs OUTSIDE any write txn via a kind-keyed `IJobExecutor` registry — Phase 2 registers NONE, so a queued job fails closed with a named reason, never stuck 'queued'. **LocalBackup** checkpoints + copies to a dated file (pure `DbPathResolver` backup-path helpers) + prunes by retention. **`IWorkerLiveness`** (AlphaLab.Data) is the pure two-fact 409 decision (`IsLive` vs `IsStale`), reached by Api→Data in Phase 3. API limits: `ResilientHttpClient` honours `X-RateLimit-Remaining` (reactive pause) + `EodhdEndpointCost` weighting through the `Count`/`Record` seam (`eod-bulk-last-day` declared, not wired). **FX-CrashedRun + FX-JobDrain + FR34_LabRunsWithoutApi green.** No migration. 489 → **527** tests; `ci.ps1` green. Findings **GG–KK** (v1.9.19).

## Phase gates (a phase is DONE only when every box is checked and committed)

### Phase 0 — Skeleton
- [x] Solution layout per CLAUDE.md incl. AlphaLab.Api (D57); CI green (build + tests + greps + the **full reference-graph guard** over every src project at the `<ProjectReference>` level, plus the AlphaLab.Web source-level `using`-isolation grep — P0-3)
- [x] AlphaLab.Worker: OnDemand launch catches up (nothing yet) and exits cleanly; --serve idles (sole writer, D59/D61)
- [x] AlphaLab.Api boots and serves OpenAPI (/openapi/v1.json) + Scalar UI (/scalar/v1), localhost; error-envelope middleware; dev CORS for the AlphaLab.Web origin (Api:CorsAllowedOrigins); stub read endpoints return empty read-models with ReadModelStamp status=no_run_yet (D66)
- [x] ConnectionStrings resolved via the shared DbPathResolver ({Arena.Id} token from the Arena config block, FR-37/D71, + {LocalAppData} token via Environment.GetFolderPath still supported — no env-var reads, D67 — + directory create); Worker, Api, and the EF design-time factory all open the SAME arena-namespaced file (this deployment: `E:\AlphaLabDatabase\sp500\alphalab.db`); bare `dotnet ef` defaults to sp500
- [x] Arena identity wired (FR-37/D71): Arena.Id=sp500 in config; logs tagged arena=sp500; AlphaLab.Web loads the one-entry Arenas registry and targets its baseUrl; FR37 tests green
- [x] EF infra-only InitialCreate (runs/catchup_log/config/worker_state/jobs — D59/D60; worker_state row seeded) + snapshot script working
- [x] Empty-DB Blazor client renders every **non-parameterized §21 screen (Home + 13)** by calling AlphaLab.Api (NFR-3, P0-1); the two parameterized screens (`/strategies/{id}`, `/why-trade/{strategyId}/{date}`) are deliberately excluded from the flat catalog (drilled into from a real row) and noted in a `ScreenCatalog` comment
- [x] appsettings.Secrets.json gitignored & untracked; no key patterns in committed files (D67)
- [x] v1.9.7 fix-up (finding 118): `SchemaStartup` executes `PRAGMA journal_mode=WAL` post-migrate, verifies `wal`, fails startup otherwise; `R1_SchemaStartup_EnablesWal` green
- [x] v1.9.7 fix-up (finding 108): `config` PK is composite `(key, version)` in `InitialCreate` (built fresh, no hand-edit — `Version` is `ValueGeneratedNever()`); two versions of one key insertable, duplicate `(key,version)` rejected by the store (`SchemaFidelityTests`)
- [x] v1.9.7 fix-up (finding 119): `tools/migrate.ps1` resolves the DB path from the Worker appsettings and passes `--connection` — snapshots and migrates the same file (verified: snapshot written, update idempotent, exit 0)
- [x] v1.9.7 fix-up (finding 121): index.html title `AlphaLab`; layout tutorial link removed (header renders arena DisplayName); ScreenCatalog `EmptyHint` per screen + day-one home note (UX-8c); Api comments say "reader plus bounded Phase-3 command writes (D59)"; `.gitignore` DB comment present
- [x] v1.9.8 fix-up (P0-1): `ScreenCatalog` carries all 13 non-parameterized §21 screens (was 8) — the empty-DB client renders every one; the 2 parameterized screens excluded by design (comment)
- [x] v1.9.8 fix-up (P0-2): `AlphaLabDbContextFactory` comment matches the `E:`-literal three-spots reality (no longer invites the `%LOCALAPPDATA%` "fix" that reddens `ConfigConsistencyTests`)
- [x] v1.9.8 fix-up (P0-3): `ci.ps1` enforces the **full** reference graph at the `<ProjectReference>` level (`Assert-ReferenceGraph`, not just Web) and its `git ls-files` call is EAP-safe (git-absent falls back to a working-tree scan, never throws)
- [x] v1.9.8 fix-up (P0-4): the two resolver tests assert two-arena path distinctness (`sp500` vs `sp100`, `Assert.NotEqual`); canonical **39** count unchanged; TEST_PLAN §8 erratum records the substituted bare-factory case
- [x] v1.9.8 fix-up (P0-6): `ArenaRegistry.IsFallback` + a visible config-error banner when the `Arenas` registry is missing (fail-closed, hard rule 10) instead of a silent self-call; `FromEntries_Empty` asserts the flag

### Phase 1 — Data foundation
- [x] Security master + ticker_history; FX-TickerChange green (checkpoint 1.1)
- [x] EODHD bars backfill (S&P 100) + daily delta; versioned bars; FX-BarCorrection green (versioned bars + FX-BarCorrection = checkpoint 1.2; the backfill CLI that runs it = checkpoint 1.10; the live S&P 100 data run is the operator's, decision #1)
- [x] Membership per D49 launch wiring (IVV primary + Wikipedia cross-check; EODHD provider built but dormant); FX-MembershipDiverge/Agree green (checkpoint 1.4; + C-4 header guard)
- [x] D70 S&P 100 slice sourced (OEF CSV + Wikipedia S&P 100 cross-check, count sanity 99–103); fja05680 community CSV ingested into historical membership; FX-AsOfMembership green (checkpoint 1.5 — OEF slice + historical ingestion + a data-layer as-of read tested; the replay-engine FX-AsOfMembership validation is Phase 4 per D70)
- [x] Sector ingestion + change log; quality gate + FX-QualityGate green (checkpoint 1.6 sector half + FX-SectorReclass; checkpoint 1.7 data-quality gate + FX-QualityGate)
- [x] Trading calendar seeded + ICalendarService (FR-30); FX-HolidayOutage, FX-HalfDay green (checkpoint 1.8; live ±30y seed run via the CLI, 1.10)
- [x] INTEGRATIONS_v1.9 ⚠VERIFY items confirmed and file updated — Phase-1 items done: EODHD/BlackRock/French/fja05680/GSPC shapes VERIFIED 2026-07-13; **call limits VERIFIED 2026-07-15** at the first live backfill (100k/day, observed 304-call spend, per-endpoint cost table, 1,000/min `X-RateLimit` limit — §1/§9.5). The §5 Anthropic batch-headers (Phase 5) and the §1 fundamentals as-reported/restated (Phase 8) ⚠VERIFY items stay flagged **by design — not Phase-1**.
- [x] Regime proxy feed (FR-38/D73, v1.9.7): `GSPC.INDX` proxy machinery + SPY.US returns cross-check; `Regime.ProxySecurityId` resolved from `Regime.ProxySource` (versioned config row); `FX-RegimeProxyBackfill` green (readiness fails closed pre-warm-up) — checkpoint 1.9; the live ≥3.8y backfill run is via the CLI (1.10)

### Phase 2 — Funnel, ledger, costs, catch-up
*Executed as checkpoints 2.1–2.12 (the 0.1–0.6 / 1.0–1.10 pattern). Done: **2.1** (Core domain contracts), **2.2** (M1 ledger schema + persistence + the finding-A fail-fast), **2.3** (D43 cost model + VirtualBroker; FX-CostModel green), **2.4** (`BarFeatureView` + Stages 1/3/5; F-LEAK + FX-ZeroScore green), **2.5** (ExitPolicy executor + Stages 4/6 + FunnelRunner; FX-ExitOnly + F-DET green), **2.6** (CA ledger part 1: dividend/split/ticker/freeze; FX-Dividend + FX-Split + FX-Unmapped green), **2.7** (CA ledger part 2: mergers/spin-off/delist + D74 proof + the processed_on amendments; FX-MergerCash/Stock/Mixed + FX-Spinoff + FX-Delist green), **2.8** (M2 regime-label service: pure `RegimeLabeler` + `RegimeLabelService` + episodes + the finding-F Regime bind; FX-RegimeHysteresis + F-LEAK green), **2.9** (the dummy roster: `BuyAndHoldModel` CW+EW + `ThresholdModel` + `CapWeightProxy` + `DummyRoster`; §5.1 total-return acceptance green), **2.10** (M3 `ux_runs_ok_forward` + the D53 staged pipeline `DailyPipeline` + zero-write `Stage1Fetch` + D77 gate→store wiring + the Eodhd/Data/Calendar/CorporateActions binds; FX-StagedPipeline green), **2.11** (D47 catch-up: the real `MissedSessionResolver` + `CatchupRunner` driving `RunDayAsync` per missed day, resumable + idempotent; the ET last-completed-session guard; FX-Outage5d + the FR-34 OnDemand tests green), **2.12** (the D72 launch order: `StaleRunRecovery → CatchupRunner → JobDrainer → LocalBackup → exit` in `OnDemandRunner`; `HeartbeatService` + `IWorkerLiveness` (the 409 decision); the API `X-RateLimit` throttle + `EodhdEndpointCost` weighting; FX-CrashedRun + FX-JobDrain + FR34_LabRunsWithoutApi green — **Phase 2 COMPLETE**).*
- [x] Six-stage funnel + ExitPolicy executor; FX-ZeroScore, FX-ExitOnly green *(2.4 Stages 1/3/5 + FX-ZeroScore; 2.5 Stages 4/6 + ExitPolicy executor + FunnelRunner + FX-ExitOnly + F-DET. All six stages present and pure in Core. **Stage-1's `liquid` gate filters nothing — proposal P1, by design, not a gap.** Stage 2's scorers are the dummies (2.9) / real strategies (Phase 6); the runner calls `IModel.ScoreUniverseAsync` and no production model exists yet, which is expected. The forced-event fill path is 2.6/2.7; persisting a snapshot + filling against the live store is 2.10.)*
- [x] D43 cost model; FX-CostModel green *(2.3 — `CostModel`/`VirtualBroker` pure in Core. The Worker wires `Costs:*` and persists `capacity_rejections` at 2.10; the arithmetic, the cap, the stamp, and the fail-closed refusals are done and tested.)*
- [x] Corporate-action semantics complete; FX-Dividend/Split/Merger*/Spinoff/Delist/Unmapped green *(2.6 part 1 + 2.7 part 2. All §13.6 kinds: dividend, split, ticker change (a D39 non-event), cash/stock/mixed **merger**, **spin-off** receipt with basis allocation, **delist** force-exit + configurable bankruptcy haircut, and unmapped→freeze. Pure `CorporateActionLedger`/`SpinoffAllocation` (Core) + `CorporateActionApplier` (Data) reading at the watermark (D76). D74 (index-drop ≠ delist) proven. The merger/spin-off/delist FEEDS are dormant (D49) — semantics + fixtures ship, production is freeze+alert until the feeds turn on.)*
- [x] B&H CW+EW + ThresholdModel dummies live; B&H total-return acceptance green *(2.9 — first code in `AlphaLab.Strategies`. `BuyAndHoldModel` (CW `ExitPolicy.Never` single proxy; EW `ScheduledRebalance` self-built whole-universe, D68) + `ThresholdModel` (above-SMA trend filter, `unregistered`) — all pure `IModel`, scoring keyed by security_id, deterministic. `CapWeightProxy` resolves the CW ETF config-driven from `MembershipPrimary` (finding I; expense-ratio bias disclosed in §5.1). `DummyRoster` (Data) registers the 3 strategies + opens 3 live accounts idempotently; `Accounts.StartingCash` = $100k as a versioned config row (finding K). **Acceptance:** a pure-engine harness (funnel → `OrderFill`/`VirtualBroker` → §13.6 dividend → mark-to-market, exactly as the 2.10 pipeline will compose it) proves total return = proxy total return − one entry cost EXACTLY (no tolerance) over a span with a dividend, and B&H enters once / never churns. **Deferred to 2.10:** running these models through the hosted D53 pipeline against the live store, and the per-account benchmark run-settings (CW `Sizing.PositionCapPct=1.0`; EW `Guardrails.MaxConcurrentPositions ≥ universe`).)*
- [x] Staged pipeline hosted in AlphaLab.Worker (D53/D59/FR-29/FR-34): Stage-1 failure writes nothing; FX-StagedPipeline green *(2.10 — **the first committed write to the live store.** `DailyPipeline.RunDayAsync` runs the four-step run-row lifecycle RUNBOOK §1 confirms: Stage-1 fetch+gate (ZERO writes; `Stage1Fetch` takes no `DbContext`, so it CANNOT write — a type-level fact) → a small committed txn opens `runs(status='running')` + `run_in_progress=1` → ONE Stage-2 transaction (bars → actions → regime → per-account CA-apply/T+1-fills/funnel-decide/equity → quality flags under the run_id → `status='ok'` → `catchup_log`) → a small txn clears `run_in_progress`. A Stage-1 reject aborts before any row (FR-29); a Stage-2 crash rolls the day back to 'running' + `run_in_progress=1` (FX-CrashedRun state) for D72 recovery. D77 gate warnings now land under the run_id (finding CC); the per-account run-settings (CW `PositionCapPct=1.0`, EW `MaxConcurrentPositions ≥ universe`) are supplied by `Phase2StrategyRegistry`. **`ux_runs_ok_forward` (M3) is the idempotency backstop** — a second 'ok' forward run of a day is rejected. **Deferred (stated seams):** the OnDemand loop that drives `RunDayAsync` per missed day is 2.11; the one-time gate sweep of the never-gated backfill is **P7**; the daily membership REFRESH (the funnel reads the stored roster) rides with the D70-widening wiring. `no-overlapping-writers` + `runs-without-API` are 2.12 (D72).)*
- [x] Regime-label service (D50/FR-26); FX-RegimeHysteresis green *(2.8 — M2 `regime_labels`/`regime_episodes`. Pure `RegimeLabeler` (Core): trend (adj_close vs 200d SMA, 5-day-confirmed 1% hysteresis) × vol (21d realized vol vs the 80th pct of its trailing-3y distribution, same confirmation), reusing `PriceStatistics` (finding N). `RegimeLabelService` (Data): proxy id from the versioned config row (MAX(version)), watermarked series read (leak-proof), `inputs_hash` provenance, forward-only D45 episode chain. Fails closed below the ≈3.8y warm-up and on a missing asOf bar. **F-LEAK** (label at asOf invariant to future bars + later corrections; watermark in the hash) + **FX-RegimeHysteresis** (±0.5% oscillation → zero flips; sustained breakout → one confirmed flip) green. Finding-F **Regime bind** closed. Pipeline wiring (Stage-2, after membership) is 2.10; regime **replay quarantine** is proposal **P6**.)*
- [x] Catch-up protocol; FX-Outage5d green *(2.11 — the D47 catch-up. `MissedSessionResolver` (Worker) is a pure query over runs + bars + calendar + clock: the missed set = every session in `[floor, lastCompletedSession]` lacking a `status='ok'` forward run, ascending. **Floor** = the session after the last 'ok' forward run's as_of, falling back on a fresh store to the latest **bar date** (the backfill watermark) so forward operation begins where the data ends — never 20 years back. **Last completed session** = the latest whose **ET close has passed** (`now_ET > CloseTime`, NOT close+offset — the offset is Scheduled-mode-only; injected `TimeProvider` + a cross-platform ET `TimeZoneInfo`, DST-correct). `CatchupRunner` replays the set oldest-first, ONE transaction per day, a fresh DbContext per day; **resumable** (a failed day rolls back its bars + leaves no 'ok' row, so the anchor doesn't advance — the next launch resumes there) and **idempotent** (session-derived watermark; a re-run is a no-op). A fail-closed day STOPS the loop (the committed prefix persists) rather than building later days on a bad one. run_kind: same-ET-day = 'live', recovered = 'catchup' (+ `catchup_log`). No LLM for past days (structural — no `IAnalysisProvider` until Phase 5). `OnDemandRunner` now drives `CatchupRunner` then exits. **FX-Outage5d** (5-session outage recovered) + the FR-34 OnDemand tests (replay-in-order, same-evening no-op, no-run-before-close, fail-closed stop) green. **Deferred to 2.12:** the D72 launch ORDER around catch-up — stale-run recovery before it, job drain + local backup after it.)*
- [x] D72 process model (v1.9.7): OnDemand drains queued jobs after catch-up (`FX-JobDrain` green); heartbeat + stale-run recovery (`FX-CrashedRun` green); `ux_runs_ok_forward` partial index created (finding 109) *(2.12 — the launch ORDER `StaleRunRecovery → CatchupRunner → JobDrainer → LocalBackup → exit` in `OnDemandRunner`. **StaleRunRecovery** clears an orphaned `run_in_progress` (marks its run 'failed') so a crashed day becomes an ordinary missed session — FX-CrashedRun green; a FRESH heartbeat fails closed (`OverlappingWriterException`, D59) = the PARTIAL **`FR34_NoOverlappingWriters`** guard (the endpoint 409 lands with FR-32). **HeartbeatService** (own scope/connection) + the pipeline's Stage-2 heartbeat stamp + a slow-txn canary. **JobDrainer** (kind-keyed registry; Phase 2 registers NONE, so a queued job fails closed with a named reason, never stuck 'queued'; the "no open txn" invariant asserted twice) — FX-JobDrain green. **LocalBackup** (`wal_checkpoint(TRUNCATE)` → dated copy under `{arena}\backups\` via the pure `DbPathResolver` helper → same-day skip → prune > `Ops.BackupRetentionDays`; a BUSY checkpoint fails CLOSED after bounded retries — no file written, v1.9.20 finding MM). **`IWorkerLiveness`** (the pure two-fact 409 decision in AlphaLab.Data, reached by Api→Data in Phase 3). API limits: `ResilientHttpClient` honours `X-RateLimit-Remaining` (reactive throttle) + `EodhdEndpointCost` weighting through the `Count`/`Record` seam (`eod-bulk-last-day` declared but NOT wired). The **`ux_runs_ok_forward` index landed in 2.10**; the catch-up loop it wraps in 2.11. FX-CrashedRun + FX-JobDrain + FR34_LabRunsWithoutApi green.)*

### Phase 3 — The honest arena
- [x] Populations (3 families + cost-free); **FX-PopDeterminism, FX-PopBands green** (checkpoint 3.3 — the pure engine in AlphaLab.Evaluation/Populations: `PopulationRng` (SplitMix64 of (familySeed, memberIndex, gridOrdinal, securityId) — deterministic, order-independent, never reseeded), `PopulationFamily`/`PopulationFamilies` (daily/banded/monthly cost-on at M=200 + the cost-free daily twin at M=50, reusing the daily seed so cost-free member i = cost-on daily member i minus drag; quarterly seed dormant), `PopulationEngine` (stateless cadence: held(t)=top-N at the re-draw grid ordinal, reconstructible per-day from persisted equity; equal sizing D-A; D43 cost on turnover), plus the Data-side `PopulationSeeder` (control_populations, idempotent) + `ControlEquityWriter` (one bulk insert/day). Wired into `DailyPipeline` Stage 2 via `PopulationMarket` (the day's `BarFeatureView` + membership + `CostModel`). Tests: FX-PopDeterminism, FX-PopBands (gross ~0 / net offset below by the cost drag, twin-by-twin), cadence-turnover, + the pipeline integration (4 populations, 21 rows/day at small M, FR-33 no-replay).)
- [ ] FX-SyntheticEdge >95th pct; FX-SyntheticNoEdge uniform
- [~] Metrics service + NW-MDE; **FX-MDE-AR1 green** (checkpoint 3.2 — the pure calculators in AlphaLab.Evaluation: `StrategyMetrics` (β-adj Jensen alpha via NW-HAC OLS, IR, Sharpe/Sortino, deflated Sharpe, maxDD/Calmar, expectancy/profit-factor/win-rate, turnover), `MdeCalculator` (D48), `NeweyWest` (Bartlett LRV + HAC sandwich), `Normal.InvCdf` (Acklam). RF = `MetricsConstants.RiskFreePlaceholderAnnual` (0, read in one place). Verified by an adversarial-review workflow (7 agents, 0 confirmed defects) + a defensive tightening (MDE T<2 → +∞). The **ledger-reading orchestration** that assembles these per strategy lands in the 3.4 evaluation step.)
- [x] Gate (Promoted/Refused/TooEarly); **FX-TooEarly, FX-PairedWin green** (checkpoints 3.4–3.5: the verdict function `PromotionGate.Decide` (inside-MDE ⇒ TooEarly, rule 6; short-track ⇒ TooEarly; else Promoted/Refused by sign) + `EvaluationScheduler` (21-day cadence, deterministic from session count) + `EvaluationStep` (per promotable strategy vs the cap-weight benchmark `buyhold:cw` — aligned equity curves → d_t → MDE + gap → `power_reports` with the verdict), wired into `DailyPipeline` post-commit. **3.5:** a Promoted candidate transitions status candidate→live and the event lands in **go_live_log** (promoted + evidence_json); a Refused verdict is NOT a kill (D63 reserves fast-kills for anti-predictive S3/S6 + the trade track; demotion/retire is the monitor's, 3.6). Tests: FX-PairedWin (Promoted ⇒ live + go_live_log), TooEarly leaves status unchanged, and **promotions ≤ chance** across a 40-member no-edge population (MDE = 2.8σ ⇒ the gate promotes only at the false-positive rate).)
- [~] Honesty read-models (D58) + AlphaLab.Api read endpoints (FR-32/33); **FR33_ForwardReadModel_ContainsNoReplayRow green** (checkpoint 3.10 — D58 DTOs in AlphaLab.Core/ReadModels/HonestyDtos.cs (`MetricCell` (display/prefix/reason/mde, with `Normal`/`DimmedInsideMde` factories), `PopulationPercentile`, `SeparationInfo`, `StrategyRow` DTO with `seat` §23.6, `AllocationRowDto`, `PopulationBand`); the 3 screen read-models retyped from `object` to the concrete DTOs. `StrategiesReadModelBuilder` (AlphaLab.Evaluation) resolves each strategy's verdict chip, tier (distinguishable-above / not-yet-distinguishable / below-or-flagged / reference), α `MetricCell` (**dimmed + `~` + reason `inside_mde` when the gap is inside the MDE** — UX-1), population percentile, separation state, and turnover caveat into DTO fields; reads only `run_kind='live'` (FR-33). Tests: **UX1_InsideMde_MetricCell_IsDimmedWithTilde** (+ snake_case serialization + seat=math), **FR33_ForwardReadModel_ContainsNoReplayRow** (a replay Promoted power_report never leaks — stays TooEarly), baseline=reference row, Promoted=distinguishable-above + undimmed. **AlphaLab.Api read endpoints land in 3.13.**)
- [ ] Separation state (D63, FR-35) in the read-models; FX-SeparationChip + UX12 read-model test green
- [x] Allocator per D51 (FR-27); **FR27_AllocatorSuite green** (checkpoint 3.7 — `EnsembleAllocator` (pure): shrinkage (w_i=τ²/(τ²+se_i²), α̃_i=w_i·α̂_i+(1−w_i)·ᾱ, τ² floored) → softmax(α̃/λ) → **ordered clamps** floor/ceiling → TooEarly tilt cap → Suspect decay (×0.75) → band hysteresis → renormalize; floors scale below 1/N past the ⌊100/5⌋=20 roster cap (finding 116); the full {α̂,se,shrink,α̃,target,applied,weight,clamps} vector is returned. `AllocationStep` reads the gate's power_reports (α̂=gap, se=MDE/z-sum) + the monitor's overfitting_status (Suspect) + the prior allocation for the band, runs the allocator, and persists `allocation_log` (weights_json, reconstructible — NFR-2); baselines/controls never allocated. Wired into the pipeline eval step. Tests: FR27_AllocatorSuite (loose-SE ⇒ equal weight, tight ⇒ tilt, Suspect decays, TooEarly cap binds, band blocks sub-threshold moves, ceiling caps, renormalization, roster>20 graceful) + monitor→allocator integration (a Suspect strategy gets suspect_decay) + the pipeline eval-day allocation_log.)
- [x] CandidateFactory pre-registration modal (D52); **FR28_Fork_RequiresHypothesisOrFlag green** (checkpoint 3.8 — `CandidateFactory`: `RegisterHypothesis` writes a `journal_entries` kind='hypothesis' row LOCKED immediately (immutable pre-registration, D52); `CreateCandidate` **requires a linked locked hypothesis OR an explicit `unregistered` flag** (merged into config_json, rendered permanently on the card — rule 16), else throws (FR-28); registers a `trials_registry` row (kind new|fork|retrain|sibling — the honest deflated-Sharpe count) and links the hypothesis to the strategy. Tests: FR28 fork-without-either fails (writes nothing), locked-hypothesis path (trial + link), unregistered marker in config_json, unlocked-hypothesis rejected, fork sets parent + kind, duplicate-id rejected. The POST command endpoint over this factory lands in 3.13.)
- [x] Monitor S2/S3/S6 minimal (flat S3 anchors); promotions ≤ chance on dummies (checkpoint 3.6 — `MonitorSignals` (pure): **S2** deflated Sharpe (elevated when deflation flips a raw>0.5 Sharpe negative), **S3** population percentile vs the flat anchors (≥95 Healthy / <25 Suspect — the anti-predictive tail; ~50th = "in_band", never a status alarm — D63), **S6** rolling-63d edge decay **capped at Warning** (a single-eval t<−1 has ~16% false-alarm, so S6 is never a single-eval Suspect — that would auto-retire honest no-edge controls; the sustained escalation is a Phase-4 refinement). `OverfittingMonitor` reads `control_equity` per member → β-adjusted member alphas (degenerate-safe), computes the strategy's percentile, persists one `overfitting_checks` row per signal + one `overfitting_status` row; **status is signal-whitelisted** (max over exactly S2/S3/S6 — `turnover_match` is never aggregated); 4-consecutive-Suspect ⇒ auto-retire (driven by S3). Wired into the pipeline eval step (matched to the daily cost-on population). Tests: **FX-SyntheticEdge** (>95th, Healthy), **FX-SyntheticNoEdge** (in-band, never Suspect), anti-predictive (<25th ⇒ Suspect via S3), persistence, + the pure signal + whitelist-aggregate tests. The FX-TurnoverMatch-StatusNeutral guard lands with the turnover row in 3.9.)
- [~] Population compute batched; full daily run incl. populations < 60s (checkpoint 3.3: batched — one shared per-family per-day market read + a single `ControlEquityWriter` bulk insert, no per-member EF round-trip; a representative ~650-member × 500-name compute-day perf test is < 20s (≫ headroom). The full multi-day pipeline < 60s at M=200 rides with the 3.4 harness extension.)
- [x] Turnover-match verification (v1.9.7 finding 115): realized turnover persisted per strategy + population; cost-match caveat in `StrategyRow` + S3 panel; **`FX-TurnoverMatch` green** (checkpoint 3.9 — `TurnoverMatch.Check` (pure): a strategy outside ±`Populations.TurnoverMatchTolerancePct` (30%) of its population's median turnover fires the caveat; `WriteCheck` persists an `overfitting_checks` signal='turnover_match' row with a **status-neutral** contribution (never aggregated). `TurnoverMatchStep` computes each strategy's turnover from its trades over the trailing 21-session window and the population's from re-simulating a member sample via the deterministic engine (turnover ⇐ selections, not returns); wired into the pipeline eval step. Tests: **FX-TurnoverMatch** (low-churn dummy vs banded population ⇒ caveat; churn-matched sibling ⇒ none), high-churn-above-band, **FX-TurnoverMatch-StatusNeutral** (a caveat row leaves overfitting_status byte-identical), + the pipeline turnover_match row. The `StrategyRow`/S3-panel rendering of the caveat lands with the read-models in 3.10.)
- [ ] GUI: bands, MDE lines, percentile chips, separation chips — *deferrable per D65 (API-only (Scalar) sanctioned until Phase 4 sign-off); if deferred, logged here as a UI-workstream item due before Phase 7 exit*

### Phase 3.5 — Save/continue
- [ ] Nightly backup + WAL verified; watermark-pinned "reproduce day X" works
- [ ] Restore drill rehearsed and logged below

### Phase 4 — Arena Replay
- [ ] D70 prerequisite: bars backfilled for every historical S&P 500 member in the replay window (community-CSV as-of membership); replay runs on S&P 500 membership, never the S&P 100 slice
- [ ] Replay engine + quarantine; FX-ReplayQuarantine green
- [ ] FX-Replay15y validation suite green (D64 plants: edge / no-edge / anti-predictive, ≥50 seeds each)
- [ ] Anti-predictive detection-speed KPI recorded: ___ days median (D63)
- [ ] Days-to-indistinguishability KPI recorded: ___ days median (D63); no-edge plant never Suspect beyond the false-alarm rate
- [ ] Plant-sensitivity check archived in the calibration report; FX-PlantRealism green (D64)
- [ ] Threshold calibration report archived (docs/calibration/); config rows written & frozen
- [ ] D56 S3 trajectory curves calibrated + frozen; FX-S3Trajectory green
- [ ] Edge-plant survival KPI (v1.9.7 finding 113): fraction promotable at 5y: ___ (floor `Replay.EdgePlantSurvivalFloor5y` = 0.90) / at 10y: ___; every edge-plant auto-retire logged with its trigger
- [ ] Joint any-signal false-alarm fraction (v1.9.7 finding 114): ___ (bound `Replay.JointFalseAlarmMaxFrac` = 0.10); per-signal contribution archived in the calibration report
- [ ] (v1.9.8, C-2) S3 percentile-threshold sampling band archived alongside the curves (~±1.5 members at M=200) — evidence for a future "should M be 500?" question
- [ ] (v1.9.23, finding 178) Allocator value-add KPI (§1.2, finding 173) validated in replay: the D51 blend overweights edge plants / sheds anti-plants faster than static equal weight, as a paired comparison with its own NW-MDE
- [ ] (v1.9.34, finding 216) Cohort maturation curve (§1.2, D88) exercised synthetically in replay against the D64 plant cohorts (plant generations as cohorts); descriptive only - confirmed absent from gate/monitor/allocator inputs

### Phase 5 — LLM layer (authoritative AI-seat spec: MASTER §23)
- [ ] Batches + caching + tiering; news budget; all §6 TEST_PLAN tests green
- [ ] One mocked month under budget: $___ ; live smoke test passed
- [ ] LLM runs as Stage 3 (post-commit, own transaction); assistant outputs land as journal entries
- [ ] (v1.9.23, finding 178) `ContextPackBuilder` + `ai_context_packs`/`ai_decisions` (D80/D81); L0/L1/L2 layering; per-seat budgets abstain on exhaustion — `FX-PackWatermark`, `FX-AiDecisionIsTheRow`, `FX-BudgetAbstain` green
- [ ] (v1.9.23, finding 178) Researcher seat (D82): hypotheses endpoint 422s without parent evidence; draft lands unlocked; `Research.*` budget renders beside the trials count — `FR23_Hypotheses_RequireParentEvidence` green; researcher-yield KPI measured from here

### Phase 6 — Real strategies
- [ ] Momentum / MeanReversion(+fast, trade track) / LowVol (+ optional Breakout) acceptance green
- [ ] (v1.9.23, finding 179) ResidualMomentum (§6.5, D83 factor join + lag-hole rule) / TimeSeriesMomentum (§6.6) / BettingAgainstBeta (§6.7) acceptance green; shared machinery landed (AllPositive, SignFlip, `RandomPop-Event`, seeded tie-break)
- [ ] French factors ingested; attribution panel with lag note (dual role per D83 — §6.5 signal join availability-lagged)
- [ ] Ledoit–Wolf service consumed by sizing/heat/overlay (+ market-proxy row for §6.7 betas)
- [ ] Blended weighted→logistic (out-of-fold provenance; mechanical signals only — §8.1)
- [ ] (v1.9.23, finding 178) AI contestant + mandatory no-LLM twin live (memory Option A; twin = control, no trial) — `FX-TwinPairing`, `FX-ContestantReplayRefused` green; day one admits Momentum + MeanReversion only (§12 trials arithmetic); prerequisite: the finding-190 cash-constraint fix landed first
- [ ] Monitor S1/S4/S5/S7/S8 complete incl. §3½ AI-seat handling (S1 skip / S4 N/A / S8 twin input — finding 199); FX-MonitorSignals, FX-AutoRetire green

### Phase 7 — Risk, regimes, observability
- [ ] Risk screen (capacity, heat, rejections, frozen); regime episodes + badges
- [ ] Data-health screen; alerting; RUNBOOK procedures verified
- [ ] UX-9 Allocation (derivation rows + clamp chips); UX9_ClampShown_WhenBinding green
- [ ] UX-10 Analysis & Journal full (outcome-closure nag); FR-28 screens green
- [ ] UX-11 admin-intervention panel (D55); FR31_ManualAction_AuditedAndScoped green

### Phase 8 — Fundamentals (contingent)
- [ ] §7.0 PIT protocol run vs EODHD Fundamentals — result: PASS / FAIL, evidence below
- [ ] If pass: D49 logged; Value/Quality + quarterly population + leakage extensions green

## Session log (newest first)

### 2026-07-19 - docs(v1.9.34): cohort maturation curve KPI - decision D88, finding 216

**Docs only - no code change; no table change and no migration applied (one additive covering index recorded in SCHEMA for Phase 3); two additive config keys (`Kpi.CohortBucketMonths`=6, `Kpi.CohortMinStrategies`=3); tests stay 551; `ci.ps1` green.** **D88** adds the lab-level diagnostic that answers "is the researcher loop learning what to test, or just recombining without gaining?": promotable strategies grouped into admission-vintage cohorts (`strategies.created_on` bucketed; optional fork-generation grouping via `parent_strategy_id`), each cohort's MEDIAN D36 population percentile (the S3 source, reused verbatim - never a second percentile) plotted against track length t in trading days - age-aligned, never wall-clock, so cohorts compare at equal maturity. Rails resolved into the `CohortMaturationReadModel` (FR-39, MASTER §22): retired members retained (no survivorship - a curve that dropped its dead members would flatter the loop), thin-cohort + sub-MDE dimming via the D58 `{value, display, reason}` shape, replay cohorts `quarantined:true` and never co-plotted (rule 26), arena-scoped (D71), and **descriptive only - never a gate, monitor, or allocator input**. Lands: MASTER §1.2 (new KPI row) / §2 (D88 + provenance + line-5 banner) / §15 / §22 (read-model bullet); BUILD FR-39 + Phase-3 wiring (table, DoD, prompt); SCHEMA storage-neutral note + the additive `ix_overfitting_checks_path` covering index (the S3 percentile path was not queryable by `(strategy_id, as_of)`; migration ships with Phase 3); CONFIG `Kpi.*` group; UX-15 + `CohortCurvePanel` (UX_DESIGN_SYSTEM 17); TEST_PLAN §5 `FX-CohortCurve` (age-alignment / no-survivorship / quarantine / thin-cohort / sub-MDE assertions) + the §8 `UX15_*` test; CHANGELOG finding 216; D-range banners and FR/UX rosters rolled to D1-D88 / FR-39 / UX-15. Standalone reference panel shipped at `docs/mockups/cohort_curve_panel.html`; the consolidated bundle regenerates in the UI workstream (D65) - the panel is a look reference, NOT a wired screen. **PHASE TAG - recorded, NOT built:** the read-model plumbing lands in **Phase 3** alongside the other D58 read-models; a synthetic replay version can be exercised at **Phase-4 calibration** against the D64 plant cohorts; the curve becomes genuinely meaningful only after several forward admission cohorts accrue track length **post-go-live**. Also brought the line-6 current-state banner current (it had lagged at v1.9.32/D86/214 through the v1.9.33/D87 pass).

### 2026-07-19 — docs(v1.9.29): frozen-position valuation (D86, finding 210) + rejected-fill policy (finding 211)

**Docs only — no code/schema/test change; tests stay 539; `ci.ps1` green.** Clears the two Phase-2-review findings that carried a "before Phase 3" deadline. **Finding 191 → D86:** the unmapped-halt freeze marks a held name at **average cost basis**, not last print — §13.6 line 422 amended (the code, `DailyPipeline.MarkToMarket`, already did this; the doc was wrong). A frozen name has no reliable current price and a stale last print misstates it in the one direction the honesty rails must never allow silently. **Terminal delisting is untouched** — it has a real final print and correctly stays last-print (`BankruptcyHaircutPct`, `FX-Delist`, and the freeze-*mechanism* wording all unchanged). **Finding 196 → blessed, no D-number:** a rejected/clipped T+1 fill is dropped and the strategy's next decision re-opens the name if still wanted; a bounded re-plan is rejected as a second decision authority (cf. D84's no-sell-to-fund) — §13.6 gains the policy note. Lands: MASTER §2 (D86) / §13.6 (line 422 + fills note) / §0 provenance / line-5 banner; TEST_PLAN `FX-Unmapped`; MANIFEST banners (D1-D86); CHANGELOG findings 210–211. **Both before-Phase-3 items now cleared** (190 & 195 resolved earlier in v1.9.25); P9 → ✔ D86, P11 → ✔ blessed.

### 2026-07-18 — docs(v1.9.26): the no-LLM twin's Stage-2 scorer — decision D85 + proposal P13

**Docs only — no code/schema/migration/config-key/test change; tests stay 539; `ci.ps1` green.** Records the scoring rule D81 left open: how the mandatory no-LLM twin scores each shortlisted name once the LLM call is removed (`FX-TwinPairing` froze everything *around* the score — pre-filter, breadth, sizing, exits, costs, seed — but not the score). **D85** (MASTER §2, refining D81) fixes it as a **frozen equal-weight z-score blend of the same pack features the contestant sees** — cross-sectionally standardized across the day's shortlist at the watermark, equal-weight averaged, no tuning, no free params, deterministic (F-DET), a `config_json` frozen param (rule 24/32), identical *form* across contestants with the feature set following `Ai.PackRecipeVersion`, ordered degenerate-day handling (drop zero-variance features → equal-score fallback + `degenerate_blend` flag if none survive or <2 names → never NaN), and not replay-admissible on its own. The rejected foils (pure equal-scoring, too weak a bar; a tuned/selected rule, p-hacking) are recorded as **P13**. Lands: MASTER §2 (D85) / §23.3 (rules 2/4) / §0 (D1–D85 roll); STRATEGY_CATALOG §9; CONFIG_REFERENCE (Ai NOTE); TEST_PLAN §6 (`D85_*` fixtures + extended `FX-TwinPairing`); CHANGELOG finding 207; `CLAUDE.md`/`MANIFEST.md` D-range roll. ORIENTATION.md already named the blend (Separation 3). Mirrors the D84/P12 shape from v1.9.25. **Note:** the v1.9.24/v1.9.25 passes are recorded in CHANGELOG but were never given session-log entries here — a pre-existing gap, not swept in this pass.

### 2026-07-18 — docs(v1.9.23): reconciliation pass — findings 174–199, decision D83, proposals P8–P11

**Docs only — no code/schema/migration/config-key/test change; tests stay 528; `ci.ps1` green.** Two CHANGELOG sections written: the **retroactive v1.9.22** section (findings 174–177 — the four v1.9.22 commits `75e9e81`/`3c870d1`/`c7bb9db`/`765e1d2` shipped catalog §6.5–6.7, UX-14 + the replay recolour + mockup consolidation, the TEST_PLAN §6 AI-seat fixtures, and the DI §4 three-seat rewrite with **no CHANGELOG rows** — itself a discipline break, closed by recording), and **v1.9.23** (findings 178–199). Highlights: BUILD reconciled to MASTER §23 (178 — FR-23 → the D82 resolution; the retired sentiment-score and blend-A/B framings removed; §23.6 items phased into Phases 3/5/6; the finding-173 KPIs phased into Phase 4 DoD / Phase 5); the three new strategies propagated (179); **D83** resolves the D41 contradiction — factor returns become §6.5's availability-lagged signal input with the deterministic MKT-fallback lag-hole rule, attribution stays diagnostic-only (180); catalog completions — TSMOM `skip = 21`, `SignFlip`, `AllPositive`, the seeded (Config.Seed, security_id) tie-break, `RandomPop-Event`, the §6.7 LW seams (181–186); the §12 trials arithmetic (187); housekeeping — D-ranges → D1–D83, UX-13/14 italics repaired, stragglers swept (188); self-improvement clarifications + **P8** (189); the eight Phase-2-review code findings **recorded, not fixed**, with binding deadlines (190–197) + **P9/P10/P11**; the `61bcd38` LocalBackup test-flake fix recorded (198); OVERFITTING_MONITOR gains §3½ AI-seat handling — S1 skip / S4 N/A / S8 twin input, the `factor_returns` dual-role note, and the low-turnover-plant calibration note (199). The SP1500 widening-target proposal gains the Grinold-overclaim + compute-scale caveats. Landed as one commit on `docs/v1.9.23-reconciliation` via PR.

### 2026-07-18 — PR #1 review fixes: findings LL–NN (v1.9.20), one commit on the branch

**Three pre-merge review findings on PR #1, fixed on `phase2-funnel-ledger` as one commit — two doc/comment corrections and one small guard. One behaviour change: the local backup now fails closed. 527 → 528 tests; `ci.ps1` green.**

- **LL (docs, rule 14)** — `Benchmark.CapWeightProxySecurityId` was defined in code (`CapWeightProxy.ProxySecurityIdConfigKey`), read by the D53 pipeline, and named as an operator action in PROGRESS — but absent from CONFIG_REFERENCE, the registry that makes a config key real. Added a `Benchmark` entry on the `Regime.ProxySecurityId` pattern: a versioned `config` row (MAX(version)) holding the proxy ETF's security_id, written by the operator after ingesting the proxy (`OEF.US` under `oef_csv`, `IVV.US` under `ivv_csv` — `CapWeightProxy.SymbolFor`); absent ⇒ the CW account holds cash, a logged readiness gap (rule 10), never a guessed symbol.
- **MM (code, rule 10)** — `LocalBackup` logged a busy `wal_checkpoint(TRUNCATE)` as a warning and copied anyway: a copy without a completed checkpoint can silently omit committed transactions, and the Api-as-separate-reader topology makes busy reachable in normal operation. Now: bounded retry (3 attempts, ≤1s busy-wait each via `CommandTimeout`, 200ms between), then **abort — loud error, failed `LocalBackupResult` (`FailureReason`), no file written**. Clean path unchanged (checkpoint → copy → per-day idempotency → filename-date pruning). Negative test `Backup_CheckpointBusy_AbortsAndWritesNoFile` holds a second reader connection's open deferred read transaction mid-read (pinning the WAL) and asserts failure + an empty backups directory.
- **NN (comment only)** — the Stage-2 `StampHeartbeat()` comment claimed a long day "stays 'live'" — but the write sits inside the Stage-2 transaction (invisible until commit, stale by the transaction's duration at commit), and the `HeartbeatService`'s own-connection beats block on the write lock during it too. Comments rewritten honest in all three places (the call site, the method, and `HeartbeatService`'s class doc): a post-commit refresh only; the real protections within one day are the run-open stamp, the 5× `StaleRunThresholdSeconds` headroom, and the 3× slow-transaction warning. The stamp itself is kept; no new liveness mechanism.

**Next session starts with:** unchanged — **Phase 3** (see the 2.12 entry below), after PR #1 merges and the operator prerequisites run (M1+M2+M3, the OEF proxy ingestion + `Benchmark.CapWeightProxySecurityId` row, the first off-machine backup).

### 2026-07-17 — Phase 2 checkpoint 2.12: the D72 launch order + backup (Phase 2 COMPLETE)

**The process model that wraps the daily write. `OnDemandRunner` now drives the full D72 launch order — stale-run recovery → catch-up → job drain → local backup → exit — with the liveness heartbeat running alongside and the API rate-limit seam shipped. This is the last checkpoint of Phase 2; all 8 gate boxes are green. No migration. 489 → 527 tests; `ci.ps1` green. Findings GG–KK (v1.9.19).**

- **`StaleRunRecovery`** (step 1) — the run-row lifecycle's step-2 commit (`runs('running')` + `run_in_progress=1`, committed BEFORE Stage 2) is what makes a crash visible; recovery reads the orphan (`run_in_progress=1` with no fresh heartbeat), marks its run `'failed'`, clears the flag — so the crashed day is replayed as an ordinary missed session by the catch-up resolver. It MUST precede catch-up (finding GG). A FRESH heartbeat instead = a live writer → `OverlappingWriterException` (fail closed, D59 sole writer) = the partial `FR34_NoOverlappingWriters` guard.
- **Liveness (`IWorkerLiveness`, AlphaLab.Data)** — the pure `WorkerLivenessEvaluator` returns TWO facts, not one: `IsLive` (`run_in_progress=1` AND fresh heartbeat — the API's 409 case) vs `IsStale` (in-progress but stale/absent heartbeat — an orphan to recover, never a 409). A missing/unparseable heartbeat fails toward stale (finding HH). The same verdict drives the Worker's recovery and, via `Api→Data`, the Phase-3 409; the threshold is caller-supplied so Data stays ignorant of Worker options. The endpoint-level 409 test lands with FR-32.
- **`HeartbeatService`** — a BackgroundService on its OWN scope/connection beats `worker_state.heartbeat_at` every `Worker.HeartbeatSeconds` while a run is in progress (D59 = one writer PROCESS, not one connection; WAL serializes, so the beat blocks on the busy timeout and lands after Stage 2 commits). The pipeline ALSO stamps the heartbeat at Stage-2 start and warns when a day's transaction exceeds 3× the heartbeat (a canary for the sp500 widen / replay scale). `HeartbeatWriter.Beat` is idle-safe (no stamp when `run_in_progress=0`, so between launches the heartbeat correctly goes stale).
- **`JobDrainer`** (step 3, behind `Worker.DrainQueuedJobsOnLaunch`) — drains queued jobs OUTSIDE any write transaction (asserted twice: `CurrentTransaction is null` on entry, and FX-JobDrain's fake executor opens its OWN write txn — a deadlock if one were held). `IJobExecutor` is a kind-keyed registry; **Phase 2 registers none**, so a queued job of any kind is marked `'failed'` with `error_json` naming the missing executor — fail closed and visible, never stuck `'queued'` (finding II). Real executors (replay, analysis) arrive with their phases.
- **`LocalBackup`** (step 4) — `PRAGMA wal_checkpoint(TRUNCATE)` → copy to `<DbBase>\{arena}\backups\alphalab-{date}.db` (skip if today's exists) → prune copies older than `Ops.BackupRetentionDays` by the date IN the filename. The backup dir derives from the resolved Data Source via the **pure** `DbPathResolver.BackupDirectory`/`BackupFilePath` (no filesystem — the `ResolvePath` precedent), arena-namespaced by construction (finding JJ). New `Ops` config section + `OpsOptions` (Core). The **off-machine** copy stays a manual operator action.
- **API limits (finding KK)** — `ResilientHttpClient` honours the 1,000/min limit reactively: it reads `X-RateLimit-Remaining` and pauses (pure `RateLimitGuard.CooldownFor`) when at/below a floor — distinct from the 100k/day headroom check. Endpoint **weighting** ships through the existing `Count(source,n)`/`Record(...)` seam via `EodhdEndpointCost` (`/eod`·`/div`·`/splits`=1, `/news`=5, `/eod-bulk-last-day`=100); the backfill uses only cost-1 endpoints so its 304 total is unchanged. **`eod-bulk-last-day` is declared but NOT wired** — a wash at sp100, it only pays off post-widening.
- **`OnDemandRunner`** now composes all four steps in order then `StopApplication`; a crash inside a day propagates (exit non-zero) and the next launch's step 1 recovers the orphan.

**Fixtures.** FX-CrashedRun (`StaleRunRecoveryTests`: orphan → 'failed' + flag cleared), FX-JobDrain (`JobDrainTests`: registered→done with own-txn proof; unregistered→failed with named reason), FR34_LabRunsWithoutApi (`LaunchOrderTests`: the whole order runs end-to-end with only Worker+Data — no Api; plus recover-then-catch-up). Plus `WorkerLivenessTests`, `HeartbeatTests`, `LocalBackupTests`, `RateLimitTests`, `EodhdEndpointCostTests`, and the `DbPathResolver` backup-path purity tests.

**No new proposals; none closed.** P1 (Stage-1 liquid) / P2 (ingestion duplicate guard) / P3 (features PIT) / P5 (drop `processed_on`) / P6 (regime replay quarantine) / P7 (one-time gate sweep) are unchanged and carry into Phase 3/4 per their deadlines. `FR34_NoOverlappingWriters` is recorded PARTIAL (the launch guard ships; the endpoint 409 is FR-32).

**Next session starts with:** **Phase 3 — the honest arena.** Metrics (Jensen's α, Newey–West, the population channel), the NW-corrected MDE + verdict read-models (D58, rule 18), the go-live gate + allocator, and the `AlphaLab.Api` command/read endpoints — including **FR-32** (the 409 that consumes `IWorkerLiveness`, completing `FR34_NoOverlappingWriters`). Operator prerequisites first: apply M1+M2+M3 (`pwsh tools/migrate.ps1 -Arena sp500`), ingest the OEF proxy + write `Benchmark.CapWeightProxySecurityId`, and take the first off-machine backup.

### 2026-07-17 — Phase 2 checkpoint 2.11: the D47 catch-up protocol

**The catch-up that drives the D53 pipeline across missed days. `MissedSessionResolver` (the real one, replacing the Phase-0 stub) computes which forward sessions still need processing; `CatchupRunner` replays them oldest-first, one transaction per day, resumable + idempotent; `OnDemandRunner` now drives it. No migration. 479 → 489 tests; `ci.ps1` green. Finding FF.**

**Shipped.**
- **`MissedSessionResolver`** — a PURE query (the runs table IS the state; no state machine): the missed set = every session in `[floor, lastCompletedSession]` that lacks a `status='ok'` FORWARD run, ascending. Two things the plan states only for the running case, resolved:
  - **Floor** = the session after the last 'ok' forward run's as_of. On a FRESH store (no forward runs) it falls back to the latest **bar date** — the backfill watermark — so forward operation begins where the historical data ends, never replaying 20 years. An empty store (no runs, no bars) has nothing to do.
  - **Last completed session** = the latest session whose **ET close has PASSED** (`now_ET > CloseTime`, close time NOT close+offset — the offset is Scheduled-mode-only, CONFIG). A pre-today session is always complete; today's only after its close. The compare is in ET (injected `TimeProvider` + a cross-platform ET `TimeZoneInfo`), which handles DST for free (§20.5).
  - **Self-correcting:** a failed Stage-2 rolls back its bars AND leaves no 'ok' row, so neither the anchor nor the exclusion set advances past a failed day — the loop resumes exactly there. The "minus ok days" is belt-and-braces (with floor = last-ok+1, every candidate is already non-ok); `ux_runs_ok_forward` keeps it sound.
- **`CatchupRunner`** — replays the missed set oldest-first, ONE atomic transaction per day (D47), a **fresh DbContext per day** (a new DI scope) so no cross-day change-tracker state leaks. A fail-closed day (a gate reject) STOPS the loop and commits the prefix rather than building later days on a bad one (the T+1 chain depends on it); a Stage-2 crash throws out of `RunDayAsync` for the same resumable outcome. run_kind: a session processed on its own ET date is **'live'**, a recovered one **'catchup'** (which also writes `catchup_log`) — both FORWARD evidence (the ledger collapses them to `RunKind.Live`). No LLM for past days — structural (no `IAnalysisProvider` until Phase 5).
- **`OnDemandRunner`** now drives `CatchupRunner` then exits (D61: catch-up IS the work in both modes; the trigger is the only difference). The D72 launch ORDER around it — stale-run recovery before, job drain + local backup after — is 2.12.

**Tests.** `MissedSessionResolverTests` (rewritten for the real resolver): empty store → nothing (the Phase-0 behaviour it subsumes); fresh-store gap from the backfill watermark; all-caught-up no-op; the ET guard both ways (before close → today excluded, after close → today included); sequential resume after a failed day. `CatchupRunnerTests`: **FX-Outage5d** (a five-session outage recovered), replay-in-order (today 'live' + earlier 'catchup', `catchup_log` holds only the recovered days), the same-evening no-op, no-run-before-close, and the fail-closed stop (prefix committed, the bad day wrote no row).

**Findings recorded (CHANGELOG FF).** The forward-start anchor + the ET last-completed-session guard (the two silent-wrong-answer traps: replay-20-years on a fresh store, or process-today-before-its-close), the self-correcting resume, and the fresh-DbContext-per-day loop.

**Deferred to 2.12 (the last of Phase 2, stated).** The D72 launch ORDER wrapping catch-up: `StaleRunRecovery` → `CatchupRunner` → `JobDrain` → `LocalBackup` → exit; the `HeartbeatService` + stale-run recovery (FX-CrashedRun); the job-drain registry (FX-JobDrain, Phase 2 registers no executor); `IWorkerLiveness` + the 409 decision (the endpoint-level test lands with FR-32, Phase 3); the API `X-RateLimit` handling + endpoint weighting; and `FR34_LabRunsWithoutApi`.

**Next session starts with:** checkpoint 2.12 — the D72 launch order + backup. `StaleRunRecovery` (a stale `run_in_progress` → mark the orphaned run 'failed', clear the flag) → `CatchupRunner` → `JobDrain` (behind `Worker.DrainQueuedJobsOnLaunch`; Phase 2 registers no executor, so a queued job fails closed with a named reason) → `LocalBackup` (`wal_checkpoint(TRUNCATE)` → copy to `{Arena.Id}/backups/` → prune > `Ops:BackupRetentionDays`) → exit. Plus the `HeartbeatService` (own scope + connection, 30s), `IWorkerLiveness` (`run_in_progress=1 AND heartbeat fresh` — the 409 decision logic, endpoint test deferred to FR-32), the `Ops` config section + `DbPathResolver`-derived backup dir, and the `ResilientHttpClient` `X-RateLimit` handling + endpoint weighting. Fixtures FX-CrashedRun, FX-JobDrain, FR34_LabRunsWithoutApi.

### 2026-07-17 — Phase 2 checkpoint 2.10: the D53 staged daily pipeline (the first committed write)

**`AlphaLab.Worker` gains the D53 staged pipeline — the sole DB writer's daily loop, and the first code to write the lab's own irreplaceable output (`trades`/`decisions`/`equity_curve`) to the live store. M3 `RunsForwardUniqueIndex` (the `ux_runs_ok_forward` partial index) is the only schema change. 460 → 479 tests; `ci.ps1` green. Findings AA–EE; P4 SETTLED on `stage_json`; P7 recorded.**

**Shipped.**
- **`DailyPipeline.RunDayAsync` (Worker/Pipeline)** — the four-step run-row lifecycle that resolves four colliding constraints (finding AA), the ONE ordering RUNBOOK §1 confirms: **(1)** Stage-1 fetch+gate, ZERO writes; **(2)** a small committed txn opens `runs(status='running')` + `run_in_progress=1` (committed so a crash is visible to D72 recovery); **(3)** ONE Stage-2 transaction — bars → corporate actions → regime label → per-account (CA-apply before fills/funnel, D53 order; fill yesterday's `stage_json` orders at today's open through `OrderFill`/`VirtualBroker`; run the funnel to decide the next session's orders; record the equity point) → D77 quality flags under the now-existing run_id → `status='ok'` → `catchup_log` if catchup; **(4)** a small txn clears `run_in_progress` on success. A Stage-1 reject aborts before any row (FR-29); a Stage-2 crash rolls the whole day back to `'running'` + `run_in_progress=1` (the FX-CrashedRun state) for the next launch.
- **`Stage1Fetch` (Worker/Pipeline)** — the fetch+gate step that **takes no `DbContext`, so it cannot write** (finding BB — a type-level guarantee, not a discipline; pinned by reflection over the ctor AND a runtime "provider hard-fails → every table untouched" test). It gates the fetched delta merged with the member's prior action feed, and drops flags on already-gated dates (≤ `LastStoredDate`) so a re-fetch never re-emits yesterday's warnings.
- **D77 gate→store wiring (finding CC)** — the gate's `Warn`s, previously discarded, now persist under the run_id (the run-row lifecycle exists partly so `run_id` is available before Stage 2 writes flags). A run-day outlier warn is proven to land with the correct run_id.
- **`Phase2StrategyRegistry` (Strategies)** — maps a persisted `strategy_id` back to its runnable `IModel` + per-account run settings, supplying the two seams stated at 2.9: CW `Sizing.PositionCapPct = 1.0` (100% in the single proxy), EW `Guardrails.MaxConcurrentPositions ≥ the universe`. An unknown strategy_id resolves to null → the pipeline skips that account (rule 10), never guesses.
- **M3 `RunsForwardUniqueIndex`** — the `ux_runs_ok_forward` partial unique index (index-only, modelled on `BarsDateIndex`): at most one `status='ok'` row per as_of among forward kinds; failed retries and replay are exempt by the filter. The idempotency backstop (a second 'ok' forward run of a day is rejected at Stage-2 commit) and what makes `catchup_log(as_of PK)` consistent.
- **Worker composition root (findings D + F)** — binds `Eodhd` (the Worker's own EODHD section — the daily delta; token read defensively), `Costs`, `Sizing` (`Mode=equal`), `Guardrails`, `Data`, `CorporateActions`, `Regime` BEFORE `AddAlphaLabMembership` (so its `TryAddSingleton` defaults no-op), closing finding F's remaining two binds. `UniverseOptions` stays unregistered (the D70-widening job, finding 151).

**Decisions closed / recorded.** **P4 (orders-table) SETTLED on `stage_json`** — the revisit window reached its 2.10 deadline and the real shape (finding Q, now exercised end-to-end by the pipeline's `FillPriorOrders`) confirms an order has no lifecycle a table would track. **P7 recorded** — the one-time gate sweep of the never-gated backfill needs a `bars.gated_through` marker (finding EE); the daily pipeline gates only the delta + a bounded context tail, so the historical backlog is a bounded follow-on, not a daily full-history re-gate.

**Findings recorded (CHANGELOG AA–EE).** **AA** the run-row lifecycle (the one ordering that satisfies FR-29 + `run_id` NOT NULL + D72 + the partial index). **BB** Stage 1 write-incapable by construction. **CC** D77 gate warnings finally land under a run_id. **DD** the Worker's Eodhd section + the last two finding-F binds. **EE** P4 closed + finding M bounded (P7).

**Deferred to 2.11/2.12 (stated seams).** The OnDemand loop that drives `RunDayAsync` per missed day is **2.11** (D47 catch-up); `no-overlapping-writers` + `runs-without-API` + heartbeat + stale-run recovery + the local backup step are **2.12** (D72). The daily membership REFRESH (re-fetching OEF/Wikipedia) rides with the D70-widening wiring — the funnel reads the current stored roster (`MembersAsOf`). The CW proxy's live ingestion + its `Benchmark.CapWeightProxySecurityId` config row are operator actions (until done, the CW account holds cash, logged).

**Next session starts with:** checkpoint 2.11 — the D47 catch-up protocol. A real `MissedSessionResolver` (replacing `Phase0MissedSessionResolver`): `SessionsBetween(lastOk+1, lastCompletedSession)` minus sessions already carrying a `status='ok'` forward run (`ux_runs_ok_forward` makes the set-difference sound), resumable (one `RunDayAsync` per day; day k failing leaves 1..k−1 committed and the next launch resumes at k), idempotent (session-derived watermark, never UtcNow), with the last-completed-session close-time guard (`IClock` + ET `TimeZoneInfo`). No Stage 3 for past days (structural — no `IAnalysisProvider` until Phase 5). Fixtures FX-Outage5d + the three FR-34 OnDemand tests.

### 2026-07-17 — Phase 2 checkpoint 2.9: the dummy roster (Buy&Hold ×2 + Threshold) + §5.1 total-return acceptance

**First code in `AlphaLab.Strategies`. Two permanent Buy&Hold benchmarks (cap-weight + equal-weight) and a trivial trend dummy, all pure `IModel`; `DummyRoster` seeds the `strategies` table + opens three accounts; `Accounts.StartingCash` = $100k as a versioned config row. The §5.1 total-return acceptance is proven EXACTLY by a pure-engine harness. No migration; two doc fixes (CATALOG §5.1 finding I, CONFIG Accounts.StartingCash finding K). 444 → 460 tests; `ci.ps1` green.**

**Shipped.**
- **`BuyAndHoldModel` (CW + EW)** — Buy&Hold is indifferent across names, so `ScoreUniverseAsync` scores every eligible name at 1.0 in both shapes; what differs is the universe the pipeline hands each account and the exit. **CW** holds ONE security (the cap-weight ETF proxy), `ExitPolicy.Never` — it enters once and the daily 1.0 score is a no-op because the proxy is already held and Never never closes. **EW** is self-built equal weight over the whole eligible universe, `ScheduledRebalance(21)` ≈ monthly (D68) — never an EW ETF. Both `Sizing.Equal` (the only Phase-2 mode).
- **`ThresholdModel`** — a deliberately trivial "above-trend" filter: 1.0 when adjusted close > its lookback-SMA, 0.0 below (dropped by the zero-score invariant → a real sparse wish list), omit thin history. Scores on **adjusted** close (D30); `ExitPolicy.ScheduledRebalance`; carries `Unregistered=true` (a dummy with no hypothesis, honestly flagged — rule 16/D52).
- **`CapWeightProxy`** — resolves the CW benchmark's ETF symbol from `Universe.Bootstrap.MembershipPrimary` (`oef_csv`→`OEF.US`, `ivv_csv`→`IVV.US`), config-driven so the S&P-500 widening flips it with no code change (finding I); unknown sources fail closed.
- **`DummyRoster` (Data write)** — registers the three strategies (config_json / polymorphic exit_policy_json) and opens a live account per strategy via `LedgerStore`, **idempotently** (a re-run adds no duplicate strategy/account/config version; a registered strategy's frozen config is never re-serialized over, D17). `Accounts.StartingCash` is a **versioned config row** (`MAX(version)`, the `Regime.ProxySecurityId` precedent) — a fresh store writes v1 = the $100k default, an operator-set row is honoured over it (finding K).

**§5.1 acceptance (the honesty bar).** A pure-engine harness composes the real Core engines exactly as the D53 pipeline (2.10) will — corporate actions before the funnel, decide at close T, fill at open T+1 through `OrderFill`/`VirtualBroker`, dividend credited on the ex-date via §13.6, mark-to-market on raw close — but in memory, so it proves the STRATEGY, not the pipeline. Over a 15-session span (a warm-up so σ exists at the fill, an entry, a dividend, a price ramp) the cap-weight account: **enters once and never churns** (exactly one trade), and its **total return equals the proxy's total return (price + dividends) minus one entry cost — EXACTLY, asserted with no tolerance.** The exactness is by design: fractional shares (`positions.shares` is REAL) and no overnight gap on the entry make §5.1 satisfiable rather than approximate (OrderBuilder's own note). Costs are always on — even a mega-cap ETF crosses the spread — and the single entry cost is the only drag.

**Docs.** CATALOG §5.1 corrected: "an S&P 500 ETF proxy" → the traded universe's proxy following `MembershipPrimary`, with the **expense-ratio bias disclosed** (~0.20%/yr OEF, ~0.03%/yr IVV sits in the benchmark — a known, bounded, one-directional bias named rather than swapped for a synthetic index; the self-built EW avoids it). CONFIG_REFERENCE gains the `Accounts.StartingCash` key.

**Findings recorded (CHANGELOG Y, Z).** **Y** — the proxy is config-driven + the acceptance harness proves §5.1 exactly + the benchmark run-settings seam for 2.10 (CW cap 100%, EW breadth ≥ universe). **Z** — StartingCash as a versioned config row (finding K), and the dummies are the first `strategies` rows.

**Deferred to 2.10 (stated, not silently omitted).** Running the dummies through the hosted D53 pipeline against the live store (the first committed write); the per-account benchmark run-settings above; the CW proxy's live ingestion (OEF.US bars + dividends, ~3 EODHD calls) is an operator action like the sp100 backfill.

**Next session starts with:** checkpoint 2.10 — the D53 staged pipeline in `AlphaLab.Worker` (M3 `ux_runs_ok_forward` partial index) + D77 gate→store wiring + the `Costs`/`Regime`/`Data` binds + the `Eodhd` Worker section. This is the first committed write and the **P4 orders-table revisit deadline** (reopen before 2.10 or it is settled on `stage_json`).

### 2026-07-17 — Phase 2 checkpoint 2.8: the FR-26/D50 regime-label service (M2) + the finding-F Regime bind

**The daily PIT regime label — trend × volatility with hysteresis — as a pure Core labeler + a Data service that reads the proxy at the watermark, stamps provenance, and maintains the D45 episode chain. M2 lands `regime_labels` + `regime_episodes`. FX-RegimeHysteresis + F-LEAK green. The finding-F Regime bind is closed. 424 → 444 tests; `ci.ps1` green. No new config keys (the `Regime` section already existed — this checkpoint makes it *bind*).**

**Shipped.**
- **`RegimeLabeler` (Core, pure, BCL only)** — the §20.1 cross product. **Trend:** `bull` if `adj_close` > its 200-day SMA, else `bear`, with **hysteresis** — a flip confirms only after the close is ≥ `TrendHysteresisPct` (1%) beyond the SMA for `TrendConfirmDays` (5) consecutive sessions; otherwise yesterday's label holds. The first label is seeded from the raw comparison (a seed is not a flip). **Vol:** `high_vol` if the 21-day realized vol ≥ the 80th percentile of its own trailing-3-year daily distribution, same 5-day confirmation. It **recomputes the whole trajectory** from the series it is given and **never reads a prior persisted label** — a persisted label was computed at a different watermark, and reading it would import a forward run's state into a replay (a quarantine leak). Reuses `PriceStatistics.RealizedVolDaily` (finding N) so D50's σ is byte-identical to D43's.
- **`RegimeLabelService` (Data)** — supplies the labeler with the proxy's watermarked `adj_close` series and persists the result: resolves `Regime.ProxySecurityId` from the **versioned config row** (MAX(version), the FR-38 precedent — not appsettings); fails closed via `IRegimeProxyReadiness` below the ≈3.8-year warm-up and on a missing asOf bar; stamps `inputs_hash = SHA-256(proxy id, parameter set, watermark)`; upserts the `regime_labels` row; and maintains `regime_episodes` — the maximal-trend-run chain (D45) — **forward-only** (a confirmed flip closes the current episode at the last session of the old trend and opens the next; extending and a same-day re-run write nothing).
- **M2 `Phase2RegimeLabels`** — `regime_labels` (PK `as_of`; `ck_regime_labels_trend`/`_vol`; `inputs_hash` provenance; no `run_kind`, no version — a derived PIT table) + `regime_episodes` (`episode_id` bare INTEGER PK, the rule-14 AUTOINCREMENT hand-edit; `end_date` NULL = ongoing). The regime tables were already declared in SCHEMA (the M1 precedent — the migration implements what the doc documents), so no DDL change to that file. `SchemaFidelityTests` 23 → **25 tables**, with regime CHECKs + `episode_id`'s no-autoincrement asserted.
- **Finding-F Regime bind** — `AddAlphaLabMembership` now accepts the bound `RegimeOptions` (the composition root owns the bind — CONFIG rule 7); before this, every `Regime:*` value was silently ignored by an unbound `new RegimeOptions()`. `IRegimeLabelService` is registered. The sibling `DataQualityOptions`/`CalendarOptions` binds land with their own consumers (2.10 / catch-up); `UniverseOptions` stays unregistered by design (it is the D70-widening proposal).

**Tests (+20 → 444).**
- **`RegimeLabelerTests` (Core, 11)** — FX-RegimeHysteresis: ±0.5% oscillation around the SMA ⇒ **zero flips**; a sustained ramp ⇒ **exactly one** flip on the 5th confirming session; a 4-session breakout ⇒ zero. Plus the raw seed, the vol classification both directions (with a realistic lookback-vs-spike ratio, so a spike is not hidden by its own short window), the cross product/date alignment, the too-short-series backstop, and fail-closed on non-positive / non-ascending input.
- **`RegimeLabelServiceTests` (Data, 7)** — fail closed on an unresolved proxy, below warm-up, and a missing asOf bar (each writes nothing); proxy id from MAX(version); computes + persists a valid label; **F-LEAK** (the label at a fixed asOf is invariant to a future bar + a later-observed correction, and byte-identical provenance at the same watermark, while a different watermark yields a different `inputs_hash` — the watermark is IN the hash); and the **D45 episode chain** (flip closes+opens, extend and re-run are no-ops).
- **`RegimeBindTests` (Data, 2)** — the passed `RegimeOptions` is the resolved instance (finding F closed); no bind keeps the CONFIG defaults.

**Findings recorded (CHANGELOG W, X).** **W** — the labeler recomputes the trajectory rather than reading a prior label (the leak/quarantine reasoning), the vol-window inclusivity + percentile convention, and episodes accruing forward from go-live (not backfilled across warm-up). **X** — the finding-F Regime bind.

**Proposal recorded (P6).** `regime_labels` has no `run_kind`; Phase-4 replay at a different watermark would overwrite the forward label. No risk today (nothing calls the service until the 2.10 pipeline; the service is quarantine-neutral by signature). Decide before Phase 4: replay computes labels in memory only, or `regime_labels` gains `run_kind` in its PK (the `equity_curve` precedent).

**Deferred to 2.10 (stated, not silently omitted).** Wiring the label service into the D53 staged pipeline (Stage 2, after membership) — the service lands the compute+persist seam so there is something to call, exactly as `DataQualityFlagStore` landed before its pipeline wiring.

**Next session starts with:** checkpoint 2.9 — the dummies (first code in `AlphaLab.Strategies`): `BuyAndHoldModel` CW (`OEF.US`, config-driven from `Universe.Bootstrap.MembershipPrimary`, ingested as a real security so §5.1 is literally true) + EW (self-built, D68) + `ThresholdModel`; `Accounts.StartingCash` = $100,000 as a versioned config row (finding K); correct CATALOG §5.1 "S&P 500 ETF proxy" → the traded universe's proxy (finding I) + document the expense-ratio bias; B&H total-return acceptance over a fixture span including a dividend.

### 2026-07-17 — Phase 2 checkpoint 2.7: corporate-action ledger part 2 + the processed_on amendments (FR-9 / §13.6)

**Mergers (cash/stock/mixed), spin-off receipt, delist force-exit — completing §13.6. Plus the D74 membership-drop-≠-delist proof and the three `processed_on` amendments. FX-MergerCash/Stock/Mixed, FX-Spinoff (+ missing-ratio), FX-Delist (+ bankruptcy) green. No migration; two new config keys. 403 → 424 tests; `ci.ps1` green (with the new `corporate_actions` guard).**

**Shipped (extending the 2.6 engine + applier).**
- **`CorporateActionLedger` part 2** (Core, pure): **cash merger** → force-close at deal cash, costs waived, P&L realized; **stock merger** → convert at the exchange ratio into the acquirer's security_id, basis carries (and SUMS if the acquirer is already held); **mixed merger** → cash leg credited + stock leg converted, one action; **spin-off** → new position in the spun-off id, basis conserved (parent loses what the receipt gains), enters even if not in-index; **delist** → force-exit at last print × (1 − haircut), costs waived. Forced-event fills stamp `cost_model_version = "corp-action-waived"` and never route through the D43 broker (§13.6: "a corporate action, not a trade").
- **`SpinoffAllocation`** (Core, pure) — the two basis-split paths §13.6 names: by-ratio (share-proportional, no prices) and first-print relative value (the honest fallback). Both conserve basis.
- **`CorporateActionApplier` part 2** (Data) — builds the `CorporateActionContext` (existing acquirer position, last print from the bar reader, haircut from config, resolved spin-off terms) and persists each effect. The applier now takes `CorporateActionsOptions`.
- **`CorporateActionsOptions`** (Core/Config; CONFIG `CorporateActions`) — `BankruptcyHaircutPct`, `SpinoffLiquidationDays` (the plan's findings B/C — keys the design called config but never had).

**The three `processed_on` amendments (the operator's three, verbatim — finding V):**
1. **Rewrote** the SCHEMA:72 comment (and the entity XML doc): `processed_on` is ALWAYS NULL, never written — a global column on a per-account op would break replay, D76 forbids UPDATE, and idempotency is one-transaction-per-day + `ux_runs_ok_forward`, not this flag.
2. **Widened** the CI guard: `ci.ps1` now `Assert-NoMatch`es `DELETE/UPDATE corporate_actions` beside the bars pair — D76 extended append-only to a second table and the guard finally follows ("it can't merge", not "we agreed not to").
3. **Logged** "drop the column" as a deferral → **proposal P5** (below), not a resolution.

**D74 proof (FR9_D74_...):** an index-membership drop is NOT a delisting — a held name that left the index but keeps printing bars, with no delist action, is neither force-exited nor frozen; the position survives untouched. (A drop stamps `index_membership.removed_on` only; `delist` comes from the exchange symbol-list feed, dormant D49.)

**Findings (CHANGELOG v1.9.17):** **T** (the two §13.6 config keys — plan findings B/C — added, with the haircut-default tension stated), **U** (merger/spin-off basis handling is underspecified; documented approximations that conserve value, fixtures pin the invariants not the soft split), **V** (the `processed_on` amendments).

**Seams stated where they can be seen.**
- **Forced-event costs waived + not capacity-capped** (§13.6): mergers/delist build the trade directly with zero costs; only funnel orders route through the D43 broker (which is why `OrderFill` refuses a `CorpAction` order).
- **A spin-off does NOT explain a missing PARENT bar** — the parent keeps trading, so a spin-off is excluded from the stoppage-freeze's "terminal event explains it" set (only mergers/delist, which stop the name, are).
- **Mixed-merger basis carries fully to the stock leg** (documented simplification): total value conserved at the event; a tax-grade cash/stock basis allocation is a nicety a paper lab does not need and the design does not specify.

**Deferred (deliberately):** the corporate-action feeds for mergers/spin-off/delist are DORMANT at launch (D49), so none of this fires on live data in Phase 2 — production behaviour is freeze+alert until the feeds turn on (Phase 7 admin / Phase 8 fundamentals). The `SpinoffLiquidationDays > 0` execution (selling a receipt after N days) is a Stage-4 behaviour deferred to Phase 7; only the exit-only default (0) is executed. The §5.1 B&H acceptance + the OEF/EW/Threshold dummies are 2.9.

**Decision proposals (need a D-number):** **P5 recorded below** (drop `processed_on`). P1–P4 unchanged.

**Next session starts with:** checkpoint 2.8 — the FR-26/D50 regime-label service (M2: `regime_labels`, `regime_episodes`), pure `RegimeLabeler` (Core, trend × vol with hysteresis) + `RegimeLabelService` (Data), reusing the shared `PriceStatistics` vol (finding N) so D50's vol cannot drift from D43's σ. Fixtures FX-RegimeHysteresis + F-LEAK.

### 2026-07-17 — Phase 2 checkpoint 2.6: corporate-action ledger part 1 (FR-9 / §13.6)

**Dividend, split, ticker change, and the fail-closed stoppage freeze — the pure engine in Core plus the Data applier that reads actions at the watermark (D76). FX-Dividend / FX-Split / FX-Unmapped green. No schema, no migration, no config-key change. 373 → 403 tests; `ci.ps1` green.**

**Shipped.**
- **`CorporateActionLedger`** (Core, pure) — the §13.6 part-1 engine, arithmetic over a `Position`: **dividend** → cash on the ex-date (shares × unadjusted cash/share, D30), position unchanged; **split** → shares × ratio, TOTAL basis unchanged (so per-share basis ÷ ratio), equity unchanged, neither a trade nor a cash event; **ticker change** → a NON-EVENT for the ledger (D39 — identity is security_id; the alias moved at ingestion), returned as an explicit `TickerRenamedNoLedgerEffect`; **stoppage freeze** → the fail-closed decision (rule 10) for a held name with no bar and no event to explain it. Merger/spin-off/delist are refused by name (§13.6 part 2 / 2.7) so a dormant feed (D49) turning on cannot be silently dropped.
- **`CorporateActionApplier`** (Data) — reads a held security's actions via `ICorporateActionReadService.GetActionsAsOf` (D76, never raw table access), filters to those effective on asOf, runs the engine, persists via `ILedgerStore`. Then runs the stoppage check over the surviving book. Wired into DI; the pipeline calls it at 2.10 (the D53 "actions before the funnel" step).
- **Registered the two watermarked read paths in DI** (`IBarReadService`, `ICorporateActionReadService`) — Phase 2 is their first consumer, so it owns the binds.

**Seams resolved (each stated where it can be seen).**
- **Idempotency is the pipeline's, not the applier's** (finding-J consequence). There is no per-action "processed" flag — `corporate_actions.processed_on` is never written. A day's actions apply exactly once because the pipeline wraps the day in one transaction and `ux_runs_ok_forward` prevents a second forward run for the same as_of. The applier just applies today's actions; catch-up replays each missed day with its own asOf, so each day's actions land once.
- **Same-ex-date dividend-then-split order (finding S).** A dividend must be paid on the pre-split shares of record. `GetActionsAsOf` orders by `(effective_date, type)` and `'dividend' < 'split'`, so the dividend applies first; the per-action position re-read then lets the split see the post-dividend share count (correct — a dividend does not change shares). Made explicit in the applier rather than left to luck.
- **The stoppage freeze fires on a SINGLE missing session (finding R).** The honest fail-closed reading: no bar today ⇒ cannot value today ⇒ freeze rather than carry a stale price. Safe for the liquid S&P 100; a "N consecutive sessions" threshold would be a new CONFIG key + D-number, so not invented. Revisit before the sp500 widening (thinner tail → possible benign single-day gaps). A dividend/split does NOT clear the freeze; only a terminal event explains a missing bar.

**Belt-and-suspenders confirmed:** the closed set of 8 action types is enforced at the DB (`ck_corporate_actions_type`, finding 121), so an unknown kind can never be stored; the applier's own `ParseType` fail-closed is defence-in-depth for the case where the CHECK is extended by migration without updating the map.

**Findings (CHANGELOG v1.9.17):** **R** (single-session stoppage freeze — a documented seam), **S** (same-ex-date dividend-before-split ordering, resolved via the D76 read order).

**Deferred (deliberately):** mergers (cash/stock/mixed), spin-off, delist force-exit, and the three `processed_on` amendments are 2.7. The applier refuses a merger/spin-off/delist action loudly (proven by test) so the gap is fail-closed, not silent — and the feeds are dormant (D49), so none exist in live data.

**Decision proposals (need a D-number):** none new. P1–P4 unchanged. (Finding R's threshold is a *potential* future CONFIG key, folded into the P1/sp500-widening decision point rather than a separate proposal — decide the same time as the liquidity gate.)

**Next session starts with:** checkpoint 2.7 — corporate-action ledger part 2 (cash/stock/mixed mergers, spin-off receipt with basis allocation, delist force-exit + configurable bankruptcy haircut) plus the D74 membership-drop-≠-delist proof and the three `processed_on` amendments (rewrite the SCHEMA:72 comment, widen the CI guard to `corporate_actions`, log "drop the column" as a deferral).

### 2026-07-17 — Phase 2 checkpoint 2.5: the ExitPolicy executor + funnel Stages 4/6 + FunnelRunner (FR-8/FR-9)

**The six-stage funnel is now complete end-to-end, pure in Core. FX-ExitOnly and F-DET green. No schema, no migration, no config-key change. 344 → 373 tests; `ci.ps1` green.**

**Shipped (all pure, `AlphaLab.Core/Funnel`).**
- **`ExitPolicyExecutor`** — the ONLY thing in the system that may close a position on signal (rule 7). Three shapes execute (`Never`, `RankBuffer`, `ScheduledRebalance`); `TargetOrTimeStop`/`ChannelExit` refuse, naming their Phase-6 owners. An **unscored name is HELD, not closed** — "no rank" is the model saying nothing, and closing on a missing input would fail OPEN into an irreversible action (the inverse of rule 10); a name that stops being scorable forever is a delisting, which §13.6 owns.
- **`PortfolioPlanner`** (Stage 4) — the wish list opens/adds; only the ExitPolicy closes. A **universe exit is never a close** (D74): a name leaving the index drops out of Stage 1 but the position is untouched. A **frozen position is held untouched** and the policy is never consulted (its price is already declared untrustworthy).
- **`OrderBuilder`** (Stage 6) — decide at close T, fill at open T+1; shares = target notional ÷ T's **raw** close (D30); **fractional shares** (load-bearing — see finding below); trades the delta on a rebalance so a name already at target trades nothing.
- **`FunnelRunner`** — runs all six stages for one strategy-day, producing a `DecisionSnapshot`. Guards the IModel contract: a model that returns null, or scores a name outside the eligible pool it was handed, fails loudly (a point-in-time violation wearing a plausible face).
- **`OrderFill`** — the T+1 fill path: a stored Stage-6 order priced through the D43 broker into a `Trade`. Costs always on; the cap clips; a missing input rejects; a forced-event order is refused (it has its own §13.6 path). Pure — 2.10 wires it to the store.
- **`DecisionSnapshot`** — the stage-1..6 snapshot that becomes `decisions.stage_json`. Versioned (`ds-1.0`), round-trips byte-exactly, refuses an unknown shape.

**The orders-table question — ASKED AND DECIDED this session (records P4 below).** There is no `orders` table and there will not be one as currently designed; `decisions.stage_json` is the carrier, exactly as SCHEMA:207 designates it ("stages 1-6 snapshot"). The operator confirmed proceeding without one. Rationale recorded so the choice is visible, not implicit: an order here has no lifecycle (decided at close T, fully resolved at open T+1 — filled/clipped/rejected, never carried to T+2), so a table would be write-once/read-once and a **second copy** of a fact stage_json must hold anyway. **The free window to revisit runs until 2.10** (the first committed run): before it, a table is a migration with nothing to backfill; after, a backfill out of JSON. The property that actually matters is pinned as a test — the T+1 run fills from the STORED snapshot and never recomputes the funnel at its later watermark (which would let an overnight bar correction silently rewrite the day's orders).

**Findings (CHANGELOG v1.9.17).** **P** — "re-size the whole book every day" is the obvious Stage-4 default and §5.1's B&H acceptance disproves it; resolved by allowing price drift and re-sizing only on `ScheduledRebalance`'s cadence. **Q** — `stage_json` is a two-job artifact (provenance + T→T+1 carrier); the carrier job forces versioning, fail-closed parse, and a readable `SecurityId` (bare number, not `{"value":n}`).

**Seams resolved (stated where they can be seen).**
- **Sizing MODE is the strategy's own (`Config.Sizing`), not the system `SizingOptions.Mode`** — a dimension candidates differ on, exactly like the `SelectionRule`. `SizingOptions` supplies only the cap. (This is also why finding-E's `Sizing.Mode=equal` appsettings key "binds only ThresholdModel" — B&H's mode comes from its own config.)
- **Fractional shares are load-bearing, not lazy.** §5.1 requires B&H's total return to equal the proxy's minus one entry cost; whole-share rounding would strand an uninvested cash stub and force the criterion to carry a tolerance — and a tolerance is where a real bug hides. A paper lab has no lot-size constraint; `positions.shares` is REAL for this and because splits produce genuine fractions.
- **`ScheduledRebalance` on a rebalance day closing a dropped name is NOT a rule-7 violation.** Rule 7 forbids an *implicit* sell (the wish list closing behind the policy's back, daily). Here the POLICY is the authority and the wish list is its input; on the other 20 days of the month the same dropout does nothing.
- **The `FunnelNote` type** (renamed from 2.4's `Exclusion`) — Stage 4 records HOLDS as well as drops, and a type called `Exclusion` carrying "held: rank 5 is within the buffer" would lie. The exclusion semantics live in the property names, not the type.

**Deferred (deliberately):** Stage 2's per-strategy scorers are the dummies (2.9) and the real strategies (Phase 6); the runner calls `IModel.ScoreUniverseAsync` but no production model exists yet. The forced-event fills `OrderFill` explicitly excludes are 2.6/2.7. Persisting a snapshot + filling its orders against the live store is 2.10 (the pieces are pure and wired there).

**Decision proposals (need a D-number):** **P4 recorded below** — the orders-table question, decided in favour of `stage_json` with a revisit deadline of 2.10. P1/P2/P3 unchanged.

**Next session starts with:** checkpoint 2.6 — corporate-action ledger part 1 (dividend → cash on ex-date; split → shares ×ratio, raw basis ÷ratio, equity unchanged; ticker change → alias only; unmapped / bar-stoppage-without-event → freeze + alert). Reads actions via `ICorporateActionReadService.GetActionsAsOf` (D76), never raw table access.

### 2026-07-17 — Phase 2 checkpoint 2.4: `BarFeatureView` + funnel Stages 1/3/5 (FR-7/FR-8/FR-11 partial)

**The point-in-time feature port's real adapter, plus three of the six funnel stages. F-LEAK and FX-ZeroScore green. No schema, no migration, no config-key change. 297 → 344 tests; `ci.ps1` green.**

**Shipped.**
- **`BarFeatureView`** (Data) — the `IFeatureView` adapter over the versioned store. It contains **no point-in-time logic of its own**: every read goes through `IBarReadService`, whose watermark rule is already built and already tested, so there is deliberately no second place for that rule to drift. The `asOf` bound (which the reader doesn't know about) is enforced here and **throws** on a future date rather than returning null — null would be indistinguishable from "no data", and callers are specifically written to tolerate thin history (catalog §2), so a leak bug would be silently absorbed as exactly that.
- **`Eligibility`** (Stage 1), **`Selection`** (Stage 3), **`Sizing`** (Stage 5) — pure, in `AlphaLab.Core/Funnel`. Stage 1 takes the roster as data (Core cannot reference Data), which also makes the whole funnel testable against a hand-built feature view with no SQLite.
- **`PriceStatistics`** (Core, pure) — see finding N below.
- **One interface addition:** `IFeatureView.PricedOn(date)`, the date-major (D78) read behind Stage 1's "priced" check. Stage 1 asks "which of the ~101 members have a bar today?", which is one cross-section read, not 101 single-bar round trips — the shape D78 exists for. Declared in 2.1, first consumed here.

**Seams resolved (the spec didn't state these; each is now stated where it can be seen).**
- **"Priced" needs BOTH bases (D30)** — a raw close (the ledger's fill basis) and an adjusted close (signals'). One without the other cannot complete the funnel, so the name isn't eligible that day. A data fact, not a threshold.
- **The window is counted in CALENDAR sessions, not in available bars.** Counting bars would let a window reach across a long halt to collect its 21 values and call the result a 21-day average. Counting sessions means a halted name returns a short window → a null ADV → a refused order (rule 10). Pinned by `FR10_TheAdvWindowIsCountedInSessions_NotInAvailableBars`.
- **A partial ADV window is null, never a shorter average** — averaging fewer days can read high or low with no way to tell which, and the number feeds the participation cap.
- **The `Adv21*` naming.** The window is bound from `Costs.AdvWindowDays`; "21" is the system's vocabulary for "the ADV window", fixed by the `capacity_rejections.adv21` column. They agree at the default; the ctor parameter is what the window actually *is*.

**Findings (CHANGELOG v1.9.17).** **L** — Stage-1's `liquid` gate filters nothing → **proposal P1** (below); not built, but the no-op is now stated in the `Eligibility` doc comment *and pinned as a test*, so it is executable rather than a comment someone skims. **N** — the realized-vol convention has two consumers (D43's σ, D50's vol component) and no stated window/denominator; defined once in `PriceStatistics` so 2.8 cannot drift from it. **O** — ADV-in-shares is not split-invariant and there is no adjusted volume to read; documented as a bounded stop-and-report seam (`Adv21Notional` is provably immune).

**Boundaries stated, so an unbuilt thing isn't mistaken for a broken one.**
- **Guardrails in Phase 2 are exactly three:** `Guardrails.MinScore`, `Sizing.PositionCapPct`, `Guardrails.MaxConcurrentPositions` — the ones the funnel structurally needs to produce a portfolio at all. Heat (needs D42's covariance), regime halts, re-entry cooldown, and the drawdown breaker are **FR-17, Phase 7**. They are carried on `GuardrailsOptions` for CONFIG fidelity and are **not read** here.
- **FR-11 is partial.** `Sizing` executes `Equal` only and **refuses** `InverseVol`/`Kelly` by name, citing Phase 6 — never a silent fallback to equal weight, which would size every position by a rule the config doesn't claim while the run looked healthy (rule 10). The position cap **clamps and leaves cash**; it never redistributes, because redistributing pushes the remaining names back over the same cap. Note the default's implication: at `PositionCapPct = 0.05` an equal-weight book needs ≥ 20 names to be fully invested.

**Deferred (deliberately):** Stages 2/4/6 are 2.5 (Stage 2 is per-strategy scoring — the dummies land at 2.9). `decisions.stage_json` is not written yet; `Exclusion` reasons are shaped for it and 2.5 persists them.

**Decision proposals (need a D-number):** **P1, P2, P3 are now written down** in the proposals section below — previously they existed only in the plan and this session's transcript, which is the failure mode the v1.9.10 flake entry exists to prevent. P1 is 2.4's; P2 (the D76 batch-duplicate guard belongs at *ingestion*) and P3 (`features` needs a PIT column) are recorded now rather than at the checkpoint that surfaces them, precisely because forgetting them is the risk. **None is built.**

**Next session starts with:** checkpoint 2.5 — the ExitPolicy executor + Stages 4/6 (`PortfolioPlanner`, `OrderBuilder`, `FunnelRunner` → `decisions.stage_json`). Constrained by the 2.2 finding that `decisions` is structurally load-bearing: there is no `orders` table, so decide-at-close-T / fill-at-open-T+1 needs somewhere for T's orders to live until T+1's run.

### 2026-07-17 — Phase 2 checkpoint 2.3: the D43 cost model + VirtualBroker (FR-10)

**`CostModel` + `VirtualBroker` in `AlphaLab.Core` — pure, no I/O. FX-CostModel green. No schema, no migration, no config-key change. 271 → 297 tests; `ci.ps1` green.**

**Shipped (`src/AlphaLab.Core/Ledger/`).**
- **`CostModel`** — `commission + half-spread(bucket) + k·σ_daily·√(Q/ADV)`. Buckets keyed by 21d ADV **notional** (mega ≥$400M → 1bp, large ≥$100M → 2.5bp, other → 5bp, inclusive lower bounds); `ParticipationCapShares` = 2% of 21d ADV **shares**; every coefficient from `Costs:*`; `ModelVersion` stamped on every fill.
- **`VirtualBroker`** — prices one order and returns a closed `BrokerResult` (`Filled` | `Rejected`), so a caller cannot forget the rejection case. `MarketInputs` carries price/ADV-shares/ADV-notional/σ, all nullable.

**The units seam, resolved and now pinned by test.** D43 never states whether Q/ADV is shares or notional, and it must be **shares** or the ratio isn't dimensionless and the square-root law means nothing. Mixing them (Q in shares over ADV in dollars) yields a tiny ratio and near-zero impact on every name — costs that *look* modelled but aren't. So: **shares** for the cap and the √(Q/ADV) ratio, **notional** for the spread bucket only (a spread is a property of how much money trades per day), and `capacity_rejections.adv21` stores the shares figure. `FR10_Impact_IsSubLinear` pins the law itself (ratio is exactly √2, not 2) so a silent swap to a proportional model reddens.

**The cap clips, it does not reject the order.** D43 says the *excess quantity* is rejected — so a 3%-ADV order fills at 2% and the shortfall is surfaced as a `CapacityClip` destined for `capacity_rejections`. Costs are priced on the **allowed** size, not the intended one: you do not pay impact on shares you never traded, and pricing the intent would double-punish illiquid names for being capacity-constrained. The cap applies to **sells too** — a book that can't exit is a real risk, and capping only entries would model a fantasy.

**Fail closed on every missing risk input (F-CLOSED / rule 10).** Each of price / ADV-shares / ADV-notional / σ absent → `Rejected` with a named reason. The point is that a missing ADV must not become "assume unlimited liquidity" and a missing σ must not become "assume zero impact" — both would price the fill as **free**, which is precisely the error the cost model exists to make impossible. Note σ = 0 is a *legitimate* input (a name that didn't move has no impact to model but still crosses the spread), and is deliberately distinct from σ missing.

**Verified.** `ci.ps1` green — **297 tests** (+26, `CostModelTests`). Notable: `FR10_Impact_MatchesTheSquareRootLaw_To1e9` (the fixture's stated tolerance, on hand-checkable round numbers: Q/ADV=0.01 ⇒ fraction 0.0002 ⇒ $200 on $1M notional); `FR10_ParticipationCap_RejectsAndLogs`; `FR10_CostsAreAlwaysOn_ThereIsNoCostFreeFlagOnThisPath` — asserts *structurally* (by reflection) that no bool parameter exists on the broker, because hard rule 5's "costs always on" is only real if a cost-free number cannot reach a forward verdict by someone adding a convenience flag. The D36 cost-free population is display-only and will get its own construction in Phase 3.

**A deliberate non-behaviour, documented in test.** A vanishingly thin ADV yields a tiny-but-positive cap and fills proportionally rather than refusing. Adding a "minimum tradeable size" floor would mean inventing a CONFIG key that CONFIG_REFERENCE does not define. Real ADVs come from real volume and never look like this; if that ever changes it is a D-number, not a quiet constant. (The `cap allows nothing` branch is reachable only via a 0% cap — a misconfiguration — and refuses loudly.)

**Red / known-broken:** none.

**Deferred (deliberately):** the `Costs` section is **not** added to the Worker's appsettings yet — nothing reads it until the Worker constructs the broker in 2.10, and shipping config no code reads is the "no dead config" defect the v1.9.6 pass fixed. `CostsOptions`' defaults already mirror CONFIG_REFERENCE and are asserted (2.1). Corporate-action fills do **not** route through the broker: §13.6 waives standard costs on a merger cash-out/conversion ("a corporate action, not a trade") and such events are not capacity-capped — they happen to you regardless of size. The §13.6 ledger (2.6/2.7) builds those trades directly with zero costs; `VirtualBroker`'s doc comment says so.

**Decision proposals (need a D-number):** none new. P1/P2/P3 still queued.

**Operator actions surfaced:** unchanged from 2.2 — M1 is committed but **not applied**; run `pwsh tools/migrate.ps1 -Arena sp500`. The Worker refuses to launch until then (the finding-A fix working as designed).

**Next session starts with:** checkpoint 2.4 — `BarFeatureView` (Data, over `GetCrossSection`/`GetSeries`) + funnel Stages 1/3/5. Carries **P1**: Stage-1's `liquid` gate filters nothing, and the code comment must say so.

### 2026-07-17 — Phase 2 checkpoint 2.2: the ledger schema (M1) + persistence + the finding-A fail-fast

**The M1 `Phase2Ledger` migration (8 tables) + `LedgerStore`/`LedgerMapping`, and the `SchemaStartup` pending-migration fail-fast that M1 makes urgent. 258 → 271 tests; `ci.ps1` green. CHANGELOG v1.9.17 findings A–B. NOT YET APPLIED to the live arena — that is an operator action (see below).**

**Finding A — the hazard M1 creates, closed in the same checkpoint that creates it.** `SchemaStartup` called `MigrateAsync` on every Worker launch. That was harmless only because nothing was ever pending: `tools/migrate.ps1` had always migrated the store before the Worker saw it. **The moment M1 ships un-applied, an ordinary evening launch would migrate the live 79 MB store with no pre-migration snapshot** — what RUNBOOK §2 forbids and rule 14 exists to prevent. BUILD 0.4 specified this fail-fast "from Phase 1"; it was never shipped. It is now: `GetPendingMigrationsAsync()` → log the pending list, fail the host non-zero, name the sanctioned path. The test asserts the store is left with **zero user tables** — proving the non-action, not merely that an exception was thrown.
The **Backfill CLI** gets the matched treatment with one deliberate asymmetry: it may still **create** an absent store (no data to lose; `snapshot-db.ps1` explicitly no-ops on a fresh install; REBUILD §2's documented bootstrap depends on it) but **refuses to migrate an existing one**. Creating ≠ migrating.

**Shipped.**
- **M1 `Phase2Ledger`** — `strategies`, `accounts`, `positions`, `trades`, `capacity_rejections`, `cash_events`, `equity_curve`, `decisions`. Hand-edited per rule 14 on the **four** rowid PKs (`account_id`, `trade_id`, `event_id`, `decision_id`); `strategies` is a TEXT PK and three tables are composite, so neither needs it. **Exactly one CHECK** (`trades.side`) — SCHEMA declares none on `strategies.status`/`accounts.run_kind`/`cash_events.type`/`trades.reason`, and adding one would make the on-disk DDL diverge from the source of truth (the `Runs_Status_IsUnconstrained` precedent). All nine money columns explicitly `HasColumnType("TEXT")` (D69).
- **Core `Ledger/`** — `Account`, `Position`, `Trade`, `CashEvent` as pure records + `RunKind`/`TradeSide`/`TradeReason`/`CashEventType`. Note `RunKind` has **two** values where `runs.run_kind` has three: `live` and `catchup` both map to `Live`, because nothing downstream may treat a caught-up day as lesser evidence than a same-day one (hard rule 1). Only `replay` is quarantined.
- **`LedgerStore` + `LedgerMapping`** (Data) — the Row↔Core boundary. Every read takes a `RunKind` and filters on it; there is deliberately **no "read all kinds" overload**, so a forward view cannot return a replay row by construction rather than by the caller remembering. Opens accounts with their opening `Deposit` cash event so equity/cash reconcile from events + trades alone. Transactions stay the caller's (the D53 pipeline will wrap a whole day in one).
- **`decisions` built now** (no FR names it) — the funnel runs exactly once and its stage-1..6 snapshot is unrecoverable from `trades` afterwards. It also turns out to be **structurally load-bearing**: there is no `orders` table, so the decide-at-close-T / fill-at-open-T+1 split needs somewhere for T's orders to live until T+1. Recorded here because 2.5 will lean on it.

**Verified.** `ci.ps1` green — **271 tests** (+13: `LedgerStoreTests` 12, `SchemaStartupTests` 9 rewritten from 2). Migration proven against throwaway temp DBs only. Notable tests: `FR9_Money_RoundTripsExactly_AtCentAndSubCentScale` (0.1+0.1+0.1 = exactly 0.30 — the D69 point); `FR9_TradeSideCheckConstraint_RejectsAnInvalidToken_AtTheDb` (the CHECK is really on disk, not just in the model); `FR19_ReplayEquity_CannotOverwriteTheForwardCurve` (run_kind is IN equity_curve's PK, so the quarantine holds at key level); `FR9_CorpActionTrade_MustCarryItsActionId_AndOnlyACorpActionMay` (both directions — a forced close without its cause is unauditable, and an action_id on a signal trade is a miscategorized fill).

**Red / known-broken:** none.

**Deferred (deliberately):** `features` stays unbuilt and its absence is now asserted with its reason in `SchemaFidelityTests` — it has no `observed_at`/`version` column, so it **cannot express the watermark read rule** (rule 4); persisting a feature at watermark W and re-reading it at W′ is a leak F-LEAK could not catch. No Phase-2 strategy needs it. Also absent: `control_populations`/`control_equity` (Phase 3), `factor_*` (Phase 6), `journal_entries`/`admin_actions` (Phase 7), `regime_labels`/`regime_episodes` (2.8/M2), `ux_runs_ok_forward` (2.10/M3).

**Decision proposals (need a D-number):** none new. P1/P2/P3 remain queued for the checkpoints that surface them.

**Operator actions surfaced (not mine to do).**
1. **Apply M1 to the live arena when you're ready:** `pwsh tools/migrate.ps1 -Arena sp500` (snapshots first, then applies). It is additive — eight new empty tables, no existing row touched. Until then the Worker will **refuse to launch** with a message naming this exact command; that refusal is the finding-A fix working, not a regression.
2. **Off-machine backup — still not yet due, but the clock is now visible.** M1 only creates empty tables, so the store stays fully re-fetchable and the 2026-07-15 Ops-log reasoning holds. It expires at the **first committed run** (checkpoint 2.10), when `trades`/`decisions`/`equity_curve` first hold rows no provider returns.

**Next session starts with:** checkpoint 2.3 — the D43 cost model + VirtualBroker (pure, Core): buckets by 21d ADV notional, `k·σ·√(Q/ADV)` to 1e-9, the 2%-ADV participation cap with `capacity_rejections`, `cost_model_version` stamped. Fixture FX-CostModel.

### 2026-07-16 — Phase 2 checkpoint 2.1: Core domain contracts (FR-8/FR-11 partial)

**The first Phase-2 code. `AlphaLab.Core` gains the domain layer the whole phase builds on — pure, BCL-only, zero I/O. No schema, no migration, no config-key change. 237 → 258 tests; `ci.ps1` green.**

**Why Core and not Evaluation.** CLAUDE.md's layout puts the funnel and ledger in Core, but the CI-enforced reference graph gives Core zero project references. That forces ports-and-adapters: Core declares the contracts and holds the pure math; Data supplies the adapters. Two payoffs, both structural rather than a matter of discipline — the §13.6 ledger (the phase's highest-risk logic, 10 fixtures pointed at it) becomes a pure function testable with **zero SQLite**, and D76 compliance is enforced *by construction* (Core has no DbContext, so raw `corporate_actions` access is impossible, not merely discouraged). Same trade `DataQualityGate` already made in Phase 1.

**Shipped (`src/AlphaLab.Core/Domain/`, `src/AlphaLab.Core/Config/`).**
- **`SecurityId`** — a `readonly record struct` wrapper, not a bare `long`. `account_id`, `run_id`, and a ticker-derived int are all longs too; wrapping makes catalog §2's "keys are security_ids, never raw tickers" (rule 2) a **compile-time** fact. Conversion is explicit so a raw long can never bind implicitly. Deliberately *not* the same trade as money — see below.
- **`HoldingHorizon`** — `Days(n)` / `ToRankExit` / `ToNextRebalance`. `Days_` returns null for the latter two, which is why `strategies.holding_horizon_days` is nullable; coercing them to 0 would read as "hold zero days".
- **`ExitPolicy`** — all five catalog §2 shapes, `[JsonPolymorphic]` with a `kind` discriminator (the `exit_policy_json` contract). **Declare 5, execute 3**: `Never`/`RankBuffer`/`ScheduledRebalance` are what Phase 2's dummies need; `TargetOrTimeStop` and `ChannelExit` are declared so a Phase-6 strategy row still round-trips, but the executor will refuse them. `TargetOrTimeStop.exitCondition` is **genuinely unspecified in the catalog** (§2 names the shape, never defines the reversion condition) — guessing would silently invent strategy behaviour, so refusing is the fail-closed answer (rule 10). An unknown `kind` throws rather than defaulting to `Never`, which would turn an unrecognized rule into a position that never exits.
- **`SelectionRule` / `SelectionMode`** — catalog §3 params (TopN 40; Threshold minScore 0.60, maxConcurrent 60). The zero-score invariant is enforced in Stage 3, not here; this only carries parameters.
- **`SizingMode`** — `Equal | InverseVol | Kelly`, mirroring the CONFIG surface. Only `Equal` will execute (FR-11 partial, finding 169).
- **`StrategyConfig`** — immutable, `Seed` required (the F-DET determinism contract), `Params` a bag (CONFIG key rule 1: per-strategy params live in `config_json`, never appsettings), `Param()` **throws** on a miss (D17: frozen params mean a missing lookback is a config error, never a default). Carries D52's `Unregistered` flag from the start, defaulting false, so a candidate is only ever branded deliberately.
- **`IFeatureView`** — the PIT port (rule 4). **Primitives only** (`SecurityId`/`DateOnly`/`double`), which is precisely what lets it sit beside `IModel` in Core; had it exposed `BarRow` it would have to move to Data, where `IModel` could not follow without breaking Strategies. Implementations resolve through the versioned bar reader, so the watermark rule has exactly one home and cannot drift. Splits raw/adjusted per D30 (signals on adjusted, ledger on raw) and ADV into **shares** (cap + the dimensionless √(Q/ADV)) vs **notional** (the spread bucket only) — the unit split D43 needs but never states.
- **`IModel`** — catalog §2 verbatim.
- **`CostsOptions` / `SizingOptions` / `GuardrailsOptions`** — defaults mirroring CONFIG_REFERENCE exactly, asserted by test. `CommissionPerTrade` is `decimal` (D69), asserted by reflection so a future edit to `double` reddens rather than silently reintroducing float error on the number the whole falsification rests on.

**Money is plain `decimal`, no wrapper — the deliberate asymmetry with `SecurityId`.** D69 already gives exactness, so a `Money` struct would buy zero correctness while costing an EF value-conversion layer and a JSON converter. The `SecurityId` wrapper buys type-separation between three interchangeable longs; a money wrapper buys nothing decimal doesn't. "Determinism beats elegance; boring explicit code over cleverness" points one way here.

**Verified.** `ci.ps1` green — build, vuln audit (the two known non-blocking NU1903s), tests, and all guards (bars greps, secrets scan, reference graph, Web-must-not-reference-Data). **258 tests** (+21: `ExitPolicyTests` 9, `DomainContractTests` 12). `AlphaLab.Core` still has zero package deps and zero project references. Notable test: `FR8_ExitPolicy_HierarchyIsClosed_SoStage4SwitchIsExhaustive` asserts by reflection that no `ExitPolicy` subtype exists outside the closed hierarchy — Stage 4's switch would otherwise silently fall through to "no exit" for a sixth shape.

**Red / known-broken:** none.

**Deferred (deliberately):** everything else in the phase — checkpoints 2.2–2.12 per the approved plan. `AlphaLab.Evaluation` stays empty (metrics/read-models are Phase 3); `ScreenReadModels.cs` untouched (row shapes are Phase 3's).

**Decision proposals (need a D-number):** none new from 2.1. Three are queued for the phase and land with the checkpoints that surface them — P1 (Stage-1 `liquid` filters nothing), P2 (the D76 batch-duplicate guard belongs at ingestion), P3 (a Phase-6 feature cache needs `features` to gain a PIT column).

**Next session starts with:** checkpoint 2.2 — the M1 ledger migration (8 tables) + `LedgerStore`/`LedgerMapping`, and the `SchemaStartup` pending-migration fail-fast (finding A — the live rule-14 hazard M1 creates).

### 2026-07-16 — v1.9.16 FR-11 sizing phasing (resolves finding 168, docs only)

**Resolves the finding-168 report from v1.9.15 (finding 169). FR-11 (inverse-vol sizing using Ledoit–Wolf covariance, D42) was claimed by both Phase 2 and Phase 6. Docs only — no schema/migration/config-key/test change; count stays 237. `ci.ps1` green.**

**Shipped (finding 169).**
- **FR-11 split partial→full (the FR-13/FR-18 convention).** Phase 2 → `FR-7..10, 11 (partial)` (heading `11 partial`), with a prompt clause that Phase-2 sizing runs only the simple modes (the dummies' cap/equal weights, `Sizing.Mode=equal`). Phase 6 → `FR-5, 11 full, 13, 15, 18 full`, and the Phase-6 LW-covariance-service line now reads as completing FR-11's inverse-vol sizing. Grounded in the `Sizing.Mode = inverse_vol | equal(dummies) | kelly(P6+)` config and DESIGN_IMPROVEMENTS §3.1 (LW covariance consumers — inverse-vol sizing, heat guardrail, momentum vol overlay — are all Phase 6). BUILD-only; D42 unchanged, no new D-number.

**Verified.** `ci.ps1` green (237, unchanged — docs only).

**Red / known-broken:** none.

**Deferred (deliberately):** the S&P 500 widening (before Phase-4 sign-off); FR-23 hypotheses (before Phase 5); the Phase-4 detection-power sweep (D64 territory). The D42 Phase-2/6 phasing question is now closed (finding 169).

**Decision proposals (need a D-number):** none new — finding 169 is a BUILD-phasing edit, not a decision.

**Next session starts with:** Phase 2 — the six-stage funnel + ledger + costs + the D53 staged pipeline.

### 2026-07-16 — v1.9.15 Phase-0/1 BUILD/CONFIG reconciliation (docs only)

**A "find inconsistencies in Phase 0 and Phase 1" review of BUILD_AND_PROMPTS §4, cross-checked against SCHEMA/CONFIG/INTEGRATIONS/TEST_PLAN/MASTER. Four genuine BUILD/CONFIG/TEST_PLAN inconsistencies fixed (findings 164–167), one reported for a phasing decision (168). Docs only — no schema/migration/config-key/test change; count stays 237. `ci.ps1` green.**

**Shipped (findings 164–167).**
- **164 — Phase-1 heading dropped FR-38.** BUILD §4 Phase-1 heading `(FR-1..6, 30)` → `(FR-1..6, 30, 38)`, agreeing with the §2 table row, the §0 changelog, and Phase-1 item (h). Only Phase 1's heading was out; Phases 2/3/4/7 match their table rows.
- **165 — stale ⚠VERIFY on GSPC index EOD.** BUILD FR-38 + Phase-1 (h) `(⚠VERIFY index EOD on the launch tier)` → `(index EOD verified 2026-07-13 on the launch tier)`, matching INTEGRATIONS §9 (VERIFIED 2026-07-13, 200, resolved). INTEGRATIONS's still-open ⚠VERIFY items (fundamentals, batch-API headers) untouched.
- **166 — three-vs-four connection-string spots (finding-138 straggler).** Phase-aware: CONFIG's standing rule → FOUR-SPOTS RULE (names the Backfill CLI); BUILD 0.3 keeps "three at Phase 0" but forward-references the Phase-1 fourth spot and stops citing DB_RELOCATION (a four-spot doc) as a three-spot rule; BUILD's Phase-1 prompt gains the sentence that the CLI is the 4th spot + `ConfigConsistencyTests` guards all four; TEST_PLAN §8 keeps "three" (correct for the Phase-0 inventory) + a phase qualifier. Dated provenance (MANIFEST/MASTER/PROGRESS/CHANGELOG) left intact.
- **167 — cost model misdated.** BUILD §0 "already live from Phase 1 (D43)" → "from Phase 2 (D43)"; D43/FR-10 is a Phase-2 deliverable.

**Reported, not fixed (168).** D42 Ledoit–Wolf covariance is claimed by both Phase 2 (FR-11 in its Key-FR set) and Phase 6 (BUILD §0 + the Phase-6 prompt implement the LW covariance service). Likely the FR-13/FR-18 "partial early, full later" convention, unstated. A BUILD-phasing call — see the decision-proposals block.

**Verified.** `ci.ps1` green (**237**, unchanged — docs only). Verified-clean and deliberately left: the 15 §21 read-models (13 non-parameterized + 2 parameterized), the five infra tables / four CHECKs / composite `config` PK, the 39-case Phase-0 test split, ports 5230/5210, count-sanity 495–510 / 99–103, the ≈3.8y regime warm-up. The MASTER §12 EODHD-primary membership row is the documented D49 target-vs-launch inversion, not a defect.

**Red / known-broken:** none.

**Deferred (deliberately):** the S&P 500 widening (before Phase-4 sign-off) and the new D42 phasing question (finding 168).

**Decision proposals (need a D-number):** none new — finding 168 (D42 Phase-2/6) is a BUILD-phasing edit, not a new decision. The S&P 500 widening stays open.

**Next session starts with:** Phase 2 — the six-stage funnel + ledger + costs + the D53 staged pipeline.

### 2026-07-15 — v1.9.14 membership provenance (contract-only)

**Resolves the membership-provenance parked proposal — contract + archival semantics only, no schema, no migration, no D-number (a finding-level fix mirroring P1R-4, exactly as the proposal framed it). Finding 163. Only the S&P 500 widening now stays open.**

**Shipped.**
- **C7 (finding 163) — membership rosters archive under the observation day, not `"latest"`.** The two membership providers (`ISharesHoldingsMembershipProvider` for IVV/OEF, `WikipediaMembershipCrossCheck`) saved their raw payload under a literal `"latest"` that overwrote every run, so "what did OEF / Wikipedia report on date X" was unanswerable (`GetMembersAsync` had no as-of; `index_membership` has no `observed_at`; the raw archive was the only possible provenance). Threaded an observation-date `asOf` into `IIndexMembershipProvider.GetMembersAsync` and archive under `{source}/{asOf}/…` (dated partitions), mirroring the P1R-4 equity/proxy fix. Callers pass the run's `AsOf` (`BackfillRunner` ×2, `BackfillPreflight`); the dormant `EodhdIndexMembershipProvider` and the `BackfillRunnerTests` stub match the new signature. Test: a second case in `RawCacheArchivalTests` asserts both rosters land under `{source}/{asOf}/` and nothing under `latest` (+1).
- **Straggler catch (from the v1.9.13 pass):** the MANIFEST title/body + CHANGELOG-coverage line were still v1.9.12, and the root `README.md` test count still read 223 — rolled current (title → v1.9.14, count → 237).

**Verified.** `ci.ps1` green (**237** total; Data 215). No schema/migration touched; the `UPDATE/DELETE bars` greps are irrelevant here.

**Red / known-broken:** none. The v1.9.10 `TestDb.ClearAllPools()` parallel-test flake is unchanged (harness-level).

**Deferred (deliberately):** the **S&P 500 widening** is now the only remaining open proposal (decide before Phase-4 sign-off).

**Decision proposals (need a D-number):** none new. Membership provenance is now resolved (finding 163); the S&P 500 widening stays open.

**Operator actions surfaced:** unchanged from v1.9.13 — (1) apply the three v1.9.13 migrations to the live arena (`pwsh tools/migrate.ps1 -Arena sp500`, proven on a copy); (2) the three v1.9.10 Part-C items (off-machine backup + restore drill; off-machine raw-cache before prune; LICENSE).

**Next session starts with:** Phase 2 — the six-stage funnel + ledger + costs + the D53 staged pipeline.

### 2026-07-15 — v1.9.13 pre-Phase-2 schema decisions (D76–D78)

**The first schema change since Phase 1 (SCHEMA last moved in v1.9.7). Three of the five parked "D76-territory" proposals — the ones Phase 2's ledger + funnel build directly on — are DECIDED and their schema/read seams landed, so the foundation isn't re-shaped mid-build.** Each: a snapshot-first EF migration (rule 14), SCHEMA in the same commit, a rule-4 leakage/round-trip test, a MASTER §2 row. Decisions **D76–D78**; decided range now **D1–D78**. Findings **160–162**. Test count **223 → 236** (Data 214). `ci.ps1` green.

**Shipped.**
- **C1 / D76 (finding 160) — `corporate_actions` versioned append-only + read-at-watermark (the significant one).** `observed_at` was written but read by nothing; ingestion was existence-skip (first-write-wins), so a dividend restatement was silently dropped and **Phase-4 replay would leak the future** (pricing a dividend from an action observed years after the watermark, breaking NFR1 — the property D40 buys for bars). Extended D40 to CAs: a `version` column; ingestion → value-diff append (mirrors `BarIngestionService` — identical re-fetch is a no-op, a changed value appends `version+1`); a new `ICorporateActionReadService.GetActionsAsOf` (latest version ≤ watermark per `(type, effective_date)`); `ix_corporate_actions_observed` + a unique `ux_corporate_actions_identity(security_id, type, effective_date, version)` (ex_date excluded — SQLite NULL-distinct would break split dedupe). §13.6 gains the read rule. Migration `CorporateActionVersioning` (no hand-edit). `CorporateActionVersioningTests` (+5, F-LEAK). **Known limitation:** a regular + special dividend on one ex-date collapse to one line (stop-and-report; any discriminator must be feed-supplied, not amount-based).
- **C2 / D77 (finding 161) — `data_quality_flags` table + store seam.** The FR-6 gate computed Warn flags with nowhere to persist and no Data-health surface. New `data_quality_flags` (bare `flag_id` PK — rule-14 hand-edit — `run_id`, nullable `security_id`, `symbol`, nullable `date`, CHECK'd `issue`+`severity`, `detail`, `observed_at`) + `IDataQualityFlagStore` (`Save`/`GetForRun`, persists warn+reject); §15 Data-health "flags" slot. Migration `DataQualityFlags`. **Foundation only** — gate→store wiring is Phase 2, the read-model/screen is Phase 7. `DataQualityFlagStoreTests` (+4).
- **C3 / D78 (finding 162) — cross-sectional bar read + `ix_bars_date`.** `IBarReadService.GetCrossSection(date, watermark)` (every security's latest visible version on one date) + `ix_bars_date`; without it a date-major read full-scans (~488k rows now, ~5M at sp500). Migration `BarsDateIndex` (index-only). Additive-ahead-of-need. `BarCrossSectionTests` (+4: pushdown, byte-identity, `EXPLAIN QUERY PLAN` proves the index serves it).
- **Existing tests updated:** the two "14 tables" assertions → 15 (`SchemaFidelityTests.Schema_ExactlyTheFifteenTables_Exist`, `SchemaStartupTests.…CreatesAllFifteenTables`); `data_quality_flags` added to the no-autoincrement guard list; the `CorporateActionIngestionTests` idempotency doc-comment notes the value-diff mechanism (assertions unchanged — identical re-fetch stays a no-op).
- **Docs:** SCHEMA (3 tables + enforcement bullets); MASTER §2 (D76–D78) + §13.6 (CA read rule) + §15 (flags slot) + §version-note; CHANGELOG v1.9.13; the D1–D75 → **D1–D78** roll (CLAUDE, README, MANIFEST, README_v1.9); the widening's premature "(D76)" citations de-numbered to "an open proposal" (incl. the `--universe sp500` error string).

**Verified.** `ci.ps1` green (**236** total; Data 214). The three migrations applied cleanly to a throwaway **copy** of the live arena DB (`E:\AlphaLabDatabase\sp500`, 79 MB / ~488k bars / 6,273 CAs) — proving the `version` default backfills existing rows and the unique index finds **no** pre-existing `(security_id, type, effective_date)` duplicates (the old existence-skip held). The live DB itself was **not** touched.

**Red / known-broken:** none. The v1.9.10 `TestDb.ClearAllPools()` parallel-test flake is unchanged (harness-level).

**Deferred (deliberately):** the two remaining parked proposals — the **S&P 500 widening** (decide before Phase-4 sign-off) and **membership provenance** (contract-only, no schema) — stay open, now un-numbered. The C1 same-ex-date multi-dividend collapse is a documented limitation, not built.

**Decision proposals (need a D-number):** none new. Three of the five prior proposals are now **decided** (D76/D77/D78 — marked in the proposals section below); two remain open.

**Operator actions surfaced:** (1) **apply the three migrations to the live arena** — `pwsh tools/migrate.ps1 -Arena sp500` (snapshots first, then applies `CorporateActionVersioning` → `DataQualityFlags` → `BarsDateIndex`); proven safe on a copy, but it mutates the 488k-bar store, so it is the operator's call. (2) The three v1.9.10 Part-C items still stand (off-machine DB backup + restore drill; off-machine `tools/raw-cache/` before prune; LICENSE).

**Next session starts with:** Phase 2 — the six-stage funnel + ledger + costs + the D53 staged pipeline, now building on the D76 CA read seam, the D77 flag store, and the D78 cross-section read.

### 2026-07-15 — v1.9.12 doc/config reconciliation (docs + CONFIG_REFERENCE only)

**A consistency-audit pass triggered by a full-repo review — reconciling documentation drift and one config-reference-vs-code gap, with no schema/migration/config-key change (rule 14). Findings 153–159. Everything is on `origin`.** The audit also *confirmed clean* the parts that matter most: SCHEMA ↔ EF entities, INTEGRATIONS ↔ providers, and the D/FR/rule numbering (D1–D75 decided, D76 proposal-only, FR-1..38, rules 1–24) — the drift was confined to hand-maintained narratives, two section refs, one test count, a push-state note, and the undocumented Backfill/Eodhd config keys.

**Shipped.**
- **Version-narrative roll (finding 153).** The running errata narrative had drifted: `docs/README_v1.9.md` stopped at v1.9.8, and MASTER §version-note + `START_HERE.md` + `MANIFEST.md`'s Revision-state stopped at v1.9.9 — while HEAD was v1.9.11. Rolled all four (the standard MASTER §version-note / START_HERE / MANIFEST / README roll set, per the v1.9.9 entry) to v1.9.12. MANIFEST's title (v1.9.7) + body were rolled too — superseding finding 152's "deliberately not rolled" note now that this pass touches the file anyway.
- **Index completeness (finding 154).** `docs/README_v1.9.md`'s doc tables were missing `REBUILD.md` (added in v1.9.11) and `DESIGN_IMPROVEMENTS_EXPLAINED.md` (the finding-134 orphan, mapped everywhere except here) — both added. Root `README.md` added to MANIFEST's file list.
- **Stale numeric (finding 155).** Root `README.md` said "200 tests" (the v1.9.9 figure); rolled to 223 to match this ledger.
- **Wrong section refs (finding 156).** Line-number-as-section errors — in the proposals block "MASTER §391" → **§13.5** (the D40 bar-watermark read rule) and "MASTER §442" → **§15** (the GUI Data-health screen, all three occurrences); in a v1.9.9 session entry "MASTER §266" → **§6** (the daily-funnel ExitPolicy bullet).
- **Push-state reconciliation (finding 157).** The v1.9.11 "Last session" line said Checkpoints 1–4 were "held for review, not pushed" and listed "push Checkpoints 1–4" as an open operator action — but `origin/main` == `HEAD` (`bbda55f`). Corrected the current-state summary; the v1.9.11 operator action is done. (The historical v1.9.11 entry below is left intact — it truthfully records that session's end-state intent.)
- **CONFIG_REFERENCE vs code (findings 158–159).** The Backfill CLI consumes an `Eodhd` section (`BaseUrl`, `ExchangeSuffix`) and a `Backfill` section (`BackfillYears`, `ApiPlanLimit`, `RawCacheRoot`, `WikipediaSp100Url`, `HistoricalMembershipUrl`) that appeared in **no** CONFIG_REFERENCE section, though the doc declares itself the only source of truth for config keys — documented in a new `tools/Backfill/appsettings.json` subsection, and `BackfillYears` moved out of the `Data` block (the live key is `Backfill:BackfillYears`, not `Data:BackfillYears`). Added a caveat that the `Calendar`/`Regime`/`Data`(quality) Options declare a `SectionName` but are currently registered with default instances (a value placed in those sections is ignored until the consuming phase wires the bind).

**Red / known-broken:** none. Docs + one doc file only; no code touched; `ci.ps1` green (223, unchanged). The v1.9.10 `TestDb.ClearAllPools()` parallel-test flake is unchanged (harness-level; see the v1.9.10 entry).

**Deferred (deliberately):** finding 151's D70-widening `CONFIG_REFERENCE` claim ("consumed by the backfill CLI") is **left unchanged** — correcting it is itself the **D76** decision (option (b)), so this pass documents the *real* keys around it without pre-empting the decision.

**Decision proposals (need a D-number):** none new — the five existing D76-territory proposals stand (corporate_actions reproducibility; `data_quality_flags`; cross-sectional bar read path; membership provenance; D70 widening).

**Operator actions surfaced:** the v1.9.11 "push Checkpoints 1–4" is **done** (all on `origin`). The three v1.9.10 Part-C items still stand (off-machine DB backup + restore drill; off-machine `tools/raw-cache/` before prune goes live; LICENSE / all-rights-reserved).

**Next session starts with:** Phase 2 — decide the D40/`corporate_actions` proposal first (the ledger prices on that feed).

### 2026-07-15 — v1.9.11 Phase-1 review remediation, cont. (Checkpoints 0, P1R-9..11 + REBUILD)

**A third fresh-eyes pass, asking one question: can a stranger clone this repo on any future date and build their own arena?** One live doc error + three code/hygiene gaps + registering & reconciling the `REBUILD.md` runbook, all with no schema/migration/config-key change (rule 14). Findings **147–152**; the S&P 500 widening gap parked for **D76**. Checkpoint 0 was committed **and pushed alone** (a live error on `origin`); Checkpoints 1–4 are committed to `main` and **held for review — not pushed** (operator's call).

**Shipped.**
- **Checkpoint 0 (finding 147) — REBUILD §5 arena-id live error, pushed alone.** §5 told a cloner the default arena is "named for a universe it doesn't hold" and to "Set your own `Arena.Id`." Per D70 both are wrong: `sp500` **is** correct (the S&P 500 lab, holding the 100-name launch slice, widening *in place* after Phase-4). Renaming strands the store; setting it in one file splits it. Replaced the paragraph (change `Arena.Id` **only** to run a *separate* arena, then in all three appsettings; link DB_RELOCATION for the mechanics). Docs only; committed + pushed ahead of the rest because it was wrong on origin.
- **P1R-9 (finding 148) — guard `Arena:Id` agreement.** The connection-string guard proved the four *template* copies match, but the template holds `{Arena.Id}`, filled per-process from each appsettings' `Arena:Id` — so byte-identical strings can open three different DBs (finding 138's other half). Extracted `AssertArenaIdsAgree(paths)`: a positive `[Fact]` locks the three real backend appsettings; a negative `[Fact]` drives the **same method** with a mismatched temp pair and asserts it throws — not a LINQ tautology. Honest note: the positive lock can't fail on current (correct) config; the failing-on-broken-guard negative is at the method level. Web/wwwroot's plural `Arenas` registry untouched. DB_RELOCATION §1 note added.
- **P1R-10 (finding 149) — reject `--universe sp500` at parse.** The flag set band `[495,510]` but the CLI's providers are hardcoded OEF + Wikipedia S&P 100, so sp500 fetched ~101 names and fail-closed at the count gate ~300 calls in — a data-shaped error for unwired code. The switch arm now throws at parse naming the cause + the D76 proposal; exits in the first second. `Parse_Sp500_WidensCountBand` → `Parse_Sp500_Rejected` (fails on pre-fix code). IVV not wired (that is D76).
- **P1R-11 (finding 150) — `--preflight` live-source check.** Between `--dry-run` (zero network) and SETUP §7 (a manual ritual) there was nothing, and fixture-backed tests keep `ci.ps1` green while a live source drifts (the first backfill already hit a Wikimedia 403, finding 130). New `BackfillPreflight` makes one **read-only** pass — DB-path writability (write-free ancestor probe), OEF + Wikipedia count-band, `/eod/AAPL.US` adjusted_close, `/div/AAPL.US` unadjustedValue (finding 139's throw → a named Fail *first*), `/eod/GSPC.INDX` index shape, calendar-age **warn** — no DB, no writes (NullRawCache), non-zero exit on any Fail, exercising the same providers a live run uses. `NyseCalendar.SpecialClosuresReviewedThroughYear = 2025` is a documentary const (mirrors `FileRawCache.RetentionDays`), **not** a config key. Probe windows asymmetric on purpose (eod/GSPC short shape-check; div wide = `o.From`, where a null unadjustedValue lives).
- **REBUILD registration + reconcile (findings 151–152).** Registered `docs/REBUILD.md` in MANIFEST + CLAUDE (finding-134 class; manifest version not rolled). Reconciled §2/§5: `--preflight` as §2's first live check; sp500 bullet rewritten (rejected at parse, widening = D76); "no command performs it" + the calendar bullet point at `--preflight`. Also corrected CLAUDE's DB_RELOCATION line "three → four edit spots + Arena:Id" (a v1.9.10 finding-138 straggler).

**Verified.** Data tests **201** (was 190, +11: P1R-9 +2, P1R-10 +0 repurposed, P1R-11 +9); `tools/ci.ps1` green (**223** total, was 212). The `--preflight` negatives (out-of-band count, missing adjusted_close, missing unadjustedValue, parent-is-a-file DB path) each fail on drifted fixtures; the DB-path check and preflight branch write nothing (NullRawCache; branch precedes Resolve/Migrate). Build clean (0 errors).

**Red / known-broken:** none new. The v1.9.10 `TestDb.Delete → ClearAllPools()` parallel-test flake is unchanged and still the first thing to check on a random Actions red (see the v1.9.10 entry) — the P1R-11 test classes add more parallel collections, so the odds tick up, but the mechanism and verdict (pre-existing, harness-level) are the same; not attributable to these fixes.

**Deferred (deliberately):** the D70-widening gap (finding 151) is **reported, not built** — a MASTER §2 / CONFIG_REFERENCE decision (D76 territory; proposal below). The `--preflight` splits probe was intentionally omitted (the operator's enumerated shape checks; splits share the already-exercised `ParseSplitRatio` path).

**Decision proposals (need a D-number):** one appended below — D70's widening is not achievable as built (dead `UniverseOptions`, a false `CONFIG_REFERENCE` claim, hardcoded providers); decide **before Phase 4 sign-off**. It joins the four v1.9.10 proposals still awaiting D76.

**Operator actions surfaced (not mine to do):** (1) **push Checkpoints 1–4** after review (Checkpoint 0 is already on origin). (2) The three v1.9.10 Part-C items still stand (off-machine DB backup + restore drill; off-machine `tools/raw-cache/` before prune goes live; LICENSE / all-rights-reserved). (3) Run `--preflight` before the next live backfill now that it exists.

**Next session starts with:** Phase 2 — the six-stage funnel + ledger + costs + the D53 staged pipeline in AlphaLab.Worker (D59). Decide the D40/`corporate_actions` proposal first (the ledger prices on that feed); the D70-widening proposal can wait until Phase-4 sign-off approaches.

### 2026-07-15 — v1.9.10 Phase-1 review remediation (checkpoints P1R-8, P1R-1..7)

**A second fresh-eyes review of the sealed Phase-1 repo — fixing fail-**open** code defects (rule-10 violations) plus audit-trail, scalability, config-consistency, and CI-hygiene gaps, with no schema/migration/config-key change (rule 14 + snapshot-first).** Findings **138–146**; three schema-change proposals parked for **D76**. Pushed at the P1R-3 review gate and again at the end.

**Shipped (this gate).**
- **P1R-8 (finding 138) — the unguarded 4th DB-path copy.** `ConfigConsistencyTests` guarded only the Worker + Api `appsettings.json` ("three edit spots"), but `tools/Backfill/appsettings.json` (added in checkpoint 1.10, path under `tools/` not `src/`) was a fourth, unchecked copy. A fresh clone following README §2 repoints the three guarded spots to `{LocalAppData}` and leaves the CLI on `E:\` → the backfill writes a full DB the Worker/Api never see, no error anywhere. The Theory now takes a subdir and guards all three appsettings copies against the resolver const; "three → four" corrected in the test doc-comment, `DbPathResolver`, README §2, DB_RELOCATION §2/§3/§5.
- **P1R-1 (finding 139) — dividend cash fails closed on a null `unadjustedValue`.** `CorporateActionIngestion` wrote `CashPerShare = d.UnadjustedValue ?? d.Value`; the fallback writes the split-adjusted value (112× wrong on the oldest AAPL fixture row) into the field the Phase-2 ledger prices on. **Audit-first (let the count pick the design):** a read-only `tools/audit-dividend-unadjusted.ps1` re-parsed the 101 archived `/div` payloads — **6,204 events, 0 null** (matches the live DB's dividend-row count), so the throw is safe. `ParseDividends(symbol, json)` throws `FormatException` naming symbol + ex-date (parse boundary, mirroring `ParseSplitRatio`); ingestion's `?? d.Value` → a defensive `?? throw` for the non-provider path.
- **P1R-2 (finding 140) — `api_usage_log` accumulates; headroom checks the day total.** `Record` overwrote spend and `FlushApiUsage` checked the run-local `_apiCalls`, so two same-day runs of 2 against a limit of 4 both passed while the DB (correctly) held 4 — the row was truthful and nothing read it. `Record` now accumulates; `FlushApiUsage` persists, **drains** the counters (a second flush adds zero), then checks headroom against the accumulated EODHD-family **day total from the DB**. The ≥50% arithmetic + the `eodhd`+`eodhd_gspc` grouping are unchanged.
- **P1R-3 (finding 141) — `GetSeries` pushes the date range into SQL.** It materialized a name's whole ~4,800-row 20-year history to return a slice; the `[from,to]` range now lives in the SQL WHERE (`string.Compare(..) >= 0/<= 0`, EF-translatable — not `CompareOrdinal`, which `SecurityMaster.ResolveAsOf` warns may not translate), served by PK `(security_id, date, version)`. SQLite's BINARY collation is ordinal, so ISO-8601 lexical == chronological. One test asserts the WHERE contains the `date` predicate (no client eval); a Theory asserts byte-identical results vs the old in-memory path across the FX-BarCorrection fixture + both watermark boundaries.
- **P1R-4 (findings 142–143) — raw payloads archive under the observation day, not the query bound.** `/eod` filed under `to`, `/div` + `/splits` under `from` (so the live run filed every dividend/split payload under `2006-07-15/`, 20y off), the GSPC proxy under `to`. An explicit `asOf` is now threaded through **both** contracts (`IMarketDataProvider` × 3 + `IRegimeProxyProvider`) and all four archive under it; a test with `to ≠ asOf` proves `asOf` (not `to`/`from`) drives partitioning. **Stop-and-report (143, no code change):** the two membership `Save` sites archive under literal `"latest"` and overwrite each run — `GetMembersAsync` has no as-of and `index_membership` has no `observed_at`, so that archive is the only possible membership provenance; recommendation recorded in the proposals below.
- **P1R-5 (finding 144) — the documented 30-day raw-cache retention now exists.** `FileRawCache.Prune(utcNow)` deletes files older than `RetentionDays = 30` (a fixed constant, not a config key) by mtime, then sweeps now-empty partition dirs deepest-first (never the root); confined to the root by construction, fails closed on a null/empty root; the CLI invokes it after a successful run. **Flagged (not decided):** the retention will eventually delete the bootstrap payloads — the only artifact behind the P1R-1 audit + an honest rebuild; whether the bootstrap set is exempt is open (see Ops/Part-C: off-machine the cache before prune goes live).
- **P1R-6 (finding 145) — SCHEMA records that the 13 `REFERENCES` clauses are documentary** (no FK constraints, no shadow indexes, `PRAGMA foreign_keys` off) — a one-bullet §Enforcement-notes edit so the DDL stays a 1:1 description of the shipped migration. Docs only.
- **P1R-7 (finding 146) — CI is now enforced + a vuln report exists.** `.github/workflows/ci.yml` runs `tools/ci.ps1` via `pwsh` on push + PR (windows-latest, setup-dotnet 10.0.x). `ci.ps1` gains a **report-only** `dotnet list package --vulnerable --include-transitive` block after build (confirmed: reports both NU1903 advisories, CI OK still prints), commented to become a hard gate once they clear. LICENSE: none added (surfaced as an operator action).

**Verified.** Data tests **190** (was 178, +12); `tools/ci.ps1` green (**212** total, was 200). Byte-real AAPL + FX-BarCorrection paths unchanged; P1R-2's cross-run test fails on the pre-fix code (a true negative test). During P1R-1 the audit's first run false-flagged all 101 files — a Windows PowerShell 5.1 `@($x | ConvertFrom-Json)` pitfall (the whole array captured as one element); assign-then-normalize fixed it, re-run clean (**6,204 events / 0 null**, matching the live DB's dividend-row count). P1R-3's `string.Compare` translation confirmed (WHERE contains the date predicate; no client-eval throw).

**Red / known-broken:** one **pre-existing, harness-level test flake**, surfaced at finalization. **Symptom:** 1 of 4 full `tools/ci.ps1` runs failed with a single, non-reproducing test failure (the throw truncated the failing test's name). **Not a P1R defect:** 8+ consecutive full-suite passes plus 5 tight `AlphaLab.Data.Tests` loops immediately after, **0 failures**; the 12 new tests are individually stable. **Mechanism:** `TestDb.Delete` calls process-global `SqliteConnection.ClearAllPools()`, which clears pooled connections across *every* parallel test collection — the added test classes widen the parallel window that occasionally exposes it. **Verdict:** pre-existing, harness-level, out of v1.9.10's scope (a test-infrastructure concern, not any P1R fix). **Where to look:** P1R-7 now gates every push + PR, so the first random Actions red will most likely be this — investigate `TestDb` connection pooling (the global `ClearAllPools()`), **not** the P1R fixes, before re-diagnosing from scratch.

**Deferred (deliberately):** the two membership `"latest"` archival sites (P1R-4b) are **reported, not changed** — a contract + archival-semantics decision (proposal below). Building the three Part-B proposals (all need schema changes this pass forbids).

**Decision proposals (need a D-number):** three appended below (D76 territory) — extend D40 to `corporate_actions` (carries the membership-provenance recommendation too); a `data_quality_flags` table; a cross-sectional bar read path + `ix_bars_date`. The first two must be decided **before Phase 2 builds the ledger / wires the gate**.

**Operator actions surfaced (Part C, not mine to do):** (1) an **off-machine DB backup + a first restore drill** — `observed_at` is not honestly reproducible (a default re-run restamps all 488,217 bars with today's date; a `--as-of`-backdated re-run *forges* byte-identical stamps that defeat D40 while appearing to honour it, and either way the version chain is lost — never backdate `--as-of` to reconstruct history); `tools/snapshot-db.ps1` exists but nothing schedules/off-machines it and this Ops log is empty. (2) **Off-machine `tools/raw-cache/`** alongside the DB snapshot **before P1R-5's prune goes live** (the bootstrap payloads are the only honest-rebuild artifact). (3) **LICENSE** — the public repo has none → all-rights-reserved by default; pick one or leave it.

**Next session starts with:** Phase 2 — the six-stage funnel + ledger + costs + the D53 staged pipeline in AlphaLab.Worker (D59), reusing the 1.1–1.10 services. **Decide the D40/`corporate_actions` proposal first** — the ledger prices on that feed.

### 2026-07-15 (docs) — v1.9.9 entry-doc dedup + setup-narrative fix (no version bump)

**Docs only, no code, no version bump.** Closed the two structural drift risks flagged last pass. **Deduplicated `START_HERE.md`:** the repo carried two hand-synced copies (root + `docs/`); the root one had silently drifted three revisions before finding 134 caught it. **Root `START_HERE.md` is now canonical; `docs/START_HERE.md` deleted** — no reference needed repointing (MANIFEST, the CLAUDE.md map, and the doc's own links already pointed at the root copy). **Fixed the setup narrative:** START_HERE step 2 and MANIFEST told a fresh cloner to *copy* `docs/CLAUDE.md` + `docs/PROGRESS.md` into the repo root, but the repo is the deliverable and already keeps them at root (no `docs/` source exists) — step 2 now reads "clone it; the layout is already in place, nothing to copy," and MANIFEST lists both as repo-root files. Recorded as CHANGELOG findings **135** (dedup) + **136** (narrative); v1.9.9 range refs bumped to 128–136. **README overlap review (report-only, as requested):** root `README.md` and `docs/README_v1.9.md` are **distinct and both kept** — root is the GitHub repo landing (pitch / status / architecture / clone-build-run, plus a 7-row key-docs pointer table), `docs/README_v1.9.md` is the design-set index (the full Tier-1/Tier-2 doc map, the build-from-docs workflow, and the per-phase doc diet). Their overlap is **partial and purposeful** (both list docs; root's table is a curated subset that points *into* the full index) — not the redundant-copy class the two START_HERE files were, so no dedup. **Resolved in the follow-up commit (same day):** `docs/README_v1.9.md` §3 step 2 + its Tier-2 rows were aligned to the same repo-root framing (they carried the same 'copy to repo root' wording), and the root `README.md` Status was refreshed from the stale "Phase 0 complete / 39 tests" to "Phase 1 complete — the data foundation / 200 tests" — CHANGELOG finding 136 (extended) + **137**.

### 2026-07-15 (docs) — v1.9.9 doc self-containment pass (no version bump)

**Docs only, no code, no version bump.** Made the design set reconstruct the project without the repo. **Deleted the three reading-convenience PDFs** (`MASTER_DESIGN_v1.9.pdf`, `AlphaLab_Explained_Plain_Language.pdf`, `AlphaLab_Master_Design_Section_by_Section.pdf`) — 2026-07-13 snapshots that drifted from MASTER (now v1.9.9), the finding-99 drift class recurring — and removed their MANIFEST/START_HERE references (the plain-language row repoints to the live `DESIGN_IMPROVEMENTS_EXPLAINED.md`). **Rolled the remaining stale D1–D73 → D1–D75** in CLAUDE.md's doc map and the root `README.md` file table (finding-95's stale-range class); a whole-set sweep found no other stale D/FR/UX range (FR-1…38, UX-1…13 already current; the "D1–D71/D73 unchanged" notes in BUILD:20, PROGRESS:230, and the CHANGELOG v1.9.8 section left as correct-in-context history). **Completed the CLAUDE.md doc map** — added the previously-orphan `DESIGN_IMPROVEMENTS_EXPLAINED.md` (also given a MANIFEST home), `CHANGELOG_v1.9.md`, and the navigation docs. **Synced the drifted root `START_HERE.md`** to the `docs/` copy: it had diverged since **v1.9.6** (missing the v1.9.6 + v1.9.9 errata notes and still carrying the dead PDF ref); the two are now byte-identical. Recorded as CHANGELOG finding **134** under v1.9.9. **Verified clean:** zero commit-ids, zero repo/username self-refs (the only `github.com` refs are the external fja05680 data source), zero live dangling file refs, no stale ranges. **Flagged for a decision (not changed):** the repo carries duplicate entry docs (root `START_HERE.md` ↔ `docs/START_HERE.md`; root `README.md` ↔ `docs/README_v1.9.md`) — a recurring drift source worth deduplicating — and MANIFEST/START_HERE describe `docs/CLAUDE.md` + `docs/PROGRESS.md` per the "copy into the repo root" package convention while this repo keeps them at root.

### 2026-07-15 (Phase 1 → docs) — v1.9.9 Phase-1 completion doc-reconciliation

**Docs only, no code.** Folded Phase 1's decisions back into the design set so a from-scratch rebuild reproduces the corrected state. **Two decisions assigned:** **D74** — index-membership drop ≠ delisting (a drop stamps `index_membership.removed_on` only; `type='delist'` is reserved for a true delisting derived from the exchange symbol-list drop-out / acquisition feed, landing with Phase 2; MASTER §2 + §13.6, BUILD Phase-2 checklist); **D75** — canonical ticker identity = EODHD dash form (`BRK-B`) via `SymbolNormalizer` + the no-separator alias map (MASTER §2 + §13.6/FR-3). **CHANGELOG v1.9.9** records findings **128–133** (D74; D75; the Wikipedia descriptive-User-Agent provider rule; the aborted-run usage-flush-in-`finally`; the EODHD per-endpoint call-cost table + 1,000-req/min limit + the Phase-2 endpoint-weighting requirement; the §2 count-sanity key correction). Version rolled v1.9.8 → **v1.9.9** through MASTER §version-note, START_HERE, MANIFEST, README (decision range now D1–D75). **Verified before editing:** grepped every committed doc for an "index drop ⇒ delist" line — **none exists** (every `delist` mention is the legitimate true-delisting corporate-action type or MASTER §6's "a wish-list drop is never an implicit sell"), so the proposed BUILD "ticker dropping out ⇒ delist" correction was **skipped as non-existent**. **Still parked (undecided, untouched):** FR-23 "hypotheses" with no §21/`jobs.kind` home (C-6); the Phase-4 detection-power sweep that changes a D64 deliverable (C-1).

### 2026-07-15 (Phase 1) — Live sp100 backfill (decision #1) + Stage-3 doc closure; UA-403 fix + usage-flush hardening

**Ran the live backfill in two operator-gated stages.** Stage 1 smoke test (`--universe sp100 --years 1`) surfaced a real fetch-layer blocker: the **Wikipedia S&P 100 cross-check returned HTTP 403** because .NET's `HttpClient` sends no `User-Agent` and Wikimedia rejects header-less requests (reproduced deterministically: no-UA → 403 / descriptive-UA → 200). EODHD (GSPC proxy, 251 bars) and BlackRock OEF both succeeded; the run **fail-closed at membership**. Stopped and reported per the surprise-is-a-finding rule — did not silently fix.

**Fixed before Stage 2.**
- `ResilientHttpClient` now sets a descriptive **`User-Agent`** on every request (`ResilientHttpOptions.UserAgent`, default `AlphaLab/1.9 (paper-trading research lab)`; overridable to add a contact, respects a caller-set UA). Documented in INTEGRATIONS **§7** + **§9.1** (Wikimedia 403, observed 2026-07-14). Pinned by two `PlumbingTests` (default UA sent; custom UA honored).
- `BackfillRunner.RunAsync` flushes `api_usage_log` in a **`finally`**, and counts each call **at success** (not batched after) — so an aborted run records the calls it actually spent (Stage 1 proved the gap: 2 calls made, 0 logged). Pinned by a new `BackfillRunnerTests` case that aborts on a simulated Wikipedia 403 and asserts `eodhd_gspc`+`oef_csv` logged while the cross-check that threw is not.
- `tools/Backfill` wired its own **gitignored `appsettings.Secrets.json`** (+ a `Condition="Exists"` csproj copy) — the plain console SDK doesn't auto-glob `appsettings.*.json` like the Worker SDK, so the CLI couldn't see the EODHD token. Secret gitignored, absent from the committable set, no token-shaped strings committed (ci.ps1 secret grep green).

**Stage 2 — full `--universe sp100 --years 20` (2026-07-15, ~5m51s, exit 0).**
- **API:** **304 EODHD calls** (101×3 [`/eod`+`/div`+`/splits`] + 1 GSPC proxy) / 100,000-per-day cap ⇒ **99.7% headroom**; `oef_csv` + `wikipedia_sp100` 1 each (free). Confirms call count is **universe-driven, not year-driven** (a 1-year run costs the same 304).
- **Data landed:** securities **102** (101 members + GSPC proxy); bars **488,217** (2006-07-17 .. 2026-07-14); GSPC proxy **5,029** bars (clears the D73 ~3.8y warm-up + the ≥15y replay floor); index_membership **101** open; corporate_actions **6,273** (dividend 6,204, split 69); calendar 15,332 (1996..2056).
- **Guards — zero fired:** count-sanity passed (101 ∈ [99,103]); **OEF and Wikipedia agreed** (both 101) → applied, not held; zero no-bar flags; no headroom breach. Append-only idempotency held on the re-run over Stage 1's partial state (GSPC 5,029 fetched / 4,778 written — the 251 Stage-1 bars recognized identical and **not** re-versioned).

**Stage 3 — INTEGRATIONS §1 VERIFIED 2026-07-15.** Replaced the "call limits" ⚠VERIFY with the measured reality: 100k/day cap (`Backfill:ApiPlanLimit=100000` correct — matches EODHD's published paid-plan default, no `/user` query needed for the limit), the observed **304-call spend + 99.7% headroom**, a **per-endpoint cost table** (`/eod`·`/div`·`/splits`=1, **`/news`=5**, **`/eod-bulk-last-day`=100**), the second **1,000 requests/minute** limit (`X-RateLimit-Limit`/`X-RateLimit-Remaining` headers), and the **GMT-midnight reset quirk** (counter shows the prior day until the first post-midnight request). §9 provider rule 5 records the dual limits. **§5 Anthropic batch-headers (Phase 5) stays flagged; the §1 fundamentals row (Phase 8) stays flagged** — both non-Phase-1.

**Findings on record (not defects).**
- **Live OEF drift 102 → 101:** the byte-real fixture (captured 2026-07-13) has 102 members; live OEF holdings now return 101. In-band [99,103], OEF↔Wikipedia agree. The fixture is stale by one name — recorded so a future fixture refresh is on the books.
- **AsOf 2026-07-15 vs newest bar 2026-07-14:** the run crossed UTC midnight at start; normal EOD lag, tolerated by the point-in-time reads.

**Phase-2 items raised.**
- **`api_usage_log` must weight calls by endpoint cost** before `/eod-bulk-last-day` (100/req) and `/news` (5/req) come online — a flat count would under-report against the 100k cap and could pass a headroom check that should fail (INTEGRATIONS §1).
- **Honor the 1,000 req/min `X-RateLimit-*` header** for wider (S&P 500) universes — not yet enforced (§9.5); the single-threaded 304-call run was far below it.
- **Quality-gate wiring:** the bootstrap backfill lands raw-but-stamped, versioned, append-only bars and **does not** invoke the FR-6 `DataQualityGate` (built/tested/registered but unwired). Bars carry **no "already-gated" flag**, so the Phase-2 D53 daily pipeline **must invoke the gate itself over the backfilled history** — it cannot assume ingestion vetted it (INTEGRATIONS §9.3).

**Phase 1 status.** All gate boxes green — **phase-complete** (sealed by this commit; under review). Data tests **178**, `tools/ci.ps1` green (**200** total). **Next:** Phase 2 — the six-stage funnel + ledger + costs + the D53 staged pipeline hosted in AlphaLab.Worker (D59), reusing the 1.1–1.10 services.

### 2026-07-14 (Phase 1) — Checkpoint 1.10: bootstrap backfill CLI (tools/Backfill)
**Shipped.** The Phase-1 bootstrap-writer CLI that ties the 1.1–1.9 providers + ingestion services into one run (decision #1; D59 note — the CLI is the Phase-1 bootstrap writer, the Worker is the Phase-2 operational writer). `BackfillRunner` lives in **AlphaLab.Data** (so the Phase-2 Worker reuses it) with `BackfillOptions` (derived `From`/`ObservedAt`/`CalendarYears`/`PlanSummary`) + `BackfillArgs.Parse` (`--universe`/`--as-of`/`--years`/`--dry-run`; unknown/incomplete flags **fail closed**). Steps: **SeedCalendar** (CalendarSeeder ±30y), **BackfillRegimeProxy** (resolve `Regime.ProxySecurityId` + fetch GSPC.INDX + ingest), **RefreshMembership** (OEF primary + Wikipedia S&P 100 cross-check → reconcile at [99,103]; on apply, apply the OEF GICS sectors), **SeedHistoricalMembership** (fja05680 parse → intervals), **BackfillSecurity** (EOD + dividends + splits → versioned bars + typed corporate actions), **FlushApiUsage** (per-source call counts → `api_usage_log` with the ≥50% headroom check, INTEGRATIONS §1). `RunAsync` sequences them; **`--dry-run`** previews the plan with **no network and no writes**. New console project **`tools/Backfill`** (Program.cs: D67 config = appsettings + appsettings.Secrets only, no env vars; migrate; wire the concrete providers with a `FileRawCache`; EODHD token required from Secrets for a live run) added to `AlphaLab.slnx` (a new `/tools/` folder — under tools/, so outside the src reference-graph guard). Raw payloads are archived by the providers' own `IRawCache`.
**Verified.** Data tests **175** (was 153); `tools/ci.ps1` green (**197** total; no migration → `SchemaFidelityTests` still 14 tables; all guards clean incl. the full reference graph). A fixture-backed `IResilientHttpClient` drives the **real** providers end-to-end **offline**: `SeedCalendarStep` seeds the window (a known half-day present); `BackfillRegimeProxyStep` resolves + ingests the 62 real GSPC bars under a versioned config row; `RefreshMembershipStep` reconciles the **real** OEF-vs-Wikipedia-S&P-100 pair (logs an audit row, count in [99,103]) and, with agreeing rosters, **applies** ~102 members + sectors (AAPL → "Information Technology"); `SeedHistoricalMembershipStep` writes thousands of intervals; `BackfillSecurityStep` ingests AAPL bars + dividend + split rows (3 EODHD calls); `FlushApiUsage` records `api_usage_log` and flags the headroom breach (3/4 EODHD); and `RunAsync(dry-run)` makes **zero** calls and **zero** writes. `BackfillArgs` parsing (full/defaults/sp500-band/derived-fields/fail-closed) covered. The real CLI dry-run (`dotnet run --project tools/Backfill -- --universe sp100 --dry-run`) prints the plan; a bad arg prints usage + a non-zero exit.
**Adversarial review (this session) — 3 real defects fixed, all pre-commit.** A 24-agent review→verify workflow over the orchestrator + console confirmed **11 findings**. Fixed: **(major, correctness/idempotency)** `RunAsync` wrongly chained the fja05680 **historical S&P 500 seed into the forward sequence** — a re-run's forward reconcile (which reads *all* open `index_membership` rows, D35) would then classify the ~400 historical-only members as **drops** and permanently close the replay intervals. Per the plan + D70 the forward `--universe sp100` bootstrap is *OEF+Wikipedia slice + GSPC proxy + member bars* only; historical membership is a **Phase-4 replay prerequisite** seeded separately (`SeedHistoricalMembershipStep` stays public but is no longer chained), so removing it both fixes the re-run mass-eviction and matches the spec — pinned by a double-`RunAsync` no-eviction test. **(major, security)** a failed-fetch exception message leaked the EODHD `?api_token=…` to stderr — `HttpFetchException` now **redacts the URL query** at the HTTP source (protects every caller, incl. the Worker; hard rule 11) with a redaction test. **(major, fail-open + config)** `BackfillArgs` swallowed a flag-shaped value (`--as-of --dry-run` silently went LIVE) and the CLI `--years` was dead (config unconditionally overrode it) — `RequireValue` now rejects `--`-prefixed values, unknown universe / non-positive / non-integer `--years` **fail closed**, and config supplies only the *default* years (CLI wins). The remaining minors were folded in too: the ≥50% headroom check now aggregates the **EODHD-family** call total against the one plan (the sources share one account) while still recording each `api_usage_log` row separately.
**Red / known-broken:** none.
**Decision proposal (Phase 4):** the `MembershipReconciler` is universe-blind (drops any open member not in the primary roster). That is fine for the forward slice today (historical is not co-mingled), but when a **replay arena** co-mingles the forward slice with the historical S&P 500 roster in one `index_membership`, the reconciler will need a **universe/source discriminator** (or a scoped read) — recorded here for Phase 4.
**Deferred (deliberately):** the **live `--universe sp100` run** against EODHD/BlackRock (backfill-then-delta over the full OEF slice + the 100-member bar sweep) is the **operator's** (decision #1) — the machinery + the offline wiring are proven; `RunAsync`'s full live member loop is intentionally not offline-tested (only AAPL has byte-real bar fixtures). The `HistoricalMembershipUrl` default points at the in-repo fja05680 fixture for a local dry/bootstrap; the operator repoints it at the GitHub raw (dated filename) or a downloaded copy for the live run. The **daily delta** (incremental daily run) is the **Phase-2 Worker** (D53 staged pipeline) reusing these services. Ongoing schema changes go through `tools/migrate.ps1` (snapshot-safe); the CLI's `Database.Migrate()` is the fresh-store bootstrap only.
**Phase 1 status.** Code-complete: checkpoints **1.0–1.10** all shipped. Remaining before the phase gate: the operator's **live backfill run** (decision #1) and the **INTEGRATIONS ⚠VERIFY** doc box (the GSPC.INDX EOD ⚠VERIFY is already resolved in INTEGRATIONS §9).
**Next:** Phase 2 — the six-stage funnel + ledger + costs + the staged pipeline hosted in AlphaLab.Worker (the operational daily writer, D59) reusing the 1.1–1.10 services.

### 2026-07-14 (Phase 1) — Checkpoint 1.9: regime proxy feed + readiness guard (FR-38/D73)
**Shipped.** The market-level regime proxy feed on which the Phase-2 PIT regime label (D50/FR-26) is computed — a named, validated, fallback-bearing feed (Golden Rule 25). `IRegimeProxyProvider`/`EodhdGspcRegimeProxyProvider` (PRIMARY): fetches **EODHD `GSPC.INDX` EOD** (`/eod/GSPC.INDX` — the fully-qualified index symbol, so **no `.US` suffix**, unlike the equity provider) and reuses the byte-real-fixture-tested `EodhdMarketDataProvider.ParseEod`. Dormant `SelfBuiltCapWeightRegimeProxyProvider` **fallback seam** (fails loud — the cap-weight index over the backfilled universe reuses the D68 benchmark machinery, Phase 3+). `RegimeProxyCrossCheck` — pure SPY.US raw-close daily-return comparison with a tolerance alarm (INTEGRATIONS §9). `RegimeProxyIngestion` — registers **GSPC.INDX** as a permanent security (exchange `INDX`, verbatim — never through SymbolNormalizer), ingests its EOD as versioned append-only bars, and **resolves `Regime.ProxySecurityId` into an append-only versioned `config` row** (a new version only when the value changes; idempotent). `RegimeProxyReadiness` — the **fail-closed warm-up guard**: refuses (+ logs a reason) below **956 sessions** (`TrendSmaDays 200 + VolLookbackYears 3 × 252 ≈ 3.8y`), never fabricating a label on a short series. `RegimeOptions` (Regime section). Registered the two DB services in `AddAlphaLabMembership`. **No migration** (the proxy stores as bars/securities; `Regime.ProxySecurityId` is a documented CONFIG key written as a versioned config row — rule 14 clean), **no invented config key**.
**Verified.** Data tests **153** (was 135); `tools/ci.ps1` green (**175** total; no migration → `SchemaFidelityTests` still 14 tables; all guards clean). **FX-RegimeProxyBackfill:** the byte-real GSPC.INDX payload parses (62 bars, `2026-04-14`→`07-13`, volume 5,032,380,000 > Int32 — proves the `long` column); ingest under the resolved id is versioned + idempotent; `Regime.ProxySecurityId` writes a single versioned config row (v1), a re-resolve writes nothing, and a value change appends v2 while v1 stays byte-unchanged (append-only); readiness **fails closed** on the real 62-session history and at the **955/956 boundary**, counts **distinct** sessions (a correction's v2 row doesn't inflate the count) and **excludes future-dated bars**; the SPY.US cross-check agrees when returns track and alarms on a divergent sample (reporting the earliest divergence); the dormant fallback and an unknown/inactive `ProxySource` fail loud.
**Adversarial review (this session) — 2 real (latent) bugs fixed.** A 15-agent review→verify workflow confirmed **8 findings** (all minor). (1) **Identity/idempotency:** `ResolveProxySecurityId` routed through `SecurityMaster.ResolveOrRegister`, whose asOf-bounded `ticker_history` resolution would **mint a duplicate GSPC.INDX** (orphaning the ingested bars) when a later resolve carried an **earlier asOf** (a catch-up/replay of a past session). Fixed: the singleton proxy identity now resolves by **symbol+exchange, independent of asOf**; pinned by a decreasing-asOf test. (2) **Cross-check correctness:** returns were paired by into-date without checking they span the **same interval**, so a gap in one feed compared a 1-day return against a multi-day one → a false alarm/agreement. Fixed: the previous priced date is carried and only identical-interval returns are compared; pinned by an interval-mismatch test. The other 6 were test-coverage gaps (versioned-bar distinct-count, config multi-version append-only, future-bar exclusion, tolerance boundary, no-overlap, first-divergence precedence) — all now closed.
**Red / known-broken:** none.
**Deferred (deliberately):** the **live ≥3.8y (Data.BackfillYears=20) GSPC.INDX backfill run** + the live **SPY.US fetch** + the rotating-sample selection are wired by the **backfill CLI (1.10)** — the provider/ingestion/cross-check are the reusable machinery it calls (decision #1). The **trend×vol label service** (FR-26) that consumes the proxy + the readiness guard is **Phase 2** (decision #3); `regime_labels`/`regime_episodes` tables stay unbuilt until then. The **self-built cap-weight fallback** activates only if the index EOD becomes unavailable (dormant, reuses the D68 machinery, Phase 3+). The cross-check tolerance (0.5%) is a documented internal constant + stop-and-report seam (INTEGRATIONS §9 names no threshold).
**Next:** 1.10 — the backfill CLI (`tools/Backfill`) that wires the 1.1–1.9 providers/seeders/ingestion into `--universe sp100` (OEF+Wikipedia slice + GSPC proxy + calendar seed), with `api_usage_log` headroom + the raw-cache archive; the live runs stay with the user (decision #1).

### 2026-07-14 (Phase 1) — Checkpoint 1.8: trading calendar + ICalendarService (FR-30/D54)
**Shipped.** `NyseCalendar` — a **pure, deterministic** generator (no I/O) encoding NYSE's published holiday rules + known half-days (MASTER §20.5's "generation script" and the "regenerate from rules" fallback): rule holidays (MLK 3rd-Mon-Jan **since 1998**, Washington's Birthday, **Good Friday via the Computus**, Memorial, **Juneteenth since 2022**, Independence, Labor, Thanksgiving, Christmas), Sat→Fri/Sun→Mon observance with the **New-Year Saturday quirk** (a Saturday Jan 1 does NOT close the prior Friday; only Sun→Mon shifts), half-days (13:00 ET) for the day-after-Thanksgiving + Christmas Eve + the **era-split Independence early close**, and a **curated, cited one-off-closure list** (9/11 Sep 11–14 2001, Reagan 2004-06-11, Ford 2007-01-02, Sandy Oct 29–30 2012, Bush 2018-12-05, Carter 2025-01-09). `ICalendarService`/`CalendarService` reads `trading_calendar` via a lazily-loaded, binary-searched session snapshot: `IsTradingDay`, `PreviousSession`/`NextSession` (strict), `SessionsBetween` (the catch-up missed-session set), `CloseTime`, `IsHalfDay`. `ICalendarSeeder`/`CalendarSeeder` — idempotent writer from the generator (a re-seed adds only new years, never rewrites history). `CalendarOptions` (Calendar section: Exchange, RunAfterCloseOffsetMinutes). Registered both services in `AddAlphaLabMembership`. **No migration** (`trading_calendar` + `TradingCalendarRow` exist from 1.0), **no invented config key** (rule 14).
**Verified.** Data tests **135** (was 112); `tools/ci.ps1` green (**157** total; no migration → `SchemaFidelityTests` still 14 tables; all guards clean). The generator is **spot-validated against ~25 real NYSE dates**: every 2024 holiday closed + half-days at 13:00; Good Friday across 2016/2024/2025 (Computus); the New-Year Saturday quirk (Dec 31 2010 open, Jan 3 2011 open); Christmas-Saturday→Friday (Dec 24 2010 full-closed, not half); Independence-Sunday→Monday (Jul 5 2021 closed, Jul 2 full); MLK absent 1997 / present 1998; Juneteenth absent 2021 / present 2022; all six one-off closures closed with neighbours trading; weekend exclusion; and **exact real annual session totals** for the quirk/closure years (2001=248, 2004=252, 2007=251, 2012=250, 2018=251, 2022=251, 2024=252, 2025=250). **FX-HalfDay** (the 2024-11-29 early close surfaces as 13:00, the trigger keys off it) and **FX-HolidayOutage** (`SessionsBetween` across Thanksgiving weekend recovers only real sessions — Thanksgiving + the weekend never fabricated) both pass, plus seeder idempotency and the strict Previous/Next navigation across a holiday weekend.
**Adversarial review (this session) — found and fixed a real bug.** A 13-agent review→verify workflow over the generator + service confirmed **2 findings**. The **correctness** one: the **Independence-Day early close is era-split** — when July 4 is a Thursday, NYSE closed early on the following **Friday July 5 before 2013**, then moved the early close to **Wednesday July 3 from 2013 on** (verified vs `exchange_calendars` + a 2002 NYSE notice). The first cut applied the modern "July 3 half when Mon–Thu" rule uniformly, mis-stamping **1996 and 2002** (July 3 half / July 5 full — backwards), corrupting close-time metadata on the historical replay window. **Fixed** (`AddIndependenceEarlyClose`, `Jul3WednesdayRuleFirstYear=2013`) and pinned by a new era-split test (2002/1996 pre-2013 vs 2024 post-2013). The second (minor): the annual-count test's loose `InRange` was tightened to **exact real totals** for the quirk/closure years (which now cross-check the generator against documented NYSE history).
**Red / known-broken:** none.
**Deferred (deliberately):** the **live ±30y seed run** (setup writes `trading_calendar` for ~1996–2056) is invoked by the **backfill CLI (1.10)** — `CalendarSeeder` is the reusable write path it calls (same pattern as the other provider writers, decision #1). The **orchestrator trigger** (session close + `RunAfterCloseOffsetMinutes`, ET-anchored) is **Phase 2** (Scheduled mode) — 1.8 lands `CloseTime` + the offset in options for it to consume. **Curated-closure completeness** and the **13:00 close-time assumption** (a pre-mid-1990s 2 pm era is out of the ±30y window) are documented stop-and-report seams; a future one-off closure is added by a re-seed / the D55 admin path when announced (never fabricated). The Phase-0 `Phase0MissedSessionResolver` is replaced by the calendar-driven catch-up in **Phase 2** (D47), which will consume `SessionsBetween`.
**Next:** 1.9 — regime proxy feed + backfill + readiness guard (FR-38/D73); `FX-RegimeProxyBackfill`.

### 2026-07-14 (Phase 1) — Checkpoint 1.7: data-quality gate (FR-6)
**Shipped.** `IDataQualityGate`/`DataQualityGate` — a **pure** evaluator (no DB reads/writes) over a security's candidate bars returning a `QualityReport` of `QualityFlag`s (issue + `Warn`/`Reject` severity + date + detail). Three FR-6 checks: **gaps** (an expected trading session with no bar — the expected-session set is a caller input; the `ICalendarService` that *produces* it is FR-30/checkpoint 1.8); **field integrity** — a null/NaN/inf or non-positive **close**, a bad open/high/low, **or a present-but-corrupt adjusted close** → `Reject` (fail closed, rule 10 — see the review note); **robust return outliers** — a daily (adjusted) return whose **median/MAD robust z** exceeds `Data.OutlierZ` (=8.0), the scale **floored at 0.1%/day**; and **adj/raw reconciliation** — the `adj_close/close` factor is piecewise-constant absent a corporate action, so a factor step in `(prev.date, cur.date]` with no dividend/split in the feed raises the `UnexplainedAdjustment` **alarm** (a missing corporate action). `DataQualityOptions` (SectionName `Data`, keys `OutlierZ`/`BarCrossCheckSampleSize`/`BarCrossCheckTolerancePct` matching CONFIG). The **Alpaca bar cross-check is a DORMANT seam** — `IBarCrossCheckProvider` + `AlpacaBarCrossCheck` that **fails loud** (no Alpaca account at launch; the Secrets Alpaca pair is optional, NFR-4; the two `BarCrossCheck*` keys are wired-but-inert). Registered `IDataQualityGate` in `AddAlphaLabMembership`. **No migration, no invented schema column or config key** (rule 14) — the reconciliation epsilon (0.05%) and the robust-scale floor (0.1%/day) are documented internal constants + stop-and-report seams.
**Two implementation calls (real-data honesty, not silently fixed).** (1) The outlier test needs a **robust** median/MAD z, not a plain sample z — a lone 12σ print self-masks (it inflates the std and scores ~4σ on a plain z), and the scale is **floored** so a near-constant/illiquid series (MAD≈0) can neither manufacture outliers from ordinary moves *nor* mask a real 40% bad print (a lone spike among flat days also has MAD≈0). A test demonstrates the plain-z self-masking directly. (2) Reconciliation is **presence-based** (a step is explained by the *presence* of any dividend/split in the window, not by matching the step magnitude) — deliberately conservative (zero false positives; only alarms when there is NO event) because the magnitude-aware residual check needs the exact `adj_close` factor arithmetic, which has no byte-real fixture to validate against yet (building it now would risk false positives on real data).
**Verified.** Data tests **112** (was 93); `tools/ci.ps1` green (**134** total; no migration → `SchemaFidelityTests` still 14 tables; bars-append-only + full reference-graph + no-secrets guards clean). **FX-QualityGate:** one synthetic series with a gap day, a NaN close, a +37% outlier print, a dividend-explained factor step (clean), and an unexplained factor step → **exactly four flags** (MissingBar/NanField/OutlierReturn/UnexplainedAdjustment), the dividend step not flagged, `HasRejects` driven only by the NaN reject, and the **same flag set on shuffled input** (order-independence). Plus units: robust-z catches a spike a plain z would mask; a mostly-flat series with a lone 40% print still flags; a large **negative** print flags (symmetry); `Data.OutlierZ` drives the cutoff (just-above flags, just-below does not); a **present-but-corrupt** adj_close (NaN/inf/0/−5) with a valid raw close is a `Reject`; a genuinely **absent** (null) adj_close is benign and does not manufacture an outlier; reconciliation window bounds (event on the prior session's date does NOT explain; event strictly inside a gap DOES); a matched dividend/split explains a step; a missing split alarms; dormant Alpaca fails loud.
**Adversarial review (this session).** Ran an 18-agent review→verify workflow over the two new source files; **11 findings confirmed**. The one **major** — a present-but-corrupt `adjusted_close` (NaN/inf/≤0) alongside a valid raw close was silently defaulted to the raw close and never flagged, a rule-10 hole on the field every downstream return prices on — is **fixed** (now a `Reject`) and pinned by the corrupt-adj test above; a related basis-mixing bug (a lone null adj_close manufacturing outliers by pricing one return on the raw close) is fixed by using **one consistent price basis** per series (adjusted throughout, else raw; never a return across a raw/adjusted boundary). The other 10 were minor test-coverage gaps, now closed by the units above.
**Red / known-broken:** none.
**Deferred (deliberately):** the **rotating-sample Alpaca bar cross-check** is a dormant seam — activation (register the provider + the sample-selection/tolerance compare) needs the optional Alpaca keys and lands with the CLI wiring (1.10) / when keys are provided. **Gap detection** consumes an expected-sessions set the **`ICalendarService` (FR-30, checkpoint 1.8)** supplies live. **Magnitude-aware reconciliation** — the presence-based check has a narrow, documented false-negative (a missing action co-located in the same window as a present unrelated event, e.g. a missing split sharing an ex-date with a present dividend, is not flagged); making it magnitude-aware needs byte-real `adj_close` factor examples from the live backfill (stop-and-report seam). **Persisting** quality flags + surfacing them on the **Data-health screen** is a read-model/screen concern (Phase 3/7). **Wiring the report to fail closed** (block the write on a `Reject`, alarm on a `Warn`) is the **Phase-2 staged pipeline** (D53) — the gate returns the report; the pipeline acts on it.
**Next:** 1.8 — trading calendar seed (±30y NYSE incl. half-days) + `ICalendarService` (FR-30/D54); FX-HolidayOutage, FX-HalfDay.

### 2026-07-14 (Phase 1) — Checkpoint 1.6: sector ingestion + change log (FR-5)
**Shipped.** `ISectorIngestion`/`SectorIngestion` + `SectorAssignment`: apply the GICS sector (from the IVV/OEF holdings CSV's Sector column, `Universe.SectorSource=ivv_csv`) to `securities`. A **reclassification** — a prior NON-NULL classification changing — writes a `sector_changes` row (old/new sector+industry, `changed_on`) and updates `securities.sector`; an **initial** classification (null → X) sets the baseline WITHOUT a change row; an **unchanged** apply is a no-op (idempotent). Never deletes. A null assignment field leaves the current value. Dormant **`EodhdFundamentalsSectorProvider`** (D49) is the post-upgrade sector source (adds industry + a staleness alarm) — throws until `Universe.SectorSource='eodhd'`. Registered `ISectorIngestion` in `AddAlphaLabMembership`.
**Verified.** Data tests **93** (was 88); `tools/ci.ps1` green (**115** total). **FX-SectorReclass:** an IT→"Communication Services" mid-period change logs a `sector_changes` row (old/new + `changed_on`) and updates the current value; an unchanged apply writes nothing; an initial null→X sets the baseline with no change row; the real IVV NVDA sector "Information Technology" applies end-to-end with **industry null** (the CSV has no industry column); the dormant EODHD sector provider fails loud.
**Red / known-broken:** none.
**Deferred (deliberately):** the **LowVol-at-next-rebalance consumption** of the sector-change log is a later phase (Phase 6 strategies) — 1.6 lands the log + the current-value update. **Industry** classification (and a sector staleness alarm) arrive with the EODHD-fundamentals upgrade (dormant, D49) — the IVV/OEF CSV supplies sector only. The **data-quality gate + reconciliation (FR-6, FX-QualityGate)** is checkpoint 1.7 (the other half of that DoD box).
**Next:** 1.7 — data-quality gate (gaps/NaNs/outliers) + dividend/split reconciliation + a dormant Alpaca bar cross-check seam (FR-6); FX-QualityGate.

### 2026-07-14 (Phase 1) — Checkpoint 1.5: S&P 100 slice (OEF) + fja05680 historical membership (FR-4/D70)
**Shipped.** Generalized the 1.4 IVV provider to **`ISharesHoldingsMembershipProvider`** parameterized by `ISharesHoldingsOptions` with `Ivv()`/`Oef()` presets (239726/`ivv_csv`, 239723/`oef_csv`) — one class serves both named feeds (the named-source distinction, Golden Rule 25, is the config source label; one C-4 fixture covers both, §2b), and the **OEF S&P 100 slice** reconciles at the **[99,103]** band via the same `MembershipReconciler`. New `HistoricalMembershipCsvParser` (fja05680 §8: `date,tickers`, the roster is one quoted field → sorted snapshots; dot `BRK.B` and bankruptcy `*Q` preserved raw) + `HistoricalMembershipIngestion` — reconstructs half-open `[added_on, removed_on)` intervals into `index_membership` (a name added/dropped/re-added gets distinct intervals; **never deletes**), as a **bulk writer** (securities registered in ~2 SaveChanges, intervals in one) so the 30-year file ingests in ~2s. `IndexMembershipReadService.MembersAsOf(date)` — the as-of read foundation (ordinal ISO compare, half-open). `*Q` is kept verbatim + dot→dash only (never stripped), so `AAMRQ`/`ENRNQ` become securities and legit Q-tickers `HPQ`/`CPQ` survive. New services registered in `AddAlphaLabMembership`.
**Verified.** Data tests **88** (was 81); `tools/ci.ps1` green (**110** total). Byte-real fja05680 parse: **2718** snapshots, 1996-01-02 → 2026-06-30, `AAMRQ`+`AAPL` in 1996 (no `TSLA`), `BRK.B`+`TSLA` in 2026 (no `AAMRQ`). Interval reconstruction (synthetic add/drop/re-add): B gets a closed `[D1,D3)` **plus** an open `[D4,∅)` (the closed row persists), dot `BRK.B`→`BRK-B`, and `MembersAsOf` is correct each day (half-open — a name is out on its removal date). End-to-end on the **real 30-year file**: `MembersAsOf(2026-06-30)` includes BRK-B/AAPL/TSLA and **excludes delisted AAMRQ**; `MembersAsOf(1996-01-02)` includes AAMRQ and excludes TSLA; the 2026 roster is in-band. OEF feed: `ToSnapshot` on the real OEF CSV = **102** canonical members incl. AAPL/NVDA/BRK-B; slice reconciliation applies at [99,103] and holds when out of band.
**Red / known-broken:** none.
**Deferred (deliberately):** full **FX-AsOfMembership** (a replay day resolving its as-of roster inside the replay engine) is **Phase 4** (D70) — 1.5 lands the ingestion + a data-layer as-of read. **Ticker-reuse disambiguation:** identity is taken from the ticker string (canonicalized), so a symbol reused by a different company across eras is treated as one security — correct for as-of *roster* reconstruction, but the identity split needs point-in-time data (delisted list / EODHD constituents, dormant per D49) and lands with the live backfill / Phase 4 (logged limitation). `*Q`→pre-bankruptcy continuity (`AAMR`→`AAMRQ` as one security) likewise needs the delisted list — deferred. OEF/IVV live fetch → user (decision #1); the sp100 Wikipedia parse is already covered (1.4).
**Next:** 1.6 — sector ingestion + change log (FR-5, sector half).

### 2026-07-14 (Phase 1) — Checkpoint 1.4: index membership — IVV primary + Wikipedia cross-check + reconciler (FR-4)
**Shipped.** The D49 launch membership machinery (no new migration — `index_membership`/`index_membership_log` and the `delist`/`ticker_change` CHECK values already exist from 1.0). New in `AlphaLab.Data`: `Providers/DelimitedCsvReader` (hand-rolled RFC-4180, no package — quote-aware over commas, **embedded newlines** (the 10-line disclaimer footer stays one field), `""` escapes, in-field thousands-commas, LF-only, BOM); `Providers/ISharesHoldingsParser` (one shape for IVV+OEF — **scan** for the header, assert it verbatim (**C-4**: renamed/moved column ⇒ `FormatException`, never a silent empty set), keep `Asset Class=='Equity'`, stop at the footer); `Providers/SymbolNormalizer.ToEodhd` (EODHD dash is the identity anchor — decision #2: dot→dash mechanical for historical/Wikipedia + a curated no-separator alias `BRKB→BRK-B`/`BFB→BF-B` for the holdings forms that can't be split mechanically); the `IIndexMembershipProvider` seam + `IvvHoldingsMembershipProvider` (PRIMARY, portfolioId 239726, no `asOfDate`), `WikipediaMembershipCrossCheck` + `WikitableExtractor` (scoped hand-rolled `id="constituents"` reader — first-`<td>` inner text with tags stripped, so it handles the S&P 500 `<a>`-linked cells and the S&P 100 plain-text cells with one path; "Symbol" first-column header guard), and `EodhdIndexMembershipProvider` (**DORMANT** per D49 — throws); `Services/MembershipReconciler` (count-sanity gate on **both** sources independently before comparison → HELD + `agreed=0` log/alert; symbol-set agreement gate → HELD on divergence; agreement path stamps `added_on`/`removed_on` (never delete, mirrors `RecordTickerChange`) + `agreed=1` diff log); `UniverseOptions` (`MembershipCountSanity=[495,510]`); an `AddAlphaLabMembership` DI seam. **Decision #5:** a membership drop stamps `removed_on` only — it does **not** write a `delist` corporate action (index removal ≠ delisting; keeps Stage-4 exits governed by ExitPolicy, hard rule 7).
**Verified.** Data tests **81** (was 55); `tools/ci.ps1` green (**103** total; bars-append-only + full reference-graph + no-secrets guards clean; no new migration → `SchemaFidelityTests` still 14 tables). Against the **byte-real** fixtures: IVV parse = header scanned & verbatim-matched (FX-IvvHeader/C-4), **exactly 504 equity** (∈[495,510]), the 4 non-equity rows (XTSLA/USD/SGAFT/ESU6) excluded, NVDA→"Information Technology", the multi-line disclaimer not parsed as data, and a code-synthesized drifted header ⇒ `FormatException`; OEF same shape = 102 equity. Wikipedia: sp500 constituents in-band with AAPL/MSFT/**BRK-B** (dot `BRK.B`→dash), sp100 plain-text cells in-band; header-drift + table-not-found fail loud. Reconciler: **FX-MembershipAgree** (add/drop applied, dates stamped, the dropped row persists with `removed_on` set, `agreed=1` log, **zero** `corporate_actions`), **FX-MembershipDiverge** (primary count 512 ⇒ HELD, no mutation, no registration, `agreed=0` log + note), and an in-band `+NEWCO` divergence ⇒ HELD.
**Guardrail (honored, not silently fixed).** Two real-data surprises surfaced with the user before coding: (1) the **three-way ticker symbology** mismatch — IVV/OEF `BRKB` (no separator) vs historical `BRK.B` (dot) vs EODHD `BRK-B` (dash); `BRKB` can't be split mechanically → curated alias map, EODHD-dash canonical (decision #2). (2) the two Wikipedia fixtures were Chrome **view-source** saves, not rendered HTML → the user re-saved the rendered pages; the "Webpage Complete" `_files/` asset folders were then deleted (no PNG/CSS/JS committed).
**Red / known-broken:** none.
**Deferred (deliberately):** OEF S&P 100 slice provider + fja05680 historical-membership ingestion → **1.5** (both CSVs already in `tests/Fixtures`); EODHD constituents provider dormant (D49); live IVV/OEF/Wikipedia fetch → user (decision #1); a real alert channel → later phase (the `agreed=0` log row is the 1.4 alert). **FR-4 "delist derivation" removed from 1.4 (decision #5)** — a membership drop is a universe exit (`removed_on` only); a true delisting (exchange symbol-list drop-out / acquisition) writes `type='delist'` later (Phase 2 corporate-action semantics). The historical 1.3-entry / phase-plan phrasing "ticker dropping out ⇒ delist" is superseded by decision #5.
**Decision proposals (need a D-number):** decision #5 (index-drop ≠ delist) — **assigned D74 in v1.9.9** (MASTER §2 + §13.6).
**Next:** 1.5 — OEF S&P 100 slice (portfolioId 239723) + Wikipedia S&P 100 cross-check (count 99–103) + fja05680 historical-membership ingestion (dot→dash + `*Q` handling).

### 2026-07-13 (Phase 1) — Real captured EODHD payloads for the 1.2/1.3 parse fixtures
**Shipped.** Replaced the INTEGRATIONS-shape-faithful synthetic payloads with **real captured EODHD responses** (captured 2026-07-13 via a throwaway capturer that reads `Secrets:EodhdApiToken` through the D67 config builder; **response body only** written to disk — never a request URL or token): `tests/Fixtures/eodhd/{eod_AAPL,eod_AAPL_adjusted,splits_AAPL,div_AAPL,eod_GSPC_INDX}.json`. Wired into the test project via a `..\Fixtures\eodhd\*.json` Content copy + a `Fixtures` loader. Rewired `EodhdParseTests`, `BarVersioningTests` (FX-BarCorrection now bases v1 on the real 2026-07-13 AAPL bar; the correction *delta* stays synthetic by necessity — one pull yields one version per date), and `CorporateActionIngestionTests` to read the captures, same assertion structure with real expected values. **Parsers unchanged** — the real payloads revealed no field/format surprise (verified below), so nothing was "silently fixed."
**Verified.** `dotnet test AlphaLab.slnx` + `tools/ci.ps1` green — Data tests **55**, **77** total. No-committed-secrets grep passes with the fixtures staged; an explicit scan of all 178 committable files for the token value = **0 hits**. Real-data confirmations the synthetic payloads couldn't give: `adjusted_close` diverges from `close` on split/dividend rows (AAPL 2019-01-02 close 157.92 / adj 37.4692; recent 2026-04-14 close 258.83 / adj 258.5917); GSPC index `volume` = 5,032,380,000 (> Int32 — proves `long` is required); splits carry the real `"4.000000/1.000000"` string ratio; dividends carry the full field set with `unadjustedValue` (old rows: adj 0.00098 vs actual 0.10976). D69 `cash_per_share` round-trips as TEXT `"0.27"`→`0.27m`. **No expected-value assertion needed changing beyond swapping synthetic literals for the real captured values** (all listed in the diff).
**Resolves** the "real-payload parse fixtures pending" flag noted in the 1.2 and 1.3 entries below. (Local commit; not pushed — user pushes at the gate. Checkpoint 1.4 not started.)

### 2026-07-13 (Phase 1) — Checkpoint 1.3: corporate-actions dividends + splits ingestion (FR-3)
**Shipped.** `ICorporateActionIngestion`/`CorporateActionIngestion`: EODHD dividends → `corporate_actions(type='dividend', ex_date=effective_date=event date, cash_per_share=unadjustedValue as decimal→TEXT D69)`; splits → `corporate_actions(type='split', effective_date=event date, ratio=new/old REAL)`. **Ingest+type only** — `processed_on` stays NULL until the ledger applies actions in Phase 2 ("Corporate-action semantics complete"). Idempotent on (security_id, type, effective_date) so a backfill re-run never duplicates an event. End-to-end from `ParseDividends`/`ParseSplits` (1.2) through ingestion, offline.
**Verified.** Data tests **53** (was 49); `tools/ci.ps1` green (**75** total). Tests: dividend row (ex_date + unadjusted cash + processed_on NULL), split row (parsed ratio + processed_on NULL), idempotent re-ingest (no dupes), and the D69 round-trip (`typeof(cash_per_share)='text'`, stored `'0.255'`, reads back `0.255m` — no float drift).
**Red / known-broken:** none.
**Deferred (deliberately):** applying corporate actions to the ledger (Phase 2); merger/spinoff/delist rows (delist is derived from IVV drop-out in 1.4; mergers/spinoffs aren't on the launch tier).
**STOP — session boundary (per user directive):** checkpoints 1.0–1.3 shipped and committed; **paused for review before 1.4** (index membership: IVV primary + Wikipedia cross-check + C-4 header guard). Two open items for the reviewer: (1) **real-payload parse fixtures** — the /eod, /splits, /div parse tests use INTEGRATIONS-shape-faithful payloads; the actual 2026-07-13 captures should be dropped into tests/Fixtures as byte-real inputs; (2) the "EODHD bars backfill (S&P 100) + daily delta" DoD box stays unchecked pending the backfill CLI (1.10) + the user's live run.

### 2026-07-13 (Phase 1) — Checkpoint 1.2: EODHD provider + versioned bars + watermark read (FR-1/FR-2)
**Shipped.** `IMarketDataProvider`/`EodhdMarketDataProvider` (FR-1): `GetEod/Dividends/Splits` via the resilient client + raw-cache archive; pure static `ParseEod`/`ParseDividends`/`ParseSplits`/`ParseSplitRatio` (INTEGRATIONS §1 shapes). `/eod` stores raw OHLCV + `adjusted_close`→`adj_close` only — `adj_open/adj_high/adj_low` left NULL (EODHD has no adjusted OHL); split string ratio parsed on `/` (`"4.000000/1.000000"`→4.0), fails loudly on drift. `IBarIngestionService`/`BarIngestionService` (FR-2/D40): correction inserts `version = MAX(version)+1`, never UPDATE/DELETE; identical re-fetch is idempotent. `IBarReadService`/`BarReadService`: latest version WHERE `observed_at ≤ watermark` (ordinal ISO compare).
**Verified.** **FX-BarCorrection green** (v1 day 10; v2 correction observed day 15): a day-12 watermark reproduces v1 byte-identically; a day-16 watermark sees v2; append-only (2 versions, prior untouched); idempotent re-fetch; adj-OHL NULL. Parse tests cover /eod field mapping, the split-ratio rule (incl. reverse split + malformed-fails-loudly), and /div ex-date + adjusted/unadjusted. Data tests **49** (was 33); `tools/ci.ps1` green (bars append-only grep clean; **71** total).
**Red / known-broken:** none.
**Deferred (deliberately):** the "EODHD bars backfill (S&P 100) + daily delta" half of the DoD box is the backfill CLI (checkpoint 1.10) + the live run (user, decision #1) — so that box stays unchecked though versioned bars + FX-BarCorrection are done. **Real-payload parse fixtures pending:** the /eod, /splits, /div parse tests currently run on INTEGRATIONS-shape-faithful payloads; asking the user for the actual 2026-07-13 captures to drop into tests/Fixtures as byte-real inputs.
**Next:** 1.3 — corporate-actions dividends + splits ingestion (FR-3).

### 2026-07-13 (Phase 1) — Checkpoint 1.1: security master + ticker_history (FR-3)
**Shipped.** `ISecurityMaster`/`SecurityMaster` in `AlphaLab.Data/Services`: permanent `security_id` identity (D39), time-ranged `ticker_history` aliases, `ResolveAsOf` (interval `valid_from ≤ asOf < valid_to`, ordinal ISO compare in memory — no dependence on EF string.Compare translation), `Register`, `ResolveOrRegister`, and `RecordTickerChange` (closes the open alias, opens a new one under the SAME id, renames `securities.current_symbol`, writes a `corporate_actions(type='ticker_change')` row — all in one atomic SaveChanges, zero identity break). Added a shared `TestDb` helper (throwaway migrated on-disk SQLite).
**Verified.** **FX-TickerChange green** (ACME→ACMX on day 40 of an 80-day series, position held through): same id on both sides of the rename, valid_from-inclusive/valid_to-exclusive boundary, one security + two aliases + one ticker_change action (zero churn), and 80 continuous bar rows joining to the single id with no break. Data tests **33** (was 30); full suite green via `dotnet test`.
**Red / known-broken:** none.
**Next:** 1.2 — `IMarketDataProvider`/`EodhdMarketDataProvider` + versioned-bar ingestion + watermark read service + `FX-BarCorrection`.

### 2026-07-13 (Phase 1) — Checkpoint 1.0: data-domain schema + shared plumbing
**Shipped.** The Phase-1 schema foundation and the provider plumbing every later checkpoint stands on. **Nine data-domain tables** added to `AlphaLabDbContext` and migration `20260713232437_Phase1DataFoundation` (SCHEMA §Identity & Market Data + §v1.8, verbatim): `securities` (+ partial unique `ux_securities_active_symbol WHERE delisted_on IS NULL`), `ticker_history` (+ `ix_ticker_hist_symbol`), `sector_changes`, `bars` (PK `(security_id,date,version)` + `ix_bars_observed`), `corporate_actions` (8-value `type` CHECK; `cash_per_share` decimal→TEXT D69, `ratio` REAL), `index_membership_log`, `index_membership`, `trading_calendar` (`session` CHECK), `api_usage_log`. Deferred to Phase 2 (unbuilt by design): `regime_labels`, `regime_episodes`, `features`, `factor_returns`, `factor_refresh_log`, the `ux_runs_ok_forward` partial index. **Rule-14 hand-edit** applied to exactly the three new bare-INTEGER-PK tables (`securities.security_id`, `corporate_actions.action_id`, `index_membership_log.log_id`) — `.Annotation("Sqlite:Autoincrement", true)` stripped; the other six new tables are composite/TEXT PKs and needed no edit. **Shared plumbing** in `AlphaLab.Data`: `Http/ResilientHttpClient` (hand-rolled, no Polly — 30s timeout, 3 retries exp-backoff + injectable jitter, circuit-break after 5 consecutive failures), `Http/RawCache` (`FileRawCache` + `NullRawCache`, archives to `tools/raw-cache/{source}/{date}/`, gitignored), `Services/ApiUsageLog` (`ApiUsageHeadroom.HasHeadroom` ≥50% rule + `ApiUsageLogWriter` upsert).
**Verified.** `tools/ci.ps1` green — build + **52 tests** (was 39: Data 30, Worker 8, Api 6, Core 5, +3 placeholders) + guards. **1.0 acceptance gate (the fragile step) passed:** `SchemaFidelityTests.Schema_IntegerPrimaryKeys_HaveNoAutoincrement` green on all three new tables + the `sqlite_sequence`-never-created backstop; `.Designer.cs`/`ModelSnapshot.cs` left untouched by the hand-edit; `dotnet ef migrations has-pending-model-changes` = "No changes …" (no `PendingModelChangesWarning`). New tests: schema fidelity (14-table set, Phase-1 CHECKs, PK auto-assign, type-CHECK rejects unknown), plumbing (retry/circuit/non-2xx, raw-cache write, headroom theory, usage-log upsert). Updated `SchemaStartupTests` to the 14-table migrated set.
**Red / known-broken:** none (the two transitive NU1903 advisories persist, non-blocking).
**Deferred (deliberately):** the Phase-2 regime/feature/factor tables above; providers + services (land in 1.1–1.10). Session scope is checkpoints 1.0–1.3, then a review stop before 1.4.
**Next:** 1.1 — security master + `ticker_history` service (`ISecurityMaster`) + `FX-TickerChange`.

### 2026-07-13 (later) — Phase 0 skeleton review fix-up (v1.9.8, P0-1…P0-6)
**Shipped.** Applied the six findings of the 2026-07-13 deep-dive review of the shipped skeleton (findings **P0-1…P0-6**; the review prose is not retained — all findings folded into the docs + CHANGELOG v1.9.8). **P0-1 (High, the one unmet DoD claim):** `ScreenCatalog` now carries all **13** non-parameterized §21 screens (added trades, go-live-log, regimes, risk, admin-interventions with UX-8c hints; was 8) — the empty-DB client renders every one; the 2 parameterized screens are excluded by design (comment). **P0-2:** the `AlphaLabDbContextFactory` doc comment matches the `E:`-literal three-spots reality. **P0-3:** `ci.ps1` gains `Assert-ReferenceGraph` (full graph at the `<ProjectReference>` level, per BUILD 0.1) and an EAP-safe `git ls-files` call. **P0-4:** the two resolver tests assert `sp500` vs `sp100` path distinctness (`Assert.NotEqual`); 39-count intact. **P0-5:** the dangling `docs/reviews/DEEP_DIVE_REVIEW_2026-07-12.md` references (8 sites) redirect to `CHANGELOG_v1.9.md` — policy: review prose is not kept as files, findings live in the docs. **P0-6:** `ArenaRegistry.IsFallback` + a fail-closed config-error banner when the `Arenas` registry is missing (hard rule 10) instead of a silent self-call.
**Verified.** `tools/ci.ps1` green — build + **39 tests** + guards (now incl. the full reference-graph guard). No architecture/schema/decision change; D1–D73 unchanged.
**Deferred (deliberately) — UI workstream (D65, due before Phase 7 exit):** the review's §B UI recommendations are now captured in the docs (they were **not** implemented — D65 sanctions API-only operation through Phase 4): the v5 design-token table + the semantic-color reservation (`--gold`=live, `--violet`=replay, `--band`=population) live in `UX_GUIDELINES_v1.9` ("Visual system — design tokens"); the shell-theming pass is documented as deferred BUILD **checkpoint 0.7g** (dark chrome, mono numerics, self-hosted fonts, restyled empty-state cards, `NotFound.razor` in-voice, Bootstrap-dist trim) — do it whenever the shell's look is worth the time. Residual-risk recommendations captured: C-2 (S3 percentile sampling band → MONITOR 8½ + Phase-4 gate), C-3 (verdict-time expectation → root README), C-4 (IVV CSV header fixture → Phase-1 prompt), C-5 (GSPC.INDX EOD verify — already on the Phase-1 checklist, no action); C-1 and C-6 logged as decision proposals below.
**Decision proposals (need a D-number):** FR-23 "hypotheses" research action has no §21/`jobs.kind` home; Phase-4 detection-power sweep across ~3 alpha levels. Both recorded below.
**Next session starts with:** Phase 1 (data foundation) — MASTER §13–14 + §20.5, SCHEMA, INTEGRATIONS, TEST_PLAN §2.

### 2026-07-13 — Phase 0 complete (skeleton)
**Shipped.** The full Phase-0 skeleton — repo + solution + all wiring, no business logic yet. `git init` + `.gitignore` (secrets / bin / obj / db). 8 src + 7 test projects in `AlphaLab.slnx` (SDK-10 XML solution); Central Package Management (`Directory.Packages.props` + `Directory.Build.props`, no `UserSecretsId`); `dotnet-ef` as a local tool. Versions pinned: `Microsoft.*`/EF/`dotnet-ef` = runtime **10.0.9**, `Scalar.AspNetCore` **2.16.11**, `Quartz.Extensions.Hosting` **3.18.2**, test stack (`Microsoft.NET.Test.Sdk 17.14.1` / `xunit 2.9.3` / `xunit.runner.visualstudio 3.1.4` / `coverlet.collector 6.0.4`).
- **Core:** D66 discriminated `ReadModelStamp`; the 15 §21 empty read-models; `AlphaLabJson` (snake_case names + enums, nulls emitted); `AlphaLab.Core.Arenas.{ArenaEntry,ArenaRegistry}` (`FromEntries` → first entry active; unit-tested, so Web references Core only).
- **Data:** `DbPathResolver` (pure `ResolvePath` + writer `Resolve`); `AddAlphaLabData(cs, arenaId, ensureDirectory)`; `AlphaLabDbContext` maps the five infra tables 1:1 to SCHEMA (four CHECKs, **no CHECK on `runs.status`**, composite `config (key,version)` PK with `Version` `ValueGeneratedNever()`, seeded `worker_state`). `InitialCreate` = five infra tables only; **hand-edited to drop `Sqlite:Autoincrement` on `run_id`/`job_id`** so both are plain `INTEGER PRIMARY KEY` per SCHEMA (rowid still auto-assigns; the snapshot keeps `ValueGeneratedOnAdd` → no `PendingModelChangesWarning`). `DefaultConnectionString` = the E: literal, byte-identical to both appsettings (three-spots rule, `ConfigConsistencyTests`).
- **Worker (sole writer, D59):** D67 config builder (`appsettings.json` + `appsettings.Secrets.json` only); `SchemaStartup : IHostedService` (registered before Quartz + the runner) migrates → `PRAGMA journal_mode=WAL` → verify `wal` or fail startup (both modes); `WorkerModeParser.Resolve` (OnDemand default; `--serve`/`Worker:Mode=Scheduled` → a Quartz stub idles); `Phase0MissedSessionResolver` (nothing to do → `StopApplication`, exit 0); every log record tagged `arena=sp500`.
- **Api (reader + bounded Phase-3 writes, D57/D59):** `AddAlphaLabData(ensureDirectory:false)` (never creates the store); native OpenAPI + Scalar served unconditionally; `/swagger`→Scalar; D60 error envelope (a 500 handler + `MapFallback`→404 `not_found` on unknown routes); dev CORS; localhost bind via `Urls` (127.0.0.1:5230); the 15 §21 stub read endpoints + `/health`, all `no_run_yet`.
- **Web (Core-only WASM):** the `Arenas` registry in `wwwroot/appsettings.json`; `ReadModelClient` targets the active `baseUrl`; empty-state pages; finding-121 hygiene (title `AlphaLab`, no tutorial links, arena `DisplayName` header, per-screen `EmptyHint`, day-one home note).
- **Tools (ASCII-only `.ps1`):** `Resolve-AlphaLabConnection.ps1`, `snapshot-db.ps1`, `migrate.ps1` (`--connection` from the Worker appsettings, gated on `$LASTEXITCODE` not stderr), `ci.ps1` (build + test + guard greps: bars word-boundary, committed key patterns, Web `using`/`<ProjectReference>` isolation).
**Verified.** `tools/ci.ps1` green — build + **39 tests** (Core 5 · Data 17 · Worker 8 · Api 6 · 3 placeholders) + guards. Worker OnDemand: `arena=sp500`, migrate, `journal_mode=wal`, nothing-to-do, exit 0 (idempotent); `--serve` idles Quartz. Api over real HTTP: `/openapi/v1.json` + `/scalar/v1` = 200; `/api/v1/strategies` = `{"stamp":{"status":"no_run_yet","run_id":null,"watermark":null,"as_of":null},"rows":[]}`; an unknown route → the D60 404 `not_found` envelope; `/api/v1/replay` `quarantined:true`; bound to `127.0.0.1` only; CORS header present for the Web origin. `migrate.ps1 -Arena sp500` snapshots then migrates the same file via `--connection`, exit 0. Web serves title `AlphaLab` + the Arenas registry. `appsettings.Secrets.json` gitignored + untracked.
**Red / known-broken:** none. Two transitive NU1903 advisories (`SQLitePCLRaw.lib.e_sqlite3 2.1.11` via EF Core; `Microsoft.OpenApi 2.0.0` via AspNetCore.OpenApi) are non-blocking (no `TreatWarningsAsErrors`) and clear on Microsoft's next 10.0.x servicing bump.
**Deferred (deliberately):** `config`/`catchup_log`/`jobs` are created-but-dormant (empty by design, not a bug). Data providers, data-domain tables, the `ux_runs_ok_forward` partial index, the D72 heartbeat/drain runtime behavior (config keys exist, behavior lands Phase 2), and `SchemaStartup`'s Phase-1 fail-fast are later phases. The browser render of the WASM screens is not automated (the client builds, the API serves it with CORS, and the served page/config were verified).
**Next session starts with:** Phase 1 (data foundation) — MASTER §13–14 + §20.5, SCHEMA, INTEGRATIONS, TEST_PLAN §2.

<!-- TEMPLATE:
### YYYY-MM-DD — Phase N
**Shipped:** …
**Red / known-broken:** …
**Deferred (deliberately):** …
**Decision proposals (need a D-number):** …
**Next session starts with:** …
-->

## Ops log (restore drills, off-machine backups, monthly hygiene)
<!-- YYYY-MM-DD — restore drill from backup {date}: PASS/FAIL, notes -->

### 2026-07-15 — Applied the D76–D78 migrations to the live `sp500` arena
The v1.9.13 code shipped the three migrations; this records that the **store** now has them (a distinct claim — a migration existing in the repo is not the same as it being applied to `E:\AlphaLabDatabase\sp500\alphalab.db`).
- **Pre-check (read-only):** the duplicate-identity query — `SELECT security_id, type, effective_date, COUNT(*) FROM corporate_actions GROUP BY security_id, type, effective_date HAVING COUNT(*) > 1` — returned **0 rows across 6,273** corporate-action rows. So the D76 `version = 1` backfill had nothing to collide with and `ux_corporate_actions_identity` could create cleanly. Worth recording because the pre-D76 ingestion guarded with `Exists(security_id, type, effective_date)` — a **DB** query — while `SaveChanges()` sat after the loop, so two rows added in one batch were invisible to the guard: a within-batch duplicate (e.g. a regular + special dividend on one ex-date in a single payload) was possible **in principle** and provably **never occurred** in this data.
- **Snapshot (rollback artifact):** `E:\AlphaLabDatabase\sp500\snapshots\alphalab-20260715-214859.db` (+`-wal` +`-shm`), taken by `tools/migrate.ps1` **before** applying (snapshot-first, rule 14). WAL was **0 bytes** at snapshot time, so the copy is consistent.
- **Applied** (via `tools/migrate.ps1 -Arena sp500`), in order: `CorporateActionVersioning`, `DataQualityFlags`, `BarsDateIndex`.
- **Verified post-migration (read-only):** `corporate_actions.version` column, `data_quality_flags` table, and indexes `ux_corporate_actions_identity` / `ix_corporate_actions_observed` / `ix_bars_date` **all present**; `corporate_actions` unchanged at **6,273 → 6,273** (the unique index dropped/collapsed nothing); the collision query re-run post-migration → **still 0 rows**.
- **State:** the live `sp500` arena is on the **D76–D78 schema as of 2026-07-15**.
- **No off-machine backup taken — deliberately, and this reasoning expires at the first Phase-2 write.** At Phase 1 the store is ~entirely re-fetchable: the market/reference data is ~304 EODHD calls (~6 min per `docs/REBUILD.md`), the trading calendar is *generated* not fetched, and the point-in-time record is a single uniform `observed_at` with **zero v2 rows** — no correction history exists to lose. So the snapshot above is sufficient and an off-machine copy buys nothing yet. That stops being true at the **first Phase-2 write**: `trades`, `decisions`, `equity_curve`, `go_live_log`, `journal_entries` and the rest of the 28 not-yet-created tables are the lab's **own output** — no provider returns them, and a lost row is lost for good. **Off-machine backup becomes mandatory then, not now.**

## Decision proposals awaiting a D-number
<!-- Anything the docs don't cover. Approved ones move to MASTER §2. -->
> **Status:** three were **DECIDED** in v1.9.13 and are now in MASTER §2 — `corporate_actions` versioning (**D76**), `data_quality_flags` (**D77**), the cross-sectional bar read (**D78**); membership provenance was **RESOLVED** in v1.9.14 (finding 163, contract-only, no D-number). All four are kept below (marked ✔) as the record; the D42 Phase-2/6 phasing (finding 168, reported v1.9.15) was **RESOLVED** in v1.9.16 (finding 169, a BUILD-phasing edit, no D-number); **FR-23 hypotheses was RESOLVED in v1.9.21 → D82** (endpoint KEPT, `jobs.kind` CHECK extended by migration — ✔ below). **Still open:** **P1** Stage-1 `liquid` filters nothing and **the S&P 500 widening** (mechanism) (both before the widening / Phase-4 sign-off — decide them together) — the **S&P 1500 widening-target** proposal is now **DECIDED → D87 (contingent** on a verified-depth 400/600 history; else S&P 500), v1.9.33; **P2** the D76 batch-duplicate guard at ingestion, **P3** `features` needs a PIT column (before any feature cache, Phase 6), **P5** drop `corporate_actions.processed_on` (with the next CA migration), **P6** regime-label replay quarantine — `regime_labels` needs a `run_kind` or replay must not persist labels (before Phase 4), **P7** a one-time gate sweep of the never-gated backfill (needs a `bars.gated_through` marker; before Phase 4), the Phase-4 detection-power sweep (D64 territory), **P8** the researcher seat's evidence-diet gate (before Phase 5), **P9** frozen-position valuation doc-vs-code (✔ **RESOLVED → D86**, v1.9.29), **P10** catch-up `observed_at` fabrication (before Phase 4), and **P11** rejected-T+1-fill policy (✔ **RESOLVED — blessed, no D-number**, v1.9.29), and **P12** the funnel cash-shortfall allocation policy (scale-to-fit chosen → **D84**; richer alternatives deferred), and **P13** the no-LLM twin's Stage-2 scoring rule (equal-weight z-score pack blend chosen → **D85**; the pure-placebo and tuned-rule foils recorded as rejected). **P4 (orders-table) is now ✔ SETTLED on `stage_json`** (revisit window closed at 2.10, decision unchanged).
- **P1 — (2026-07-17, from the Phase-2 build / checkpoint 2.4 / CHANGELOG finding L) Stage-1's `liquid` gate filters nothing, and after the sp500 widening that stops being harmless.** §6's funnel diagram describes Stage 1 as "(in-index flag, liquid, priced)". `grep -in liquid` across MASTER finds nothing that defines `liquid` as an **eligibility** concept: D20 says "Liquid default" (an S&P-500 universe is liquid by construction, so no filter is needed), and D43's "liquidity bucket" prices a *fill*, not a *name*. There is no `Eligibility.*` config block and no threshold to read. Not inventing `Eligibility.MinAdvUsd` is **correct and stays** — "never invent a key" is binding, and a threshold chosen by an implementer is a design decision smuggled in as an implementation detail. The only non-inventing substitute is a data-availability check ("a computable 21-session ADV window with non-zero volume"), which **every S&P 100 name passes** and which duplicates what `DataQualityGate` already catches: a no-op wearing the costume of a filter, which is worse than nothing. **So the accurate statement is "Stage-1 `liquid` currently filters nothing" — not "liquid pending a threshold"**, and that is what the `Eligibility` code comment and `FR7_Stage1_LiquidGateFiltersNothing_AVanishinglyIlliquidNameIsStillEligible` both say. **Why this must be decided, and when:** today the no-op is harmless — a liquidity gate would remove nobody from the S&P 100. After the D70 widening to the S&P 500 the tail genuinely **is** less liquid, and Stage 1 will still filter nothing while §6 still says it does; Phase 3 will meanwhile have read a funnel with a liquidity stage and trusted it. Proposal: either (a) define an `Eligibility.*` threshold (a new CONFIG key → needs a D-number), or (b) strike `liquid` from §6's Stage-1 description. **Decide before the sp500 widening, not at it.** Pairs with the D70-widening proposal below.
- **P2 — (2026-07-17, from the Phase-2 build / the D76 seam) the batch-duplicate guard belongs at INGESTION, and the ledger cannot detect the collapse.** Per D76, two genuinely-distinct actions on one `(security_id, type, effective_date)` — a regular + a special dividend sharing an ex-date — collapse to one versioned line. The ledger-side handling is right and stays: its input is a **list**, the dividend handler applies every entry and asserts nothing about count, so when a feed-supplied discriminator eventually splits the pair, `GetActionsAsOf`'s grouping changes and **the ledger needs zero change**. **But that does not catch the seam, and this entry exists so nobody believes it does.** The collapse is *upstream*: `GetActionsAsOf` groups by `(Type, EffectiveDate)` and takes max version, so the ledger receives one entry, never two — the planned N-dividends-on-one-date test runs against a read path that **would already have collapsed them**, proving the ledger is future-proof rather than proving the seam is caught. The detector must sit at **ingestion**, where the payload is still intact. This **sidesteps D76's own caveat** ("any discriminator must be feed-supplied, not amount-based") because it *is not a discriminator*: a duplicate identity **within a single input batch** is provably not a restatement — one payload is one observation. It doesn't decide which entry is the special; it detects that the identity isn't unique in this payload, which is precisely the seam D76 names. Proposal: fail closed at ingestion (rule 10), mirroring D76's "stop-and-report seam" wording. **Not built** — not a Phase-2 FR, and zero occurrences across 20y × 101 names (the Ops log's pre-check confirms 0 collisions in 6,273 rows), so it is prospective, not present.
- **P5 — (2026-07-17, from the Phase-2 build / checkpoint 2.7 / CHANGELOG finding V) drop the `corporate_actions.processed_on` column — a deferral, not a resolution.** The column is now provably dead: ALWAYS NULL, never written (a global column on a per-account operation would make a replay skip an action a forward run marked; D76 forbids UPDATE here; ledger idempotency is one-transaction-per-day + `ux_runs_ok_forward`). A column that can never be correctly written and buys nothing is bait, not documentation — the honest end state is to drop it. **Why not now:** dropping it is a schema migration (rule 14, snapshot-first) + a D-number, and the live store already carries it, so this is not free. **This session did the two things that ARE free** — rewrote the SCHEMA/entity comment so no future implementer wires against it, and widened the CI guard so it can never be written. Dropping the column is logged here as the deferred final step, to be taken with the next `corporate_actions` migration (or a dedicated one) rather than bundled speculatively now. Changes SCHEMA + a migration → needs a D-number.
- **P4 — (2026-07-17, from the Phase-2 build / checkpoint 2.5 / CHANGELOG finding Q) an `orders` table vs `decisions.stage_json` — DECIDED in favour of stage_json, with a revisit deadline.** The word "orders" appears nowhere in SCHEMA as a stored thing and no FR names one; SCHEMA:207 designates `decisions.stage_json` as the "funnel stages 1-6 snapshot", and Stage 6 *is* orders. The operator asked whether one is needed and confirmed proceeding without it (this session). **Decision:** stage_json carries the orders decided at close T until the T+1 run fills them. **Rationale:** an order here has no lifecycle — decided at close T, fully resolved at open T+1 (filled / clipped to `capacity_rejections` / rejected with a reason), never working or carried to T+2 — so a table would hold write-once/read-once rows and would be a *second copy* of a fact stage_json must record anyway, and two copies of one fact drift. **Cost acknowledged:** stage_json is opaque to SQL (a "which orders never filled?" query means parsing JSON), but `capacity_rejections` already carries the rejection audit as a real table and Phase-3 read-models run on `trades`/`equity_curve`, not orders. **The revisit window is real and bounded:** M1 is committed but unapplied and the first real writes land at **2.10**, so until then a table is a migration with nothing to backfill; after 2.10 it is a backfill out of JSON. Reopen before 2.10 if the real stage_json shape (now in hand) argues for it; otherwise this is settled. **Not a schema change as decided** (stage_json already exists) — a table WOULD be a D-number + migration, which is why it is logged here rather than done. **✔ SETTLED (2026-07-17, checkpoint 2.10): the revisit window closed on `stage_json`, decision unchanged.** The real stage_json shape built at 2.5 (finding Q) and exercised end-to-end by the D53 pipeline at 2.10 confirms the rationale exactly — the pipeline's `FillPriorOrders` reads yesterday's `Stage6Orders` back out of `stage_json` and fully resolves each (filled / clipped to `capacity_rejections` / rejected) in a single T+1 run, so an order genuinely has no lifecycle a table would track, and `capacity_rejections` already carries the SQL-queryable rejection audit. Nothing in the shape-in-hand argues for a table. Closed.
- **P6 — (2026-07-17, from the Phase-2 build / checkpoint 2.8 / CHANGELOG finding W) `regime_labels` has no `run_kind`, and Phase-4 replay would overwrite the forward label.** The regime label is a market-level fact keyed by `as_of` (PK `as_of`, no `run_kind`), and `RegimeLabelService.ComputeAndSave(asOf, watermark)` upserts one row per session. In **forward** operation this is exactly right — each `as_of` gets one deterministic label at the forward watermark, and `inputs_hash` carries the (proxy id, params, watermark) provenance. But **Phase-4 replay** recomputes historical labels at the *replay* watermark, which differs from the forward one, so it would produce a different `trend`/`vol`/`inputs_hash` for the same `as_of` — and with `as_of` as the sole PK, that write **overwrites the forward label**, which rule 1 (replay is quarantined from every forward view) forbids. **No risk today:** nothing calls the service yet (the D53 pipeline wiring is 2.10), and the 2.8 service is quarantine-neutral by construction (its signature is `asOf + watermark`, it carries no `run_kind` and cannot tell which it is). The decision belongs to whoever turns replay on. Proposal: either (a) the pipeline computes regime labels for replay **in memory only** and never persists them (the label feeds the funnel; it need not be stored for a replay), or (b) `regime_labels` gains a `run_kind` column and the PK becomes `(as_of, run_kind)` — the `equity_curve` precedent, where `run_kind` is IN the key precisely so a replay of a day cannot overwrite the forward row. (b) changes SCHEMA + a migration → a D-number; (a) is a pipeline rule. **Decide before Phase 4**, alongside the replay engine.
- **P3 — (2026-07-17, from the Phase-2 build / the `features` deferral) a Phase-6 feature cache needs `features` to gain a PIT column before anything may persist into it.** The `features` table has **no `observed_at`/`version`/`watermark` column**, so it cannot express the read rule hard rule 4 demands: persisting a feature computed at watermark W and re-reading it at W′ is a leak that **F-LEAK could not catch** (the test compares reads, and both reads would return the same poisoned row). Phase 2 therefore does not build it — no Phase-2 FR needs it (B&H never re-scores; ThresholdModel is trivial; both need only price, and computing `IFeatureView` on the fly over the versioned bar reader is leak-proof by construction). D53's Stage-2 order is preserved for what exists; the "features" step is simply absent. Proposal: before any code writes to `features`, add a PIT column — a SCHEMA change needing a D-number. Phase 6's real features will say whether a cache is needed at all and which columns it needs, so deciding now would be the wrong order.
- **P7 — (2026-07-17, from the Phase-2 build / checkpoint 2.10 / CHANGELOG finding EE / the plan's finding M) the one-time gate sweep of the never-gated backfilled bars needs a `gated_through` marker, not a daily full-history re-gate.** The bootstrap backfill (PROGRESS 2026-07-15) landed ~488k bars that `DataQualityGate` never vetted, and bars carry no "already-gated" flag. The plan's finding M asks the pipeline to gate the backfilled history — but the two obvious readings are both wrong: re-fetching + re-gating 20 years every day would hammer the EODHD budget, and gating the whole stored series every day would **re-emit every historical warning on every run** (flag spam). What the D53 pipeline actually does (2.10) is the correct ONGOING behaviour — gate the fetched delta with a bounded context tail, and drop flags on dates already gated (≤ `LastStoredDate`) so nothing re-emits. That leaves the **historical backlog** ungated. Proposal: a **one-time sweep** — run `DataQualityGate` over each security's full stored series once and persist the flags, gated by a new `bars.gated_through` (or a `data_quality_sweeps` marker) so it runs exactly once per security and the daily path can trust "everything ≤ gated_through was checked." Changes SCHEMA (a marker column/table) + a migration → needs a D-number. **Not urgent** (the backfilled S&P 100 bars are EODHD-clean; the gate's value is forward), so it is logged rather than built; decide before Phase 4 replay depends on flag completeness, or before the sp500 widening enlarges the ungated backlog.
- ✔ **RESOLVED v1.9.21 → D82** (MASTER §2 + §23.4; the hypotheses endpoint is KEPT — `POST /api/v1/analysis/hypotheses`, `jobs.kind` CHECK extended by snapshot-gated migration, output = a draft `journal_entries` hypothesis citing parent evidence, operator-as-registrar; BUILD's FR-23 line updated in v1.9.23, finding 178). **(2026-07-13, from the v1.9.8 review / A.3-2 / C-6) FR-23's "hypotheses" research action has no home.** FR-23 lists research-assistant endpoints as "briefs, **hypotheses**, skeptic", but §21's command list has only `POST /analysis/brief` + `POST /analysis/skeptic`, and the `jobs.kind` CHECK allows only `('replay','analysis_brief','analysis_skeptic')`. Before Phase 5 planning, decide: either add a snapshot-gated migration extending the CHECK + a §21 command entry (per finding 121's "enum CHECKs extend only via migration"), or strike "hypotheses" from FR-23. No action until then.
- **(2026-07-13, from the v1.9.8 review / C-1) Phase-4 detection-power sweep.** `Calibration.Plant.AlphaAnnualPct` defaults to a single 2.0% edge, so the Phase-4 report certifies edge-plant *survival* at one alpha but never answers "how big must an edge be before this lab certifies it within my patience horizon?". Proposal: sweep the edge plant across ~3 alpha levels (e.g. 2/4/8% ann, same seeds/plant) and archive an empirical detection-power section (P(promoted by t | α) curves + median days-to-promotion per level), validating the analytic MDE end-to-end against the machinery. Changes a Phase-4 deliverable (D64 territory) → awaiting a D-number before editing D64.
- **P8 — (2026-07-18, from the v1.9.23 reconciliation / MASTER §23.4) the researcher seat refuses to propose while any D52 outcome is overdue — the loop enforces its own evidence diet.** D82's parent-evidence rule makes every proposal cite an outcome/finding/attribution row, but nothing stops the seat proposing while unclosed outcome nags pile up — D52's outcome-closure nag is a GUI nudge, not a gate, so the generator can compound on stale ground truth. Proposal: the hypotheses endpoint refuses (422, named reason) while any journal outcome is overdue past its declared evidence window — an unclosed outcome starves the generator's evidence base, so the loop should stop generating instead of relying on the nag. **Counter-consideration (why this needs a decision, not a reflex):** a hard gate can deadlock the loop if the operator is away (nothing closes ⇒ nothing proposes); a grace window (refuse only when an outcome is > N days overdue) may be wanted, which would be a new CONFIG key. Changes the endpoint contract (+ possibly a key) → needs a D-number. **Decide before Phase 5** (the endpoint ships there).
- ✔ **DECIDED v1.9.29 → D86** (MASTER §2 + §13.6 line 422 amended to cost-basis; §0 provenance; TEST_PLAN `FX-Unmapped`; D-range banners → D86; CHANGELOG finding 210 — option **(a)** chosen; the code already conformed, this aligns the doc; terminal delisting untouched). **P9 — (2026-07-18, from the Phase-2-close code review / finding 191) frozen/unpriced-position valuation: MarkToMarket's cost basis vs §13.6's last print — doc and code disagree silently.** `DailyPipeline.MarkToMarket` values a held name with no bar today at its **average cost basis** (and its comment defends this: neither a gain nor a loss recognised), while MASTER §13.6 says a frozen position's valuation **freezes at last print**. Either is defensible; disagreeing silently is not. Proposal: (a) amend §13.6 to cost-basis valuation (a D-number — it changes the stated fail-closed semantics), or (b) fix the code to last-print. **Decide before Phase 3** — `equity_curve` read-models consume this valuation, so the choice shapes the first honest screens.
- **P10 — (2026-07-18, from the Phase-2-close code review / finding 194) catch-up stamps recovered days with the session-derived watermark — a fabricated PIT observation date.** Recovered days write `observed_at = {as_of}T22:00:00Z`, as if the data had been observed the evening of the session, when it was actually observed at recovery time (possibly days later). This may be the right trade — it keeps a recovered day byte-identical to the same day run live, which is D47's idempotency story — but it is a **point-in-time fiction** that deserves a D-number stating it, not a code comment. Replay reasons over `observed_at` (the D40/D76 read rules), so the fiction's blast radius must be stated before replay exists. Proposal: (a) a D-number blessing the session-derived stamp with its rationale and stated limits, or (b) stamp true observation time and make catch-up's determinism story explicit about the consequences. **Decide before Phase 4.**
- ✔ **RESOLVED v1.9.29 — behaviour blessed, no D-number** (MASTER §13.6 fills note; CHANGELOG finding 211; option **(a)** drop-and-let-the-next-decision-re-open, documented; the bounded re-plan (b) rejected as a second decision authority, cf. D84). **P11 — (2026-07-18, from the Phase-2-close code review / finding 196) rejected T+1 fills are dropped — a one-day rejection becomes a silent permanent underweight.** `FillPriorOrders` logs a `FillResult.Rejected` (recording a capacity clip when present), then drops the order; nothing re-plans it, and the next decision may or may not re-open the name. A participation-cap rejection on one thin day can become a permanently missing position with no visible record beyond a log line. Proposal: state the policy — (a) drop-and-let-the-next-decision-re-open (current behaviour, blessed and documented, with an "unfilled by capacity" surface), or (b) a bounded re-plan (carry the order forward ≤ N sessions under the same cap). (b) changes Stage-6/fill semantics → a D-number. **Decide before Phase 3** (population fills hit the same cap, so the choice shapes the null bands too).
- **P12 — (2026-07-18, from finding 190 / D84) the funnel's cash-shortfall allocation policy — scale-to-fit was chosen (D84); the richer alternatives are recorded here, not built.** When a day's intended new opens exceed available cash, Stage 5 now **scales every open down proportionally** so the total equals available cash (D84 — the chosen policy **(a)**). Two richer behaviours were considered and are logged for a later decision: **(b)** fill the **highest-scored** names in rank order until cash runs out and drop the rest (concentrates the day's cash in the best names rather than shrinking all of them — but makes a name's inclusion depend on the cash-ordering of a single day, a determinism path the seeded tie-break would have to cover), and **(c)** something else. **Explicitly considered and REJECTED — do not build either without a decision reversing this:** **"sell a held position to fund a more attractive new one"** and **"a per-trade maturity / expected-profit-timeline gate that exits a name early to free cash for a better one"** — each creates a **second exit authority** competing with the strategy's declared `ExitPolicy`/`Horizon` (rule 7: only the ExitPolicy or a forced corporate-action event closes), and would contaminate the cross-account comparison (two accounts running the "same" strategy diverge on a cash-driven exit rule neither declared). Any of (b)/(c) changes Stage-5 selection→sizing semantics → a D-number. **Left as a proposal for the operator to decide**; scale-to-fit is a correct, solvent default in the meantime.
- **P13 — (2026-07-18, from the twin-scorer proposal / D85) the no-LLM twin's Stage-2 scoring rule — the equal-weight z-score pack blend was chosen (D85); the rejected foils are recorded here.** D81 mandates that every AI contestant runs beside a mechanics-identical no-LLM twin and makes the paired contestant-minus-twin difference the headline "does the AI add value?" number (M.1), but it never said **how the twin produces a per-name score at Stage 2 once the LLM call is removed** — `FX-TwinPairing` froze everything *around* the score (pre-filter, breadth, sizing, exits, costs, seed) but not the score itself. The scorer sets the counterfactual the LLM is measured against, so it had to be pre-registered, not left for a Phase-6 implementer to invent. **Chosen — (a) the equal-weight, cross-sectionally-standardized (z-score) blend of the same pack features the contestant sees** (→ **D85**, MASTER §23.3): the contestant's whole job is to weigh the factsheet more intelligently than a naive combination of it, so an equal-weight blend of that same factsheet isolates **weighting skill** while holding information constant — the exactly-right foil. Frozen in the contestant's `config_json` at fork time (rule 24/32) so the baseline can't be shopped for after results are seen; identical *form* across contestants (feature set follows `Ai.PackRecipeVersion`) so "does the AI add value?" stays comparable across experiments; ordered degenerate-day handling (drop zero-variance features → equal-score fallback + `degenerate_blend` flag if none survive or <2 names → never NaN). **Considered and REJECTED — recorded so the choice cannot be silently reopened (a change to the twin-rule *form* would need a new D-number reversing this):** **(b) pure equal-scoring** (every shortlisted name scored equally, the pure placebo) — measures the LLM's contribution over expressing no view at all: clean, but too weak a bar ("the AI beat scoring everything evenly" looks impressive while meaning little, since no one would ship equal-scoring); **(c) a tuned or selected simple rule** — the strongest-looking foil, but it reintroduces a post-hoc choice riggable after seeing results, rejected on p-hacking grounds (the exact failure D81's frozen-policy discipline exists to prevent). **Decided → D85** (docs pass v1.9.26); recorded here as the settled-with-alternatives companion, mirroring P12/D84.
- ✔ **DECIDED v1.9.13 → D76** (MASTER §2 + §13.6; SCHEMA `corporate_actions`; CHANGELOG finding 160; migration `CorporateActionVersioning`). **(2026-07-15, from the v1.9.10 Phase-1 review / P1R-4b) `corporate_actions` observed_at is unversioned + unread — dividend restatements are lost and replay leaks the future (the significant one).** `grep -rn watermark src/` returns exactly one service (`BarService`). `corporate_actions` carries `observed_at NOT NULL` that **nothing reads**, has no version column, and `CorporateActionIngestion.Exists(security_id, type, effective_date)` makes ingestion **first-write-wins**. Consequences: an EODHD dividend **restatement is silently discarded forever** (the FR-6 presence-based reconciliation only alarms when *no* event exists); two dividends on one ex-date (a regular + a special) — the second is **silently dropped** by the same dedupe key; and **Phase-4 replay will leak the future** — the ledger prices dividends from `corporate_actions` with no `observed_at ≤ watermark` filter, so a replay pinned to 2015 sees actions observed in 2026, breaking NFR1 (same inputs + watermark + seeds → identical outputs), the exact property D40 exists to buy. MASTER §13.5 states the read rule for **bars**; it was never extended to the feeds the ledger prices on. Proposal: extend D40's versioned-append-only + read-at-watermark discipline to `corporate_actions` (a version column + a CA read service filtering `observed_at ≤ watermark`, mirroring `BarService`), OR consciously scope D40 to bars and state how replay reproducibility survives that. Changes SCHEMA (`corporate_actions`) + MASTER §2/§13.6 + a migration → **D76 territory**. Decide **before Phase 2 builds the ledger** on top of it.
- ✔ **DECIDED v1.9.13 → D77** (MASTER §2 + §15; SCHEMA `data_quality_flags`; CHANGELOG finding 161; migration `DataQualityFlags`; `IDataQualityFlagStore` seam). **(2026-07-15, from the v1.9.10 Phase-1 review) FR-6 quality flags have no home.** The SCHEMA has **zero** tables for quality flags (`grep -i quality docs/SCHEMA_v1.9.md` returns nothing), and MASTER §15's Data-health screen lists cross-check status, watermark, catch-up log, factor freshness, calendar, and API headroom — **no flags**. So `DataQualityGate` computes a `Warn` (gap / outlier / unexplained adjustment) and the report is discarded; the 2026-07-15 backfill entry already defers "persisting quality flags + surfacing them on Data-health" but there is nothing to persist *into*. In a lab whose thesis is that a number is never shown without the honesty that qualifies it, an alarm nobody can see is not an alarm. Proposal: a `data_quality_flags` table + a Data-health slot in §15, decided **before Phase 2 wires the gate** (the wiring is already a Phase-2 item). Changes SCHEMA + MASTER §15 + a migration → needs a D-number.
- ✔ **DECIDED v1.9.13 → D78** (MASTER §2; SCHEMA `bars` `ix_bars_date`; CHANGELOG finding 162; migration `BarsDateIndex`; `IBarReadService.GetCrossSection`). **(2026-07-15, from the v1.9.10 Phase-1 review) No cross-sectional bar read path, and no index to serve one.** `IBarReadService` is per-security (`GetBar`, `GetSeries`). Phase-2's six-stage funnel and Phase-4 replay are **date-major**: "every name at date D at watermark W". `bars` has `PK (security_id, date, version)` + `ix_bars_observed` — nothing serves `WHERE date = ?`, so a cross-sectional read scans. Proposal: an `ix_bars_date` (or equivalent) plus a batch (date-major) read API on `IBarReadService`. Both are schema/contract changes under the snapshot-first rule → needs a D-number. Cheaper to decide now than mid-Phase-2, when the funnel is already built against the per-security shape.
- ✔ **RESOLVED v1.9.14 → finding 163** (contract-only, no schema/D-number; `asOf` threaded into `IIndexMembershipProvider.GetMembersAsync`, rosters archive under `{source}/{asOf}/`; `RawCacheArchivalTests`). **(2026-07-15, from the v1.9.10 Phase-1 review / P1R-4b) Membership roster payloads overwrite each run — no dated archive of "what the index said on date X".** The two membership `rawCache.Save` sites (`WikipediaMembershipCrossCheck`, `ISharesHoldingsMembershipProvider`) archive under literal `"latest"`, so every run overwrites the prior roster payload. `IIndexMembershipProvider.GetMembersAsync` carries no as-of and `index_membership` has no `observed_at`, so the raw archive is the *only* artifact that could ever answer "what did OEF / Wikipedia report on date X" — and none is retained. Proposal: thread an observation date into `IIndexMembershipProvider` and archive under it (dated partitions, like the P1R-4 equity/proxy fix). Changes the **provider contract + archival semantics only — no schema column, no migration** (its CHANGELOG row, finding 143, says the same), so it is decided on its own timeline, not folded into a schema decision. Rhymes with — but is **not** part of — the D40/`corporate_actions` proposal above (that one is schema + migration; this one is contract-only); the two share the provenance theme, not a D-number.
- **(2026-07-15, from the v1.9.11 Phase-1 review / P1R-10 / finding 151) D70's post-Phase-4 widening is not achievable as built.** D70(a) promises the forward universe widens sp100→sp500 "by a config flip + a backfill delta — no code change." Verified against the tree, both legs are unmet. `CONFIG_REFERENCE:39-40` documents `Universe.Bootstrap.Universe = "sp100"` as "consumed by the backfill CLI and Stage-1 eligibility," but the CLI reads `--universe` from argv (`BackfillArgs.Parse`) and hardcodes its bands — it never reads `UniverseOptions`; the doc states a false thing about the code. `UniverseOptions` (`MembershipPrimary="ivv_csv"`, `MembershipCountSanity=[495,510]`, `SectorSource="ivv_csv"` — the S&P 500 setup) is **bound by nothing** (no `Configure`/`GetSection`/`.Bind`/`.Get<>`, no appsettings `Universe` section) and its defaults *contradict* the CLI's actual behaviour, so a future reader would reasonably wire against dead config. `Program.cs:71,75` hardcode `Oef()` + `wikipedia_sp100`. So the widening — a milestone the whole build plan points at — will require code changes to `Program.cs` + the arg parser. **Cost note:** the repo already carries everything the widening needs *except the wiring and the decision* — `tests/Fixtures/IVV_holdings.csv`, `tests/Fixtures/sp500_constituents.html`, and `ISharesHoldingsOptions.Ivv()` all exist. So option (a) is not "build S&P 500 support" but "bind `UniverseOptions`, map `MembershipPrimary` (`ivv_csv`/`oef_csv`) to the existing presets, source the band from config" — testable against fixtures already on disk, which materially lowers the cost. Proposal: (a) make provider selection config-driven per D70(a) so the flip is genuinely a config flip; or (b) correct D70 to state the widening needs a wiring checkpoint and fix `CONFIG_REFERENCE`'s "consumed by the backfill CLI" claim. Changes MASTER §2 (D70) + CONFIG_REFERENCE (+ code under (a)) → **D-number territory**. Decide **before Phase 4 sign-off, not at it.** (v1.9.11 P1R-10 already rejects `--universe sp500` at parse so the trap can't fire meanwhile.)
- **[ACCEPTED → D87, v1.9.33 — the target is now the S&P 1500, CONTINGENT on a verified-depth historical S&P 400/600 as-of-membership source; else it stays the S&P 500. The open questions below are carried into D87 as its Phase-4 gate; the widening *mechanism* proposal above and P1 remain open.]** **(2026-07-18, from the operator) extend D70's widening TARGET from the S&P 500 to the S&P Composite 1500 — a *scope* proposal, distinct from the widening-*mechanism* proposal directly above.** D70(a) currently widens **sp100 → sp500 in place** after Phase-4 sign-off; this proposes the target become **sp1500**. It is **not** the same as the "widening not achievable as built" proposal above — that one is about the *mechanism* (dead `UniverseOptions`, hardcoded `Oef()`+`wikipedia_sp100` providers) and stands whatever the target; this one is about *which* universe the wired flip lands on. Both must clear before the widening. **Rationale.** (1) **Breadth is the binding constraint.** Grinold's Fundamental Law, IR = IC × √BR, makes √1500/√500 ≈ **1.73×** the information ratio for identical skill — at N=101 the lab is breadth-starved, not data-field-starved. (2) **Still an in-place widening.** S&P 1500 = S&P 500 + S&P MidCap 400 + S&P SmallCap 600, so sp500 is a **strict subset**: same D70 pattern, same arena, **no new `Arena.Id`, no D71 multi-arena work** (rule 23). (3) **A free quality tilt without fundamentals.** S&P's inclusion criteria require four consecutive quarters of profit plus a positive most-recent quarter; S&P's own research attributes ~**74.7%** of the S&P 600's long-run outperformance vs the Russell 2000 to quality-factor exposure. The lab cannot build a quality screen (no fundamentals feed), but S&P's index committee applies one as a **membership rule the lab already ingests**. (4) **A free size classification.** Sub-index membership (500/400/600) is a size label inside one arena with no fundamentals field — size conditioning without a second arena and without splitting breadth. (5) **Rejected alternative — Russell 2000:** no S&P-branded historical-constituents source, so as-of membership for replay is unavailable → it **cannot pass Phase 4** (D70's replay-runs-on-as-of-membership prerequisite, rule 22); also ~40% unprofitable constituents. **Open questions — resolve BEFORE this becomes a decision (do not assume):** (i) Does **EODHD carry historical constituents for the S&P 400 and S&P 600**, not just the S&P 500? D49's fja05680 community CSV is **S&P 500 ONLY**; if EODHD is the only source, this widening depends on the **Fundamentals tier** — which would give fundamentals a trigger that is *not* Phase 8's contingent Value/Quality but **membership**. Verify coverage; do not assume. (ii) **D43's cost buckets:** "other: 5bp" is near-empty at N=101 and becomes populated by S&P 600 names at sp1500 — validate the 5bp half-spread against **real S&P 600 ADV notional** before relying on it. (iii) **Constraint to record:** a **Quality strategy must never be tested in this arena** — the index conditions on profitability at the membership level, so the universe is **pre-screened for the very factor being tested** (a matched-null / frozen-parameter hazard, rules 8–9). **(iv) Breadth claim qualified (v1.9.23):** Grinold's √(1500/500) ≈ 1.73× IR assumes the **bet count** scales with the universe; a Top-40 book selected from 1500 names does **not** triple its bets — BR is bets, not names. The honest claim is a better selection pool plus a free size dimension; the IR gain arrives only if selection breadth N widens as well, which immediately meets the D43 cost model and participation caps in the small-cap tail. The decision must not be made on the 1.73× overclaim. **(v) Compute scale (v1.9.23):** the LW covariance solve, the population machinery (~650 members), and the funnel were sized for ~100 names; the Phase-3 "full daily run < 60s" DoD must be **re-derived, not assumed, at 1500 names**, and the `eod-bulk-last-day` wiring stops being optional. **Changes MASTER §2 (D70's target) + likely CONFIG_REFERENCE (`Universe` band + membership source) + possibly the Fundamentals-tier trigger → D-number territory.** Decide **at Phase-4 sign-off, per D70's existing gate** — not before it, and never silently at the flip. Pairs with the S&P 500 widening-mechanism proposal above and with **P1** (the Stage-1 `liquid` no-op, which stops being harmless exactly when the thinner S&P 600 tail enters).
- ✔ **RESOLVED v1.9.16 → finding 169** (BUILD-phasing edit, no new D-number; FR-11 split partial→full — Phase 2 "(partial)" for the dummies' simple/equal sizing, Phase 6 "full" for inverse-vol + LW covariance — per the FR-13/FR-18 convention, grounded in the `Sizing.Mode` enum + DESIGN_IMPROVEMENTS §3.1; D42 itself unchanged). **(2026-07-16, from the v1.9.15 Phase-0/1 review / finding 168) D42 Ledoit–Wolf covariance is claimed by both Phase 2 and Phase 6 — reported, not fixed.** FR-11 (inverse-vol sizing *using Ledoit–Wolf covariance*, D42) is in Phase 2's Key-FR set (FR-7..11), but BUILD §0's v6 changelog says "Phase 6 adds … Ledoit–Wolf covariance (D42)" and the Phase-6 prompt (§4) implements the LW covariance service "consumed by sizing, heat, and momentum's vol overlay" — while Phase 6's Key-FR cell omits FR-11. The likely intent follows the FR-13/FR-18 "partial early, full later" convention (Phase 2 sizes the B&H/Threshold dummies simply; the LW covariance service lands in Phase 6 with the real strategies), but the FR lists don't say so: Phase 2 claims FR-11 unqualified and Phase 6 omits it. Proposal: either (a) mark FR-11 "(partial)" in Phase 2's row + add "FR-11 full" to Phase 6 (mirroring "16..18 (partial)" → "18 full"), or (b) move FR-11 to Phase 6 entirely. A **BUILD-phasing edit, not a new D-number** (D42 itself is unchanged); decide before Phase 2 wires sizing. Reported this pass, not actioned (mirrors finding 151's stop-and-report).
