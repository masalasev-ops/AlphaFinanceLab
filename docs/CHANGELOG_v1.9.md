
---

## v1.9.95 — Phase 6.2: the lifecycle seam closed (2026-08-06)

*Recorded 2026-08-06. **Phase 6 checkpoint 6.2.** Decision **D133** new; findings **389—390**, each with the rule-26 Consequences field. No migration and no schema change: `strategies.config_json` is an existing TEXT column and `strategies.status` carries no CHECK (SCHEMA declares none, deliberately), so `control` needs no DDL. Full suite green at 1,172 tests. Next-free finding after this entry: **391**.*

### finding 389 — the lifecycle seam was two holes, and the second one was invisible

`CandidateFactory` writes a `strategies` row plus a `trials_registry` row and stops; the registry returned null for anything outside three hardcoded ids; `DailyPipeline` logged a warning and skipped. That much was known. **What the 6.2 survey added is that the warning was the LESSER hole**: `DailyPipeline` iterates **ACCOUNTS**, not strategies, and only `DummyRoster` opened accounts — for its three ids. So an admitted candidate with no account was **never reached to be warned about at all**. It could clear the D89 detectability gate, spend a trial from a budget that raises the Bonferroni floor for every candidate after it, and never trade a day, while nothing in any log said so. A fix aimed only at the registry would have closed the visible half and left the silent one. Closed by `StrategyRoster`, **forward only**: a replay opens its own accounts (D37) and generation 2, which the D106/D117 harness reproduces, was built from exactly the roster the replay seeds — opening candidate accounts there would change what trades and risk `FX-RecomputeParity` for no gain, since only a forward track judges a strategy (rule 1). An admitted row this build cannot construct still gets **no** account: an account the funnel skips every day is a worse lie than the missing account it replaced.
**Consequences:** `StrategyRoster` (new) + its wiring in `DailyPipeline`; `StrategyRegistry` replaces `Phase2StrategyRegistry` with one row-driven entry point shared by the daily run and the seeding backtest, so the two can never accept different strategy sets. `RunDay_UnknownStrategyAccount_Skipped_RunStillCommits` is unchanged and still passes — unknown still means unknown.

### finding 390 — a control would have taxed its own arena

`CandidateFactory.CreateCandidate` hardcoded `Status = "candidate"` and added a `trials_registry` row **unconditionally**. D81 rule 4 requires the no-LLM twin to register as a control that spends **no** trial — it exists to price the seat, not to compete — so a twin created through the only available path would have incremented the deflated-Sharpe trials count for **every other strategy in the arena**, raising the D89 admission floor for candidates that had nothing to do with it. The defect had no symptom yet only because the contestant seat does not exist until Phase 6.5; it would have surfaced as a quietly harder gate, attributable to nothing. `CreateControl` now writes `status='control'` with no trial, exempt from the D52 pre-registration gate **rather than smuggled past it with the `unregistered` marker** — that marker means something different (a candidate created without a linked hypothesis) and renders permanently on the strategy card under rule 16. A control is deliberately un-preregistered, not carelessly so. The `SignalRegistrar` doctrine applied: *a registration is NOT a candidate*.
**Consequences:** `CandidateFactory` (a `status` parameter, the conditional trials write, `CreateControl`); the unregistered marker now writes THROUGH D133's shape instead of around it via an options-less `JsonNode`, closing the two-writers-one-key hole D133 names. Fixtures `D81_ControlRegistration_WritesNoTrialsRow` and the control-beside-treatment trials count.

*Verification: `check-register` green at **133 rows, D1..D133**; `dotnet test` green — **1,172 tests**, seven projects; `ci.ps1` green. One behaviour found by a failing test rather than by reading: the seeding backtest's pre-flight runs BEFORE the replay that seeds the legacy roster, so a row-requiring check refused the three dummies for not yet existing — `CanRun` distinguishes "runnable in principle" from "resolvable from a persisted row", and the distinction is documented at both.*

---

## v1.9.96 — Phase 6.3: the funnel primitives and the population hookup (2026-08-07)

*Recorded 2026-08-07. **Phase 6 checkpoint 6.3.** Decisions **D134** and **D135** new; findings **391—392**, each with the rule-26 Consequences field; proposal **P21**. No migration and no schema change: the cadence-family declaration lives in the existing `config_json` TEXT column, in D133's frozen string bag. Full suite green at 1,196 tests across seven projects. Next-free finding after this entry: **393**.*

### finding 391 — D133's TOLERANT property was documented, not implemented: a stamp discarded every unknown key

`StrategyConfigJson`'s class doc states that a historical payload deserializes to a config *"whose unknown keys are PRESERVED for round-tripping"*. They were not. `Read` populates only the declared members, `StrategyConfig` has nowhere to hold anything else, and `WithUnregisteredMarker` was implemented as `Write(Read(x) with { Unregistered = true })` — so **every key the typed shape does not name was silently dropped at the moment of stamping**. This is not theoretical: `AnalysisEndpoints` accepts a caller-supplied `config_json` and `CandidateSpec` carries it verbatim (its own test fixture is the literal blob `{"lookback":126}`), so a candidate created through the API lost whatever it froze — a frozen param (D17) vanishing at the exact moment of freezing, with no error and no log. Found by 6.2's own `D81_ControlRegistration_WritesNoTrialsRow` when 6.3 routed a second stamp through the same path: the typed round-trip ALSO invents keys, and the fixture's `DoesNotContain("unregistered")` caught the invented `"unregistered":false`. The fixture was right on both counts and neither was relaxed. Both stamps now mutate the object form and re-emit canonically. **D133's framing survives intact:** *"through the shape"* always meant ONE writer under ONE convention — the canonical key order plus `AlphaLabJson.Options`, both still applied — and it never meant a typed round-trip; the typed round-trip is what made it lossy.
**Consequences:** `StrategyConfigJson.WithUnregisteredMarker` (rewritten) and the new `WithFrozen`, both over a shared `Stamp` helper. D133's row is unchanged: the defect was in the implementation of a property the row already claims, so there is nothing to amend — the row was right and the code was not. Fixtures `D133_StampingAMarker_PreservesEveryUnknownKey_RatherThanRoundTrippingThemAway` and `D133_WithFrozen_NeverOverwritesADeclaredValue_BecauseThatWouldBeAnUnrecordedFork`.

### finding 392 — a right-number/wrong-subject citation, which `check-register` structurally cannot catch

STRATEGY_CATALOG §3's tie-break rule cited **(D40; v1.9.23)**. D40 is *versioned append-only bars*; it says nothing about ties, and no register row for the seeded tie-break existed at all. Check 3a verifies that a cited `D<n>` RESOLVES TO A ROW — it cannot verify that the row says what the citing text claims — so the citation passed every sweep while pointing at a different decision. The consequence was not cosmetic: a design rule that lives in exactly one document, under a citation that looks authoritative, went unimplemented for three phases while the shipped code did the opposite of what it required. Two of the catalog's three `D40` citations are CORRECT (§2's watermark note and §13's determinism line, both genuinely D40's subject), which is why a bulk re-citation would have been wrong and each was read before deciding. **The generalisable lesson, recorded rather than guarded:** an automated semantic check is not proposed here — it would need to compare a citation's CLAIM against a row's SUBJECT, which no grep can do. What is available is the discipline rule 25 already states: a rule that binds behaviour takes a register row, and a rule found citing someone else's row is a finding.
**Consequences:** `docs/STRATEGY_CATALOG_v1.9.md` §3 re-cited to **D134**, with the correction stated inline so a reader of the old text is not left wondering. `tools/check-register.ps1` is UNCHANGED and deliberately so — no new check is added, because the check that would have caught this cannot be written.

### The two decisions

**D134 — the Stage-3 tie-break is a seeded stable order over (`Config.Seed`, `security_id`).** The catalog has required it since v1.9.23; the code broke ties by ascending `security_id`, which is ingestion order — one of the three orders the rule names as forbidden. **The evidence that settles the framing:** ties are not a boundary event. `BuyAndHoldModel` scores a literal 1.0 for every eligible name and `ThresholdModel` emits only 1.0/0.0, so the tie-break IS the selection rule whenever the qualifying count exceeds the cap. Measured read-only against the frozen store: `threshold:sma50`'s 60-name cap cut into a block of exact 1.0 ties on **4,789 of 5,031** replay sessions, and its truncation count EQUALS its tie-cut count — every truncation it ever made was a tie cut. On 2020-06-15 the book was the 60 lowest security_ids out of 412 names tied at 1.0. Its 33,332 replay trades are an artefact of ascending integer id, and two siblings differing only in `Config.Seed` drew identically, which makes the seed a frozen param that did nothing. Nor does the problem dissolve when the real families land: `brk:L252` scores exactly 1.0 for every name at its trailing high, which three places in the repo already call ordinary.
**Landed NOW, before the first registration, and that is the decisive reason** — not the defect. No forward day has ever run in this arena (the store holds `run_kind='replay'` only), so nothing live is disturbed; whereas registering Momentum and MeanReversion at 6.10 under the superseded order would make the correction a **fork under rule 8**, spending a trial and raising the D89 floor (N′ = live trials + 1) for every candidate after it. That is rail 1 exactly: *the rails that change what a number MEANS land before the number is produced.* Forward-only scoping was rejected on a second, independent ground: `SeedingBacktestEngine` produces S1's backtest reference under `run_kind='replay'`, so a forward/replay split would corrupt S1 by construction — and 6.2 had just spent D133 making the two consumers agree on WHICH strategies run.
**The cost, priced rather than discovered later.** `FX-RecomputeParity` is NOT the tripwire and never was: it seeds equity curves directly and never reaches the funnel, so it cannot fail on a selection change — the plan's framing rested on that error and it was checked before acting. Generation 2 is **not re-run** (replay is skip-if-committed; only an explicit `--reset` re-simulates, and D95's one-generation guard would delete all 5,031 sessions to do it). What is genuinely lost is bounded and named: the three replay accounts' funnel outputs cease to be RE-DERIVABLE under the new code, and the `allocator_value_add` line in the 2026-08-03 sign-off report — a check D107 kept freeze-gating — stops being reproducible, because `AllocatorValueAddKpi`'s roster reads `threshold:sma50`'s curve (168 of 239 replay `allocation_log` rows carry its weight). Nothing frozen loses INTEGRITY: the four calibrated rows are built from D64 plants, which bypass the funnel entirely, and the report's sha256 still matches on disk. Scoring this change falls OUTSIDE the D106/D117 harness — by that decision's stated RATIONALE (*"those change the trades every later number is computed from"*), not by its parenthetical list, which does not enumerate selection.
**Two coupled sites, not one.** `FunnelRunner.RanksOf` carries the same order and says in prose that it must — *"two different tie orders would let a name be simultaneously inside the top N and past the exit buffer"* — and that invariant had no test, because ascending id gave the two sites no way to disagree. A seeded order gives them one, so it is now a fixture. The mixer moved to `AlphaLab.Core.Numerics.Mix` so the funnel's order and `PopulationRng`'s score source share ONE SplitMix64: the arithmetic is unchanged and `FX-PopDeterminism` proves it.
**The `percentile_rank` convention, settled in the same row.** The catalog uses it as a primitive in four scoring blocks and never defines it. Ruled MID-RANK in [0,1], and the reason is a hard-rule-7 consequence rather than taste: under `(rank-1)/(n-1)` the worst-ranked name scores exactly 0.0 and `Selection` refuses it with *"score 0 is not > 0 — never selectable (rule 7)"* — a reason that would be FALSE, since the name was scored legitimately and merely came last. Mid-rank's minimum is `0.5/n > 0`: last, not excluded. It lives in `AlphaLab.Core` because `AlphaLab.Strategies` does not reference `AlphaLab.Evaluation`, so the existing `Statistics.PercentileRank` was structurally unreachable from the models that need it; rather than copy it, `Statistics` now DELEGATES.

**D135 — a strategy declares its matched population's cadence family; nothing derives it.** Hard rule 9 requires every strategy to be ranked against its turnover-matched population and D36 makes the cadence family the matching unit, but **nothing in the repo mapped a strategy to a family**: `strategies.family` is the STRATEGY family (momentum|meanrev|lowvol), a different namespace, and the pipeline hardcoded the daily cost-on population for every promotable strategy. `CandidateFactory` contained no population code at all, though STRATEGY_CATALOG §5.2 has asserted since v6 that *"the factory wires every new candidate to its matched random population by cadence family"*. DERIVATION was rejected on evidence: only `ScheduledRebalance` carries an interval, so `Never`, `TargetOrTimeStop` and `ChannelExit` would have no answer, and §6.4 resolves Breakout by SPAWNING a family, which no derivation rule can express. ONE resolver replaces three copies of `Family == "daily" && CostsOn` — the last of which, in `BandInputs`, carried a comment asserting the copies must agree while citing a `DailyPipeline` line number that had already moved. The population and the D56 trajectory curves key off the SAME resolved family and move together, because a curve states what a no-edge percentile looks like within THAT family's null. Fail-closed is the catalog's own answer: a declared family with no spawned population leaves S3 `undefined`, never a fall back to the daily null.

*Verification: `check-register` green at **135 rows, D1..D135**; `dotnet test` green — **1,196 tests**, seven projects; `ci.ps1` green. Behaviour on the existing roster is UNCHANGED by D135 (every frozen row resolves to daily by the recorded compatibility rule) and CHANGED by D134 by construction — which is the point, and which is why it lands while the forward ledger is still empty.*

---

## v1.9.97 — Phase 6.4: the forward membership refresh (2026-08-07)

*Recorded 2026-08-07. **Phase 6 checkpoint 6.4.** Decisions **D136** and **D137** new; findings **393—395**, each with the rule-26 Consequences field. Migration **M11** (`observed_at` + `source` on `index_membership_log`; ALTER ADD COLUMN, no CHECK, no backfill) — authored here, applied by the operator snapshot-first per rule 14. Full suite green. Next-free finding after this entry: **396**.*

### finding 393 — the membership reconcile had been reading the wrong universe since 2026-07-23, and it broke in two directions at once

`MembershipReconciler` decided what was *currently a member* by reading `index_membership WHERE removed_on IS NULL` over the whole table. That was correct while the table held only the forward roster. The Phase-4 historical seed co-mingled the full S&P 500 as-of history into the same table, and from that day the read named a set the forward reconcile is not authoritative for. **ONE cause, TWO defects with opposite symptoms, and the count-sanity gate could catch neither because it bounds the two SOURCES and never the store.** *Mass eviction:* every open name outside the S&P 100 slice classified as a drop — **402** of them in the live arena (604 open rows, 503 distinct open ids, a 101-id slice snapshot) — so an ordinary quiet refresh would have stamped `removed_on = today` across the S&P 500 roster. *Invisible adds:* a genuine new S&P 100 entrant is ALREADY open as an S&P 500 member, so it never appeared in the add set, never reached `MaintainSliceSnapshot`, and was filtered out of the forward universe permanently by the slice intersection — the exact *“removals applied, adds vanished”* asymmetry that helper's own doc says it exists to prevent, reintroduced beneath it. The second is the worse one: it is silent, it degrades the universe at every index rebalance, and unlike the eviction it is NOT repaired by re-seeding.
**Consequences:** D136 (the ruling). `MembershipReconciler` takes its diff from `IIndexMembershipRead` — the same resolution the funnel trades — plus two corollaries that fall out of it: an entrant already holding an open interval gains no second row, and a drop closes the CURRENT spell (latest `added_on`) rather than whichever row an unordered `.First()` happened to return. Fixtures `FX-ForwardReconcileScope` (four, over a co-mingled store at readable scale).

### finding 394 — the recorded rationale for the re-bootstrap trap was wrong in both of its adjectives

PROGRESS carried the instruction *“do not re-run the forward bootstrap before the widening lands”* on the stated grounds that a re-run *“would permanently close the replay intervals”*. The instruction is right and its magnitude is exact (402, not merely “~400”); the reason is not. **It does not touch replay:** membership reads are half-open `[added_on, removed_on)` and the replay window ends 2025-12-31, so the as-of roster resolves identically before and after a stamp dated today — verified by simulating the stamp over the live store, where `MembersAsOf('2025-12-31')` is 503 either way. **It is not permanent:** `HistoricalMembershipIngestion` is an idempotent upsert keyed on (security_id, added_on) that re-assigns `removed_on`, so re-seeding the same CSV re-opens every stamped row; the wording predates the checkpoint-4.3 upsert that obsoleted it. The REAL harm is future-facing: `MembersAsOf` collapses 503 → 101 for every date at or after the stamp, which destroys the rule-22 WIDENING TARGET and writes 402 false universe-exit events into as-of state. **Why a wrong rationale mattered enough to record:** it points the fix at the wrong place. A reader protecting replay would have reached for a run-kind guard; the defect is in the reconcile's universe scope, and a run-kind guard would have left both halves of finding 393 open while feeling safe.
**Consequences:** the corrected rationale is written into D136 rather than left in PROGRESS, so the register carries it. The operational instruction stands unchanged; RUNBOOK gains the fact that a mass eviction is recoverable by re-seeding, so an accidental re-run triggers a repair rather than a panic.

### finding 395 — no alarm in this codebase has ever travelled from a table to a screen

The survey assumed 6.4 could copy a sibling alarm's surfacing. There is none to copy. `AlpacaBarCrossCheck.GetDailyBarsAsync` throws `NotSupportedException` by design and has zero callers, so `cross_check_mismatch` is never produced; `RegimeProxyCrossCheck.Compare` is a pure comparator whose only callers are tests, and its own comment claiming the backfill CLI wires it is **false in the tree today**; and nothing in `AlphaLab.Api`, `.Core`, `.Evaluation` or `.Web` reads `data_quality_flags` at all — `/api/v1/data-health` returns a hardcoded `NoRunYet` constant over a read-model whose `Rows` are untyped and always empty. So 6.4 is ESTABLISHING the pattern, not following one, which is a materially larger claim than the checkpoint assumed and is recorded rather than absorbed silently.
**Consequences:** D137's read-model half is the first alarm state in this arena to become legible outside SQL. Scope is deliberately bounded to the read-model: the Phase-7 Data-health grid is NOT built, and that declination is recorded in `docs/phase6/README.md` with its owning phase. `RegimeProxyCrossCheck`'s false “wired by the backfill CLI” comment is left in place and NOT corrected here — it belongs to the regime-proxy feed, is outside this checkpoint, and is recorded as **P22** so the correction is owed rather than forgotten.

### The two decisions

**D136 — the reconcile's scope and cadence.** The diff is taken against the forward roster; the refresh runs ONCE PER LAUNCH, before the catch-up loop, stamped with the current session, in its own small transaction, forward only. **Cadence is forced by the providers rather than chosen:** INTEGRATIONS §2/§2b drop `asOfDate` deliberately (*“the same freeze trap”), so a fetched roster answers for NOW — a per-day refresh would stamp each REPLAYED session with today's names and fabricate as-of membership. Stamped with today instead, the write is invisible to every recovered day by construction, via the same half-open property that makes the trap harmless to replay. Rule 12 is preserved: the fetch writes nothing and the apply sits outside every daily write transaction, on D72's precedent for launch-scoped work. A provider outage writes nothing and lets freshness go stale rather than failing the trading day — not a rule-10 exemption, since refusing to CHANGE the universe on unverified data is the same instinct pointing the same way.

**D137 — per-row provenance and the three-state health read-model.** `observed_at` + `source`, both nullable, both without a CHECK so a third source is an INSERT and not a rebuild (finding 324 applied forward). **Pre-migration rows are NOT backfilled:** the only available value is `as_of`, the run date, and writing it would manufacture provenance that looks authoritative and is false. The read-model resolves THREE states, and the third exists because of that ruling — with NULL rows in the store a two-state model renders them STALE, and an operator would open the lab on day one and chase a divergence that does not exist. Unprovenanced ≠ old. Freshness and last-validation stay separate fields (UX-11a); every state carries a reason, including the absences (UX-17). **The state is VISIBLE, not FIRING, and the passivity is recorded rather than implied** so no later reader assumes an active alarm exists.

*Verification: `check-register` green at **137 rows, D1..D137**; `ci.ps1` green; M11 emits exactly two `ALTER TABLE ... ADD ... NULL` statements and `dotnet ef migrations list` shows it Pending. Behaviour on the live store is unchanged until the operator applies M11 and launches the Worker — at which point the refresh runs for the first time, against a reconciler that can no longer evict 402 names.*

---

## v1.9.98 — Phase 6.5 (PR 1): the S6 band-conformance remedy (2026-08-08)

*Recorded 2026-08-08. **Phase 6 checkpoint 6.5, first of two PRs** (the split is recorded below). Decisions **D138** and **D139** new; findings **396—398**, each with the rule-26 Consequences field. No migration. Next-free finding after this entry: **399**.*

### finding 396 — S6's false-alarm rate was structural, and the mechanism is not the one the checkpoint assumed

The plan framed the remedy as *" + LQ + u"make first-Suspect time depend on behaviour, not track length" + RQ + u"* " + EM + u" and the arithmetic behind that framing is correct but SECOND-ORDER, and it points the repair at the wrong place. **The first-order mechanism:** S6's anti-predictive arm tests the intercept t-stat against ZERO, comparing a 40-name daily-churn COST-PAYING population member against a near-cost-free buy-and-hold benchmark. That family's documented drag is 21.9 %/yr, which over a 63-session window is a mean shift of roughly —1.7 to —2.7 t-units " + EM + u" so P(t < —1) per evaluation is about **0.76—0.96**, not the ~0.16 the code comment asserted. Everything fired at the arithmetic minimum BECAUSE that probability is near one. So the plant was simultaneously **inside** its own cost-matched band (at its median, by construction " + EM + u" the band is percentiles of the SAME cost-on family) and **below zero** on the t-stat, and `MonitorSignals.S6`'s early return let the second win before the first was ever evaluated. The D63-conformant verdict was computed on the same call and thrown away.
**Consequences:** D138. The remedy is CONFORMANCE rather than invention " + EM + u" " + SEC + u"3 already required the judgement be made *" + LQ + u"vs its population band" + RQ + u"* and D63 already stated *" + LQ + u"an edgeless strategy is never *below* a cost-matched band" + RQ + u"*, so below-the-band is what anti-predictive MEANS and below-zero is what paying costs means. `MonitorSignals.S6`, `OverfittingMonitor`'s call site, `MonitorRecompute.RecomputeS6`, and OVERFITTING_MONITOR Appendix A's stale `critical_windows: 3` ladder (which still encoded the pre-Change-3 rule §3 had already repealed " + EM + u" two live readings of one rule, the relational defect rule 25 exists to stop).

### finding 397 — the recompute harness held a second copy of the very rule it exists to score

`MonitorRecompute.RecomputeS6` duplicated S6's branch order rather than calling it " + EM + u" in the class whose own comment says *" + LQ + u"a second copy of a rule here is a second definition that would drift" + RQ + u"*. The consequence is specific and would have been silent: the band-conformance remedy would have had to be applied TWICE, and `FX-RecomputeParity` would have gone green either way, because both sides of that comparison run current code and both would have carried the same copy. A harness that scores rule changes cannot hold its own copy of the rule; that is not a style point, it is the difference between measuring a change and measuring nothing.
**Consequences:** `RecomputeS6` now calls `MonitorSignals.S6`; `MonitorSignals.S6` takes `negativeAlphaT` and `sustainEvals` as overridable arguments so the harness scores a change THROUGH the rule rather than around it. Finding 340 deepens: the rule now needs band POSITION rather than membership, so the contribution-token recovery path can no longer serve an S6 rule change " + EM + u" a stored `elevated_neg_alpha` row was written by a rule that returned before consulting the band and records nothing about which side it was on. Refused out loud; the DerivedBand tier gate already makes that branch unreachable for a spec that touches S6.

### finding 398 — the confirmation slice's obvious command confirms nothing, and its only alternative destroys the evidence

D117 clause 2 makes a confirmation slice a sign-off requirement precisely because *" + LQ + u"parity exercises the UNCHANGED path, so it structurally cannot validate the changed one" + RQ + u"*. But the obvious command " + EM + u" `replay-calibrate --from/--to` against the arena that holds the generation " + EM + u" skips every session as already-committed, and `CalibrationOrchestrator` checked only `StoppedEarly`. It would therefore archive a report built entirely from generation 2's OLD-rule rows, recomputed under the NEW rules: a document that reads exactly like a confirmation and confirms nothing. **A false negative with a filename is worse than no report, because it is citable.** The only flag forcing re-simulation is `--reset`, which deletes the whole generation. An operator following the DoD literally gets either a fake pass or a destroyed sign-off.
**Consequences:** D139. A zero-session run fails closed and archives nothing " + EM + u" the refusal returns BEFORE the report is written, so nothing is left on disk to be mistaken later for evidence. **NOT a blanket refusal:** `tools/resume-calibration.ps1` re-runs only verification + freeze on a finished replay, which is a zero-session run BY DESIGN " + EM + u" that path is now opt-in and must declare itself. Two existing fixtures were breaking against the blanket form, which is how the counter-case was found; both now say they are resumes, which is the more honest reading of what they always tested.

### finding 399 — generation 2's PROMOTIONS do not reproduce, and nobody had ever checked

`FX-RecomputeParity` had only ever been run against generation **1** (every recompute artefact on disk is dated 2026-08-02; generation 2 froze 2026-08-03). Run against generation 2 for the first time at 6.5, it **FAILS**, and the shape is precise:

| artefact | stored | recomputed | differing |
|---|---:|---:|---:|
| `overfitting_status` | 95,600 | 95,600 | **0** |
| `go_live_log(Promoted)` | 144 | 151 | **94** |
| `go_live_log(WouldRevert)` | 18,533 | 18,533 | **0** |

Moved (same edge, different date) 83 · gained 9 · **lost 2** (`plant:edge:monthly:2:28`, `:2:36`). The implied detectability floor moves with it: **α\*(H) frozen 6.95 %/yr → recomputed 6.56 %/yr**.

**NOT CAUSED BY THE 6.5 REMEDY, and this was established rather than assumed.** The identical command was run on `main` with no 6.5 change present and produced the identical counts; the two archived reports are **BYTE-IDENTICAL (sha256 `0994d74c…`)**. Statuses reproducing 95,600/95,600 is the corroborating evidence from the other direction " + EM + u" an S6 rule change moves STATUSES first, and none moved. So the defect is pre-existing, lives entirely on the GATE side, and was invisible because parity had never been pointed at this generation.

**The report's own named suspect does not survive checking:** it warns that *" + LQ + u"`GateOptions` is not as-of resolvable … reproducing this generation's promotions rests on the `Gate` block being unchanged since it ran, an assumption the harness cannot verify from the store" + RQ + u"*. No commit has touched a `Gate` key since generation 2 froze, and the Worker's appsettings carries no `Gate` section at all (pure C# defaults). The cause is therefore **not identified**, and is recorded as open rather than guessed at.

**Consequences:** MASTER §25.3 governs and is followed rather than worked around " + EM + u" *" + LQ + u"the harness is not used for its purpose and generation 2 stands; the equality is never relaxed to a tolerance" + RQ + u"*. So (a) D117 clause 2's **confirmation slice is NOT run** in this PR: it exists to validate the remedy THROUGH the harness, and running it against a harness that has just failed its own trust check would produce exactly the artefact D139 was written to prevent " + EM + u" a document that reads like sign-off evidence and is not. (b) The 6.5 remedy therefore ships **unscored**, which is stated in the PR rather than implied complete. (c) **This is a 6.10 blocker independent of 6.5**: α\*(H) is the floor the D89 admission gate reads, and a floor that does not reproduce cannot be the number a first registration is judged against. Recorded as **P23**.

### The decisions

**D138** " + EM + u" the band is consulted BEFORE the anti-predictive arm, which now requires the strategy to be BELOW it. The threshold and the sustain rule are untouched; the arm gains the precondition the spec always stated. **Why no threshold could have worked, and the argument cites no metric moving (the C1 discipline):** the anti plant's PLANTED signal is —2 %/yr against a common-mode drag of —21.9 %/yr " + EM + u" about **1:11** " + EM + u" giving ~0.19 of expected t-separation against a noise SD of 1.0. No cut point on a statistic whose signal is a tenth of its shared offset separates anti-predictive from no-edge. That is why v1.9.73/75's measurements moved 15,379 status rows and ZERO no-edge outcomes, and why all three recompute reports record a separation of 0.00 " + EM + u" the established sense in which *" + LQ + u"the remedy is behavioural, not a threshold" + RQ + u"* is a result rather than an opinion.

**D139** " + EM + u" the zero-session refusal, and the scratch-arena procedure for the slice. **The D71 exemption is recorded with its REASON, not merely its permission:** rule 23 forbids calibration being COPIED BETWEEN ARENAS because a curve is a property of its pool, horizon and noise, so a curve that travels acquires false provenance. A scratch arena that is a byte copy, read from and then DESTROYED, is not a second arena acquiring a curve " + EM + u" nothing survives to be mistaken for calibration. Scoped: read-only under a throwaway id, `--report-only`, destroyed at the end, and the scratch id never appears in a committed artefact.

### The split, recorded because it is a judgement and not a diff-size preference

6.5 ships as TWO PRs. **PR 1** (this one) is the remedy, the harness change and the refusal. **PR 2** is the gate ordering, the Warning acknowledgment and the auto-retire reconciliation. They **fail differently**: PR 1 red means the NUMBERS are wrong; PR 2 red means a RULE is wrong. Those are different diagnoses and different fixes, and holding them in one PR means diagnosing one while the other is still a live possibility. **The ordering is forced rather than chosen:** PR 2's auto-retire reconciliation depends on the remedy's band being conformant, so PR 1 merges first.

*Verification: `check-register` green at **139 rows, D1..D139**. Evaluation 287, Worker 215. **OWED AND NOT YET DONE in PR 1:** `FX-RecomputeParity` against GENERATION 2 (every recompute artefact on disk is generation 1, 2026-08-02) and the confirmation slice itself " + EM + u" both require the live store and are recorded here as outstanding rather than implied complete.*

---

## v1.9.101 — Phase 6.5a (review remediation, PR 1): a corporate action restates its pending orders (2026-08-11)

*Recorded 2026-08-11. **Phase 6 checkpoint 6.5a, PR 1 of the review remediation** (the new gate box and its precedent are recorded in PROGRESS). Decision **D142** new; findings **400—403**, each with the rule-26 Consequences field. No migration, no schema change, no config key: no new table, no new column, `decisions.stage_json` bytes unchanged, and the share tolerance is an internal constant rather than a documented threshold. Full suite green at 1,260 tests across seven projects. Next-free finding after this entry: **404**.*

### finding 400 — the T+1 fill was denominated in yesterday's share unit, and one direction of it fabricated cash

`DailyPipeline.RunAccountDayAsync` applies corporate actions (`:387`) and then fills the prior session's stored orders (`:391`) — the D53 ordering, and correct. But `FillPriorOrders` read `DecisionSnapshot.Stage6Orders` back verbatim and handed `order.Shares` to the broker with nothing between: no re-read of the book, no conversion. So a split effective on the fill date restated the position and left the order in the old units. **The two directions are not symmetric, and the asymmetry is why this is high rather than critical** — it is also what a reader who sees only the headline will get wrong. A **forward** split (the common case: 4:1, 2:1) GROWS the book, so the stale sell can never oversell: the day commits, equity is arithmetically correct, and the only defect is that an ExitPolicy CLOSE silently left part of the line open. No guard can catch that one, which is exactly why the restatement is the fix and the oversell refusal is only a backstop. A **reverse** split shrinks the book below the stale sell, and `PostFill`'s `newShares <= 1e-9` branch — written for a clean close — absorbed the negative remainder: the row was deleted and the full over-large proceeds credited, so the ledger gained cash that never existed, with no exception, no flag and no log line. It had an `existing is null` throw two lines above (a sell against NO position) and nothing for a sell LARGER than the position. **Found by review, not by a fixture, and the reason is recorded because it generalises:** none of the three seeded strategies emits a sell inside the harness's window, and `FakeMarketData`'s `_splits` dictionary had been read since 2.10 with no writer — so the sell side of the fill path had never been driven end to end, and no test had ever planted a split through the real ingest path.
**Consequences:** `PositionMath` (new, Core) owns the sell arithmetic and the oversell refusal; `DailyPipeline.PostFill` delegates to it. `OrderBuilder`'s private `1e-9` moved there too — it bounds the smallest delta Stage 6 will route AND the remainder at which the ledger calls a line closed, and those must be one number. **D142** is the decision; MASTER §13.6's **Split** bullet gains the pending-order clause; TEST_PLAN gains **FX-SplitPendingOrder**. Harness levers `FakeMarketData.AddSplit`, `PipelineHarness.PlantPriorOrder` and `RescaleBarsFrom` are new. The guard was landed one commit BEFORE the restatement, with the reverse-split fixture asserting the day rolls back, so the red is a recorded passing state rather than an argument — `..._AnOversellNoCorporateActionExplains_RollsTheWholeDayBack` keeps it falsifiable now that the restatement has made the split path unable to reach it.

### finding 401 — the ratio could not have come from the account's book, and the frozen store contains the case that proves it

The obvious implementation reads the ratios off `CorporateActionOutcome`, which the pipeline was already discarding at `:387`. It is wrong, and silently so. `CorporateActionApplier.ApplyForAccount` iterates the securities the account **HOLDS** (`:69`), so a pending **BUY** into a name not yet held is invisible to it. The funnel sizes an open at T as `targetNotional / rawClose_T`; a reverse split lands at T+1; the open prints at `P/r`; the fill buys the ordered count at the restated price and spends `1/r` of the intended notional — no guard, no throw, no log, and D84's "opens are sized against cash on hand" blown through at the fill rather than at sizing. **A ratio is a property of the SECURITY and the window; only a termination is a property of the book.** This is not a hypothetical: the step-0 probe found the shape in the frozen generation — `threshold:sma50`, 2010-06-08, a 21.4 sh open into a name with `book@decision = none` on a 2-for-1 date. Had the map been book-derived, that row would have been invisible to the fix *and* to the probe that measured it.
**Consequences:** `ShareUnitRestatements` (new, Data) resolves ratios per security over the day's ORDER set, memoised per day, through `ICorporateActionReadService` — so replay inherits `DateCeilingCorporateActionReads` for free rather than needing a second code path. `CorporateActionLedger.UnitRestatementRatio` (new) answers for an action with no position in hand, which `Apply` structurally cannot; `UnitRestatementRatio_AgreesWithApply` iterates `Enum.GetValues<CorporateActionType>()` so a new kind cannot enter the dispatch without failing there first. `LedgerMapping.ToDomain(CorporateActionRow)` — the applier's private mapper, MOVED rather than copied, because its `AppliedOn` decides which day an action lands on and two copies of that is the drift D142 exists to remove. `CorporateActionWindow` (new, Core) gives the `(prev, asOf]` predicate one definition for the same reason; its test asserts the PARTITION property rather than three examples. Fixture `FR9_D142_ASplitOnAnUnheldName_StillRestatesThePendingBuy` fails if the map is ever book-derived again.

### finding 402 — a delist or cash merger with a pending order in the same name: one arm stops the day, the other fabricates a position (RECORDED, NOT FIXED)

Found while designing D142 and deliberately left for its own PR. A terminal event resolves the line in step (a) and the stale order is still filled in step (b). **The sell arm** hits `PostFill`'s `existing is null` throw and rolls the WHOLE day back — every account, every population row, the regime label — with a message that blames the funnel ("the funnel planned a close/trim against a book the ledger disagrees with") for something the funnel did correctly. That is an ordinary corporate event taking the day down. **The buy arm is worse and is the reason this jumps the queue:** `PostFill`'s buy leg has no existence check at all, so a pending `Wishlist` add into a name force-closed that morning **creates a brand-new position in a delisted or merged-out security**, which then freezes forever on the next session's stoppage check — silent, permanent, and indistinguishable from a real holding. A cash merger fails *accidentally* safe today only because a merged-out target usually has no bar and the broker rejects on a missing price; "usually" is not a mechanism. D142 deliberately reports **no factor** for a terminal event — there is no number that converts an order into a line that no longer exists — so the remedy is cancellation with a logged reason, in the same family as the capacity rejection ("surfaced, never silently dropped"), not a conversion.
**Consequences:** none in this PR, by choice. Recorded against the **6.5a** gate box as the next item, ahead of the two findings it was originally scheduled behind; the plumbing D142 builds (the captured outcome, the window predicate) carries the termination set at near-zero marginal cost, so shipping it second costs a fraction of shipping it fourth. Step 0's probe reports **class B = 0** at the generation's watermark, so no stored replay day contains this geometry and the frozen generation is unaffected either way.

### finding 403 — D142's own whole-or-nothing constraint is prose that nothing enforces, and the dangerous path is the DEFAULT (RECORDED, NOT FIXED)

D142's `Consequences` field says generation 2 must be regenerated **whole or not at all**, because a partial re-run mixes pre- and post-D142 arithmetic in one generation — what D95's one-generation-per-arena rule exists to prevent. Nothing checks it. `ReplayRunner` **skips same-watermark committed days** (`:157-162`), so a plain `replay-calibrate` against this arena IS that partial re-run, and `--reset` — the safe path — is the OPT-IN. **The dangerous path is the default and the safeguard is a sentence in a decision row.** That is D140's own defect class (a fact recorded where nothing examines it) sitting inside the decision written to apply D140, which is why it is filed the moment it was noticed rather than left in a Consequences field. It also generalises past D142: the register will accrue further arithmetic changes, and each silently inherits the same trap. **The shape when it is built** — not now, so this PR stays one finding wide: refuse a `replay-calibrate` against an arena whose committed generation predates a recorded arithmetic change unless `--reset` is present, failing closed in D139's pattern (which already refuses a calibration run that would simulate zero sessions). What "a recorded arithmetic change" resolves to is part of that design and is not assumed here.
**Consequences:** none in this PR, by choice. Queued in the **6.5a** box as its own named item with its own PR, ahead of the read-model and enforcement findings, because it protects the only artefact in the arena that took 5,031 sessions to produce and cannot be reconstructed from anything else. D142's row names finding 403 explicitly, so the constraint is attributed rather than orphaned.

### The decision

**D142 — a corporate action that restates a position also restates that security's pending orders, and a sale that still exceeds the holding is refused.** The full row carries the direction distinction, the not-a-recompute argument against `OrderFill`'s stored-snapshot doctrine, the delta-rescales-too proof, and the order-set-not-book scoping. Its generation-2 consequence is stated in **two parts, in this order**, because either half alone misleads a reader deciding whether to regenerate: (1) the contamination is CONFINED TO THE FUNNEL PATH — three of 403 replay accounts write `decisions` rows at all; (2) WITHIN that path it touched TWO OF THE THREE EXPOSED ACCOUNTS. *"Two of 403"* is arithmetically true, reads as half a percent, and would lead straight to "immaterial". Plant immunity is stated as a **structural property** — the 400 plants are equity-only fixtures the pipeline skips before the funnel, so they cannot be reached by this defect by construction, at any watermark — rather than as an observation about one probe run, because that is what licenses the frozen D56 curves, the D121/D122 band, P23's parity and the D117 slice standing without anyone re-running anything.

*Verification: `check-register` green at **142 rows, D1..D142**; `ci.ps1` green — **1,260 tests** (from 1,216), seven projects, zero skipped, all nine guard greps. `FR25_D142_ASplitDayWithAPendingOrder_ReproducesByteIdentically` pins NFR-1 on the new path. Step 0's probe (`docs/calibration/2026-08-11-d142-pending-order-corporate-action-probe.md`) was taken BEFORE any code, because `ReplayRunner` skips same-watermark committed days and a post-fix partial re-run would have produced the mixed-arithmetic generation D95 forbids: class A = 10, class B = 0, at watermark `2026-07-24T22:00:00Z`. The generation STANDS; the constraint is regenerate WHOLE or not at all — now attributed to finding 403 rather than left as an unenforced sentence. §4b of the probe artefact records the MEASURED answer to whether the divergence outlives its session: in all ten cases the affected name is not traded again for +21 sessions, so it does not; nine of the ten are deltas rather than closes; the one genuine whole-line close left exactly 50 % of the line on the book; and whether the +21 rebalance re-converges is recorded as NOT ESTABLISHED rather than assumed.*

---

## v1.9.102 — Phase 6.5a (review remediation, PR 2): the terminal-event cancellation (2026-08-11)

*Recorded 2026-08-11. **Phase 6 checkpoint 6.5a, PR 2.** Decision **D143** new; findings **404—405**, each with the rule-26 Consequences field. No migration, no schema change, no config key. Full suite green at 1,267 tests across seven projects. Next-free finding after this entry: **406**.*

### finding 404 — a delist with a pending sell took the whole day down, for an ordinary corporate event

`DailyPipeline` resolves §13.6 events in step (a) and fills the prior session's stored orders in step (b). A delist force-exit or cash merger removes the position in (a); the stale sell then reached `PostFill`'s `existing is null` throw in (b), and Stage 2 rolled back **the entire day** — every account, every population row, the regime label — leaving the run row `running` and `run_in_progress` set for D72 to clean up. The message blamed the funnel (*"the funnel planned a close/trim against a book the ledger disagrees with"*) for something the funnel did correctly: the close was planned against the book that existed at close T, and a corporate action resolved it overnight. A delist is not an anomaly, and a lab that stops on one cannot run unattended. **The cash-merger arm was worse than it looked** because it appeared to work: a merged-out target usually has no bar, so `VirtualBroker` rejected on a missing price and the day survived. That is the absence of data doing the job of a rule, and `FR9_D143_ACashMergerOnADayWithAPendingSell_CancelsTheOrder` gives the target a bar specifically so the fixture would have failed before this change rather than passing for the wrong reason.
**Consequences:** `CorporateActionLedger.TerminatesPosition` (new) classifies the closed effect hierarchy with **no discard arm**, so a future effect fails to compile until someone rules on it; `DailyPipeline.RunAccountDayAsync` stops discarding `CorporateActionOutcome` (the reshape D142 landed exists for exactly this) and hands `FillPriorOrders` the set of lines closed this morning. **D143** is the decision; MASTER §13.6 gains the cancellation clause beside D142's restatement clause; TEST_PLAN gains **FX-TerminalEventPendingOrder**. Verified as a falsifier, not asserted: with the cancellation disabled, three of the four fixtures fail and the non-terminal dividend control still passes — which is what proves that control tests the boundary rather than the fix.

### finding 405 — the buy arm fabricated a position in a delisted security, and nothing would ever have flagged it

The louder half of finding 404 announced itself by stopping the run. This half did not. `PostFill`'s buy leg has **no existence check at all**, and legitimately so — buying into a name the account does not hold is precisely how a position opens — so a pending `Wishlist` add into a security force-closed that morning simply **created a new position in it**. The row is indistinguishable from a real holding: it carries a cost basis, it marks to whatever the last print was, and on the next session the §13.6 stoppage check freezes it (no bar, no terminal event *that day* to explain it) where it sits **permanently**, since no unfreeze path exists before Phase 7's admin actions. So the failure mode is a frozen phantom holding in a delisted name, acquired by a fill the funnel never intended, in an account whose equity curve is forward evidence. **No guard could have caught this one from inside `PostFill`** — the state is only impossible given knowledge the fill path did not have, which is why the remedy is the cancellation upstream rather than a refusal at the write. This is the reason F26 was moved ahead of F4, F23 and F5 in the 6.5a queue: the findings it passed are all recoverable by inspection, and this one is not.
**Consequences:** the same cancellation closes it — `FR9_D143_APendingBuyIntoATerminatedName_DoesNotFabricateAPosition` asserts no trade and no position row. **What is NOT done, stated rather than implied:** the cancellation surfaces as a log line and writes no durable row. `capacity_rejections` does not fit — one row per account-security-day, three columns carrying D43 participation semantics, no reason field — and inventing a table is a schema change this remediation does not need. A durable surface belongs with the Phase-7 Risk screen, and is recorded there rather than claimed here.

### The decision

**D143 — a §13.6 event that ends a line cancels that security's pending orders.** The terminal-event half of D142, which deliberately reports no restatement factor for a termination because no number converts an order into a line that no longer exists. The row carries the effect-keyed classifier and why its false cases are listed explicitly, the spin-off distinction (the parent keeps its shares and stays tradable), and the durable-surface gap.

*Verification: `check-register` green at **143 rows, D1..D143**; `ci.ps1` green — **1,267 tests** (from 1,260), seven projects, zero skipped, all nine guard greps. Falsifier check run deliberately: disabling the cancellation turns three of the four new fixtures red while the dividend control stays green. Step 0's D142 probe reported **class B = 0** at generation 2's watermark, so no stored replay day contains this geometry — the frozen generation, the D56 curves and the D117 slice are untouched and no regeneration question arises.*

---

## v1.9.103 — Phase 6.5a (review remediation, PR 3): the arithmetic-vintage guard (2026-08-11)

*Recorded 2026-08-11. **Phase 6 checkpoint 6.5a, PR 3.** Decision **D144** new; finding **406**, with the rule-26 Consequences field. One new config key (`Replay.LedgerArithmeticVersion`, DB-row only) recorded in CONFIG_REFERENCE; no migration and no schema change — `config` is an existing table and the row is an ordinary append under rule 24. Full suite green at 1,277 tests across seven projects. Next-free finding after this entry: **407**.*

### finding 406 — D142's whole-or-nothing constraint was prose nothing examined, and the dangerous path was the default (opened as finding 403; BUILT here)

D142 recorded that generation 2 must be regenerated **whole or not at all**, because a partial re-run mixes pre- and post-D142 arithmetic inside one generation — what D95's one-generation-per-arena rule exists to prevent. Nothing checked it. `ReplayRunner` skips same-watermark committed days, which is precisely what makes a run resumable, so a plain `replay-calibrate` against the arena **is** the partial re-run; `--reset`, the safe path, is the opt-in. **The safeguard was a sentence in a decision row, and the hazard was what an operator gets by typing the ordinary command.** That is the defect class D140 names — a fact recorded where nothing can examine it — sitting inside the decision written to apply D140, which is why it was filed the moment it was noticed rather than left in a Consequences field, and why it was built before the read-model and enforcement findings it was originally scheduled behind: it protects the only artefact in the arena that cannot be reconstructed from anything else. **The failure it prevents is silent in every direction.** Nothing about a mixed generation looks wrong — the curves still build, the report still renders, `check-register` stays green — and after the fact the arithmetic behind any given stored row is simply unknowable. The asymmetry is what settles the design: refusing costs one extra flag on the rare legitimate regeneration; not refusing costs the evidence base, with no signal that anything happened.
**Consequences:** `LedgerArithmetic` (new, Core) carries the version constant, the config key, and the history of what each version means — the constant is meaningless without it, so they live together. `ReplayRunner` gains `GuardArithmeticVintage` + `StampArithmeticVintage`, placed beside `GuardSingleGeneration` because they answer the same question about the same generation. **D144** is the decision; CONFIG_REFERENCE gains `Replay.LedgerArithmeticVersion`. Four fixtures, and the fourth is the one that matters most: `D144_ResetIsExempt_AndRestampsTheFreshGeneration` catches the version of this guard that would have turned a mixing hazard into a dead end by blocking the remedy along with the hazard. `D144_AnUnstampedGeneration_IsRefusedRatherThanAssumedCompatible` pins the sp500 case specifically — generation 2 predates the marker, and "no marker" must mean "not identifiable", never "compatible". **Practical effect on sp500 today, stated plainly:** generation 2 can be regenerated with `--reset` or left alone, and nothing else. That is D142's constraint made operative rather than advisory, and it deliberately narrows what the operator can do by accident.

### The decision

**D144 — a replay generation records the ledger arithmetic that produced it, and a build with different arithmetic refuses to add sessions to it without `--reset`.** The row carries what counts as a version bump (values a day writes, never anything recomputed from stored rows — so the constant neither becomes decorative through overuse nor uselessly stale through underuse), why an unstamped generation is refused rather than assumed compatible, why `--reset` must stay exempt, and why the `ResolveCurrent`-shaped read is correct here rather than a D141 breach.

*Verification: `check-register` green at **144 rows, D1..D144**; `ci.ps1` green — **1,277 tests** (from 1,273), seven projects, zero skipped, all nine guard greps. This closes the 6.5a items opened by D142's own record; the queue continues with F4 (the Stage-1 severity filter), F23 (the degenerate-MDE gate verdict) and F5 (the §13.6 freeze rail).*

---

## v1.9.104 — Phase 6.5a (review remediation, PR 4): the Stage-1 filter learns about severity (2026-08-11)

*Recorded 2026-08-11. **Phase 6 checkpoint 6.5a, PR 4.** Decision **D145** new; finding **407**, with the rule-26 Consequences field. No migration, no schema change, no config key. Full suite green at 1,280 tests across seven projects. Next-free finding after this entry: **408**.*

### finding 407 — the one moment a corrupt bar could be refused was the moment the filter dropped the refusal

`Stage1Fetch.Gate` filtered quality flags by DATE ONLY — anything at or before `LastStoredDate` was discarded — while the unfiltered bars went on to be ingested regardless. A vendor CORRECTION of an already-stored date that the FR-6 gate **rejected** therefore had its rejection thrown away: the day did not fail closed, and the corrupt bar was appended as a new version. **Rule 3 then makes that permanent** — unremovable by construction, latest-visible forever, and feeding ADV, the participation cap, realized vol and the population band every verdict is judged against. The append-only guarantee is a protection precisely because a bar cannot be edited afterwards, which also means the only moment a bad one can be refused is before it is written; a filter that drops the refusal at exactly that moment turns the guarantee into a trap.

**P7's rationale is preserved rather than overturned, and separating the two is the whole content of the fix.** The fetch window carries a 40-session context tail, so ~40 stored dates are re-gated every single day; re-emitting their WARNINGS is duplicate-flag spam, and re-fetching 20 years to gate the historical backlog would hammer the API budget. Both recorded concerns are about warnings and cost. Neither is an argument for suppressing a reject — the filter simply never distinguished them, and nothing in the record claims it should.

**Why the rule is not "keep every reject", which is the clause that keeps the arena runnable.** P7's own open backlog is ~488k backfilled bars the gate never vetted, so a STORED bar can itself be gate-rejectable. If an unchanged re-fetch of one aborted the day, it would abort every subsequent run too — a permanent wedge with no operator remedy, and a worse failure than the one being fixed. The distinguishing question is the one the writer asks: does the fetched bar DIFFER from the stored latest-visible one? An identical re-fetch is an idempotent no-op with nothing new to refuse; a differing one is a correction about to be appended. A reject on a date with no stored bar at all also survives — a gap being filled is new data, and refusing it is the fail-closed direction.
**Consequences:** `Stage1Fetch.Gate` gains the severity+correction clause and `Stage1Target`/`ProxyTarget` gain `StoredBars`, resolved by the orchestrator exactly as `PriorActions` and `LastStoredDate` already are — `Stage1Fetch` still carries no `DbContext` (finding BB), because "Stage 1 wrote nothing" is checkable only while it structurally cannot write, and making the parameter REQUIRED means no call site can skip it without a compile error. `BarIngestionService.Differs` is promoted from private to public and reused rather than restated: a gate whose notion of "correction" drifted from the writer's would pass a bar ingestion then appends, or abort a day over a value nothing would have written. **D145** is the decision. Six fixtures, and the falsifier was run rather than argued: disabling the new clause turns the two "a reject must survive" tests red while the three P7 drop-tests and the wedge-guard stay green — which is what shows the P7 behaviour is genuinely untouched. **P7 stays open for its actual subject** (the one-time sweep of the never-gated backfill, still needing a `gated_through` marker and its own D-number); D145 narrows what P7's daily-filter clause was claiming and does not discharge it. **No stored row changes and no generation is affected** — this alters which days ABORT going forward, never any value already written.

### The decision

**D145 — Stage 1's already-gated filter is severity-aware.** A warning on a re-gated date is still dropped; a reject survives when the fetched bar is a correction of the stored one, or when no stored bar exists. The row carries the P7 split, the anti-wedge reasoning, the one-definition argument for `Differs`, and why the stored series is resolved by the orchestrator rather than read inside Stage 1.

*Verification: `check-register` green at **145 rows, D1..D145**; `ci.ps1` green — **1,280 tests** (from 1,277), seven projects, zero skipped, all nine guard greps.*

---

## v1.9.105 — Phase 6.5a (review remediation, PR 5): the rule-6 boundary (2026-08-11)

*Recorded 2026-08-11. **Phase 6 checkpoint 6.5a, PR 5.** Decision **D146** new; finding **408**, with the rule-26 Consequences field. No migration, no schema change, no config key. Full suite green at 1,285 tests across seven projects. Next-free finding after this entry: **409**.*

### finding 408 — the one function that enforces rule 6 turned an absence of evidence into a directional verdict

`PromotionGate.Decide` is 34 lines and is the ONE place hard rule 6's *"inside the MDE ⇒ TooEarly"* lives. Thirteen review auditors examined what feeds it and what reads it; none opened it. It had two defects on the same line pair.

**The boundary.** The test was a strict `<`, so a gap exactly equal to the smallest detectable effect fell through to the sign test. The case that makes this more than a rounding nicety is the **degenerate pair**: a strategy that has never traded has a flat equity curve, so the paired estimator returns gap 0 *and* MDE 0. `0 < 0` is false, `0 > 0` is false, and the verdict was **Refused** — an absence of evidence rendered as a directional finding that the strategy is *worse* than the benchmark. That is the exact inversion the MDE rail exists to prevent.

**The asymmetric guard.** The pre-flight checked `IsNaN` on the gap and `IsInfinity` on the MDE, never `IsNaN` on the MDE. A NaN MDE therefore fell straight through — `Math.Abs(gap) <= NaN` is false — and the sign test decided a verdict off an unestimable bound.

**Why it reached further than the gate.** `SeparationState` treats any non-TooEarly verdict as decisive, so the spurious `Refused` also un-dimmed the alpha cell (UX-1), sorted the row into below-or-flagged, and **suppressed the `IndistinguishableFromRandom` chip for precisely the strategy most indistinguishable from random** — a rule-21 inversion produced by a comparison operator. The chip is D63's flagship product, and the path that removed it needed no lucky percentile draw and no unusual data: only a strategy that had not traded yet.
**Consequences:** `PromotionGate.Decide` — `<=` and a symmetric NaN guard. **D146** is the decision. Six fixtures, including `D146_ATooEarlyVerdictKeepsTheChip_WhileARefusedOneSuppressesIt`, which pins the read-model mechanism from both sides so the fix is shown to restore the chip rather than merely relabel a verdict nobody reads. Falsifier run: reverting to `<` and the old guard turns three of the new tests red while the eight pre-existing gate tests stay green. **Deliberately NOT changed, and pinned by `D146_ARealGapAgainstADegenerateMde_STILLDecides`:** a real gap against an MDE of exactly 0 still decides. That combination comes from a degenerate BENCHMARK (the raw-gap fallback), and whether MDE 0 means infinite precision or an unusable estimate is a separate question about what the estimator MEANS — a proposal, not this PR. **No stored row changes** — existing `power_reports.verdict` values are untouched and the frozen generation is unaffected; this changes what the gate decides from here.

### The decision

**D146 — a gap equal to its MDE is inside it, and an unestimable MDE adjudicates nothing.** The row carries the degenerate-pair case, the asymmetric-guard defect, the read-model consequence via `SeparationState`'s `decisive` shortcut, and the explicit statement of what was left alone and why.

*Verification: `check-register` green at **146 rows, D1..D146**; `ci.ps1` green — **1,285 tests** (from 1,280), seven projects, zero skipped, all nine guard greps.*

---

## v1.9.106 — Phase 6.5a (review remediation, PR 6): the freeze rail (2026-08-11)

*Recorded 2026-08-11. **Phase 6 checkpoint 6.5a, PR 6 — the last of the queue D142's review opened.** Decision **D147** new; findings **409—410**, each with the rule-26 Consequences field. Ledger arithmetic bumps `la-2` → `la-3` (D144). No migration, no schema change, no config key. Full suite green at 1,291 tests across seven projects. Next-free finding after this entry: **411**.*

### finding 409 — a frozen position was re-sized and traded on every rebalance day, beneath a comment saying it was held untouched

§13.6 / rule 10 freeze a position when an unmapped corporate action or a bar stoppage makes it unpriceable: its valuation is pinned and the ExitPolicy is never consulted on it. `PortfolioPlanner` put the frozen name into **`Holds`** — and `PortfolioPlan.ToSize` unions `Holds` with `Opens` on a WHOLE-BOOK rebalance. So Stage 5 re-sized it and Stage 6 traded it, **priced at the very number the freeze had declared untrustworthy**, on every rebalance day, indefinitely. The comment three lines above said "held untouched".

A freeze exists to say *we do not trust this price*. Trading the position at that price is not a weaker version of the rule; it is the rule inverted, on a schedule.

**Why nothing caught it: there were NO `PortfolioPlanner` tests at all.** Not thin ones — none. So the comment and the behaviour drifted apart with no failing test between them. It was also invisible on ordinary days, because `OpensOnly` sizes only `Opens`; it took a 21st session to appear, which is why a code reading found it and three phases of running did not.
**Consequences:** `PortfolioPlan` gains a `Frozen` list and the planner fills it instead of `Holds` — a separate collection is what makes "untouched" structural rather than documentary, since `ToSize` then cannot reach it by construction. `Stage4Snapshot` gains `Frozen` as an **optional, defaulted** member and `snapshot_version` is deliberately **not** bumped: `FromJson` refuses any version but the current one, so a bump would make every stored `ds-1.0` row unreadable at the moment of deploy — including the row a run reads to fill YESTERDAY's orders and every row `reproduce-day` replays. Additive-and-tolerant is the discipline D133 applied to `config_json`. New file `PortfolioPlannerFreezeTests` — the planner's first — including an anti-vacuity case so a planner that sized nothing could not satisfy the rest.

### finding 410 — an ordinary buy performed D55's audited unfreeze, with no admin action and no audit row

`PostFill`'s buy leg rebuilt the position from four fields, so `Frozen` and `FrozenReason` were dropped on every buy. A fill into a frozen name therefore **cleared the freeze** — the same state change D55 makes typed, confirmed and audited, produced silently by the funnel with no `admin_actions` row and no operator. The sell leg two lines below used `existing with { … }` and preserved the flags, so the asymmetry was plainly unintentional: nothing in either leg looked wrong when read alone, and nothing compared them.

The unfreeze half is worse in kind than the sizing half. D55 exists precisely so that state changes of this weight leave a record; a fill that clears the flag produces the change with no record at all, and the position rejoins the tradable book as though an operator had resolved it.
**Consequences:** `PositionMath.ApplyBuy` (new) joins `ApplySell`, so both legs are expressed the same way in Core and are unit-testable without a pipeline — which is what stops the asymmetry recurring. `DailyPipeline.PostFill` delegates both. Fixtures pin that a buy into a frozen line keeps the flag and its reason, and that a buy which OPENS a line starts unfrozen at the fill date.

### The decision

**D147 — a frozen position is not sized, not traded, and a fill never clears the freeze.** The row carries both defeat sites, the snapshot-compatibility reasoning, and the arithmetic bump.

**This is a ledger arithmetic change.** A rebalance day's stored trades differ given identical inputs, so `LedgerArithmetic.Version` bumps **`la-2` → `la-3`** — the first bump driven by a decision that is not about corporate actions, and exactly the case D144's rule was written for. Generation 2's stored rows are unaffected and it remains subject to D144's whole-or-nothing constraint; a partial re-run against it was already refused (it is unstamped) and now would be refused on the version too.

*Verification: `check-register` green at **147 rows, D1..D147**; `ci.ps1` green — **1,291 tests** (from 1,285), seven projects, zero skipped, all nine guard greps. Falsifier run: restoring the frozen name to `Holds` turns two of the four planner fixtures red while the anti-vacuity and OpensOnly cases stay green.*

---

**The 6.5a queue that D142's review opened is now closed** — F1, F26, F27, F4, F23 and F5, six PRs, decisions D142—D147. The remaining review findings (F3+F10, F6, F9, F12+F17+F24, F13+F15, F16, F18+F21, F25) stay listed in PROGRESS with their evidence.

---

## v1.9.107 — Phase 6.5a (review remediation, PR 7): "sustained" means sustained (2026-08-11)

*Recorded 2026-08-11. **Phase 6 checkpoint 6.5a, PR 7.** Decision **D148** new; findings **411—413**, each with the rule-26 Consequences field. D136 gains its missing amendment naming; D47 and D53 Status cells corrected. No migration, no schema change, no config key; **no D144 arithmetic bump** (see finding 412). Full suite green at 1,297 tests across seven projects. Next-free finding after this entry: **414**.*

### finding 411 — the register carried two live specifications for how membership refreshes, and check-register structurally cannot see that

D136 (v1.9.97) ruled the membership reconcile runs **once per launch, against the forward roster, outside every daily write transaction** — and named neither of the rows it replaced. **D47** still carried "membership refresh (as-of, reconstructed from EODHD historical constituents)" inside its per-day chain; **D53** still listed `membership` inside the Stage-2 atomic transaction. Both readings were live, both quotable in good faith, for three versions.

This is precisely the **D87/D109 shape rule 25 was written after**, and the reason it is a rule rather than a convention is on display: `check-register`'s 3b/3c relational checks validate only rows that **declare** a relationship, so an *undeclared* amendment is invisible to them by construction. Three sweeps passed over it. Membership is the universe every strategy trades and every population is matched against; a corpus carrying two live specifications for how it refreshes cannot be the authority rule 25 claims it is.
**Consequences:** D136 gains an explicit "**AMENDS D47 AND D53**" clause with the reason it should have been there at v1.9.97; both stale clauses are struck inline (the surrounding rulings of D47 and D53 are unchanged and still govern); both Status cells become `amended-by D136` in the same commit. `check-register` now reports 19 amended-by rows and stays green — which is the point: it could not have caught this, so the fix is the naming, not a new check.

### finding 412 — every arm of the separation state was a single-point test, and the second one was found only because the enumeration was required

MASTER §20.8 defines the D63 separation state in three clauses, two of which say **sustained**: `distinguishable` — "the percentile path has been SUSTAINED above `P_edge(t)` (mirroring S3 Healthy)"; `emerging` — "the path is SUSTAINED outside the population's central band". `SeparationState` tested `s3Path[^1]` — one point — for both, and compared it against a literal `95.0`.

**The literal was not a rounding of the curve; it was a different rule, and the conservative one.** `P_edge(t)` is calibrated and track-length-dependent, and sp500 generation 2's frozen curve runs **71.0 at t=252** rising to ~83 — *below* 95. So reading the real bar ADMITS more paths, and fixing that arm alone would have raised the false-positive rate. The sustain requirement is not a separable improvement; it is the other half of the same correction, which is why the plan insisted the two land together.

**The `emerging` arm was not in the finding.** The review named only `distinguishable`'s hardcoded 95. `emerging` surfaced because this PR was required to enumerate every path that can set or clear the chip *before* claiming the rail settled — and the enumeration is the only reason it did not ship still broken, in the PR that said it was fixed.
**Consequences:** `SeparationState` reads the bar off the row the monitor wrote (`threshold_json.p_edge_at`, else `healthy_anchor`) rather than re-resolving config — the monitor and the read-model previously judged the same strategy on the same day against different numbers, and reading the persisted bar makes that impossible rather than unlikely. §20.8's "mirroring S3 Healthy" settles which sustain applies: the monitor's own `sustain_evals` from the same row, deliberately not a second knob. **D148** is the decision. The enumeration is committed at `docs/calibration/2026-08-11-separation-state-path-enumeration.md`; it states its closure argument for the producing side and records that the CONSUMING side is unguarded — no D91-style reflection closure exists for the D63 family — as a proposal rather than a claim. **No D144 bump:** this changes monitor thresholds and a read-model, never the values a day writes to `trades`/`positions`/`cash_events`/`equity_curve`, which is D144's own stated test. **But `replay-recompute --verify-parity` against generation 2 WILL now report differences** on any session whose stored S3 contribution was `above_edge` from an unsustained print — the harness re-derives under current rules and compares to what was stored, so a changed rule is exactly what it surfaces. That is the fingerprint, not a failure, and it is the shape D142 recorded for `reproduce-day`.

### finding 413 — S3's adverse arm demanded persistence and its favourable arm did not

OVERFITTING_MONITOR §3 states the calibrated band as "**Suspect** below `P_noise(t)` **sustained** · **Healthy** above `P_edge(t)` **sustained** · **Warning** between". Only the Suspect arm had it. So one unlucky print below the noise curve correctly read Warning, while one lucky print above the edge curve read **Healthy** — a signal biased by construction, in the flattering direction, and the signal §20.8 defines the separation state to mirror.
**Consequences:** new contribution token `approaching_edge` (above the curve, not yet sustained) and `MonitorSignals.ContinuesAboveEdgeStreak`; `OverfittingMonitor` carries the streak; **`MonitorRecompute` threads it in lockstep, which the COMPILER enforced** — the required parameter broke the build at the harness's own call site, which is the D117 seam working rather than a reviewer noticing. `FX_S3Trajectory_EdgePlant_ReadsAboveTheCurve` is renamed `..._ONCE_SUSTAINED` and now shows the two-step transition; it previously asserted the single-print behaviour as correct. Also in this entry: **F25** — PROGRESS's gate boxes for 6.3/6.4 ticked with their register rows, **6.5 recorded as legitimately OPEN with the three items PR 2 owes** (gate reorder so this evaluation's status feeds this evaluation's gate; Warning acknowledgment; auto-retire reconciliation) and the `suspect_vetoed` chip explicitly scoped OUT; and the **Phase** field corrected from "4.5 — Signal Library" to Phase 6 stage 6.5a, three phases stale.

### The decision

**D148 — "sustained" means sustained on both arms and in both places.** The row carries why the literal 95 was the conservative error, why the two halves could not ship separately, the read-the-persisted-bar argument, and the parity consequence.

*Verification: `check-register` green at **148 rows, D1..D148** (19 amended-by); `ci.ps1` green — **1,297 tests** (from 1,291), seven projects, zero skipped, all nine guard greps.*
