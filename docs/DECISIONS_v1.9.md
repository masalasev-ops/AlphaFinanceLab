# AlphaLab — the decision register (DECISIONS_v1.9)

*Extracted verbatim from `MASTER_DESIGN_v1.9.md` (v1.9.90): its §0 "Design-refinement history" and §2 "Decisions log", section numbering retained so every existing §0/§2 citation still resolves. This file is the register AND the count — no range is restated anywhere. A register row is changed only by another register row (CLAUDE.md hard rule 25 / D109); enforced by `tools/check-register.ps1`.*

---

## 0. Design-refinement history (how the design reached v1.9)

*The design was hardened across several review passes; the earliest predate the build, later passes reconcile shipped phases back into the design. Historical labels below (v4/v5/v6) name the early passes, not shipped versions. This section summarizes the largest pass (the "v6" external-review pass); the subsequent gap-closure, UI-swappability, and verdict-economics/calibration-realism work is captured in the v1.8 and v1.9 banners in `MASTER_DESIGN_v1.9.md`'s front-matter and in the decision log.*

### The external-review pass (historically "v6")

v5 closed the soft spots where phantom alpha could be manufactured *inside* the system. v6 is the response to an external design review, and it closes two different classes of gap: **(A) unhandled real-world data dependencies** that would have silently broken the system on contact with live data, and **(B) an honesty gap in the system's own stated purpose** — the power math in v5's Appendix C, taken to its conclusion, implies that "crowning a winning strategy" on forward daily data is a decades-scale problem, so the system's success criteria are now defined around what it *can* deliver on human timescales. In one list:

1. **Success criteria reframed around falsification (D38).** Detecting an effect of the size this arena can actually adjudicate requires **years to decades** of forward data even with paired testing — the arithmetic is in §1.1, and the band itself is MEASURED per construction and arena (**D122**), never assumed. The system's measurable objectives are therefore: fast, trustworthy **falsification** of no-edge strategies; honest **`TooEarly`** verdicts; regime education; and factor attribution ("what is my strategy really?"). Binary promotion is expected to be a **rare, ceremonial event**, and the system's KPIs (§1.2) do not include "found a winner."
2. **EODHD becomes the named primary data provider (D35)** — daily bars (raw + adjusted), splits & dividends, **index membership with historical constituents**, **sector/industry classification**, and a news feed — one validated subscription closing three previously-silent data dependencies (membership, sectors, news) at once. Alpaca is retained behind `IMarketDataProvider` as the free cross-check/fallback provider. Every data feed now has a **named source, a validation method, and a fallback** (§13).
3. **Random controls become populations, not single twins (D36).** One seeded random twin is one noise path — comparing against it is itself a coin flip. Each cadence family now runs a **population of M seeded random controls (default M=200)** and Monitor S3 becomes a **percentile-rank test against the population's band**. Controls need no market-data budget and trivial compute; this is the cheapest large upgrade to the system's discriminating power.
4. **Arena Replay mode (D37):** the full pipeline — funnel, ledger, gate, monitor, allocator — can run over historical data on a simulated clock, clearly labeled `replay`, **never admissible as forward evidence**, used to (a) validate the judging machinery itself (do the gates promote randoms ≤ chance over 15 simulated years?), (b) calibrate monitor thresholds before years of wall-clock pass, and (c) shake out bugs. The v5 backtest engine is subsumed into this as the strategy-seeding special case.
5. **Security master with permanent identity (D39).** Symbols are not identities: ticker changes (FB→META), ticker reuse, spin-offs, and cash/stock/mixed mergers would have presented to the v5 pipeline as spurious delistings and unrelated new listings, corrupting held positions and the append-only guarantee's usefulness. v6 adds `security_id` as the permanent key, a ticker-history table, and full corporate-action coverage (dividend, split, ticker change, merger cash/stock/mixed, spin-off, delist) with defined ledger semantics for each (§13.6).
6. **Bar-revision policy (D40).** Providers correct bars after the fact; "append-only, never delete" collides with corrections unless revisions are first-class. Bars are now **versioned append-only** (`(security_id, date, version, observed_at)`); nothing is ever updated or deleted; each run pins a **data watermark** so determinism (NFR1) is defined as *same inputs + same data snapshot + seeds → identical outputs*, and any run is reproducible forever against the snapshot it saw.
7. **Factor-attribution and risk-free data sources named (D41).** The attribution regression (market, size, value, momentum, profitability-as-quality proxy) uses the **Ken French Data Library daily factor series** (free; updated with a publication lag), and the **risk-free rate is the French RF series** (FRED 3-month T-bill as fallback). Attribution is **diagnostic-only** — so the lag is acceptable and documented rather than silently ignored. *(v1.9.23: the same factor series additionally serves as an availability-lagged signal input for residual momentum only — D83; attribution's diagnostic-only status is unchanged.)*
8. **Covariance estimator specified (D42):** the correlation-aware heat guardrail and inverse-vol sizing use **Ledoit–Wolf shrinkage** on 252-day daily returns (EWMA single-index fallback). A raw sample covariance of a 500-name universe is singular and of a 40-name book is noisy; "supplied covariance matrix" is no longer hand-waved.
9. **Slippage model parameterized (D43):** fills pay **half-spread by liquidity bucket + square-root market impact** `k·σ_daily·√(Q/ADV)` with stated defaults (k=0.1, participation cap 2% ADV) — because the cost model is the single most consequential number for whether momentum survives net, it cannot be an unstated shape.
10. **Trade-level evidence track (D44):** strategies with many short trades (fast mean reversion) accrue per-trade expectancy evidence far faster than daily-return alpha regressions; a per-trade expectancy test (block-corrected for clustering) is now a parallel evidence channel with its own MDE, so verdict speed matches each strategy family's natural sample rate.
11. **Regime evidence counter (D45):** every regime-conditional statement carries the **number of distinct regime episodes observed** ("momentum leads in bull markets — n = 1 bull episode" is an anecdote, and the UI now says so). Prevents the regime dashboard from becoming a story generator.
12. **LLM economics tightened (D46):** the daily read runs on the **Anthropic Message Batches API** (scheduled, non-interactive — half price), with **prompt caching** for the static instruction block and **per-task model tiering** (cheap model for news extraction/classification; stronger model for research briefs and skeptic reviews). The **news ingestion budget** is specified (the token sink was never the call count — it's how much raw text enters the prompt): article caps, per-article truncation, dedupe. The daily **sentiment score is demoted to experimental** status; the research-assistant roles (briefs, hypotheses, skeptic) are the primary LLM value. *(v1.9.21/D79–D82: the sentiment score and its with/without-Claude blend A/B are retired; the LLM instead runs as the contestant seat priced by a no-LLM twin — §7, §23.3, rule 32.)*
13. **Multi-day catch-up protocol (D47):** a laptop that was off for a week replays missed trading days **in order** — bars delta → corporate actions → membership refresh → funnel — one ACID transaction per day, using as-of membership reconstructed from EODHD's historical constituents. Idempotent single-day re-runs existed in v5; ordered multi-day recovery did not.
14. **MDE autocorrelation correction (D48):** Appendix C's formula assumed i.i.d. daily differences; active-return differences are autocorrelated, which understates σ and makes the honesty metric itself optimistic. The MDE now uses the **Newey–West long-run variance** of the paired difference series (OVERFITTING_MONITOR_v1.9 Appendix C).
15. **MVP discipline note (§17.1):** the biggest risk to the project is scope, not statistics. The Phase 0–3.5 stretch is the longest run of build before a real strategy exists; the phase gates are the protection, and `PROGRESS.md` is the most important document throughout.

Everything from v5 that the review endorsed is carried forward unchanged: the two-layer mental model, β-adjusted (Jensen's) alpha with IR (D26), dual benchmarks (D27), turnover matching (D28 — now applied to populations), per-strategy exits/horizons (D29), ledger realism (D30), paired tests + MDE (D31), inverse-vol default sizing (D32), the PIT fundamentals gate (D33 — now with EODHD named as the first candidate source to validate), and PIT regime labels (D34).

---

## 2. Decisions log (what we chose, and why)

The decision numbers are stable identifiers used throughout the build. The table below records only *which design pass introduced which range, and its theme* — the decisions themselves are in the register that follows. All consolidated as revision **v1.9** (v1.9.1/v1.9.2 errata merged; D71 detailed in `ARENA_ARCHITECTURE_v1.9.3.md`). One-line summaries are retained so the *why* is never lost.

| Pass | Decisions | Theme |
|---|---|---|
| initial design ("v4") | **D1–D25** | the original architecture, scope and philosophy |
| first review pass ("v5") | **D26–D34** | scope containment; the twin/control and metric choices |
| external-review pass ("v6") | **D35–D48** | data provider, control populations, Arena Replay, the append-only invariants |
| setup & secrets (v6.3) | **D49** | the launch tier and secrets handling |
| gap-closure pass | **D50–D56** | the specifications written out in full in §20 |
| UI-swappability, coherence, naming | **D57–D62** | the API boundary, honesty-in-read-models, the sole-writer Worker, `AlphaLab.*` |
| verdict-economics + calibration-realism | **D63–D65** | verdict language per channel; the D64 plants; the bootstrap universe |
| build-readiness | **D66–D67** | prerequisites and the single secrets file |
| v1.9.1 consistency | **D68–D69** | ledger money as TEXT decimal |
| v1.9.2 consistency | **D70** | replay always runs the arena's full as-of membership, never the slice |
| v1.9.3 multi-arena | **D71** | arena isolation — detailed in `ARENA_ARCHITECTURE_v1.9.3.md` |
| v1.9.7 deep-dive | **D72–D73** | Worker liveness, heartbeat, append-only config versioning |
| v1.9.9 Phase-1 reconciliation | **D74–D75** | canonical ticker identity (EODHD dash form) |
| v1.9.13 pre-Phase-2 schema decisions | **D76–D78** | corporate-action versioning; `data_quality_flags`; the cross-sectional bar read |
| v1.9.21 AI-role | **D79–D82** | the three AI seats, the context-pack contract, contestant determinism/evidence, the researcher loop + trials budget (§23) |
| v1.9.23 reconciliation | **D83** | factor returns' dual role for residual momentum — *amends D41* |
| v1.9.25 funnel-cash-constraint | **D84** | Stage-5 opens sized against cash — *resolves finding 190* |
| v1.9.26 twin-scorer | **D85** | the no-LLM twin's Stage-2 equal-weight z-score blend — *refines D81* |
| v1.9.29 frozen-valuation | **D86** | a frozen unmapped-halt position marks at cost basis, not last print — *amends §13.6* |
| v1.9.33 sp1500-target | **D87** (superseded by D109) | widening target amended to the S&P 1500, contingent on a verified-depth 400/600 source, else S&P 500 — *amends D70(a)* |
| v1.9.34 cohort-maturation | **D88** | the cohort maturation curve KPI — descriptive only, never a gate/monitor/allocator input |
| v1.9.35 phase4-prebuild | **D89** | the FR-40 detectability-at-admission gate |
| v1.9.37 save/continue | **D90** | the end-of-day book persisted in `position_snapshots`, closing the NFR-1 mutable-state gap |
| v1.9.38 signal-library | **D91** | the Signal Library registered as Phase 4.5 — descriptive only |
| v1.9.39 Phase-4 build (Arena Replay) | **D92–D99** | the D95 frozen-watermark contract; `run_kind` on the regime tables (D93); calibrated curves as versioned config rows (D98); the `422 detectability_refused` outcome (D99) |
| v1.9.42 two-pass calibration | **D100–D101** | stop acting on uncalibrated verdicts + record the would-be retires; D63-conformant flat anchors; curve-based out-of-sample metrics; the per-cadence plant ladder with a rule-selected primary |
| v1.9.46 freeze-gate | **D102** | *(superseded by D107 pre-implementation)* `joint_false_alarm` is reported, never freeze-gating — resolving the finding-251 ∧ amendment-C1 deadlock that made the D98 config freeze unreachable |
| v1.9.47 AI decision transparency | **D104–D105** | the AI seat's record must be complete enough that re-execution is never required (§23.8); `reproduce-day` replays the logged AI decision rather than re-calling the model |
| v1.9.48 recompute-harness | **D106** | monitor-rule and metric changes are scored by recomputing from stored rows rather than re-simulating (§25) |
| v1.9.50 category freeze-gate | **D107** | checks ASSERTING properties of the pass-1 flat-anchor monitor are reported-only, never freeze-gating — `joint_false_alarm`, `would_be_edge_survival_5y`, `anti_detection_speed`; membership by what a check asserts, never what it reads; every reported-only check cites its decision number in code, and a test pins the exact set — *supersedes D102* |
| v1.9.51 phase-4.5 reconciliation | *(no D-number)* | the 4.5 prompt reconciled against D92–D107 (four touch, eleven do not); findings 290–292 — the 126 horizon closed on the statistic, the widen-before-go-live ordering, the digest's as-of requirement |
| v1.9.52 signal trend-flag inference | **D108** | the trend flag is computed on a uniform 5-year window for both horizons, against a **t** reference whose df derive from the effective independent sample; the pinned constant is the significance level α per arm, never a critical value — *amends §24.2, closes finding 290's open item* |
| v1.9.55 register discipline + arenas | **D109** | a register row is changed only by another register row (rule 25, Status column, `tools/check-register.ps1`); breadth arrives as SEPARATE ARENAS, not an in-place sp1500 widen — *supersedes D87* |
| v1.9.59 stranded prohibition | **D111** | the Quality-strategy prohibition survives D109, is broader than superseded D87 scoped it, and is gated on verifying the membership methodology it rests on |
| v1.9.57 proposal quality | **D110** | the researcher is graded PER PROPOSAL on two never-blended numbers (detectability margin + calibration skill); descriptive only, never LLM-computed, and the researcher never reads its own score |
| v1.9.91 the decisions pass | **D124–D129** | calibration-skill silence (a zero trend is an absence of input); the twin paired difference as a SEPARATE instrument with its own §1.2 KPI row; the researcher boundary as FREEZE (propose new, never alter running); the shortlist rule + the ONE dispersion exception to §24.5; the sanctioned proposal triggers; the typed proposal shape — *amends D110, D81, D82, D91* |
| v1.9.92 the annual budget | **D130** | one authored spend number (`Llm.AnnualBudgetUsd`), one pre-registered estimate (the expected-output seeds, MODELLED, with a derived recalibration trigger), every other cap DERIVED and fixture-recomputed; the estimator lockout (finding 380) fixed by feeding the estimate the pre-registered value instead of the API ceiling — *amends D24* |
| v1.9.94 phase-6 open | **D131, D132** | the gate pairs against the CAP-WEIGHT BENCHMARK, never Live (commensurability, not convention) — five prose sites corrected, the `Revert` producer ruled, the vs-Live question DEFERRED with its trigger; nothing writes to `features` in Phase 6 — *amends D31* |
| v1.9.95 the lifecycle seam | **D133** | `strategies.config_json` becomes READABLE and typed (additive + tolerant, byte-stable), so a frozen param is enforced rather than documented — the admission→running seam and the control path build on it |

| # | Decision | Rationale | Status |
|---|----------|-----------|--------|
| D1 | Personal research tool, not a product | Reg/competition burden isn't worth it; value is idea generation + learning | active |
| D2 | Simulation, not advice | Avoids adviser classification; keeps it a lab | active |
| D3 | SQLite (one file) | Zero infra; upgrade only if hosting ever needed | active |
| D4 | Claude via the Anthropic API, not local Ollama | No GPU dependency; stronger reasoning; model is config | active |
| D5 | *(Hosting model superseded by D57/D67)* Blazor GUI from day one | Watch the lab run; pure C#. The client is now a **standalone Blazor WebAssembly** app consuming `AlphaLab.Api` (it can never hold secrets or domain references) | superseded-by D57/D67 |
| D6 | Daily cadence only; intraday deferred behind interfaces | Calmer, cheaper, cleaner to evaluate | active |
| D7 | Local math does all calculation; Claude only reads text | Deterministic and free | active |
| D8 | Live vs Candidate loop judged on forward P&L | Honest selection | active |
| D9 | Plain financial naming | Understandable to anyone | active |
| D10 | Metrics = expectancy, Sharpe/Sortino, alpha, drawdown, profit factor — not win rate | Win rate is uninformative | active |
| D11 | Factor-based roster with decay awareness | Decades of evidence; modest, decaying edges | active |
| D12 | Diversification + robustness over optimization | Knife-edge optima = noise | active |
| D13 | Overfitting Monitor as a first-class gate | Makes self-deception enforceable | active |
| D14 | Alpha vs benchmark; permanent Buy&Hold + Random controls | Skill vs drift vs noise floor | active |
| D15 | Meta-classifier is a fusion component, not the architecture | Scope containment | active |
| D16 | Claude reads are forward-only | Today's model knows past futures | active |
| D17 | Frozen-parameter rule; changes fork a new strategy | No tuning-until-green | active |
| D18 | Claude repositioned as research assistant | Higher value than a sentiment number | active |
| D19 | *(Superseded by D35)* Alpaca as first market-data provider | Was: genuinely free; retained as cross-check/fallback | superseded-by D35 |
| D20 | Universe = current S&P 500, membership-over-time | Liquid default; membership is state, not filter | active |
| D21 | Append-only history — never delete bars | Survivorship, valuation, audit trail | active |
| D22 | ACID + WAL + file-copy backups + snapshot-before-migration; SQLite kept | Single-writer daily tool | active |
| D23 | Goodhart defense: trials registry + deflated Sharpe + controls + S8 + no-hand-tuning | The researcher is the deepest risk | active |
| D24 | Enforced LLM cost cap + shortlist cap + graceful degradation **AMENDED by D130 (v1.9.92): the cost cap is no longer authored — every spend cap derives from the one authored annual number.** | Runaway cost structurally impossible | amended-by D130 |
| D25 | LM Studio as optional dev/test + A/B contestant, not auto-failover | Consistent signal beats cheap inconsistency | active |
| D26 | Alpha = beta-adjusted (Jensen's) alpha + IR; never a raw return gap | Low-beta strategies otherwise structurally rigged to lose | active |
| D27 | Dual permanent benchmark: cap-weight + equal-weight | Equal-weight/size effect otherwise reads as alpha | active |
| D28 | Turnover-matched random controls + cost-free reference | Control differs from strategy only in random picks | active |
| D29 | Exits/horizons are per-strategy spec (`ExitPolicy`); zero-score never selectable | Exits are strategy logic; Kelly's `b` undefined without horizons | active |
| D30 | Ledger conventions: raw prices, ex-date dividends, split share-adjustments, delist force-exit; signals on adj_close | ~1.5%/yr phantom alpha otherwise | active |
| D31 | Paired tests on daily active-return differences; MDE beside every comparison; ensemble weights primary **AMENDED by D131 (v1.9.94): the paired test’s opponent is named — the cap-weight benchmark, never Live.** | The cheapest large power upgrade; honest low-power action | amended-by D131 |
| D32 | Default sizer inverse-vol under a portfolio vol target; Kelly Phase 6+ opt-in | Kelly's inputs unestimable early; correlation ignored per-position | active |
| D33 | Fundamental strategies contingent on a named, validated PIT source (Phase 8) | Free PIT fundamentals essentially don't exist | active |
| D34 | Regime labels must be point-in-time computable + leakage-suite covered | Retrospective labels are look-ahead | active |
| **D35** | **EODHD is the primary data provider** — raw+adjusted daily bars, splits/dividends, **index constituents (current + historical since 2000)**, **sector/industry classification**, and news, on the All-World/Fundamentals tier — behind `IMarketDataProvider` / `IIndexMembershipProvider` / `IReferenceDataProvider` / `INewsProvider`. **Validation:** membership cross-checked daily against the free iShares IVV holdings CSV (divergence alerts; count sanity 495–510); bars cross-checked on a rotating sample against Alpaca's free tier. **Fallbacks:** Alpaca (bars), Wikipedia scrape (membership), static GICS snapshot (sectors, with staleness alarm) | One validated ~$20–30/mo subscription closes three previously **silent** data dependencies (membership, sectors, news) that Alpaca cannot provide, and restores the originally-intended source. Dual-source validation is what makes it "named and validated" per the D33 standard, not just named | active |
| **D36** | **Random control populations:** M seeded random controls per cadence family (default **M=200**; config), sharing the family's breadth, sizing mode, `ExitPolicy` shape, and cost model; Monitor **S3 becomes a percentile-rank test** of the strategy against its population's distribution; charts render the population's 5–95% band; one cost-free population (smaller, M=50) tracks pure noise for display. Controls are simulated as lightweight ledger-only accounts (no GUI accounts) | A single twin is one noise path — the v5 S3 two-sample test against it was itself noisy (the control could be a lucky or unlucky coin-flipper). A population gives a stable empirical null distribution at near-zero cost (randoms need no data and trivial compute); "the strategy sits at the 97th percentile of 200 matched randoms" is a far stronger and more stable statement | active |
| **D37** | **Arena Replay:** the entire pipeline (funnel, ledger, gate, monitor, allocator, populations) runs over historical windows on a simulated clock. Everything it produces is stamped `run_kind='replay'`, stored in parallel (`*_replay` scoping), **never admissible as forward evidence, never displayed beside forward numbers, never a promotion basis**. Its jobs: machinery validation (randoms promoted ≤ chance over 15 simulated years; anti-predictive detection-speed and days-to-indistinguishability KPIs, D63), **threshold calibration** for the monitor's config defaults, multi-day catch-up testing, and bug-shaking. The v5 walk-forward backtest engine becomes the strategy-seeding special case of this machinery | v5's monitor thresholds (`suspect_below: 0.25`, `auto-retire after 4`, …) were guesses that forward operation would take years to falsify; Phase 3's own acceptance test ("randoms promoted ≤ chance over a long simulation") already required full-arena simulation — v6 makes it a first-class, permanently-available, clearly-quarantined mode | active |
| **D38** | **TOMBSTONED (D115, v1.9.87): SUPERSEDED by D122 (v1.9.87) — the expected effect is MEASURED per construction and per arena, never an asserted 1–3 %/yr constant.** Original text: git history + the CHANGELOG provenance trace (v1.9.87 records the supersession). Live content rescued to: **D122** in full — the falsification framing, the §1.2 laboratory KPIs, `TooEarly` as the expected verdict and promotion as ceremonial are all restated there, since none of them depended on the number | See D122 | superseded-by D122 |
| **D39** | **Security master:** `security_id` is the permanent identity key everywhere (positions, bars, trades); tickers are time-ranged aliases in `ticker_history`. Corporate-action coverage extends to **ticker changes, cash mergers, stock mergers, mixed mergers, and spin-offs**, each with defined ledger semantics (§13.6). EODHD symbol-change and corporate-action feeds drive it; unmapped events fail closed (position frozen + operator alert, never silently mispriced) | Ticker changes (FB→META) and reuse would present to a symbol-keyed pipeline as a delisting plus an unrelated listing — corrupting held positions, history joins, and the audit trail within the first year of live running. Spin-offs create positions out of nothing; the v5 ledger had no way to express that | active |
| **D40** | **Versioned append-only bars:** bar corrections are inserted as new `(security_id, date, version, observed_at)` rows — **never UPDATE, never DELETE** (CI greps for both). Each run records a **data watermark** (max `observed_at` visible to it); reads resolve to the latest version ≤ the watermark; determinism (NFR1) is redefined as *same inputs + same watermark + seeds → identical outputs*, making every historical run reproducible forever against the exact data it saw | Providers revise bars after the fact. Silent in-place correction breaks determinism and the audit trail; refusing corrections keeps known-bad prints. Versioning gives both PIT purity ("what did we believe that day") and eventual accuracy ("what do we believe now"), explicitly | active |
| **D41** | **Factor & risk-free sources named:** attribution regresses on the **Ken French Data Library daily factors** (Mkt−RF, SMB, HML, UMD, RMW as the quality proxy; optional CMA), refreshed monthly with the library's publication lag; **RF = the French RF series** (FRED DGS3MO fallback). Attribution is **diagnostic-only** — never a trading input, never in the funnel — so the lag is acceptable and stated on the panel ("factor data through <date>"). **Amended by D83 (v1.9.23) for §6.5's signal use** — the same series doubles as an availability-lagged signal input for residual momentum only; attribution's diagnostic-only status is unchanged | §3.4's regression was specified with no data source — the same class of silent dependency D33 exists to prevent. Diagnostic-only framing is what makes a lagged free source acceptable | amended-by D83 |
| **D42** | **Covariance = Ledoit–Wolf shrinkage** on 252 trading days of daily returns over the relevant name set (held + wish-list); fallback: EWMA (λ=0.97) single-index model on numeric failure. Used by the correlation-aware heat guardrail and any portfolio-vol computation. Estimator + window are config and logged per run | A raw sample covariance is singular for 500 names and noisy for 40; "supplied covariance matrix" was hand-waved in v5, making the heat guardrail's "predicted portfolio vol" numerically fragile fiction | active |
| **D43** | **Slippage model parameterized:** per-fill cost = commission (config, default $0) + **half-spread by liquidity bucket** (keyed by **21-day ADV notional** — mega ≥ $400M/day: 1bp; large ≥ $100M/day: 2.5bp; other: 5bp) + **square-root impact** `k · σ_daily · √(Q/ADV)` with default `k=0.1`, and a **participation cap** Q ≤ 2% of 21-day ADV (excess quantity is rejected, logged, and surfaced — capacity awareness made concrete). All coefficients config; the cost model version is stamped on every fill | Whether momentum survives net of costs is the single most consequential number in the system; an unstated cost shape is an unfalsifiable one. Square-root impact is the standard, defensible retail-scale default | active |
| **D44** | **Trade-level evidence track:** strategies whose natural unit is the trade (fast mean reversion, breakout) additionally accrue a **per-trade expectancy test** — mean net P&L per trade with a moving-block bootstrap (trades cluster in time and regime), its own MDE, and the same `TooEarly` discipline. Rendered beside, never instead of, the daily-alpha track | A 5-day-holding strategy produces hundreds of trades while a monthly rebalancer produces dozens of daily-return observations' worth of information; forcing all families through the daily-return lens wastes the fast families' sample rate. Block correction keeps the extra power honest | active |
| **D45** | **Regime evidence counter:** every regime-conditional metric displays **n = number of distinct regime episodes observed** (not days), and any regime claim with n < 3 episodes is rendered with an explicit "anecdote" badge. Episode = a maximal contiguous run of one PIT label | A few years of forward data contains perhaps one bear market; "momentum leads in bulls" from n=1 episode is a story, not a finding. The regime dashboard must carry its own power honesty like every other screen | active |
| **D46** | **LLM economics:** daily reads run on the **Anthropic Message Batches API** (scheduled + non-interactive ⇒ half price); the static instruction block uses **prompt caching**; **per-task model tiering** (cheap/fast model for news extraction & classification; stronger model for research briefs, regime summaries, skeptic reviews) is config per task. The **news ingestion budget** is enforced upstream of the provider: max articles per read (default 25), per-article truncation (default 2,000 chars after local extraction), title-hash dedupe, universe/macro relevance filter. The daily sentiment score is **retired** — this D46 framing is **superseded by D79–D82**: the LLM's arena presence is the contestant seat priced by its mandatory no-LLM twin (§7, §23.3, rule 32), and the research-assistant roles are the primary LLM value | Cost was already capped (D24); v6 optimizes within the cap and names the true token sink — raw news text entering the prompt, not call count. Honesty about the score: a single market-level number was a low-information feature, and the contestant-vs-twin A/B that replaced it faces the same MDE reality as everything else | amended-by D79-D82 |
| **D47** | **Multi-day catch-up protocol:** on startup the orchestrator computes missed trading days and replays them **strictly in order** — per day: bars delta → corporate actions → ~~membership refresh (as-of, reconstructed from EODHD historical constituents)~~ → funnel — one ACID transaction per day, resumable, idempotent. A `catchup_log` records each recovered day. **AMENDED BY D136 (recorded v1.9.107, finding 411): the membership clause above is STRUCK.** The refresh is not per-day, not inside the transaction, and not reconstructed from EODHD historical constituents — D136 made it once per launch, against the forward roster, outside every daily write transaction. D136 ruled it without naming this row, so both readings stayed live and quotable for three versions; that is the rule-25 condition, and it is closed here rather than left for the next reader to discover. The rest of D47 — ordered replay, one ACID transaction per day, resumable, idempotent, `catchup_log` — is unchanged and still governs | Personal systems fail here constantly; single-day idempotency (v5) does not compose into correct week-long recovery without ordered replay and as-of membership | amended-by D136 |
| **D48** | **MDE uses the Newey–West long-run variance** of the paired daily difference series (`σ_LR` replaces `σ_d`; lag by the strategy pair's max holding horizon, capped) in Appendix C; the same correction applies to the trade-level track's block bootstrap | Active-return differences are autocorrelated; the i.i.d. formula understates σ and makes the anti-overclaiming number itself overclaim. The honesty metric must be honest | amended-by D118 |
| **D49** | **Budget-tier launch configuration:** EODHD "All World" (~$19.99/mo) at launch — bars/splits/dividends/news from EODHD; **membership: IVV holdings CSV primary + Wikipedia cross-check** (dual free sources; D35's fail-closed validation unchanged); **sectors from the IVV CSV's GICS column**; replay as-of membership seeded from the free community CSV (caveat logged); the EODHD-constituents slot stays dormant behind `IIndexMembershipProvider`. **Phase 8 entry condition:** upgrade to a fundamentals-bearing tier for one trial month → run the §7.0 PIT protocol → keep the tier only on a pass. Secrets live in one gitignored `appsettings.Secrets.json` — never in the repo (D67; SETUP_v1.9 §5) | Phases 0–7 — the complete core laboratory — run properly on the cheapest tier; the only blocked phase (8) was already contingent, and the interface seams make the eventual upgrade a provider swap plus a config flip, not a rewrite | active |
| **D50** | **Regime labels specified:** PIT label = **trend × volatility** on the cap-weight proxy. Trend: `bull` if adj close > 200-day SMA else `bear`, with **hysteresis** (flip requires ≥1% beyond the SMA for 5 consecutive sessions; config). Volatility: `high_vol` if 21-day realized vol ≥ its own trailing 3-year 80th percentile (same 5-day confirmation) else `normal_vol`. Four labels; episodes (D45) run on the trend component. Full spec §20.1; params in CONFIG `Regime.*`; ships with F-LEAK + a hysteresis fixture | The label was load-bearing (S5, regime halts, dashboard, episode counter) but undefined — the one undocumented decision on the quant path. Hysteresis prevents label flapping from manufacturing spurious "episodes" | active |
| **D51** | **Ensemble allocator fully specified:** shrink each strategy's forward net β-adj alpha toward the roster mean by its own NW standard error (James–Stein-style; short tracks ⇒ ~equal weight), map shrunk alphas to targets via a bounded softmax (temperature config), then clamp in order: floor/ceiling → TooEarly tilt cap → Suspect freeze/decay → ±band movement → renormalize. Every input persisted to `allocation_log` (weight reconstructible, NFR-2). Full math §20.2 / DESIGN_IMPROVEMENTS §3.5 | The doc named the allocator the *primary* improvement mechanism, then specified it in one paragraph — the largest improvisation surface in the build. This uses only quantities the gate already computes and degrades to equal weight exactly when evidence is weak | active |
| **D52** | **Research journal / hypothesis registry:** new `journal_entries` table (kinds: hypothesis, observation, decision_note, skeptic_review, outcome). CandidateFactory **requires** a linked, immutable-once-locked hypothesis (falsifiable claim + confirm/refute metric + pre-declared evidence window) or an explicit `unregistered` flag rendered permanently on the strategy card; retirement/verdict triggers an outcome-closure nag. The §1.2 "operator learning" KPI is measured from this table | The KPI and Golden Rule 18 mandated pre-registration but no table, FR, or screen existed — a stated KPI was unimplementable. Mechanizing the link turns discipline into plumbing | active |
| **D53** | **Daily run restaged:** Stage 1 **fetch** (all provider HTTP, zero DB writes, payloads to raw-cache, quality gate on staged data; hard failure aborts before any row); Stage 2 **commit** (ONE atomic write transaction: bars → actions → ~~membership~~ → features → regime label → funnel+fills → metrics → cadence-day monitor/gate/allocator → run row — **`membership` STRUCK, amended by D136, recorded v1.9.107 / finding 411: the refresh runs once per launch, outside this transaction**); Stage 3 **LLM** (batch submitted post-commit; results land in their own small transaction whenever ready; late/missed = a no-read day). Invariant reworded: **one atomic write transaction per trading day** (Golden Rule 16). Catch-up = Stages 1–2 per missed day; no Stage 3 for past days (D16) | As previously written, a SQLite write transaction spanned third-party HTTP calls *and* an async batch that can take hours — stalling WAL readers and making atomicity hostage to network latency. Staging keeps the real correctness property and drops the impossible one | amended-by D136 |
| **D54** | **Trading calendar is a named, validated feed:** `trading_calendar(date, session∈{full,half}, close_time_local)` seeded ±30y from NYSE holiday rules, spot-validated vs ≥2 exchange notices; `ICalendarService` answers is-trading-day / prev-next session / sessions-between / close time. Consumers: orchestrator trigger (session close + config offset, ET-anchored so DST never shifts the run vs the market), D47 catch-up, decide-at-close/fill-at-open pairing, warm-up counting | "Missed trading days," T/T+1 pairing, and "18:30 local" all presupposed a calendar no document specified — exactly the silent-dependency class D35 exists to eliminate | active |
| **D55** | **Admin actions first-class & audited:** the two manual paths (insert a typed corporate action to unfreeze a position; override a persistent membership divergence) become GUI actions behind a **typed confirmation** (retype the symbol), passing the same validation as provider rows, writing the domain row (`source='manual'`) + an `admin_actions` audit row (who/when/what/why/affected accounts), then re-running the affected ledger step in its own transaction. No other manual write path exists (Golden Rule 29) | The runbook depended on an "admin tool" with no FR, no audit trail, and no UI — an unaudited write path into the ledger, the most dangerous surface in the app | active |
| **D56** | **S3 thresholds become track-length-aware trajectories:** Phase-4 replay calibration produces `P_noise(t)` (envelope below which a no-edge strategy falls at the configured false-alarm rate) and `P_edge(t)` (median trajectory of a planted 2%/yr edge — plants specified by D64). S3 at track length t: **Suspect** below `P_noise(t)` sustained; **Healthy** above `P_edge(t)` sustained; **Warning** between. Flat anchors (Healthy ≥ 95th sustained / Suspect < 25th sustained — the Suspect anchor is the *anti-predictive tail*, D63-aligned) remain only as pre-calibration anchors. The S3 panel plots the strategy's percentile path against both curves | The flat thresholds contradicted §1.1: a genuine edge of the size this arena can adjudicate sits in the 60th–90th percentile for years, so the arena's steady state would have been every honest strategy permanently Warning/Suspect, feeding S6's auto-retire. Trajectories keep anti-predictive detection fast while only demanding separation the horizon can deliver | amended-by D122 |
| **D57** | **`AlphaLab.Api` is the single UI boundary.** A new ASP.NET Core minimal-API project sits between the core/evaluation services and every front end, exposing (a) **read endpoints** — one per §15 screen — returning the D58 read-models as JSON, and (b) **command endpoints** for the handful of user-initiated actions (create/pre-register candidate, request a research brief / skeptic review, apply a D55 admin action, launch a replay). The API owns auth-less localhost binding by default (personal tool), input validation, and an OpenAPI document (Scalar UI). Blazor is re-scoped as a **client** of this API; it holds no direct references to `AlphaLab.Evaluation`/`AlphaLab.Data` domain services. Full spec §21 | The UI must be swappable (Blazor → Angular/React/mobile) without touching the quant core. Blazor Server's in-process SignalR calling of C# services is not a boundary another framework can consume; a JSON API is. This also makes the eventual hosted/multi-client story a deployment change, not a rewrite. Cost: one thin project and a serialization pass — cheapest before Phase 0, painful after Phase 3 | active |
| **D58** | **Serializable honesty read-models.** Every screen is served by a plain-DTO read-model computed in `AlphaLab.Core`/`AlphaLab.Evaluation` (not in the UI), in which the honesty rules are already *resolved into data*: an α field ships with `{value, display: normal|dimmed, prefix: ""|"~", reason: "inside_mde"|…}`; a strategy row ships its `verdict_chip`, `tier`, and `population_percentile`; an allocation row ships each `clamp_bound` on the arrow it affected; every replay artifact ships `quarantined: true`. The UI's only job is to render these fields verbatim — it computes no thresholds, sorts no tiers, and decides no dimming. Tests move from Blazor view-model tests to **read-model unit tests** in `AlphaLab.Evaluation.Tests` (framework-agnostic) | If the honesty logic lives in the UI, every new front end re-implements it and can silently violate it (an Angular screen showing a green number the Blazor screen dimmed). Computing it once, server-side, into serializable data makes the guarantees **un-bypassable and shared by every client**, and makes the UX rules testable without a browser. This is what actually makes the swap safe, not just possible | active |
| **D59** | **Process model after the API split.** Two processes, one solution: **(1) `AlphaLab.Worker`** — a .NET Generic Host that owns the daily staged pipeline (D53) and catch-up (D47) and is the **sole DB writer**; it runs **on-demand by default** (launch → catch up → exit, D61) with an optional resident Quartz scheduler behind `Worker.Mode`. **(2) `AlphaLab.Api`** — the read/command HTTP boundary; its command endpoints perform *small, bounded* writes (create candidate, journal outcome, admin action) but **long-running or scheduled work it does not run itself** — it enqueues an intent the Worker executes. `AlphaLab.Web` (or any UI) is a third, optional process. All three open the same SQLite file in WAL mode; the single-writer guarantee holds because scheduled runs and catch-up live only in the Worker, and the API's bounded writes take the write lock briefly and never overlap a daily run (the Worker exposes a `run_in_progress` flag the API checks; a command arriving mid-run is queued, not raced). Replaces the earlier "scheduler in-process inside AlphaLab.Web," which D57 invalidated by emptying AlphaLab.Web of domain logic | After D57 the daily job had no home — AlphaLab.Web is now a thin client and the API must stay a thin boundary. A dedicated Worker is the honest owner of the one writer, keeps the API stateless and fast, and makes "the API was down but the lab still ran last night" true. Two tiny hosts beat cramming a scheduler into either the UI or the request pipeline | active |
| **D60** | **API contract conventions (so any client is a drop-in).** (a) **Versioned base path** `/api/v1/…`; breaking changes bump to `/v2` and the OpenAPI doc carries the version. (b) **Uniform error envelope** `{ error: { code, message, details? } }` with conventional status codes (400 validation, 404, 409 conflict e.g. command-during-run or stale pre-registration, 422 domain-rule rejection e.g. missing hypothesis / bad admin token, 503 when a dependency like the LLM budget is exhausted). (c) **Long-running commands are async jobs:** `POST /api/v1/replay` and the two `POST /api/v1/analysis/*` calls return **202 + `{ job_id }`**; progress arrives on `GET /api/v1/stream/jobs/{job_id}` (SSE) with a `GET /api/v1/jobs/{job_id}` poll fallback; the eventual result lands in its normal table and read-model. (d) **Read consistency:** every read-model carries the `run_id` + `watermark` it was projected from, so a client can tell whether it is seeing today's committed state (ties to NFR-1). (e) **Money/ratios as strings or integer minor units** in JSON — never floats — to preserve ledger exactness across languages. (f) **Time is UTC ISO-8601**; the market calendar (D54) supplies session/close context. All conventions are in the OpenAPI doc | "Returning the read-model" left the failure and long-running paths unspecified — exactly where a second client (Angular, mobile) would diverge or a replay launch would block a request thread for hours. Pinning the envelope, the async-job pattern, versioning, and float-free money makes the contract genuinely portable and the honesty numbers exact on every client | active |
| **D61** | **On-demand run mode is the default; the scheduler is optional.** `AlphaLab.Worker` supports two modes via `Worker.Mode`: **`OnDemand` (default)** — the Worker performs catch-up-through-the-last-completed-session and **exits**; this is the "open it each evening, it updates and quits" path for a machine that is not always on. **`Scheduled`** — the Worker stays resident and Quartz triggers the run at session-close + offset (D54), for an always-on host. **Both modes do the identical work** through the same catch-up protocol (D47) and staged pipeline (D53); the only difference is the trigger (you vs. a timer). On launch, either mode: compute missed sessions from the calendar, replay them in order (idempotent — a second launch the same evening is a no-op), and — the one guard — process only through the **last *completed* session**, never a half-run of a day whose close hasn't happened yet. Forward-only LLM (D16) is unchanged: catch-up days get ledger/metrics but no back-dated news read, so intermittent use yields sparser (never incorrect) LLM coverage | Quartz on a laptop that sleeps is the awkward fit — it fires whether or not the machine is awake, misses the trigger when asleep, then falls back to catch-up anyway. Catch-up (D47) was always the real engine; the scheduler only *triggers* work it already does. Making OnDemand the default matches how a personal, sometimes-off machine is actually used, removes a dependency that mostly doesn't help, and keeps `Scheduled` one config flip away for a future always-on box — no rewrite | active |
| **D62** | **Project name: AlphaLab.** The system is named **AlphaLab** and the .NET solution uses **`AlphaLab.*`** as its root namespace (`AlphaLab.Core`, `AlphaLab.Data`, `AlphaLab.Strategies`, `AlphaLab.Evaluation`, `AlphaLab.Llm`, `AlphaLab.Worker`, `AlphaLab.Api`, `AlphaLab.Web`, and mirrored `*.Tests`). "Alpha" names the exact quantity the lab measures honestly (β-adjusted alpha vs. construction-matched benchmarks and a random-control null); "Lab" preserves the research-not-advice posture. **Before publishing a repo, do the availability pass** — GitHub org/repo, NuGet package prefix, and a finance/dev-tool collision check — since the name is attractive and may be partly taken; the namespace itself is unaffected by any external collision | The name should read as finance on sight (a research lab that measures alpha) without implying a broker, fund, or advisory service — consistent with D1/D2 (personal research tool, not advice). A single root namespace keeps the swap-the-UI story clean and the solution legible | active |
| **D63** | **Verdict economics corrected; `IndistinguishableFromRandom` is a first-class outcome.** Because D36's populations are turnover-matched and cost-inclusive, an edgeless strategy sits at the **median** of its band — the population channel can never falsify it, only declare non-separation. Consequences: (a) each strategy carries a **`separation_state ∈ {none, emerging, distinguishable}`**, computed in the D58 read-models from its percentile path (spec §20.8, FR-35); once track ≥ `Verdicts.SeparationMinTrackDays` (default 252) a state of `none` renders the **`IndistinguishableFromRandom` chip** with its day count on every screen, beside (never instead of) the gate verdict (UX-12); (b) §1.2's falsification-speed KPI splits into **anti-predictive detection speed** and **indistinguishability honesty**; (c) fast-kill language is reserved for the trade-level track (which tests vs zero, D44) and anti-predictive S3/S6 breaches | The design's own §1.1 previously claimed "no-edge strategies retired in months" — arithmetic its control construction cannot deliver, since an edgeless strategy is never *below* a cost-matched band. Selling falsification speed the lab cannot produce is the same class of overclaim D38 exists to prevent; naming the real products (indistinguishability — fast; anti-predictive kills — fast; confirmation — decades) keeps the operator's expectations honest through the multi-year `TooEarly` era | active |
| **D64** | **Planted-strategy fixtures specified — the foundation under every calibrated curve.** Three plant kinds (spec §20.9, FR-36): **no-edge** = the family's own population process with fresh seeds; **edge** = alpha injected as a **regime-conditional, autocorrelated overlay** on turnover-matched random picks — active sessions drawn from a persistent two-state process with stationary activity `Calibration.Plant.ActiveDayFrac` (default 0.25) and mean run length ≈ the family's holding horizon, per-active-day drift scaled so the annualized overlay nets to `Calibration.Plant.AlphaAnnualPct` (default 2.0), optional regime multipliers (default bull 1.25 / bear 0.5, renormalized) — **never constant daily drift**; **anti-predictive** = the mirrored negative overlay (`AntiAlphaAnnualPct`, default −2.0). Each curve = per-t **median over ≥ `SeedsPerPlant` (default 50) seeds**, archived with its 25–75% band. **Mandatory plant-sensitivity check:** calibrate against both the realistic plant and a naive constant-drift plant; if `P_edge(t)` diverges by > `SensitivityMaxGapPts` (default 10 percentile points at any t ≥ 126d), the realistic plant's curves are adopted and the divergence is a permanent section of the calibration report. Curves are stamped with their replay data vintage (membership source + survivorship caveat, §13.4) | A naive constant-drift plant separates from its band smoothly and early; a realistic lumpy factor edge spends years inside it. Calibrating `P_edge(t)` on the naive plant would flag every honest strategy Warning for years — recreating exactly the failure D56 was written to fix, but now invisibly, *inside the calibration itself*. The plants are the most consequential number-generating process in the system; specifying them, multi-seeding them, and measuring their influence (instead of assuming it) is what makes the D56 curves trustworthy | active |
| **D65** | **Thin vertical slice is the sanctioned build order.** The strategic target is reaching **Phase 4 (Arena Replay) fastest**, because replay is where the machinery is proven and where the D63 verdict economics are *measured* instead of assumed — every screen built before that is speculative. Concretely: S&P 100 universe through Phase 4; the lab is **operated API-only (via the Scalar UI) until Phase 4 sign-off**; the D58 read-models and `AlphaLab.Api` endpoints are built and tested on schedule in Phase 3 (the honesty lives there — non-negotiable), but **Blazor screens become a deferrable parallel workstream** — start any time after Phase 3, hard deadline Phase 7 exit. Phase 0 still stands up the empty Blazor client (cheap; proves the wiring); per-phase *screen* obligations move to the UI workstream (§17.1, BUILD §2) | Phases 0–4 are the highest-abandonment-risk stretch of a multi-month solo build; every week spent on rendering before the machinery is validated raises that risk, and calibration may invalidate what was rendered. D57/D58 already made the UI a swappable client of tested read-models — deferring it costs nothing the architecture hasn't already paid for, and the read-model tests (not browser tests) were already the enforcement point | active |
| **D66** | **Read-model stamp is a discriminated union, not a nullable flat stamp.** `ReadModelStamp` is always a present object carrying `status: "no_run_yet" | "stamped"`; `run_id`, `watermark`, and `as_of` are non-null **iff** `status == "stamped"`. Empty/pre-first-run state is `{status:"no_run_yet", run_id:null, watermark:null, as_of:null}` — Phase 0's universal case. **Semantics:** the discriminant is about *run-context presence only* — a strategy that exists but has zero trades is still `"stamped"` (a run happened; its collections are just empty); `"no_run_yet"` means no run has ever been committed to the system. Replay quarantine is orthogonal — replay read-models are `"stamped"` with `quarantined:true`, never a third status. `status` serializes as a string. **Test (Phase 3):** before any run every screen is `"no_run_yet"`; after the first committed run every *forward* read-model is `"stamped"` with non-null run_id+watermark and never reverts | D60 requires every read-model to be "always stamped," but pre-first-run there is genuinely nothing to stamp. A nullable flat stamp would push the guarantee into a doc note ("null only before the first run") that consumers can ignore — the exact convention-over-type failure D58 exists to prevent. A status discriminant makes "always stamped" unforgeable: every client must branch on `status`, so the empty case cannot silently rot into "nullable forever." A flat discriminant (not polymorphic subtypes) keeps JSON deserialization trivial for any D60 client | active |
| **D67** | **Secrets: one gitignored `appsettings.Secrets.json`; no environment variables, no User Secrets store.** This is a local-only, single-machine tool, so the config builder is exactly `AddJsonFile("appsettings.json").AddJsonFile("appsettings.Secrets.json", optional:true)` in **AlphaLab.Api and AlphaLab.Worker** — the WASM AlphaLab.Web client never loads secrets (its browser-served `wwwroot/appsettings.json` carries only the non-secret `Arenas` registry — D71). No `AddEnvironmentVariables`, no `AddUserSecrets`. Keys (`Secrets:EodhdApiToken`, `Secrets:AnthropicApiKey`, optional Alpaca pair) live only in the gitignored `appsettings.Secrets.json`; the committed `appsettings.json` holds non-secret config only. `.gitignore` lists `appsettings.Secrets.json`; the Phase-0 CI grep still scans committed files for key patterns. No `UserSecretsId` in `Directory.Build.props`. Supersedes the earlier env/User-Secrets guidance (changelog finding 33), NFR-4, and golden rule 11 | Env vars and a separate User Secrets store were operational ceremony aimed at multi-user/CI/deployment scenarios this lab does not have. A single layered JSON file, gitignored, gives the same protection that actually matters (keys never enter git) with the one-file simplicity a personal local tool wants; the CI grep on committed files remains the real leak guard | active |
| **D68** | **Equal-weight benchmark convention pinned: self-built equal weight of the eligible universe, monthly rebalance**, running through the same funnel/ledger/cost machinery as every other account. An EW ETF proxy is *not* used | The EW benchmark must match the construction the random populations and LowVol are judged against and must embed the D43 cost model; an ETF proxy embeds the fund's own rebalance timing and expense drag instead. Closes the open item in STRATEGY_CATALOG §5.1 (previously "record as a D-number at Phase 2") | active |
| **D69** | **Ledger money is C# `decimal` end-to-end, persisted as TEXT** (EF Core's default SQLite `decimal` mapping — exact decimal strings) in every ledger money column: fill prices, commissions/spread/impact costs, cash-event amounts, cost basis, equity/cash, starting cash, and corporate-action cash legs. Market-data prices (`bars`) and derived statistics (alpha, Sharpe, MDE, expectancy CIs, …) remain REAL. The API serializes money as strings/minor units per D60 — now backed by an exact store | D60 promises "the ledger's exactness survives the JSON boundary," which is empty if the ledger itself is IEEE doubles: REAL money columns accumulate binary rounding across thousands of fills and dividends. TEXT-decimal costs nothing at this scale (all statistics compute in C# on in-memory arrays anyway) and makes the exactness claim true from the first fill | active |
| **D70** | **The S&P 100 slice is sourced and scoped; Arena Replay always runs on S&P 500 as-of membership.** (a) The D65 forward universe through Phase 4 sign-off is the **S&P 100**, and it is a real named feed like every other (Golden Rule 25): **iShares OEF holdings CSV primary** (same BlackRock ajax pattern as IVV) + **Wikipedia S&P 100 list cross-check**, fail-closed on divergence, count sanity 99–103, config `Universe.Bootstrap.*`. After Phase 4 sign-off the universe widens to the S&P 500 (D20; **target amended to the S&P Composite 1500 by D87** — contingent on a verified-depth 400/600 historical-membership source, else S&P 500) by a config flip + a backfill delta — no code change. (b) **Replay never runs on the 100-name slice:** replay and threshold calibration always use **S&P 500 as-of membership** (the fja05680/sp500 community CSV at launch per D49; EODHD historical constituents post-upgrade), with bars backfilled for **every historical member inside the replay window, including delisted names**, as a **Phase 4 entry prerequisite** |  | amended-by D109 |
| **D71** | **Multi-arena isolation via the multi-instance model** (full spec: `ARENA_ARCHITECTURE_v1.9.3.md`). An **arena** is a single-universe lab isolated at the storage layer: its own SQLite file, Worker+Api instances, and snapshot/backup dirs, all derived from one `Arena.Id`. **Calibration is arena-scoped and never shared** (cost model D43, covariance D42, control populations D36, verdict curves D56/D63, EW benchmark D68, replay report — all stamped `Arena.Id`). The **frontend selects one active arena**; leaderboards never merge across arenas (side-by-side panels only). A **cross-arena meta-allocator is out of scope**. **No SCHEMA change** — the arena boundary lives in config, storage paths, and process instances, not row shape. Start with `sp500`; adding an arena later is a config + instance operation, not a rewrite | Physical isolation buys correctness (no accidental pooling of large- and small-cap competitions, which have different cost models and control populations) and near-zero new code (the DB path is already config-driven). Merging leaderboards across universes is the same category error as ranking a daily strategy against a monthly one. The membership machinery (D20/D39/D70) already moves a security between universes over time, independent of arenas | active |
| **(D71 note)** | The vertical slice (D65) left "S&P 100" without a named source — violating Golden Rule 25 — and silently implied a 15-year replay on a universe with no historical-membership feed (every named source is S&P 500-based). Calibrating the D56 curves on a 100-name slice would also calibrate thresholds for a universe the lab will not run forward. Sourcing the slice keeps the fail-closed standard; pinning replay to S&P 500 membership keeps the calibration honest and matches §14.2's storage math (~1,000 historical ids ≈ 5M rows) |
| **D72** | **The Worker's process model is completed: OnDemand executes queued jobs, and the run flag is crash-safe.** (a) The OnDemand launch order is **schema → catch-up → drain queued `jobs` (replay, analysis) → backup → exit** (`Worker.DrainQueuedJobsOnLaunch`, default true); catch-up always precedes job execution (a replay must see a caught-up store) and jobs never run inside a daily write transaction; the Api's 202 body states the execution model ("queued — runs at the next Worker launch, or start the Worker now") and `GET /jobs/{job_id}` shows queue position. (b) The running Worker writes `worker_state.heartbeat_at` at least every `Worker.HeartbeatSeconds` (default 30); on launch it clears any `run_in_progress=1` whose heartbeat is older than `Worker.StaleRunThresholdSeconds` (default 300), marks the orphaned `runs` row `failed`, and logs the recovery; the Api treats a stale heartbeat as not-in-progress for its 409 decision and surfaces "stale run detected" on Data-health | As specified through v1.9.6, a replay launched from the UI on the default OnDemand deployment sat `queued` until an unspecified future launch (D59 says the Worker executes jobs; D61's default Worker exits) — unusable exactly where the vertical slice needs it, since Phase 4 iterates replay launches. And `run_in_progress` is necessarily written outside the daily transaction (the Api must see it mid-run), so a mid-run crash left it 1 forever, 409-ing every command. Drain-on-launch keeps the "open it each evening" posture with zero new processes; heartbeat staleness is the standard cure for an orphaned advisory flag | active |
| **D73** | **The regime proxy is a named, validated feed (closing the last silent data dependency).** Primary: **EODHD `GSPC.INDX` EOD series** (the membership index symbol, reused — index EOD verified 2026-07-13 on the launch tier, INTEGRATIONS §9). Validation: rotating cross-check vs `SPY.US` daily returns (tolerance alarm; SPY's daily tracking error is negligible for a trend/vol label). Fallback: a **self-built cap-weight index** over the backfilled universe bars with as-of membership (the D68 benchmark machinery, cap-weighted), stored with a stable `security_id` so `regime_labels.inputs_hash` keys a real row. **Backfill prerequisite:** ≈3.8 years of proxy history (200-day SMA + trailing 3-year vol distribution) before the first regime label; the full replay window before Phase 4. **The S&P 500 proxy is pinned even during the D70 S&P 100 slice** — regimes are market-level facts, and switching proxies at the Phase-4 widen would fabricate a label discontinuity. Wired as FR-38; feed spec INTEGRATIONS §9 | D50's label is load-bearing (S5, regime halts, D45 episodes, the regime dashboard) **and sits on the calibration critical path**: the D64 edge plant modulates its drift by the PIT label, so an unnamed or degenerate proxy silently mis-calibrates the D56 curves the whole monitor trusts. Golden Rule 25 (named source, validation method, fallback) applied to the one feed every prior pass missed — `Regime.ProxySecurityId: null // set at Phase 1` named no source, no validation, no fallback, and no warm-up requirement | active |
| D74 | **Index-membership drop ≠ delisting.** A name leaving the index stamps `index_membership.removed_on` only — it does **not** write `corporate_actions(type='delist')` or set `securities.delisted_on`. `type='delist'` is reserved for a **true delisting** detected from the exchange symbol-list drop-out (`exchange-symbol-list/US?delisted=1`) / acquisition feed (dormant at launch per D49), landing with Phase 2's "corporate-action semantics complete" (§13.6). Implemented in checkpoints 1.4/1.5 | A universe exit (cap threshold, replacement) is not a security-lifecycle event — most dropped names keep trading — and forcing a synthetic delist would fire a Stage-4 close outside `ExitPolicy`, breaching hard rule 7 (D29). The canonical TEST_PLAN FR-4 fixtures never assert a delist CA on an index drop | active |
| D75 | **Canonical ticker identity = EODHD dash form (`BRK-B`).** All source dialects normalize into it via `SymbolNormalizer`: mechanical `.`→`-` for the historical-CSV/Wikipedia dot form (`BRK.B`), plus a curated alias map for the iShares IVV/OEF no-separator forms that cannot be split mechanically (`BRKB`→`BRK-B`, `BFB`→`BF-B`). Anchored to the EODHD market-data provider so bar joins need no translation on the hot path; the alias map is a **stop-and-report seam** (extend it if a live backfill surfaces another no-separator class share). Implemented in checkpoints 1.4/1.5 (FR-3) | The three feeds spell class shares three ways (IVV/OEF `BRKB`, historical/Wikipedia `BRK.B`, EODHD `BRK-B`); without one canonical form Berkshire/Brown-Forman silently drop from every roster and bar joins mismatch. Anchoring to the market-data provider's form keeps the highest-volume join (bars) translation-free | active |
| **D76** | **`corporate_actions` is versioned append-only + read-at-watermark, like bars (extends D40 to the feed the ledger prices on).** A `version` column (DEFAULT 1); a value-diff restatement of the same `(security_id, type, effective_date)` appends `version = MAX(version)+1` — never an UPDATE/DELETE. `observed_at` is the point-in-time key; the read rule is "per (type, effective_date), latest version WHERE `observed_at ≤ watermark`" (`ICorporateActionReadService`, mirroring `BarReadService`). Ingestion changes from existence-skip to value-diff-append; a `ux_corporate_actions_identity(security_id, type, effective_date, version)` unique index enforces it (ex_date excluded — SQLite NULL-distinct would break split dedupe). §13.5's bars rule extended to §13.6. **Known limitation (documented):** two genuinely-distinct actions on one identity (a regular + a special dividend on the same ex-date) collapse to one versioned line — a stop-and-report seam; any future discriminator must be feed-supplied, not amount-based. Pre-Phase-2 (before the ledger builds on it) | Without versioning, an EODHD dividend **restatement is silently discarded** (first-write-wins) and **Phase-4 replay leaks the future** — the ledger would price a dividend from an action observed years after the replay watermark, breaking NFR1 (same inputs + watermark + seeds → identical outputs), the exact property D40 exists to buy. §13.5 stated the read rule for bars; it was never extended to the feed the ledger prices on | active |
| **D77** | **A `data_quality_flags` table persists the FR-6 gate's findings.** `DataQualityGate` is pure and its `QualityFlag`s (issue/severity/symbol/date/detail) were computed and discarded. A `data_quality_flags` table (bare `flag_id` PK, `run_id`, nullable `security_id`, `symbol`, nullable `date`, CHECK-constrained `issue`+`severity`, `detail`, `observed_at`) + an `IDataQualityFlagStore` seam (`Save`/`GetForRun`) land the sink; a "flags" slot is added to the §15 Data-health screen. Persists BOTH warn and reject flags (the audit trail). Gate→store wiring is Phase 2; the read-model projection is Phase 7 — this lands the table so there is something to persist into. Pre-Phase-2 (before the gate is wired) | In a lab whose thesis is that a number is never shown without the honesty that qualifies it, **an alarm nobody can see is not an alarm.** The gate computed gap/outlier/unexplained-adjustment warns with nowhere to go and no Data-health surface; deciding the table before Phase 2 wires the gate avoids re-shaping the sink mid-build | active |
| **D78** | **A date-major (cross-sectional) bar read + `ix_bars_date`.** `IBarReadService` gains `GetCrossSection(date, watermark)` — every security's latest visible version on one date — served by a new `ix_bars_date` on `bars(date)`; the watermark/version resolution mirrors `GetSeries`. The consumer (the six-stage funnel, Phase 2; replay, Phase 4) is not built yet — this is additive-ahead-of-need. Pre-Phase-2 | The funnel and replay are **date-major** ("every name at date D at watermark W"), but `bars`' PK leads with `security_id` and the only secondary index is on `observed_at`, so a `WHERE date = ?` cross-section **full-scans** (~488k rows at sp100, ~5M at sp500, per date, per stage). Cheaper to decide the index + read shape now than to retrofit them after the funnel is built against the per-security contract | active |
| **D79** | **AI occupies exactly three seats, and the arena prices every seat (§23.1).** (a) **Researcher** (primary): reads locally stored verdicts, separation states, attribution, monitor statuses, regime episodes, and closed journal outcomes, and proposes the next pre-registered hypotheses and forks (D82). (b) **Contestant:** an LLM decision layer runs as a first-class `IModel` over a deterministic local pre-filter, on its own account, always paired with a mechanics-identical no-LLM twin (D81). (c) **Advisor** (deferred, opt-in): LLM allocation advice evaluated as a paired A/B against the D51 allocator, never wired to applied weights until it has priced positive. New golden rule 32: no AI output is an input to any component that judges AI outputs | The lab's stated goal is a self-improving decision system driven by AI over locally stored data. The seats put AI where current models are strong (evidence-based reasoning, idea generation and criticism, judgment over compact structured facts) while the machinery nobody should trust an AI to grade stays pure. The seats are separable - the researcher improves the lab even if the contestant prices at zero - and the twin construction makes "does AI add value?" the fastest honest alpha question the lab can answer (M.1 pairing) | active |
| **D80** | **The context-pack contract: every byte the AI sees is assembled locally, compressed, watermarked, and persisted (§23.2).** Packs are built only through the versioned read services / `IFeatureView` at (asOf, watermark), persisted to `ai_context_packs` with a SHA-256 hash and token estimate; **raw series never enter a prompt** - only locally derived features. Prompts are layered for cache stability (L0 static instructions, prompt-cached; L1 lesson set, cache-stable between policy versions; L2 the small daily fresh block). D24 budgets apply per seat; on exhaustion the contestant **abstains** (empty score map), never a padded or stale decision. **No vector store** (§14.2 reaffirmed): pack assembly is SQL over relational facts at a watermark; semantic recall over journal text, if ever wanted, lands as sqlite-vec in the same .db | The operator's requirement is that data stored in the DB is never re-bought as tokens - and it doesn't have to be, because the numeric spine already computes every feature locally, so the model needs a few KB of judgment-relevant derivatives, not history (~$0.01-0.03/trading day at Batches pricing, §23.2). The persisted hashed pack is the AI analog of NFR-1 (what did it see, exactly?), makes leakage testable, and makes the cache layering honest | active |
| **D81** | **Contestant rules: the persisted output is the decision; policy is frozen; evidence is forward-only; the twin is mandatory (§23.3).** One API call per (strategy, asOf); the response persists to `ai_decisions` before use and every re-run replays the row (determinism = f(inputs, watermark, seeds, stored AI outputs)). Prompt text, model id, pack recipe, and shortlist size are D17 frozen params - any change forks (rule 24 extended). Forward-only per D16: no replay seeding, no S1, nothing contributed to Phase-4 calibration. A contestant never enters without its mechanics-identical no-LLM twin; the paired difference is the headline number. **Memory: Option A (default)** - the lesson set is part of the frozen policy, updating only at forks; **Option B** (permitted, pre-registered) - a rolling memory updated by a frozen rule R over locally stored outcomes, state derivable at the watermark, judged whole via the twin | Persist-before-use makes a nondeterministic sampler compatible with F-DET and the audit trail; frozen policy stops prompt-tweaking becoming the new p-hacking; forward-only refuses the single most seductive leak in this design space structurally (today's model has read about past futures); the twin converts "the AI made 8%" into "the AI added 0.7% over identical mechanics, MDE ±2.4%" - the actual question. The A/B memory split keeps D17's spirit while giving "improves from locally stored past data" a legitimate, declared home | amended-by D125 |
| **D82** | **The researcher seat closes the generative loop, under a trials budget (§23.4).** FR-23's "hypotheses" action is resolved KEPT: `POST /api/v1/analysis/hypotheses` joins brief/skeptic (`jobs.kind` CHECK extended by migration); output is a **draft** `journal_entries` hypothesis - the AI proposes, **only the operator pre-registers** (rule 30 unchanged). Every proposal must cite parent evidence (an outcome id, finding, or attribution row; 422 without one). `Research.ForkBudgetPerYear` (default 6) + `Research.MaxConcurrentCandidates` (default 3); spend renders beside the deflated-Sharpe trials count. Measured by the two new §1.2 KPIs (researcher yield; allocator value-add) | The Phase-2 review found the self-improvement loop's generative step entirely manual and unspecified - everything downstream of a candidate existing was specced, nothing about how verdicts feed the next one. The parent-evidence rule makes it a loop; the budget reconciles "self-improvement by forking" with S2 (every trial spends everyone's significance); operator-as-registrar keeps a human veto on every experiment without making the human the generator | amended-by D126 |
| **D83** | **Factor returns graduate to a point-in-time signal input — for residual momentum (§6.5) only (amends D41).** The Ken French factor series may be joined as a signal input **availability-lagged** — a factor observation is usable at `asOf` only if its report/publication availability is ≤ `asOf` at the watermark — never as-published-backdated; the leakage suite extends through the residualization regression (the catalog §6.5 fixture). **Lag-hole rule (one convention, stated once):** the library refreshes monthly with weeks of publication lag, so the most recent weeks of a formation window routinely have no factor data; for those uncovered days the residual falls back to the **single-factor market residual** computed from the locally ingested market proxy (zero lag, always available), and the per-day factor source (FF3 vs MKT-fallback) is **deterministic at the watermark**. Omitting uncovered days (which biases the window toward stale months) and zero-filling are both **forbidden**. Attribution's use of the same series stays diagnostic-only exactly as D41 states — D83 changes only §6.5's signal path | §6.5 (`ResidualMomentumModel`, v1.9.22) regresses returns on the FF3 factors to score residual momentum — a signal use D41's "never a trading input" framing forbade; the contradiction had to be resolved, not papered over. Availability-lagging keeps the PIT discipline (rule 4) that made the lagged free source acceptable in the first place; the MKT-fallback keeps the formation window complete and deterministic instead of quietly biased toward stale months | active |
| **D84** | **Stage 5 sizes new opens against available CASH, not total equity — scale-to-fit (resolves finding 190).** New-open notionals are bounded so their total never exceeds the account's cash on hand (post corporate actions, post the day's T+1 fills); when the capped equal-weight book would spend more, every target is scaled DOWN proportionally so the total equals available cash, and on a near-zero-cash day nothing is opened (the correct sparse outcome). **No held position is ever sold to fund a new open** — cash frees up only as the `ExitPolicy` fires (rule 7). The `Sizing.PositionCapPct` clamp is unchanged and binds in addition. A **whole-book rebalance** (`ScheduledRebalance`/D68) is exempt: it re-weights the entire book against equity and self-funds (targets sum ≤ equity; held + cash = equity ⇒ cash stays ≥ 0). Money stays `decimal` (D69). **Bounded residual:** sizing decides at close `T` on `T`'s prices, so a single day's opens can still overshoot cash by the overnight gap + fill costs — a small, self-correcting excursion (next day available cash ≤ 0 ⇒ opens nothing until an exit frees cash) whose exact fill-time handling is finding 196 | Sizing new opens against equity let an already-invested account buy more than the cash it held — implicit leverage — and an `OpensOnly` book is never re-sized, so the overspend ratcheted across days (the RankBuffer case). Populations and the allocator size against equity too, so the leverage artifact would have contaminated the calibration null bands; §23.6 names this fix the AI-contestant prerequisite. Scale-to-fit is the minimal rule that keeps the account solvent without inventing a second exit authority (the alternatives are recorded in PROGRESS P12) | active |
| **D85** | **The no-LLM twin scores by a frozen equal-weight z-score blend of the contestant's pack features — fixing the Stage-2 scorer D81 left open (refines D81).** D81 fixed the twin as mechanics-identical with no LLM but never said how it produces a per-name score once the LLM call is removed; D85 fixes that as pre-registered contestant policy. **(1) Same information:** the twin scores using exactly the pack features the contestant is shown for each shortlisted name — never a signal the contestant did not receive. **(2) Equal-weight, standardized blend:** each pack feature is put on a common scale (cross-sectional z-score across the day's shortlist, at the watermark) then averaged with **equal weights** — no per-feature weights, no tuning, no free parameters; per-feature sign conventions (higher-is-better) are fixed in the recipe, declared once, never fitted. **(3) Deterministic and pure:** computed from watermark-visible pack values only — no API call, no randomness beyond the shared seed, replayable like any mechanical scorer (F-DET); it plugs into the same Stage-3→6 machinery, so **only the Stage-2 score differs between the pair**. **(4) Frozen policy:** the twin's scoring rule is a D17 frozen param in the contestant's `config_json`, pre-registered at fork time beside the prompt hash, model id, pack recipe, and shortlist size — any change forks a new candidate (rule 24/32) exactly as a prompt change does, which is what stops the twin from being rigged into a weak foil after results are seen. **(5) Recipe-coupled, identical form:** the *form* (equal-weight z-score blend) is identical for every contestant for comparability; the *feature set* it blends is whatever that contestant's pack recipe (`Ai.PackRecipeVersion`) contains, so a richer pack is measured against a correspondingly richer naive baseline (the fair comparison, since the LLM saw the richer pack too). **(6) Degenerate-day handling, ordered so two implementations cannot diverge:** first drop each zero-variance feature (identical across the shortlist ⇒ z-score undefined ⇒ contributes 0); then, if no nonzero-variance feature remains **or** the shortlist has fewer than 2 names, fall back to equal scores across the shortlist and set a `degenerate_blend` flag — one fallback covering the <2-name day, the all-features-flat multi-name day, and the empty-feature-set edge alike; a run never divides by zero or propagates NaN into selection. **Replay:** the twin is a `status='control'` paired to a replay-refused contestant and exists only to price the seat *forward*, so it is not replay-admissible on its own (consistent with `FX-ContestantReplayRefused`) | The contestant's entire job is to weigh the factsheet more intelligently than a naive combination of it, so an equal-weight blend of that same factsheet is the exactly-right foil: it isolates **weighting skill** while holding information constant. Pure equal-scoring across the shortlist measures against too weak a bar ("the AI beat scoring everything evenly" looks impressive but means little); a tuned or selected simple rule is the strongest-looking foil but reintroduces a post-hoc choice that can be rigged after seeing results (rejected on p-hacking grounds). Freezing the rule at fork time keeps D81's anti-p-hacking spirit intact; identical form keeps "does the AI add value?" comparable from one experiment to the next while recipe-coupling the feature set keeps each individual A/B fair. Compatible with D81's memory split (no twin memory in Option A; in Option B the twin mirrors the same frozen rule R minus the LLM) | active |
| **D86** | **A frozen (unmapped-halt) position is valued at average cost basis, not last print (amends §13.6).** For the unmapped-event / bar-stoppage freeze, the equity curve marks the held name at its average cost basis — neither gain nor loss recognised — because a frozen name has no reliable current price and a stale last print can misstate it in the one direction the honesty rails must never allow silently. **v1.9.43 (finding 275) refinement:** cost basis is the mark for a genuinely `Frozen` position ONLY. The original `MarkToMarket` applied it to ANY missing bar, conflating a frozen halt with a plain **vendor data gap** (a session the name actually traded but the feed lacks — e.g. OEF 2014-04-22): a non-frozen data-gap holding now **carries its last known raw close forward** (`BasisMath.MarkOne` + `BarFeatureView.LastRawCloseOnOrBefore`), never jumping to a years-old cost basis and fabricating a one-day equity round-trip. **Terminal delisting is unaffected** — it has a real final print and correctly force-exits at last print (haircut configurable). | §13.6 said "freeze valuation at last print," which the ledger never implemented; the read-model'd equity_curve (Phase 3) consumes this valuation, so the doc-vs-code silent disagreement had to be closed before the first honest screens are built. Cost-basis is the conservative, non-fabricating mark for a position the system has itself flagged as unpriceable — but a data gap is not such a flag, so it must not inherit that mark (finding 275) | amended-by D119 |
| **D87** | **TOMBSTONED (D115, v1.9.70): SUPERSEDED by D109 (v1.9.55) — breadth arrives as SEPARATE ARENAS, never an in-place widen of an arena holding a live experiment.** Original text: git history + the CHANGELOG provenance trace (v1.9.30 opened it; v1.9.49 recorded the sign-off statements). Live content rescued to: the Quality-screen prohibition → **D111**; the breadth decision itself → **D109**; the Grinold-overclaim + compute-scale caveats → CHANGELOG v1.9.23. Verified none remains (v1.9.70) | See D109 and D111 | superseded-by D109 |
| **(D87 note — VOID)** | **VOID AS WRITTEN (D109).** Items 1-9 below presuppose an IN-PLACE widen, which D109 forbids for an arena already holding a live experiment; the per-arena equivalents are the D71 registration steps in ARENA_ARCHITECTURE §6, run at each new arena's OWN Phase-4 sign-off. The list is retained as the historical record of what was recorded, NOT as work to do. Original text follows: **S&P 1500 widening prerequisites — gated at Phase-4 sign-off (D87); recorded, NOT built:** (1) **VERIFY** a historical S&P 400/600 as-of-membership source of sufficient depth (full replay window) + accuracy — the **go/no-go gate**; if unmet, the target reverts to the S&P 500 (rule 22). (2) The widening **mechanism** (finding 151, target-agnostic): register `UniverseOptions` in DI, drive the membership provider + count band from config (not the hardcoded `Oef()`+`wikipedia_sp100`), and replace the `--universe sp500` parse-reject with real sp500/sp1500 arms. (3) New live feeds: `ISharesHoldingsOptions.Ijh()`/`.Ijr()` presets (portfolioIds, VERIFIED dates, C-4 header fixtures), independent 400/600 cross-checks, and the historical 400/600 source + parser. (4) An sp1500 count band (~[1490,1520], config-bound). (5) A cap-weight benchmark proxy for sp1500 (a source→symbol mapping in `CapWeightProxy.SymbolFor` + operator ingestion, reusing `BackfillCapWeightProxyStep`). (6) Throughput: honor `X-RateLimit-*` (the 1,000/min limit binds at ~4,500 calls) and wire `eod-bulk-last-day` (cost-100). (7) Cost-model validation: D43's "other: 5bp" bucket against real S&P 600 ADV, and fix the Stage-1 `liquid` no-op (P1) before the thin small-cap tail enters. (8) Re-derive the "full daily run < 60s" DoD at 1500 names (LW covariance + populations sized for ~100). (9) The Quality-strategy prohibition above. (10) The arena rename `sp500`→`sp1500` — a **separate future pass, only after (1) verifies**. | Recording the checklist keeps D70's "decide at Phase-4 sign-off, never silently at the flip" honest: the flip is not a config toggle today (finding 151), and its feasibility rests on (1), which no current feed satisfies. |
| **D88** | **The cohort maturation curve: a lab-level KPI plotting each admission cohort's median matched-population percentile against track length; descriptive only (new §1.2 KPI, §22 read-model FR-39, panel UX-15).** Promotable strategies group into cohorts by admission vintage (`strategies.created_on` bucketed by `Kpi.CohortBucketMonths`, default 6; optional secondary grouping by fork generation via `parent_strategy_id`, same math). For each cohort the curve plots the cohort's MEDIAN D36 population percentile (the same percentile the S3 panel computes against the matched random populations, reused verbatim - never a second percentile) against track length t in trading days, each strategy aligned to its own age, never wall-clock, so cohorts are compared at equal maturity. Rising later-cohort curves mean the researcher loop is genuinely improving what it proposes; flat or declining means recombination without gain. Six rails, resolved into the `CohortMaturationReadModel` (§22, FR-39), never left to a client: (1) age-aligned x-axis, never wall-clock; (2) NO survivorship - benched/retired strategies (`status='retired'`) remain in their cohort and keep contributing their percentile path for the record, exactly like the benched-keeps-trading principle; (3) a segment with fewer than `Kpi.CohortMinStrategies` (default 3) live members at t renders dimmed with reason `thin_cohort`, and a cohort-to-cohort median gap inside its own NW-MDE renders dimmed too (the D58 `{value, display, reason}` shape) - never as a claimed improvement; (4) replay cohorts (`run_kind='replay'`, e.g. the D64 plant cohorts) carry `quarantined:true` and are NEVER co-plotted with forward cohorts (D37, rule 26); (5) arena-scoped (D71) - cohorts never merge across arenas; (6) descriptive only - NEVER an input to the promotion gate, the overfitting monitor, or the allocator. Rendered on the Analysis/Journal research surface (UX-15, PlannedBadge until Phase 3); storage-neutral (SCHEMA enforcement note; one additive covering index recorded, migration with the Phase-3 read-model build) | §1.2 grades the researcher loop on yield but could not answer "is the loop actually learning what to test, or just recombining without gaining?"; comparing admission cohorts at equal age against the matched-random null answers that without ever crowning a winner. Two rails are load-bearing: **no survivorship** - a cohort curve that silently dropped its dead members would flatter the loop, which is exactly the self-deception the lab exists to catch; and **descriptive only** - the KPI observes the loop, it does not steer it, and no future phase may wire it into a gate, monitor, or allocator verdict (steering on its own diagnostic would tune the loop against the monitor, the rule-8 hazard at lab level). Reusing the S3 percentile verbatim inherits the D36 matched-random null for free and forbids a second, divergent percentile definition | active |
| **D89** | **The detectability-at-admission gate: before a candidate enters the arena, CandidateFactory refuses it if its pre-registered expected effect, if real, could not clear the NW-MDE within `Gate.DetectabilityHorizonYears` given the trials-budget cost it adds (promotes the parked v1.9.8 C-1 proposal; §20.3, FR-40).** The gate reads the candidate's pre-registered `expected_effect_ann` (a new fourth field on the D52 hypothesis, beside claim + metric + evidence window) and compares it against the NW-corrected MDE floor implied by `Gate.DetectabilityHorizonYears` (default **10** since D121; 3 as originally issued) and the incremental trials-budget cost the candidate adds, using the C-1 detection-power curves as the empirical calibration. Those curves (empirical P(promoted by t given alpha) plus median days-to-promotion, swept across ~3 edge-plant alpha levels on the same seeds/plant and archived in `docs/calibration/`) are actioned at Phase 4 by this decision. A refused admission is a new create-path outcome in the same set as the 422 (no hypothesis) and 409 (run in progress) results specified in §20.3; the researcher loop (§23.4) is subject to it. An `unregistered` candidate carries no hypothesis and so no `expected_effect_ann`, and is admitted under its permanent unregistered marking rather than the gate. The gate acts at admission only and never re-tunes or re-gates a live strategy (rule 8). AMENDED TWICE: D116 added the plausibility ceiling (the upper bound this row lacked); D121 set the horizon to 10 years | Every underpowered admission spends scarce trials budget on a candidate the lab can never certify within any reasonable patience, inflating the multiple-testing penalty (S2) for names it already knows it cannot separate from random; refusing at admission is the natural companion to the TooEarly verdict the gate would otherwise reach years later. Calibrating the floor against the C-1 curves keeps the refusal empirical rather than an analytic guess, and reusing the pre-registered `expected_effect_ann` keeps the operator's own stated claim, not an implementer's number, as the thing being tested for power | amended-by D121 |
| **D90** | **The end-of-day book is persisted as-of: `position_snapshots` records every account's held book at each session's close, inside the same Stage-2 transaction that writes `equity_curve` (SCHEMA `position_snapshots`; §13.5; FR-25).** One row per (account, session, security, run_kind), mirroring `positions` plus the snapshot's own keys — including `frozen`/`frozen_reason`, which the funnel renders verbatim into `decisions.stage_json`. Written once per day and never revised (the `equity_curve` idempotency contract: a re-run of a recovered day rewrites that day's rows rather than duplicating them). It is what the `reproduce-day` command seeds a re-run from: the book as the PREVIOUS session closed | NFR-1 promises any historical run is reproducible forever (§13.5), and every other input already honours it — bars (D40), corporate actions (D76) and context packs (D80) are versioned append-only and read at a watermark. `positions` was the one exception: it is current STATE, and corporate actions rewrite it in place (a split's share count, a merger's conversion, a spin-off's new line) through `UpsertPosition` with no reversible trade row. So the book at the start of a past session was not recoverable from `trades` — and without it, the reproducibility claim held for the data the lab read but not for the ledger it acted on, which is precisely where the corporate-action, D84 cash and D86 valuation bugs live. Recording the book closes that gap with the same as-of discipline the rest of the store already has, at one small row per holding per day | active |
| **D91** | **The Signal Library is a dedicated Phase 4.5: every registered cross-sectional signal is graded daily by Spearman rank-IC, and the grade record is descriptive only (new §24; FR-43..46).** *(Trend inference amended by D108, v1.9.52; horizon 126 closed by finding 290, v1.9.51.)* For signal S on day t: score every stock in the Stage-1 eligible pool as-of t (membership is state, D20) from data observable at t only; the grade is the Spearman rank correlation between that ranking and realized t to t+k total returns on the adjusted series the control populations compound on; one `signal_ic` row per signal per day per horizon (k = 21 and 63 trading days; **126 is CLOSED — rejected for v1**, v1.9.51, finding 290). Trend inference: rolling 1y/5y mean rank-IC (`SignalLibrary.RollingWindowsYears`) with Newey-West standard errors, lag = horizon; both windows are reported and the pre-registered trend flag (stable / decaying / gone, or `insufficient` below the `n_eff` floor) is computed on the **5-year** window for both horizons, never hand-read — the pinned constants are the per-arm significance levels α (`SignalLibrary.TrendDecayAlpha` / `TrendGoneAlpha`, versioned config rows), written at build checkpoint 4.5.2 before the first grade row [D108]. The v1 set is seven pre-registered scorers from the documented catalog families (mom:L252s21, mom:L126, rev:L21, lowvol:L252, brk:L252, resmom:L252, bab:L252); TSMOM is excluded (a time-series rule; rank-IC is the wrong grade). Storage is two tables (`signals`, `signal_ic`); no cross-section is persisted (deterministic recompute from versioned bars, the replay philosophy); the Worker remains the sole writer (D59); the backfill runs after D70. Boundary: never an input to the allocator (D51), any gate, sizing, or eligibility; the rule-32 direction is preserved (math flows into the AI seat; nothing here consumes AI output); registry rows are frozen instruments; no parameter sweeps to maximize IC (a sweep is candidate selection and belongs to the arena, where it pays the trials tax). Consumption: Phase 5 wires one digest line per signal (1y rank-IC, 5y rank-IC, trend flag) through the swappable/disableable/placebo-able evidence-prior seam; Phase 6 IModels wrap the same ISignal implementations, parity pinned by `FX-SignalParity`; Phase 8 fundamental signals register into the same harness with `report_available_date`-keyed observability. If it ever acts, that is post-Phase-8 Learning Researcher scope with its own guards **AMENDED TWICE: D108 (the trend-flag inference); D127 (the pre-filter dispersion exception — the ONE named crossing of §24.5’s descriptive-only boundary).** | A strategy yields one portfolio number per day; a signal's ranking yields hundreds of cross-sectional observations per day, so predictive decay shows in the grade series years before an arena verdict can (overlapping, correlated observations; the NW trend inference accounts for that). Grade trend vs arena trend separates "edge gone" from "implementation drag", which the arena alone cannot do. The trials tax gets aimed at families whose rules still predict (post-publication decay: McLean and Pontiff 2016). Grades need only the scoring formula plus bars, so one backfill yields two decades of history on day one. Fractional numbering (Phase 3.5 precedent) avoids renumbering; the slot lands after D70 sets the library's breadth and before Phase 5 needs the seam's first occupant | amended-by D127 |
| **D92** | **Catch-up records the TRUE observation instant (v1.9.39, Phase-4 build; resolves finding 194 / proposal P10).** `DailyPipeline.RunDayAsync` gains an explicit `watermark` parameter (the replay/reproduce seam); a 'catchup' day stamps `NowIso()` captured once at entry — carried by `runs.watermark` AND every ingested bar/corporate-action `observed_at` — never the session-derived `{as_of}T22:00:00Z` fiction. A 'live' day keeps the session-derived stamp: the run happens the same evening, so it is a bounded approximation of the truth, and it is what keeps a same-day re-fetch a value-diff no-op. `reproduce-day` passes the STORED watermark explicitly (no re-derivation can reconstruct an honest recovery instant). Test `FX-CatchupObservedAt` | Replay reasons over `observed_at` (D40/D95); a fabricated PIT stamp on recovered days would tell it a late recovery was seen the session evening — the exact class of fiction the point-in-time discipline exists to forbid. Honest stamps and NFR-1 coexist because reproduction pins the recorded watermark | active |
| **D93** | **Regime rows are run-kind-scoped (v1.9.39, Phase-4 build; resolves proposal P6).** `regime_labels` PK becomes `(as_of, run_kind)` and `regime_episodes` gains `run_kind` (+ the `(run_kind, start_date)` index); `RegimeLabelService.ComputeAndSave` takes the run kind and each kind maintains its own label rows and episode chain (M5) | A replay recomputes the label from a different watermark over its own window; with `as_of` alone in the key that recompute would OVERWRITE the forward label — a replay write reaching a forward row. FR-41's `replay_regime_outcomes` FKs `regime_episodes`, so replay MUST persist episodes for its window; per-kind chains are the only shape that satisfies both the FK and the quarantine. The `equity_curve` run-kind-in-the-PK precedent, applied | active |
| **D94** | **`corporate_actions.processed_on` is DROPPED (v1.9.39, Phase-4 build; resolves proposal P5).** M5 removes the always-NULL column behind a fail-loud precondition (a CHECK-violation guard aborts the migration if any non-NULL row exists — nothing is dropped on a store that would lose data), via a plain `ALTER TABLE … DROP COLUMN` rather than the EF rebuild (whose regenerated DDL re-adds the AUTOINCREMENT the rule-14 hand-edit stripped) | The column was conceived as a per-action "applied" flag but is a GLOBAL column on a PER-ACCOUNT operation, and stamping it would make a replay skip an action a forward run had marked. Ledger idempotency comes from one-transaction-per-day + `ux_runs_ok_forward` (+ the finding-192 `(previousSession, asOf]` window); the deferral's only rationale was migration cost, which Phase 4's M5 pays anyway | active |
| **D95** | **The replay watermark contract: a FROZEN REAL version watermark + a simulated-date ceiling; one replay generation per arena.** Every replay run row carries `W_replay` = MAX(`observed_at`) over bars ∪ corporate_actions at replay start (backfilled history has only one observed version, stamped at backfill wall-clock — an emulated historical watermark would return nothing, and fabricating one would be the finding-194 fiction reborn). "What was knowable" is emulated on the DATE axis: bars/membership are date-bounded by their callers, and corporate-action reads pass the `DateCeilingCorporateActionReads` decorator (effective/ex date ≤ the simulated day), so a future-effective action is invisible to an earlier simulated day. Committed replay rows at a DIFFERENT watermark refuse a new run (mixed vintages would poison the D56 curves); `--reset` deletes the generation from replay-scoped tables only (never bars/CA/config); same-watermark committed days are skipped (crash-resumable). Run-row timestamps and heartbeats use the REAL clock: no replay output derives from the clock, and a simulated-time heartbeat would read stale to a concurrent launch's liveness check — whose recovery would start the sole writer against a mid-write replay (FR-34's hazard) | Determinism stays f(inputs, watermark, seeds): a re-run pinned to the recorded `W_replay` sees nothing observed later. The deliberate trade-off — replay does not emulate REVISION history (it reads the backfill vintage) and is slightly LESS informed than a true historical observer (a declared-but-not-yet-effective action is invisible) — is the conservative direction, stamped into the curves' vintage | active |
| **D96** | **Versioned config resolves AS-OF the run's watermark for run-scoped reads (v1.9.39, Phase-4 build; resolves proposal P14a — landed BEFORE the first calibration config write).** `ConfigReadService.ResolveAsOf(key, watermark)` = MAX(version) among rows with `changed_on` ≤ the watermark; `DailyPipeline`'s config reads, the stored providers, the regime service, and the monitor's calibrated-row reads all use it. `ResolveCurrent` (plain MAX(version)) remains for operational reads outside any run's provenance | Without it, the first calibration freeze would silently change what every earlier committed day "read" on a re-run — the exact gap P14a recorded. Forward behaviour is unchanged (a forward run's watermark is at/after every row it could see); reproduce-day and replay become config-faithful. Proven by the config cousin of the perturbation-diverges test: a post-asOf config insert leaves the committed day byte-identical | active |
| **D97** | **The D70 historical backfill gates every staged series IN-MEMORY before commit (v1.9.39, Phase-4 build; the scoped resolution of proposal P7).** A Reject-severity flag excludes the name from ingestion entirely (fail closed per name); warnings and exclusions land in a DURABLE, DETERMINISTIC coverage artifact (canonical JSON, no run-mechanics counters — an idempotent re-run is byte-identical, so replay never depends on which attempt ingested the data) plus the versioned `Backfill.HistoricalGateSweep` marker row. Ticker-reuse suspects (the same canonical symbol with disjoint membership spells > 2 years apart) are flagged and EXCLUDED until the operator resolves the identity — EODHD resolves a symbol to ONE history, and ingesting would attribute the wrong company's bars. The persisted-flags sweep of the forward slice stays open under P7 | Replay honesty requires the replay universe to have passed the FR-6 gate; gating at ingestion time — when the data is in hand — satisfies that without a `bars.gated_through` migration or a fabricated run row for `data_quality_flags` | active |
| **D98** | **Calibrated monitor values are versioned config rows with a defined shape (v1.9.39, Phase-4 build).** Keys: `Monitor.S3.PNoiseCurve.{family}` / `Monitor.S3.PEdgeCurve.{family}` (value_json = piecewise-linear knots on the evaluation-cadence grid — linear between, FLAT beyond both ends — plus the 25–75% seed band, sustain_evals, the false-alarm rate, the C-2 sampling band, and the D64 vintage block), `Monitor.S6.AutoRetireEvals` (the finding-113 patience knob, an integer row from the first freeze), `Calibration.DetectionPower` (the C-1 curves: per-α knots of P(promoted by t) + median sessions-to-promotion), `Calibration.ReportRef` ({path, sha256} — the DB↔report cross-reference). The freeze is ONE transaction of append-only version+1 INSERTs (finding 108's composite PK is what makes it implementable); `--report-only` writes the report and no rows | The monitor reads the curves as-of the run watermark (D96) with the flat anchors as the pre-calibration fallback (both-rows-or-neither), so freezing can never retro-change a committed day and a half-frozen pair can never judge a curve against a flat opposite | active |
| **D99** | **The FR-40 refusal is `422 detectability_refused` with a Bonferroni trials haircut and the C-1 empirical floor (v1.9.39, Phase-4 build; actions D89's create-path outcome).** Refuse iff `expected_effect_ann` (an annualized FRACTION, the fourth pre-declared hypothesis field — REQUIRED on new hypotheses at the API) < max(analytic, empirical): analytic = `(z_{1−α/(2N′)} + z_power)·σ_LR·252/√H` with N′ = the forward trials count + 1 (the candidate is charged for the deflation its own registration causes) and σ_LR = the median of recent forward `power_reports` sigmas, else the replay generation's (the calibration-vintage estimate); empirical = α*(H), the smallest swept plant-α whose frozen C-1 curve reaches P(promoted by H) ≥ `Gate.Power`, interpolated between levels — +∞ when no level reaches it (nothing is detectable at that horizon; admitting on hope would be fail-open). Degraded modes are distinct and honest: no C-1 row ⇒ analytic-only; no σ anywhere ⇒ UNASSESSED admits (a pre-calibration lab has no honest number to refuse against). The refusal carries structured details (expected/floor/analytic/empirical/horizon/N′/σ-source) in the D60 envelope and persists NOTHING (no strategy, no trials row, no orphaned hypothesis) | The gate exists so the trials budget is never spent on a claim the arena cannot adjudicate (§20.3); Bonferroni is the simplest defensible reading of "net of the incremental trials-budget cost it adds", and the C-1 curves are the machinery's own measured detection power — the empirical calibration D89 mandated | active |
| **D100** | **Two-pass calibration: during a calibration replay the monitor STOPS ACTING on its own (uncalibrated) verdicts but keeps RECORDING them (v1.9.42, findings 270–272; §20.9).** A D64 plant under `run_kind='replay'` is never auto-retired — it stays promotable and emits S3 rows for the full window (so the D56 curves are built from full-length trajectories, not survivors) — but the would-be retire is written to `go_live_log` as a `WouldRevert` row carrying its triggering signal. Verification metrics are ADDED, never narrowed: the retained finding-113 survival reads the would-be-retire log (`would_be_edge_survival`), and two curve-based OUT-OF-SAMPLE metrics (`noedge_curve_breach_validate` / `curve_based_edge_survival` — per-plant sustained breach of the built `P_noise` on the held-out validate segment) are added under their OWN `Replay.*` keys (`NoEdgeCurveBreachMaxFrac` / `CurveBasedEdgeSurvivalFloor`). The flat-anchor fallback is brought into D63 conformance (flat-S3 sub-25th requires SUSTAIN; S6 inside-band caps at Warning — `Monitor.FlatAnchorSustainEvals`, OVERFITTING_MONITOR §3) | The curves don't exist yet mid-calibration, so the flat-anchor verdicts are uncalibrated BY CONSTRUCTION; acting on them retired the plants and (a) truncated the trajectories the curves are built from and (b) hard-failed the KPIs read from that status. The fix stops the *acting*, not the recording. Comparability caveat: because the flagging itself changed, the post-fix joint-false-alarm is not comparable to the pre-fix 1.00 and is not independent validation — only the curve-based metric validates the curves | active |
| **D101** | **Per-cadence plant strength ladder with a rule-selected primary (v1.9.42, B3, finding 273; §20.9).** Edge strength is per-cadence, not one global `AlphaAnnualPct`: the MONTHLY family carries `Calibration.Plant.MonthlyEdgeLadderPct` (default geometric 2/4/8/16 — the 16% rung is an explicit *detection-sanity* level), which is BOTH the promotable cohort AND the C-1 detection-power sweep (the per-rung promotion is the primary finding). `PrimaryEdgeIds` is rule-selected — the smallest rung clearing that cadence's pre-registered offline `cost_drag+MDE` floor (`DailyMdeFloorPct`/`MonthlyMdeFloorPct`) ⇒ the monthly 16% rung. Daily keeps a 2% edge plant as a SURVIVAL case (in `FloorEdgeIds`, out of `PrimaryEdgeIds`) | The 2% daily primary could never promote (0 promotions on the aborted run): a daily random-redraw book carries ~21.9%/yr cost drag, dwarfing any plausible overlay — NOT a bad-bar artifact. A stated rule, not a hand-picked plant, keeps the primary honest (the anti-threshold-fitting principle); the daily finding is bound to the *plant*, and finding-115 turnover matching means it does not transfer to daily-cadence *strategy* families | active |
| **D102** | **TOMBSTONED (D115, v1.9.70): SUPERSEDED by D107 (v1.9.50) — the reported-only rule became the CATEGORY of checks asserting properties of the pass-1 flat-anchor monitor.** Original text: git history + the CHANGELOG provenance trace (v1.9.46 adoption; v1.9.50 supersession). Live content rescued to: **D107** in full — the 0.10 threshold, the per-signal contribution table and the archival obligation are all unchanged there. Verified none remains (v1.9.70) | See D107 | superseded-by D107 |
| **D103** | **`noedge_curve_breach_validate` is a RATE PER EVALUATION OPPORTUNITY (v1.9.82; taken at its own trigger; finding 361).** The reserved row said the form is chosen THEN, on the rule text - and trigger (b) fired exactly as written: generation 2 measured **6 of 50** no-edge plants sustain-breaching (**12 %**) against a recorded trigger of *"at or above 8 %, or 4 or more of 50"*. **THE FORM: the bound counts sustained-breach OPPORTUNITIES, not plants.** A validate path of n evaluation points offers `n - SustainEvals + 1` start positions for a sustained run; a breach is a start position whose whole run sits on the far side of P_noise; the statistic is breaches divided by opportunities, pooled across plants. Overlapping windows count, deliberately - they are the chances the same rule had to fire, and de-duplicating them would re-introduce a dependence on where a streak happened to begin. **WHY THIS FORM AND NOT ANOTHER:** `CONFIG_REFERENCE` declares `NoEdgeCurveBreachMaxFrac` as *"the curves' own out-of-sample false-alarm RATE"* and `CurveBuilder` builds P_noise AS the `falseAlarmRate` quantile, so the declared quantity is per-point by construction; option (i) is the only candidate that RESTORES the documented contract rather than amending it. The per-plant EVER predicate it replaces was a LIFETIME probability - monotone non-decreasing in validate-window length against a FIXED bound - so identical behaviour observed for longer fails by construction, which is why generation 1 passed at 3/50 and generation 2 failed at 6/50 on a window of the same length. **SCOPE IS ONE METRIC, NOT BOTH.** `curve_based_edge_survival` KEEPS the per-plant form: its key is declared as *"the fraction of floor-edge plants that do NOT sustain-breach"*, with no rate gloss, so there declaration and implementation already agree and a change would be an AMENDMENT requiring its own justification and its own `CONFIG_REFERENCE` edit. **THE CORROBORATION - the reason to believe the repair and not merely the pass:** the repaired metric reads **2.6 %** (91/3550 opportunities) against the independently-implemented point-level `noedge_pnoise_breach_validate` at **2.9 %** (104/3600) on the same paths - two separately-built instruments agreeing to 0.3 points, where before the repair they read 12 % and 2.9 % | Amendment C1 governs and the hazard was pre-registered in the framing itself: the fix is `ReplayVerification`-only, re-derives from persisted `overfitting_checks.value`, invalidates no session and costs ONE RESUME OF MINUTES - so it both unblocks a freeze and is cheap, which is exactly the shape of change that must be justified on the rule text alone. The defence is that the rule text was read and the option set collapsed in v1.9.46 **while this check was PASSING at 3/50**, with Q3 naming the three-orders-of-magnitude cost gradient specifically so it could not decide; the register row then deferred only the TAKING. Recorded under its reserved number rather than a new one because D103 was allocated for precisely this, and because the substantive content is WHICH QUANTITY THE BOUND NAMES - a threshold whose implementation measures something other than what its contract declares is not a calibrated threshold, it is a number that happens to be compared against something | active |
| **D104** | **AI decision transparency: the record must be complete enough that re-execution is never required (v1.9.47; §23.8; Phase-5 requirement, recorded before the code exists).** Every AI decision persists **four** artifacts, not one: (a) the **exact context pack as stored bytes**, never a summary or a regenerable recipe; (b) the **raw model output**, not only the parsed result; (c) the **parsed decision AND what the funnel actually did with it**; (d) the **model string, prompt version, and sampling parameters**. Each catches a distinct failure the others cannot see: (a) wrong input, (b) misparse, (c) misapplication, (d) behaviour change after a model swap. Two invariants ship with the seat, not after it: the **leakage invariant** — no field in a context pack may carry an `observed_at` later than the simulated as-of, and a pack may contain **only** fields drawn from a permitted whitelist (`FX-PackNoLeak`, closure-tested, distinct from `FX-PackWatermark`'s byte-identity assertion); and the **twin divergence index** — every contestant-vs-twin divergence resolves to **exactly one** `ai_decisions` row with an exact counterfactual, and that index is the intended debugging entry point (`FX-TwinDivergenceIndex`), never a bulk log search. **Two cautions, recorded as binding:** the model's stated reasoning is **more output, not a causal account** — log it when it arrives for free, never build a debugging path that depends on it; and the **rule-32 corollary** — these artifacts are read by humans and by **nothing that judges AI output** | Every other component in this system earns reproducibility by **re-running**: the same inputs at the same watermark with the same seeds give the same answer, and NFR-1 makes that executable (§13.5). **An LLM cannot offer that** — it will not reproduce byte-identically, and no amount of temperature pinning makes it a guarantee. So the AI seat cannot borrow the reproducibility mechanism the rest of the lab uses, and the only remaining route to auditability is that **the record itself is complete**, i.e. sufficient to answer every question anyone will later ask without calling the model again. Everything above derives from that one substitution. Recorded now, while `src/AlphaLab.Llm` is an empty placeholder, deliberately: a specification that **predates** its implementation is what makes the two checkable against each other — the same property that made finding 285 findable at all, since D26 had stated "never a raw return gap" years before the code that did | active |
| **D105** | **`reproduce-day` replays the LOGGED AI decision; it never re-calls the model (v1.9.47; amends §13.5 / FR-25; Phase-5 requirement).** The FR-25 reproduce-day path asserts **byte-identical** decisions, fills, equity points and population draws against what was committed (`FX-ReproduceDay`). For any session containing an AI decision that assertion is satisfiable **only** by replaying the persisted `ai_decisions` row, so reproduce-day resolves AI decisions from the store exactly as the funnel does (§23.3 rule 1) and makes **zero** model API calls — the same provider-seam proof `FX-AiDecisionIsTheRow` already applies to a re-run, now extended to the reproduce-day path (`FX-ReproduceDay-AiSession`). NFR-1's determinism statement for AI-seated strategies is correspondingly **f(inputs, watermark, seeds, stored AI outputs)** | Without this, byte-identical reproduction is **impossible by construction** for any session in which an AI seat traded — the model would return something different and the assertion would fail on a correctly-committed day, turning the lab's strongest reproducibility claim into a test that cannot pass. Settling it now costs one sentence; discovering it in Phase 5 costs a redesign of either the seat or the NFR-1 proof. This is the same class of pre-emption as D104 and is recorded in the same pass | active |
| **D106** | **Monitor-rule and metric changes are scored by recomputing from stored rows rather than re-simulating (v1.9.48; §25; adopted work, not a proposal).** The raw inputs the monitor and gate consume are already persisted per strategy per evaluation, so a verdict at a different threshold is a pure function over stored rows with no simulation — verified, not assumed (§25.1). The harness **covers anything downstream of the simulation** (signal thresholds, sustain counts, the alpha definition, gate rules, verification metrics) and **covers nothing that changes the simulation itself** (cost model, sizing, eligibility, plant construction), because those change the trades every later number is computed from. Within the covered side there are two tiers: **direct-read** (thresholds, sustain counts — `overfitting_checks.value` + `contribution` alone) and **derived-input** (a band-definition change — member window alphas re-derived from `control_equity`); the harness classifies a candidate change by which PARAMETERS its rule specification touches, and refuses or escalates on any specification it cannot classify. Config resolves **as-of** the recomputed session via `ConfigReadService.ResolveAsOf`, never current config. Before it is trusted, it must reproduce generation 1's `overfitting_status`, `go_live_log` Promoted **and** `go_live_log` WouldRevert rows EXACTLY under the CURRENT rules, deterministically on re-run (`FX-RecomputeParity`); on failure the harness is not used for its purpose, generation 2 stands, and the equality is never relaxed | Every monitor-rule change today costs a **multi-day replay** to evaluate, which is why findings 280 and 285 are bundled into one generation-2 run rather than scored independently. The justification is the **rate**, not this one saving: findings 280–283 are a single defect class and more instances are plausible, so the harness turns a recurring multi-day evaluation into minutes. It is **not free** — new code with its own bug surface, and a buggy harness gives wrong answers fast, which is worse than slow correct ones; the acceptance test exists to make that failure loud rather than silent. It rests on one condition that is part of the decision: the allocator computes weights but **moves no money**, so no verdict feeds back into a later session. If the allocator ever moves capital, the no-feedback premise dies and the harness dies with it | amended-by D117 |
| **D107** | **Checks ASSERTING a property of the pass-1 flat-anchor monitor are reported-only, never freeze-gating (v1.9.50; supersedes D102, which never reached code).** Membership is by what a check **asserts**, never what data it **reads** — the same discriminator that keeps `FX-PackNoLeak` distinct from `FX-PackWatermark`. Three members: `joint_false_alarm`, `would_be_edge_survival_5y`, and `anti_detection_speed` — the third **passed** at full scale and is in the category anyway, which is what shows it was drawn on principle rather than around the failures. Two verdict-*touching* checks stay gating for stated reasons: `edge_retires_logged` (it asserts **log integrity** — every would-be retire carries its triggering signal, a property that holds regardless of calibration state, so a Fail is a recording regression) and `allocator_value_add` (its subject is the **allocator**; verdicts were an input, not the claim — and its reading is a LOWER BOUND, measured while the monitor flagged 43/50 no-edge plants). The mechanism is a per-check **decision citation** (`VerificationCheck.ReportedOnlyPer`) consumed by `NoFailures`/`AllGreen`, rendered in the archived report's outcome cell, with a test pinning the **exact** reported-only set | The Phase-A gate-wiring audit established that **no per-check exemption existed** — `NoFailures` treated all twelve checks identically — so D102 was not implementable as a flag flip, and implementing it alone would not have unblocked the freeze (a second Fail, `would_be_edge_survival_5y`, sat outside it). Justified by the pre-registered amendment-C1 caveat plus the fact that each failing member has a **calibrated analogue that PASSED** (`curve_based_edge_survival` 98/100; `noedge_curve_breach_validate` 6 %). This is the **third check to leave the gate**, stated rather than buried: a CATEGORY leaving is defensible where three individual removals would not be, because the category carries a written membership test, a passing member, and a guard — three case-by-case removals would be the finding-279/281 pattern (discovering members one failure at a time) relocated into the gating set itself | active |
| **D108** | **The Signal-Library trend flag is inferred on a uniform 5-year window for BOTH horizons, against a t reference whose df derive from the effective independent sample; the pinned constant is the significance level α per arm (v1.9.52; amends §24.2; closes finding 290's open item).** Three defects are resolved together, of which finding 290 named only the first. At the previous 1-year window with k = 63: (1) the effective independent sample is **~4** observations; (2) a **normal** reference is wrong at that size — t(df≈3) = 3.182 against z = 1.96, so a nominal 5 % test has an actual size near **15 %**; and (3) — the one a reference change alone cannot fix — **NW lag = horizon puts `lag/T` at 63/252 = 0.25**, far outside the range where the Newey–West estimator is reliable (the standard bandwidth at T = 252 is ~4–5 lags), so a corrected critical value would sit on an unreliable standard error. At 5 years all three clear: `n_eff ≈ 20`, t(df≈19) = 2.093, `lag/T = 0.05`. **Consequences that are part of the decision:** the pinned constant becomes **α**, since the critical value depends on df and therefore cannot be authored in advance (the `SignalLibrary.Trend*Z` keys are renamed `*Alpha` accordingly, and become **versioned config rows** rather than appsettings values so `ResolveAsOf` can reach them — finding 292); and **effective n is promoted from a printed caveat to a load-bearing INPUT**, with its own derivation fixture and per-arm df (`n_eff − 1` level, `n_eff − 2` trend) | **The decisive reason is that the trend arm fits a slope, so it needs more data than the level arm — and the window must satisfy the harder arm.** Responsiveness is not the cost it appears: the trend arm fires *within* the window, so window length does not set detection lag the way it would for a level-only test (a downward slope inside a 5-year window is detectable while the 5-year mean is still positive). Rejected on the record: keeping 1 year with a t reference (defect 3 survives); per-horizon windows (statistically tailored, but it makes the two flags claims about different spans of history); and dropping the significance claim altogether, which is self-defeating — it would force an **IC-magnitude** cutoff, and a magnitude cannot be blind-pinned, which is precisely the curve-fitting the pin-before-any-grade-row rule exists to prevent | active |
| **D109** | **The post-Phase-4 widening target is NOT the in-place S&P Composite 1500; breadth arrives as SEPARATE ARENAS under D71 (v1.9.55; supersedes D87, re-amends D70(a)).** The `sp500` arena keeps its universe. Additional breadth is registered as its own arena (`sp400`, `russell2000`, ...) with its own store, Worker+Api pair, calibration and snapshot dirs, per rule 23 — never as an in-place enlargement of an arena that already holds a live experiment. **FOUR ARGUMENTS, RECORDED SEPARATELY BECAUSE THEY ARE NOT THE SAME ARGUMENT AND DO NOT CARRY EQUAL WEIGHT.** **(1) D87’s own ground was the arena’s SELECTION POOL**, and it already disclaimed the Grinold √(1500/500) ≈ 1.73× IR figure as a bet-count overclaim (breadth = bets, not names) — so D87 never rested on a statistical-power claim and cannot be defended with one. **(2) THE COHORT CURVE IS WHAT OVERTURNS D87 ON THE ARENA’S OWN TERMS (D88/FR-39):** cohort accumulation carries no universe-size term, and widening mid-experiment confounds the curve by mixing admission cohorts drawn from different universes — the same in-place-change confound that already rules out an in-place widen for that curve. This argument is decisive and internal to the arena. **(3) `n_eff` = window ÷ horizon CARRIES NO NAME COUNT**, so more names add no independent observations to the Signal Library’s trend test (§24.2). This concerns a DIFFERENT INSTRUMENT and is recorded as corroboration only — **it does not by itself overturn D87**, which was never about that instrument. **(4) ARENAS NEVER POOL (rule 23, UX-13)**, so separate arenas buy INDEPENDENT REPLICATION rather than precision — one rule confirmed separately in sp500, sp400 and russell2000 is stronger evidence than the same rule in one larger sample, because replication across universes, liquidity regimes and cost structures is what distinguishes a real effect from a fitted one. **THE RUSSELL 2000 REJECTION IS NARROWED, NOT LIFTED:** rule 22 rejected Russell 2000 as a WIDENING TARGET, because an unverified-depth historical-membership source cannot be folded into an arena whose replay window already rests on verified membership. That rejection is silent on Russell 2000 as a SEPARATE ARENA, where D71 isolation means a shallower membership history degrades only that arena’s own calibration and can never contaminate `sp500`. A russell2000 arena therefore needs its own D70-style sourcing gate at ITS OWN Phase-4 sign-off, not rule 22’s. **The D87-note prerequisite checklist (10 items) is VOID as written** — items 1–9 presuppose an in-place widen; the per-arena equivalents are the D71 registration steps in ARENA_ARCHITECTURE §6. | Recorded under its own number because rule 25 requires it: the sign-off work had already moved to separate arenas in practice while D87’s row still read in-place sp1500, so both readings were live and either could be quoted in good faith. That is exactly the failure rule 25 and `tools/check-register.ps1` exist to prevent, and D87 is its worked example rather than an isolated slip | active |
| **D110** | **The proposal-quality score: the researcher is graded PER PROPOSAL, on two numbers published side by side and NEVER blended (v1.9.57; findings 308/309).** The lab's success criterion is that the researcher's proposals improve over time, **chained** — each round improves on the LAST one, not merely on day one — while §1.2's researcher yield and D88's cohort curve are both **aggregate**, so no per-proposal number existed and the chain had no links (finding 308). Two scores, each a DIFFERENT INSTRUMENT. **(1) DETECTABILITY MARGIN** = `expected_effect_ann` ÷ `detectability_floor_ann`, the D89/FR-40 floor that `DetectabilityGate` already computes at admission and currently DISCARDS; available at proposal time, needing no outcome closure. **IT IS CONFOUNDED BY THE TRIALS TAX AND ITS DIRECTION IS NOT A QUALITY SIGNAL:** the floor carries a Bonferroni term in the `trials_registry` count, so the bar RISES as trials accumulate and a researcher of constant quality proposing constant effect sizes shows a DECLINING margin purely from the moving bar. It can rise, fall or saturate, and none of those directions is by itself evidence about the researcher. **The control arm is what cancels it** — both arms face the same floor at the same time, the D36 matched-population idiom one level up — so the margin is **RECORDED BUT NEVER READ AS A QUALITY SIGNAL UNTIL THE CONTROL ARM EXISTS**; the control-arm decision and this one are ONE decision, even though the control arm takes its own number when Phase 5 defines it. **(2) CALIBRATION SKILL** = a proper LOG scoring rule on a new pre-registered `prior_prob` (the researcher's stated P(confirmed)) against the LEAVE-ONE-OUT confirmation base rate *b*: `ln(p̂) − ln(b)` on `confirmed`, `ln(1−p̂) − ln(1−b)` on `refuted`, and **0 on `inconclusive`** — no information was gained, which is also the SEAM between the channels, since the margin is the leading indicator of the inconclusive rate. **CALIBRATION SKILL IS TAX-ROBUST WHERE THE MARGIN IS NOT:** *b* falls as confirmation gets harder, so the reference moves WITH the tax and only a prior moving DIFFERENTLY from it registers as skill — a structurally different instrument, not a slower copy of the first. Log rather than Brier because Brier is bounded at 0 and saturates, which is the ceiling this decision exists to remove; propriety is recorded as INSURANCE should R1 ever be relaxed, not as today's defence, since a researcher that cannot see its score is not optimising it. **FOUR RAILS. R1** descriptive only, and **the researcher NEVER reads its own score** (D88's rail verbatim — never a gate, monitor or allocator input, and never a context-pack field; it reads closed journal OUTCOMES, which D79 already permits, so the chain runs on measured facts). **R2 mechanically computed, NEVER LLM-computed** — rule 32 bars AI from JUDGING, not from being MEASURED. **R3** chained via a TREND (the §24.2 `SignalTrend` shape) publishing its own **minimum detectable improvement rate**, with the failure branch PRE-REGISTERED: a READABLE flat or declining trend means the loop is not improving (D88's *recombination without gain*, at proposal level), is NOT evidence about market efficiency, and the response is to change the researcher's **INPUTS** under their own decision — never the measurement, the clamp, the thresholds or the budget. **R4** never blended: two incommensurable quantities, and a single number would need a weight nobody can derive. **The two score parameters — `Kpi.ProposalPriorClamp` (the clamp *c*, so p̂ is scored on [*c*, 1−*c*]) and `Kpi.ProposalScoreMinClosed` (the closed-outcome count below which *b* falls back to 0.5 with reason `thin_base_rate`) — are VERSIONED CONFIG ROWS resolved through D96 `ResolveAsOf`, never appsettings values** — they are SCORE INPUTS, so a mid-experiment change breaks the very comparability the chain depends on, and an appsettings value is not as-of resolvable (the D106 `GateOptions` limit), so a later recomputation could not reproduce the original score. `journal_entries` gains `prior_prob` and `detectability_floor_ann`, both nullable and additive, **built at Phase 5 checkpoint 5.7** beside the endpoint that writes them. **THE BUDGET BIND (finding 309), stated here because it constrains this decision:** R3 forbids raising `Research.ForkBudgetPerYear` to make the trend readable, AND the budget determines whether the trend is ever readable — both true, so the resolution cannot BE the budget. It is that the trials tax is **per-arena** (each arena its own store and its own `trials_registry`; D71/rule 23), so a second arena adds proposal capacity WITHOUT raising either arena's bar. The control arm gets NO such relief: it must face the same conditions, so it runs in the SAME arena and doubles that arena's tax. Two questions stay open and are deliberately NOT decided here — whether the improvement score is defined per-arena or lab-wide, and how *b* is computed if proposals span arenas **AMENDED TWICE: D113 (the paper control; the floor as at assessment); D124 (calibration skill declared STRUCTURALLY SILENT for the current signal set — a zero trend is an absence of input, never a reading about the researcher).** | The criterion the lab is actually graded on had no per-proposal instrument, so *better than its own last proposal* was unmeasurable by construction while reading as though it were measured. Specifying it BEFORE Phase 5 builds the hypotheses endpoint avoids building 5.7 twice — the stated prior is a request field and a schema column, not an afterthought — and before any proposal exists, since an unscored proposal is a permanently missing link in a chained criterion. The two channels stay separate because they FAIL differently: the margin is immediate and tax-confounded, calibration is slow and tax-robust | amended-by D124 |
| **D111** | **The Quality-strategy prohibition SURVIVES D109, is BROADER than the superseded D87 row scoped it, and its factual premise must be VERIFIED before it is enforced against a named arena or lifted (v1.9.59; carries forward the constraint stranded in D87, which D109 superseded).** **WHAT WAS STRANDED.** D87 carried a live constraint — *"a Quality strategy must never be tested in this arena — membership pre-screens for profitability (a matched-null / frozen-parameter hazard, rules 8-9)"* — and D87 is superseded by D109, while the D87-note checklist item that pointed at it is VOID. A prohibition inside a superseded row is invisible: nothing in the corpus points at it, and the Phase-8 roster carries `Quality` as a registered strategy (STRATEGY_CATALOG §7.2). **IT DOES NOT DEPEND ON THE WIDENING.** D87 wrote *"this arena"* meaning the sp1500 arena it was creating, but the MECHANISM it named — an index membership rule that screens on profitability — is a property of the index family, not of the widening. D109 removed the sp1500 arena; it did not remove the mechanism, so the constraint outlives the row that carried it. **THE SCOPE IS THEREFORE BROADER, AND IS STATED AS A PRINCIPLE RATHER THAN A STRATEGY NAME: a strategy must not be tested in an arena whose MEMBERSHIP RULE SCREENS ON THE SAME CHARACTERISTIC THE STRATEGY SCORES.** Two distinct confounds follow, and both are about measurement rather than performance: **(a) the cross-section is truncated on the scored characteristic**, so a claim that the characteristic predicts returns cannot be made out-of-sample from a universe in which its low tail was removed a priori; **(b) addition and deletion are asymmetric** — an index screens at ENTRY but does not eject a name the moment it fails the screen, so the universe holds firms that passed once and have since deteriorated, which is a selection process correlated in time with the very characteristic being scored. **THE FACTUAL PREMISE IS UNVERIFIED IN THIS CORPUS, AND THAT IS THE OPERATIVE PROBLEM.** Whether any given arena's membership rule screens on profitability is recorded NOWHERE except inside D87's own row: no source of truth states it, and it has never been checked against the index provider's published methodology. D87 asserted it while reasoning about the S&P 400/600 and never established it for the S&P 500 this lab runs on today. **A prohibition resting on an unverified premise is neither enforceable nor liftable.** **THEREFORE — VERIFY, THEN DECIDE; FAIL CLOSED MEANWHILE.** Before Phase 8 registers `Quality`, the arena's membership methodology is read from the index provider's PUBLISHED rules and recorded in INTEGRATIONS beside the membership feeds, stating per arena which characteristics its inclusion rule screens on — the same verify-then-decide discipline as D70's sourcing gate and the §7.0 PIT protocol, and it is a documentation task, not a build. **Until that record exists, `Quality` stays prohibited in EVERY arena.** Failing closed costs nothing — Phase 8 is contingent on §7.0 and has not started — whereas failing open ships a confounded matched null into the one channel (D36) the lab's honesty rests on. **ENUMERATED SO THE PRINCIPLE IS NOT OVER-APPLIED:** checked against the current roster, Momentum, MeanReversion, Residual Momentum, Time-Series Momentum, Breakout, LowVol and Betting-Against-Beta score price, return, volatility or beta characteristics, and **Value** scores valuation ratios; none of those is a stated index inclusion criterion, so **none is prohibited by this decision**. `Quality` is the only collision on the roster today, and every new strategy is checked against the recorded methodology at registration | Recorded under its own number because rule 25 requires it and because the alternative was worse than untidy: a live prohibition was sitting in a superseded row while the phase that would violate it carried the strategy on its roster, so the most likely way it surfaces is a Quality strategy quietly failing its matched null in two years with nobody able to say why. Stated as a principle rather than a name so the next fundamental strategy is checked rather than assumed, and gated on verification rather than asserted, because the premise it rests on turned out never to have been established anywhere the lab treats as authoritative | active |
| **D112** | **The researcher seat refuses to propose once the count of OVERDUE journal outcomes reaches `Research.MaxConcurrentCandidates` — a DERIVED bound, not a new key (v1.9.60; closes proposal P8).** D82's parent-evidence rule makes every proposal cite an outcome, finding or attribution row, but nothing stopped the seat proposing while unclosed outcome nags accumulated: D52's outcome-closure nag is a GUI nudge, not a gate, so the generator could compound on ground truth that was never established. **THE TRIGGER IS A COUNT, AND ITS BOUND IS DERIVED FROM AN EXISTING NUMBER.** `POST /api/v1/analysis/hypotheses` refuses (422, named reason) when the number of `journal_entries` outcomes past their declared `evidence_window_days` and still unclosed is **≥ `Research.MaxConcurrentCandidates`** (default 3). **NO NEW CONFIG KEY IS INTRODUCED, DELIBERATELY.** A grace period in DAYS would be an undefended constant of exactly the class finding 309 flagged in `Research.ForkBudgetPerYear`: nothing would derive N, and the value would govern whether the researcher can propose at all. `Research.MaxConcurrentCandidates` *is* derived — CONFIG_REFERENCE ties it to the "1 Live + 2–3 Candidates" roster shape and §8 states that shape is *"bounded by statistical honesty, not compute"* — so it is the count of claims the lab can honestly carry in flight at once, which is precisely the quantity a saturated evidence base must be measured against. Tolerating one or two late outcomes while preventing the pile-up is the shape P8 asked for; the count form delivers it and the day form does not. **THE TRIGGER IS OPERATOR BEHAVIOUR, NOT RESEARCHER BEHAVIOUR** — a forcing function on the human who owes a closure, never a penalty on the seat. Stating so is load-bearing: unstated, a later reader concludes the researcher is being disciplined for something it did not do. **A REFUSAL IS RECORDED AND COUNTED.** The 422 carries its named reason and the refusal increments a counter published **beside the D110 proposal-quality score**, the way the effective sample is published beside a trend flag — because a blocked proposal is a **GAP in the D110 proposal stream**, and an unattributed gap reads as researcher inactivity when it is operator debt. The same machinery carries D113's unscorable-proposal refusal, so one mechanism serves both. **REJECTED, WITH REASONS:** a HARD gate on any single overdue outcome (P8's literal form) deadlocks the loop whenever the operator is away — nothing closes, so nothing proposes; NO gate keeps D52's nag and loses the evidence diet entirely, which is the defect P8 opened on; a `Research.OutcomeOverdueGraceDays` key reintroduces the undefended constant; and three other existing numbers were checked and rejected as the bound — `Research.ForkBudgetPerYear` (explicitly underived, finding 309, so a gate built on it inherits the defect), `Kpi.CohortMinStrategies` (numerically 3 but a cohort RENDERING threshold, semantically unrelated), and `journal_entries.evidence_window_days` (it DEFINES "overdue", so it is the predicate's input and cannot also be its bound) | P8 was opened 2026-07-18 carrying a "decide before Phase 5" trigger because the hypotheses endpoint ships there, and the trigger fell due. It is a decision rather than a build detail because it changes the endpoint contract. The count-not-days form is the substantive content: P8's own counter-consideration asked for a grace window, and the grace window's SHAPE (tolerate a little, refuse a pile-up) is right while its CONSTANT would have been undefended — so this keeps the shape and derives the constant from a number the corpus already justifies, which is the finding-309 lesson applied at the moment it would otherwise have been repeated | active |
| **D113** | **The D110 detectability-margin control arm is a PAPER control, differenced on the EVIDENCE-PRIOR SEAM: it proposes, is never admitted, and costs zero trials (v1.9.60; coupled to D110, which it amends).** D110 recorded that the margin is *"recorded but never read as a quality signal until the control arm exists"* and that the control *"runs in the SAME arena, doubling that arena's tax"*. **THE DOUBLING PREMISE IS WITHDRAWN.** `score_detect = expected_effect_ann ÷ detectability_floor_ann` is computed at PROPOSAL time and admission is irrelevant to it, so a control whose proposals are never admitted writes no `trials_registry` row, spends no trials budget, halves no throughput and raises no candidate's floor. Verified against the code rather than argued: `DetectabilityGate.Assess` is a pure read returning `FloorAnn` on **both** its admit and refuse paths, and the only forward `TrialsRegistry.Add` is inside `CandidateFactory.Create` — i.e. at registration only. **IT IS NOT A NEW MECHANISM:** the corpus already has proposals that are never admitted, since the researcher proposes and only the operator registers (rule 30), so a control whose proposals are never approved is mechanically identical to one the operator declines. **WHAT THE CONTROL CONTROLS FOR — stated because without it this decision would define a control that controls for nothing.** Two identical researchers produce a margin difference of **ZERO by construction**: the tax cancels, and so does everything else. At Phase 5 there is no learning mechanism to withhold (memory Option B and the Learning Researcher are post-Phase-8), so the treatment/control difference is the **EVIDENCE-PRIOR SEAM** that §24.6 already requires to be swappable, disableable and placebo-able — that property exists for exactly this use. **TREATMENT** = the researcher seat with the D91 signal digest wired into its pack; **CONTROL** = the same seat, same pack recipe, same floor, same run, with the digest **PLACEBO'D** (the default — shuffled or synthetic grades of identical shape, holding pack shape and token count constant so a difference cannot be an artefact of prompt length) or **DISABLED**. The measured difference answers a real question — *does the signal digest improve the detectability of what the researcher proposes* — and it makes the post-Phase-8 Learning Researcher's seam a **measured** hook rather than an assumed one. **THE COMMON/DIFFERENCED LINE IS DRAWN EXPLICITLY:** the arena's as-of detectability floor is a **COMMON** pack field both arms receive; the digest is the difference; the per-rule floor-over-signal ratio (finding 307) rides the digest and therefore lands on the **TREATMENT** side unless a later decision moves it, which would change what the digest is. **TWO PAIRING CONSTRAINTS, both structural.** (1) Both arms are assessed in the **SAME job run, BEFORE any admission** — the floor resolves from current state (`ResolveCurrent`, a live trials count, the most recent 50 `power_reports`), so an admission between the arms moves it by one Bonferroni step and the difference stops being clean. (2) Both arms draw on `Ai.Researcher.MonthlyBudgetUsd`, so exhaustion **between** them would emit a treatment proposal with no control — an unpaired observation silently entering the margin series. Exhaustion must abstain **BOTH arms or neither**; headroom is checked for the pair before either dispatches. **THE AMENDMENT TO D110:** `journal_entries.detectability_floor_ann` is redefined from *"the floor AS AT ADMISSION"* to **the floor as at ASSESSMENT, stamped when the score is formed**. A paper control never reaches admission, and `Assess` is today called only from inside `CandidateFactory.Create` on a **locked** hypothesis, so the admission-time reading would leave the control permanently unscorable. Assessment-time capture also makes the two arms comparable, which admission-time capture could not: a real proposal is admitted only after the operator locks it, by which time other admissions may have moved the floor. A proposal whose floor cannot be computed at all (`unassessed_no_sigma` — no `power_reports` row with σ > 0 for either run kind) is **REFUSED with a named reason and counted through D112's machinery**, never written as a silently unscorable row and never given a third column. **THE ASYMMETRY, recorded because it is why D110's two channels need different treatment:** the margin is TAX-CONFOUNDED and needs a control, but that control can be paper-only; **calibration skill is TAX-ROBUST and needs no separate arm at all**, because the leave-one-out base rate IS its control — it is what an uninformed but calibrated null would score — and it can be scored only on ADMITTED proposals, since it needs closed outcomes | D110 deferred the control arm to *"its own number when Phase 5 defines it"* and priced it at a doubled trials tax; Phase 5 is that moment, and the price turned out to be wrong — the tax is paid at ADMISSION, not at proposal, so a control that never seeks admission is free of it. Recorded under its own number because rule 25 requires D110's column meaning to change by a register row, and because the arm difference is the substantive content: the version of this decision that says only "never admitted, same run" would have shipped a **duplicate** and measured nothing, which is the exact failure a control exists to prevent. The seam difference also buys the lab its first measurable claim about an evidence prior, ahead of the phase that will depend on one | amended-by D114 |
| **D114** | **The researcher's decision record is SUBJECT-KEYED, and the placebo is BLIND (v1.9.70, findings 330/331; amends D113).** **Clause 1 — subject-keyed records.** D104 requires the four transparency artefacts for EVERY AI decision, but `ai_decisions` was contestant-shaped (`strategy_id` NOT NULL, "the funnel reads this row"), which left the researcher's pack-to-output link, prompt version and effort configuration unrecorded — `analysis_cache` carries none of the three. Resolution, ZERO migration: `strategy_id` in BOTH `ai_context_packs` and `ai_decisions` is redefined as the record's **SUBJECT** — a strategy id for the contestant; **`job:{job_id}#treatment` / `job:{job_id}#control`** for the researcher's D113 arms. The meaning changes, the shape does not; SCHEMA is updated in the same commit (rule 25's discipline applied to a column's documented meaning). This also makes `ux_ai_context_packs` uniqueness hold BY VALUE for researcher packs rather than by SQLite's accidental NULL-distinctness. `sampling_json` carries `{seam, seed}` — artefact (d), how the call was configured; `applied_json` records `{journal_entry_id, arm, locked:false}` — artefact (c), the decision became draft N awaiting the operator. **Clause 2 — the placebo is blind.** THE PROMPT NEVER DECLARES THE SEAM MODE. The v1.9.67 implementation emitted `Evidence-prior seam: placebo` into L2 — the control was TOLD its evidence was fake, which defeats the placebo's entire purpose (holding everything constant EXCEPT the information) and violated D113's own "the packs differ ONLY in the digest field". Arm identity lives exclusively in the RECORDS — the subject string, the journal title, `sampling_json` — never in anything the model reads. Enforced by `D113_TheArmsDifferOnlyInTheDigestField_AndThePlaceboIsBlind`, which asserts the persisted packs differ in exactly one field at equal token estimate and that no prompt carries a seam/arm marker | D113 defined the control correctly and the v1.9.6x build shipped the exact failure its text warned against — the arms differed only by an unblinded prompt literal, so the control controlled for nothing (finding 330/331; found by the architecture-drift review, not by any fixture, which is finding 333's lesson). The amendment makes the blindness explicit rather than implied, because implied invariants are the ones that get built wrong — this one was. Subject-keying is recorded as a decision rather than a convenience because it changes what a documented SCHEMA column means, and rule 25 requires that to happen by register row | active |
| **D115** | **A row whose Status becomes `superseded-by <D>` is TOMBSTONED in the same commit (v1.9.70; extends rule 25).** The row's body is compressed to a one-line gravestone: `TOMBSTONED (D115, <version>): SUPERSEDED by <D> (<version>). Original text: git history + the CHANGELOG provenance trace. Live content rescued to: <rows, or none — verified>.` The ROW persists — check-register 3a resolves every cited D-number to a §2 row, historical citations keep their anchor, and the register stays the count — but the QUOTABLE DEAD TEXT is removed. Why: a superseded row's full text is the one place in the corpus where a dead reading can still be quoted in good faith — D87's in-place-S&P-1500 text was exactly that for four versions, and D111 exists only because a live constraint sat STRANDED inside it. Tombstoning makes stranding structurally impossible: content in a superseded row is by definition dead, so anything live MUST be rescued explicitly at supersession time — the D111 discipline forced by construction rather than remembered. **Applied retroactively (v1.9.70), each row verified before compression:** D87 (the Quality-screen prohibition → D111; the breadth decision → D109; the Grinold-overclaim caveat → CHANGELOG v1.9.23) and D102 (the reported-only rule → D107's category, threshold and per-signal table unchanged there). **Deliberately NOT removed:** amended rows (an amended decision is still in force), reserved D103 (a number-holder — deletion invites renumbering collisions), historical CHANGELOG/PROGRESS citations (the immutable provenance trace), and same-line successor pointers in living prose (they ARE the anti-lingering mechanism there) | The operator's directive: void decisions must not linger. Hard-deleting rows was considered and rejected — it breaks check-register 3a and dangles every historical citation, re-creating the D87 failure mode from the opposite direction. The gravestone keeps the anchor and removes the content, which is the part that could mislead | active |
| **D116** | **The detectability gate gains a PLAUSIBILITY CEILING, derived from the frozen C-1 sweep — and the researcher's pack carries both ends of the band (v1.9.71; findings 336/337; amends D89).** D89 compared in ONE DIRECTION only: `expected_effect_ann < floor` ⇒ refuse. A claim of 400%/yr therefore sailed through while 0.5%/yr was refused, and the context pack showed the seat the FLOOR and no upper bound — one scale cue, pointing up, in a prompt that instructs the seat to state an effect size as a number (finding 337). That is an escalation channel built into the recipe: the cheapest way to satisfy a floor is always a larger claim. **THE CEILING IS `top swept rung × (top rung ÷ second-from-top rung)`** — the largest edge the arena ever simulated, times the ladder's OWN geometric step — read from the same frozen `Calibration.DetectionPower` row the empirical floor already reads (`DetectionCurves`, shared by the gate's `ResolveCurrent` path and the pack's `ResolveAsOf` path so the two ends cannot drift apart). Live sp500 arena: `16 × (16÷8) = 32%/yr`. **NO NEW CONSTANT AND NO NEW CONFIG KEY, WHICH IS THE WHOLE DEFENCE.** The plant ladder is a statement this arena already committed to about the range of edges worth simulating, chosen from offline clean-MDE arithmetic and frozen at Phase 4; α* is ALREADY selected from it, so the ceiling is the other end of a measurement the gate already opens, per-arena by construction (D71) and self-re-deriving when generation 2 re-freezes. An authored number would be `ForkBudgetPerYear = 6` again (finding 309). **WHY ONE STEP ABOVE THE TOP RUNG, NOT THE TOP RUNG:** the floor is selected from the SAME ladder and collides with it — this arena's rule-selected primary IS the 16% rung (offline monthly floor ~15.9%), so `ceiling = max(rung)` would give `floor == ceiling` and an admissible band of a single point. The ladder's own step is the smallest headroom that is still read rather than invented. **DELIBERATELY LOOSE, RECORDED AS SUCH:** 32%/yr does not catch a merely optimistic 12%/yr claim. The ceiling bounds ABSURDITY; the instrument for ordinary over-claiming is D110 calibration skill, which is slower but principled, and a tighter ceiling would need a number nobody can derive while risking the refusal of a real find. **THREE FAIL-OPEN VALVES**, matching the gate's existing 'nothing honest to say ⇒ admit' posture: no frozen curves ⇒ no ceiling; fewer than two distinct positive rungs ⇒ no STEP ⇒ no ceiling; `ceiling ≤ floor` ⇒ the ceiling is REPORTED but INERT — never an empty band. The boundary is INCLUSIVE (a claim equal to the ceiling is the last admissible one). **THE PACK CARRIES IT (recipe cp-1.0 → cp-1.1)** beside the floor as a COMMON field — both D113 arms, never differenced, since only `signal_digest` is the arm difference — because refusing at admission punishes the escalation weeks after the number was stated while publishing both ends bounds it at the moment of stating. Not a D110 R1 breach: the band is a property of the ARENA, not a grade on the researcher. The L0 instruction bumps to `rs-1.1` to name the band. **BOTH VERSION BUMPS WERE FREE EXACTLY THEN:** the live store held ZERO researcher proposals, so no D110 margin series lost comparability; after the first scored proposal the bar for cp-1.2 is a discontinuity in the one measurement the lab is graded on. **THE REFUSAL SPLITS BY REASON** — `detectability_refused` (D99's code, meaning unchanged: too small), `implausible_effect` (too big), `floor_unreachable` (finding 336: no swept rung reaches `Gate.Power` within the horizon, so NO claim is admissible and none would have helped). Additive codes, not a /v2 break. **REJECTED, WITH REASONS:** an implied information-ratio bound ('IR above 2 is implausible') — true in the literature, but 2 is a fresh undefended constant, the finding-309 mistake with better PR; `ceiling = max(rung)` — the collision above; a soft FLAG instead of a refusal — it records escalation without bounding it, and the floor's own shape is a refusal | The lab's governing objective is that proposals improve over time, which makes the seat's stated effect size the quantity most exposed to drift — and the gate bounded it from below only. Recorded under its own number because rule 25 requires D89's behaviour to change by a register row, and because the DERIVATION is the substantive content: a ceiling is trivial to add and worthless if its number cannot be defended, which is why the sweep-derived form is the decision and the value is a consequence | active |
| **D117** | **The recompute harness's build-time settlements: report-only, scoped sign-off gated on a confirmation slice, a retire-exempt scope rule, and a third input tier (v1.9.72; findings 338-340; amends D106).** §25.5 recorded two questions as *settled when built*; this is that moment, and two more were forced by reading the code the harness has to reproduce. **(1) WHERE IT WRITES: NOWHERE.** The harness emits a markdown artefact under `docs/calibration/{arena}/` and writes no row. A distinct `run_kind` was REJECTED — it needs a CHECK migration and, worse, adds a third run kind that every quarantine filter, read-model and guard in the corpus would have to be re-audited against, since rule 1 reasons in forward-vs-replay terms throughout; that audit, not the migration, is the cost. A vintage column was rejected as a larger schema change to tables whose append-only guarantees are currently expressed without one. If a recomputed number is ever frozen, `Calibration.ReportRef`'s existing `{path, sha256}` shape already carries a report as evidence, so report-only costs nothing later. **(2) IS A RECOMPUTED NUMBER SIGN-OFF EVIDENCE? YES, FOR RETIRE-EXEMPT SUBJECTS, GATED ON TWO CHECKS.** `FX-RecomputeParity` must hold (§25.3, exact, three artefacts) AND a **CONFIRMATION SLICE** must agree: a narrow `replay-calibrate --from/--to` under the CORRECTED rules, compared with the harness over that same window. The second is not belt-and-braces — it is the only check that exercises the path the harness is actually used on. Parity reproduces generation 1 under the UNCHANGED rules; the code that differs under a changed rule is exactly what parity never runs. Without the slice, the claim rests on a code-reading argument, and this corpus has been wrong that way before (findings 281/282, corrected by 289). With it, generation 2 costs hours instead of days and every later monitor-rule change costs minutes. **(3) THE HARNESS REFUSES A SUBJECT THAT RETIRED (finding 338).** §25.1's *"no feedback breaks it"* verified the allocator and noted plants are retire-exempt without stating that the exemption is LOAD-BEARING. Two loops, not one: **promotion** never changes the evaluated set (`promotable` = effective status in `{candidate, live}`), so the alpha change has ZERO feedback; **retire** removes a subject, which stops its rows, so the "would not have retired" direction has no data to recompute from. Plants are exempt (D100) and are 500 of 503 strategies, which is precisely why the cohorts the curves are built from are recomputable in BOTH directions. The one non-exempt subject (`threshold:sma50`) holds the only `retired` row in 95,769 and the only `Revert` row — it is EXCLUDED and NAMED in the report, never silently dropped. **(4) A THIRD TIER, `equity-derived` (finding 339).** §25.2's table had rows for stored columns and for `control_equity`, but its own prose listed *"the alpha definition"* as covered and no row supplied its inputs — the paired series from `equity_curve` plus `StrategyMetrics.JensenAlpha`. A v1 built to the table would refuse the one change generation 2 most needs. **AND A RE-CLASSIFICATION (finding 340):** S6's negative-alpha threshold is `derived-band`, NOT `direct-read`, because a row that took the negative branch never evaluated band membership and therefore recorded none — move the threshold and exactly those rows fall through to a check whose input was never stored. §25.1's claim that band membership is recoverable from `contribution` holds only for rows that did not take that branch. **THIS VERSION IMPLEMENTS `direct-read` AND `equity-derived` AND REFUSES `derived-band` OUT LOUD** — §25.2's own warning is that a harness reading only stored columns would APPEAR to cover the band case and quietly answer it wrongly, so refusing is conformance, not a gap | §25.5 said these were to be settled at build and this is the build. They are one decision because they are one question — what the harness's answer is EVIDENCE OF — approached from four sides: where it may leave a trace, when it counts, whom it may speak for, and what it can read. Recorded under its own number because rule 25 requires D106's meaning to change by a register row, and because (2)'s gating condition is the substantive content: the version of this decision that says only *"parity passed, therefore trust it"* would license the harness on a test that structurally cannot cover its use | active |
| **D118** | **The gate's EFFECT and the MDE it is judged against are ONE ESTIMATOR PAIR, produced together (v1.9.74; closes finding 285, corrects finding 345; amends D48).** `EvaluationStep` formed the effect as `mean(r_s − r_b) × 252` — a RAW ACTIVE-RETURN GAP with no beta term — and persisted it as `power_reports.observed_gap_ann`, from which the promotion gate, the allocator's weights and the Strategies screen's alpha cell all read. D26 says *"Alpha = beta-adjusted (Jensen's) alpha + IR; **never a raw return gap**"* with the rationale *"low-beta strategies otherwise structurally rigged to lose"*, and hard rule 6 repeats it; `StrategyMetrics.JensenAlpha` already existed and was already correct, so this was a WIRING gap (finding 285). **THE FIX IS NOT MERELY THE NUMERATOR, WHICH IS WHY THIS TAKES A DECISION.** Substituting Jensen's α while judging it against the MDE of the β = 1 difference series pairs an intercept with the noise of a DIFFERENT estimator — not a coherent test, and the mistake the recompute harness made first (finding 345), which made its measured curve a LOWER BOUND. So per (strategy, as-of): ONE `NeweyWest.Ols` fit at one lag yields **effect** = `α × 252`, **mde_ann** = `ZSum · AlphaSe · 252`, and **sigma_lr** = `AlphaSe · √T`. **THE σ DEFINITION IS LOAD-BEARING, NOT COSMETIC:** three downstream formulas already divide a persisted σ by √T or √H — `MdeCalculator`'s own arithmetic, the allocator's shrinkage SE (`se = mde/zsum`), and **`DetectabilityGate`'s ANALYTIC FLOOR**, which reads the median `sigma_lr` of recent `power_reports`. Defining σ as *the effective long-run sigma implied by the HAC α standard error* keeps all three dimensionally correct with NO change to any of them, and keeps the detectability floor measuring the noise of the quantity the gate now judges on. Any other choice leaves one of the three silently comparing α against the spread of something else. **THE DEGENERATE CASE IS DEFINED, NOT LEFT TO CHANCE:** a benchmark with no variation leaves β UNIDENTIFIED (every (α, β) with `α + β·c` at the mean fits identically), so the pair falls back to the β = 1 convention — which IS the raw-gap pair, effect and MDE together, never a mismatch. That is not rule 10's silently-defaulted MISSING input: the input is present and its beta adjustment is VACUOUS, and it matches `OverfittingMonitor.SafeAlpha`'s convention since Phase 3. A returned `Beta` of null is how a caller tells no adjustment was applied; it cannot fire on live data, where a real cap-weight account varies every session. **`raw_gap` IS RETAINED AND IS NOT DEAD CODE:** the recompute harness's parity arm must reproduce generation 1, which the raw gap produced (§25.1 — the harness must reproduce the known defect before it can be trusted to evaluate the fix for it). The live gate no longer calls it; deleting it as unused would break `FX-RecomputeParity` against every stored generation, permanently. **NOT ADDRESSED, RESTATED:** every `JensenAlpha` call site still passes `rfDaily: 0.0`, so the corrected path is not yet on the French RF series — finding 285's own second-order note, and Phase 6's work. **The columns are NOT renamed:** `observed_gap_ann` now holds an alpha, and a rename is a migration over 95,770 rows plus every reader for cosmetic gain — deferred with its reason recorded rather than left to be re-discovered | Finding 285 sat open from v1.9.51 because it was filed as *"inert while every account is beta-matched"* — every D64 plant is an overlay on its own base, so the calibration structurally cannot expose it, and Phase 6 registering real low-beta strategies is when it goes live. The recompute harness made the fix measurable before applying it (35 promotions moved earlier, 30 gained, **0 lost**), which is the first time a rule change in this corpus has been priced before landing. It takes a decision rather than being a bug-fix commit because the ESTIMATOR PAIRING is the substantive content: the version of this change that swaps only the numerator looks correct, passes every existing test, and quietly judges α against the wrong noise | active |
| **D119** | **A position with no bar today marks at its LAST KNOWN print — frozen or not; cost basis only for a name never priced at all (v1.9.77; finding 352; amends D86).** D86 chose cost basis for a frozen name so an unpriceable position would never be silently misstated by a stale print, and finding 275 confined that mark to genuinely-frozen positions while a plain vendor gap carries the last close forward. But the §13.6 stoppage freeze fires on a SINGLE missing session — the same session the gap appears — so the carry-forward branch was unreachable for exactly the case it was written for: OEF 2014-04-22 (one vendor gap in 5,031 sessions) froze and marked the benchmark account at its 2006 cost basis, fabricating a −26.9 %/+36.9 % equity round-trip that entered every tracking-error and detectability measurement downstream (finding 348's contamination). The freeze reason's own words are "freezing at the last print", and the ledger now does what they say: `BasisMath.MarkOne` no longer takes `frozen` at all, so the mark structurally CANNOT depend on the flag — a freeze halts ACTION (no trading until the operator resolves), never VALUATION. Cost basis survives as the deep fallback for a name never priced ≤ today. Terminal delisting unaffected (a real final print force-exits at last print). | A years-old cost basis carries no information about today and misstates in EITHER direction — the "conservative" mark was the one fabricating impossible days in the benchmark itself; the last print is the best point-in-time estimate the store holds, and it is what §13.6's freeze message already promised | active |
| **D120** | **The stored corpus is re-audited by the CURRENT gate + member-window plausibility detectors; remediation is roster EXCLUSION, never bar deletion (v1.9.77; findings 349–351, 353).** The live store's bars were ingested 2026-07-15..23; the R2 spike-and-revert Reject landed 2026-07-24 (v1.9.41, e786a0f) and screens fresh ingests only — so the corpus everything replays over was never screened by the guard that now protects it: 55 securities carry 1,763 >×10 close-to-close jump-days (finding 350), and a sub-threshold class flaps below ×10 while priced like microcaps inside membership spells (finding 351). The `store-sweep` Worker verb re-runs the gate over each stored latest-version series and adds two detectors the gate deliberately lacks because they need MEMBERSHIP context: single-event dividend yield ≥ `Data:SweepMaxSingleDividendYield` (default 0.5) and 63-session median dollar volume < `Data:SweepMinMemberDollarVolume` (default $1M), both judged ONLY inside membership spells so a legitimate post-index microcap is never flagged. REPORT-ONLY (the D117 discipline): one markdown artefact under `docs/calibration/{arena}/`, no row/flag/config writes; the OPERATOR reviews the evidence — a detector hit is a CLAIM, not a verdict; the v1.9.77 review kept 11 flagged names whose prices verified real (volume-field artefacts and genuine specials/spinoffs) and excluded 28 — and edits `Universe:Exclusions` (finding 266's deny-list, the rule-3-compliant substitute for deleting bars: the bars stay, the roster forgets them). Exclusions remove FICTIONAL series, not real losers — recorded as a calibration caveat per D49 — and the sweep is re-runnable after any bulk backfill. | Three prior document sweeps could not catch this class: each document and each series was internally consistent; the defect was the corpus PREDATING its own guard. Fail-closed screening must reach backward as well as forward, and the only rule-3-compliant remediation for already-ingested garbage is removing the name from the roster the replay prices | active |
| **D121** | **The detectability horizon is TEN years, chosen before generation 2's curves existed (v1.9.79; amends D89, which D116 also amends).** The admission floor is `z x TE / sqrt(H)`. At generation 1's contaminated noise (TE 22.0 %/yr) the 3-year floor was ~36 %/yr - ABOVE D116's 32 %/yr plausibility ceiling, which is finding 336's closed gate restated analytically: no claim could be both detectable and plausible. Generation 2's clean noise (TE ~8 %/yr) puts the 3-year floor at ~13 %/yr, so the gate reopens - but only to claims of 13-32 %/yr, a band that for a real equity strategy is entirely too-good-to-be-true. **That is a perverse incentive, not a safe default:** the FLOOR would push the researcher's claims up exactly as D116's ceiling holds them down, squeezing every admissible hypothesis into implausibility - the escalation channel D116 exists to close, re-opened from below. Ten years puts the floor at ~7 %/yr, which admits realistic edges. The horizon encodes HOW LONG THE LAB WILL WAIT for a verdict - a preference, not a fitted quantity - and D110 grades the researcher PER PROPOSAL at registration, so a longer horizon does not slow the learning loop; it only widens what may honestly be proposed. Costs no re-run: the frozen `Calibration.DetectionPower` row stores the full P(promoted by t) knot curve, not a horizon-collapsed number, so the gate applies the horizon at decision time. | **Chosen BEFORE the generation-2 curves were computed, deliberately.** Picking the horizon after seeing which value opens the gate is choosing a parameter by looking at the answer - amendment C1's prohibition and the spirit of rule 8's "never tune against the monitor". The operator was asked with the trade-off stated and the floors tabulated from theory alone, and pre-registered 10 years while the run was at 10 % and no curve existed to be tuned against | active |
| **D122** | **The expected effect is MEASURED per construction and per arena, never asserted as a constant (v1.9.87; supersedes D38).** D38 fixed the lab's calibration to "the realistic 1–3 %/yr prize" and stated that every screen, gate and expectation was tuned to it. That number was never a cap — no code reads it, and none ever did — but it was an ASSERTED CONSTANT derived from ONE construction (long-only, large-cap, this cost model), and the operator has put that construction itself in question. **A number that follows from a premise the lab may discard cannot be the thing every expectation is calibrated to.** WHAT REPLACES IT: the arena measures its own detectable band and publishes it — the frozen `Calibration.DetectionPower` row gives α*(H) and D116's ceiling gives the upper end, so the band is READ from the generation in force rather than quoted from a paragraph. sp500 at generation 2: **[6.95 %, 32 %]/yr at the D121 horizon**. A different construction (long-short, a different cost model, another arena) produces a different band BY MEASUREMENT, not by re-negotiating a sentence. **D38's CONCLUSION SURVIVES and is restated here, because it never depended on the number:** the lab is graded on §1.2's laboratory KPIs — verdict honesty, anti-predictive detection speed, indistinguishability honesty (D63), control calibration, leakage integrity, attribution coverage, operator learning — never on "found a winner"; `TooEarly` remains the expected common verdict and binary promotion a rare, ceremonial event. That framing rests on the prize being HARD TO DETECT, which the measured floor now states directly and per-arena **ALSO AMENDS D56:** its rationale illustrated the flat-cut defect with "a genuine 1–3 %/yr edge sits in the 60th–90th percentile for years" — an inherited citation of the retired constant. The illustration is restated against the measured band; D56's substance (trajectory curves rather than flat percentile cuts) is untouched | An asserted constant and a measured band answer the same question with different honesty. The constant was defensible while it was the only number available; generation 2 replaced it with a measured one, and keeping both invites the corpus to be quoted two ways — the failure mode rule 25 exists to prevent. Recorded as a supersession rather than an edit because D38 is a register row and rule 25 admits no other mechanism, and because the SUBSTANCE is which KIND of quantity calibrates the system: a forecast carried in prose, or a measurement carried in a frozen row that a re-run can move. No code changes — a full search of src/tests/tools found no reference to the figure (every "haircut" in code is the bankruptcy, Bonferroni or deflated-Sharpe haircut), so this is a documentation supersession with no behavioural surface | active |
| **D123** | **WHICH CONSTRUCTION the lab trades is decided by MEASUREMENT, and the instrument is a report-only `construction-study` verb (v1.9.88; Phase 5.5, FR-47).** D122 made the expected effect a property of the CONSTRUCTION and the ARENA rather than an asserted constant; this is the measurement that statement implies. The detectability floor is `ZSum · TE / sqrt(H)`, so TRACKING ERROR decides what an arena can adjudicate at all — and generation 2 froze this arena's band at α*(10y) = 6.947 %/yr under the only construction the lab has ever run (long-only, judged against a broad benchmark), against realistic factor tilts of a few percent. The verb measures, for each registered `ISignal` over the stored history, a monthly-rebalanced TOP tail against the equal-weight SCORED universe (long-only) and TOP-minus-BOTTOM (long-short, dollar-neutral, benchmark cash), reporting NW-corrected TE (σ_LR, never naive — the conservative estimator, because a study arguing for a lower floor must not pick the one that flatters it), gross and net effect, and the floor. **THE DECISION NUMBER IS THE INFORMATION RATIO, NOT THE FLOOR, and this is the correction the study forced on its own first design:** a long-short book is roughly 2× leverage on the same cross-sectional bet, so it scales TE AND effect together and the floor rises exactly as fast as the effect that must clear it. Since `t = IR · sqrt(T)`, detectability depends on the information ratio alone; the live smoke run demonstrated it outright, `resmom:L252` returning TE ×3.01, effect ×3.01, IR 0.374 -> 0.373. The comparable quantity is `years-to-detect = (ZSum/IR)^2`, and `FX-ConstructionScaleFree` pins the arithmetic so the framing cannot regress. **RAILS.** (1) REPORT-ONLY on the D117 clause-1 discipline: one archived markdown artefact, never a row, a flag or a config value, so no false positive can quietly move the arena. (2) NO SECOND POINT-IN-TIME VIEW — scoring runs through the real `BarFeatureView`, and the realisation panel is backward-looking by construction (the `BandInputs` precedent, §25.2) and loads through `IBarReadService.GetCrossSection`, so the watermark rule is never restated. (3) D91 SURVIVES INTACT: informing a BUILD decision is not the allocator, a gate, sizing or eligibility, and `AlphaLab.Evaluation/Construction` is deliberately outside the consumer directories `ci.ps1` scans for `ISignal` — it must never be added to them. (4) NOTHING MEASURED HERE MAY SET A PRE-REGISTERED `expected_effect_ann` (rule 16 / D52): choosing the number you then pre-register by looking at measured data is exactly what pre-registration prevents, and the rendered report repeats this where a future reader meets it. (5) BORROW IS AN ASSUMPTION, not a measurement — D43 has no borrow term and this arena buys no borrow data, so two levels are reported and the 0 bp column is the optimistic bound that can settle a negative answer outright; if the verdict FLIPS between them, the answer depends on data this arena does not have and NOTHING is decided. **THE PHASE ENDS AT A DECISION.** An IR gain ≈ 1.0× means the construction bought leverage rather than information and long-only stands; a material gain with years-to-detect inside the gate horizon justifies a SEPARATE build phase (`TradeSide.Short`, a borrow term in D43, corporate-action inversion, margin — D84 is NOT reopened here — long-short control populations, a fresh calibration generation), none of which is in scope under this number. A negative answer is publishable and cheap: §1.2 grades this lab on operator learning and verdict honesty, never on having found a winner. | active |
| **D124** | **Calibration skill is STRUCTURALLY SILENT for the current signal set: an inconclusive-dominated outcome stream is an ABSENCE OF INPUT, never a reading about the researcher (v1.9.91; finding 370; amends D110).** Finding 370's bar, derived from the horizon and the confidence/power pair alone: at H = 10 years this arena can only adjudicate a sustained information ratio ≥ ZSum/√H = **0.886**. The measured best LONG-ONLY is **0.279 (brk:L252)** — 3.2× short of the bar, and long-only is the construction in force since D123 answered NO on long-short; the best across all fourteen (signal × construction) pairs is **0.392 (bab:L252, LONG-SHORT — a construction D123 declined)**. Every pre-registered outcome for the current signal set therefore closes `inconclusive`, and D110 scores an inconclusive at **0** — no information was gained, in that row's own words. A zero calibration-skill trend is therefore NOT evidence the researcher is failing to improve; it is no evidence at all, and it renders as "no reading available" with its reason inline (UX-17), never as 0.00. WHAT SURVIVES, so this row is not read wider than it is: (a) **D88's cohort maturation curve** — median matched-population percentile against track length, descriptive, never needed a verdict to read; (b) **D110's detectability margin** — computed at proposal time against the D113 paper control, needing no closed outcome. WHY THIS IS NOT R3's FORBIDDEN MOVE: R3 bars changing a measurement BECAUSE it read flat; nothing has read — the silence follows from the horizon and the confidence/power pair, which contain no measurement of the researcher. `FX-InconclusiveScoresZero` pins the premise (TEST_PLAN §6; implemented with the D110 scorer, Phase 6) | A zero that reads as a verdict indicts the researcher for the horizon's arithmetic; naming the silence STRUCTURAL keeps the score honest and names the two instruments that still read | active |
| **D125** | **The contestant-vs-twin paired difference is a SEPARATE INSTRUMENT from the promotion gate's benchmark-relative channel; finding 370's bar does NOT apply to it (v1.9.91; amends D81).** Evidence by file and line: MASTER Appendix M.1 already asserts the near-twin difference series is 5–10× quieter, with power scaling as the SQUARE of the quieting (the "verdicts in about a year" figure is DERIVED from that arithmetic, never measured — the D122 caveat now sits on it); `EvaluationStep.cs:80/:95` pairs every promotable against `benchCurve` (default `buyhold:cw`), so the gate has never read the twin; a grep for the twin difference across `src/` and `tests/` returns EMPTY — D81's "headline number" is UNMEASURED. This row instructs: the paired difference is published as its own read-model number with its OWN Newey–West-corrected MDE and track length beside it, always together (the new §1.2 KPI row; the UX-18 slow panel), DESCRIPTIVE ONLY — never a gate, monitor-verdict, or allocator input (S8 consumes it as a divergence TRIPWIRE when it exists — a tripwire, not a verdict; the read model arrives at Phase 6). SCOPE LIMIT: does NOT change gate behaviour and does NOT reopen finding 373 — the twin stays a control (`strategies.status='control'`), outside the gate, and the gate's opponent question (P19) stays open exactly as finding 373 recorded it | Power scales with the square of the quieting (M.1): the twin channel can read in roughly a year what the benchmark channel cannot in ten; one bar for both would discard the lab's fastest honest instrument — and an unmeasured headline number is a claim the lab has not yet earned | active |
| **D126** | **THE BOUNDARY, AS FREEZE: the researcher may PROPOSE a new contestant candidate; it may NEVER alter a RUNNING contestant's frozen params (v1.9.91; amends D82).** MAY: propose a NEW contestant candidate, citing parent evidence and spending fork budget, with the operator pre-registering per rule 30 (D52); it enters with its OWN mechanics-identical no-LLM twin and earns from scratch. NEVER: alter a running contestant's frozen params — instruction text, model id, pack recipe, shortlist size, memory option, twin scorer (the D17/D81/D85 frozen set in `config_json`). Any such change IS a fork under a new `strategy_id`, never an edit. D81 already enforces the second half mechanically; this row makes the FIRST half explicit — permission to propose a contestant was previously only implied by D79's seat description, while nothing could describe one (D129 supplies the shape). GUARD (TEST_PLAN §6 spec; Phase 6 DoD): a proposal that targets an EXISTING contestant id is refused **422** via the same refusal path as a proposal with no parent evidence; a proposal creating a NEW contestant candidate passes | The freeze is what keeps the twin comparison meaningful — a mid-life edit changes the measured object — and what keeps improvement GENERATIONAL: selection over held-fixed individuals, never tuning a live one (rule 8 applied to the AI seat) | active |
| **D127** | **THE SHORTLIST RULE: the contestant's deterministic local pre-filter is TWO STAGES — hard pass/fail, then rank by CROSS-SIGNAL DISPERSION — plus a watermark-seeded random slice; the signal set it reads joins the frozen params (v1.9.91; amends D91).** D79 and MASTER §23.1 name "a deterministic local pre-filter" and config pins its SIZE (`Ai.Contestant.ShortlistSize`); no document ever said WHICH names — a live D17 hole, since a change to the unwritten rule would not have forked. **STAGE 1 — hard pass/fail, computable at t:** every pack feature present and fresh at the watermark; no delisting; no halt (no bar at asOf visible at the watermark ⇒ not actionable — the Eligibility "not priced" shape); plus a liquidity floor that is **INACTIVE ON THE CURRENT POOL** — this row is the D-number `Eligibility.cs`'s standing comment demands, the floor lives in the PRE-FILTER (downstream of `Eligibility.Resolve`; Stage 2 may only score a subset of Stage 1's pool), and on today's S&P 100 it removes nobody, so Stage 1 reduces in practice to data-freshness + delist + halt; it starts filtering when a wider D109 universe's tail enters. Not a score. **STAGE 2:** rank Stage-1 survivors by CROSS-SIGNAL DISPERSION — the spread of the standardized scores across the registered mechanical signals for that name — and take the top N (N = the budget-derived shortlist size, computed once and FROZEN per generation); PLUS a random slice of ceil(N/5) drawn from the Stage-1 survivors, seeded from the watermark so it is reproducible. Same list to both arms. **WHY DISPERSION:** names where signals AGREE are slots where the twin's equal-weight blend is obviously right and the contestant can only match it; names where signals CONFLICT are where judgment can differ — ranking on conflict concentrates the experiment on discriminating cases, which raises the divergence rate and shortens time to a readable verdict. **WHY NOT RETURN-PREDICTIVE:** it would hand the contestant a directional bet; the tilt cancels in the paired twin difference (same shortlist, both arms) but NOT in the promotion gate, where the contestant is judged benchmark-relative and the filter's tilt would read as its alpha. **WHY THE RANDOM SLICE:** without it a flat reading cannot separate "the contestant adds nothing" from "the filter fed it badly"; with it the paired difference splits by selection route after the fact. **THE D91 AMENDMENT, narrow:** MASTER §24.5's descriptive-only boundary gains ONE named exception — the contestant-arena pre-filter may read registered signal scores for CROSS-SIGNAL DISPERSION ONLY, never directionally. Dispersion is a SECOND-MOMENT quantity and carries no directional bet — that is what keeps the pre-filter off §24.5's ban list in substance and not merely in wording ("identical for both arms" protects only the twin comparison, not the gate). The ban on the library reaching the allocator, any gate, sizing, or promotion eligibility is UNCHANGED; the CI closure scan gains a SINGLE recorded allowance for the dispersion statistic on the pre-filter path, never a blanket exemption. **GUARD:** a dispersion tilt can still correlate with volatility while carrying no direction — the shortlist's realised factor exposures (beta, size, idiosyncratic vol) are published against the Stage-1 pool's (the UX-19 shortlist-provenance view); systematic drift is a FINDING about the filter, not an accepted cost. **SCOPE LIMIT:** this measures whether the contestant adds value WHERE MECHANICAL SIGNALS CONFLICT — narrower than "the AI adds value"; a limit on the claim, not a bias in the measurement. **FREEZE:** the dispersion SIGNAL SET joins `config_json`'s frozen params (a `shortlist_recipe` id, the `Ai.PackRecipeVersion` idiom) beside instruction text, model id, pack recipe and shortlist size — a later signal registration must never silently change a running contestant's shortlist (D126 forbids). **IMPLEMENTER NOTE:** per-name signal scores are not persisted (`signal_ic` is grades-only; §24.4 — no cross-section is persisted); the all-signals × whole-pool walk exists in `SignalIcEngine` (ops-wired, not daily) and the shared one-pass + deterministic top-k with the security_id tie-break in the construction study's `SelectFrom` — collect scores in one pass, never recompute per signal. Fixtures (TEST_PLAN §6 spec; Phase 6 DoD): `FX-ShortlistDeterminism`, `FX-ShortlistParity`, `FX-ShortlistSignalSetFrozen`, `FX-ShortlistNoDirectionalRead` | The unspecified selector is exactly where a directional tilt or a silent un-freeze would hide; dispersion buys verdict speed (more divergences per day) while staying direction-free, and the random slice buys attributability | active |
| **D128** | **THE SANCTIONED TRIGGERS: what may PROMPT a proposal, and what may not (v1.9.91).** The corpus said what a proposal must CITE (D82's parent evidence) and never what should PROMPT one. **PERMITTED — strategies:** the signal library's pre-registered trend flag reading `decaying` or `gone` (§24.2; descriptive, outside the gate). **PERMITTED — contestants:** (a) a collapse in the twin divergence rate — behavioural, readable in weeks, descriptive not inferential: an experiment that has stopped producing differing decisions can never produce a verdict; (b) an explicit indistinguishable verdict on the twin difference once sufficient track exists (the D125 channel). **FORBIDDEN, with the reasons recorded so a future reader cannot re-propose them:** (i) raw profitability over any window shorter than the MDE — noise wearing a signal's clothes; every fork it triggers spends trials and raises the D89 floor for every other candidate via the Bonferroni haircut; (ii) any relative-rank trigger such as "below the best strategy" — with N strategies, N−1 are always below the best, so it fires permanently and ranks on luck, which the random control populations exist to expose (rule 9); (iii) anything that re-gates a live strategy — rule 8 freezes live params and the D89 gate is admission-only; re-reading the gate against a live strategy is tuning-against-the-monitor by another name. **FALSIFIER, pre-registered:** if a D128-triggered proposal produces a candidate that is retired by the crowd comparison within one evaluation window, the trigger is firing too early and D128 is revisited — by a decision (rule 25). Fixture (TEST_PLAN §6 spec; Phase 6 DoD): a profitability-derived trigger cannot be recorded as a proposal's stated cause (refused at the D129 stated-cause field) | In a pre-registered system, p-hacking re-enters through WHEN you look, not what you claim; naming the permitted triggers closes the last unpriced degree of freedom | active |
| **D129** | **THE PROPOSAL SHAPE: typed fields alongside the prose; the seat FILLS the form, the operator REVIEWS, EDITS AND REGISTERS (v1.9.91).** Today the seat writes unstructured markdown into a draft journal entry (`BodyMd` = the raw model output; the four required fields exist only as prompt prose) and the operator hand-authors every parameter at registration — which is why D126 could grant permission to propose a contestant while nothing could describe one. The proposal gains TYPED FIELDS alongside its prose: for a strategy — the family, parameters, holding horizon and exit policy; for a contestant — the instruction text, model id, pack recipe version, shortlist size and twin scorer; for both — the stated CAUSE (the D128 trigger it claims). **THE SEAM IS PRESERVED AND STATED:** structure removes the operator's translation work, never the operator — the draft stays unlocked (`locked = 0`); registration remains the ONLY path to a `strategies` row and remains operator-initiated (rule 30 / D52 unchanged). Prose stays beside the fields: the argument is what the operator reads; the fields are what registration consumes. Fixture (TEST_PLAN §6 spec; Phase 6 DoD): a filled proposal cannot itself create a strategy row | A form the seat fills and the operator edits is auditable in both directions; free markdown the operator retypes is where transcription error and silent parameter invention live | active |
| **D130** | **THE ANNUAL BUDGET: `Llm.AnnualBudgetUsd` is THE one authored spend number; every other spend cap is DERIVED from it and recomputed by a fixture; one PRE-REGISTERED ESTIMATE feeds the pre-flight estimator (v1.9.92; amends D24; findings 380—382).** Before this row, eight spend and scope bounds existed and NOT ONE was annual — an operator constraint of the form "$X per year" had nowhere to live, and every cap below it was authored rather than derived (the defect finding 309 recorded against `Research.ForkBudgetPerYear`, generalized). **THE DERIVATION IS THE CONTENT** (the D116 discipline): reserve = 0.15; committed = AnnualBudgetUsd × 0.85; contestant 60% / researcher 40% of committed; daily = /252, monthly = /12; the global daily cap carries a ×1.15 intraday overshoot allowance; the token ceiling = floor(daily cap / mean uncached input rate) — at the authored 100: contestant 0.20/day, researcher 2.83/month, global 0.39/day, 130,000 tokens/day (finding 320's knob finally enforced). Rounding is to the cent, stated with the formulas. The RATIOS ARE THIS DECISION'S STRUCTURE — changeable only by a row amending D130, never a config edit (rule 25). `FX-BudgetDerivation` recomputes every derived cap from the committed appsettings, so a hand-edit of a derived cap fails the suite. **THE ONE PRE-REGISTERED ESTIMATE (neither authored nor derived):** the per-task `ExpectedOutputTokens` seeds feeding the pre-flight estimate — MODELLED, NOT MEASURED (provenance: the v1.9.91 design conversation; ~700 compact reads, ~1,500 long-form), with a pre-registered recalibration trigger: after N completed calls per task the seed is replaced by the p90 (a HIGH PERCENTILE, never a mean — the ceiling was the maximum and produced the lockout; a mean would let half of calls breach the cap in aggregate) of observed output in `analysis_cache`, N = MaxCalls × the 21-session evaluation window (both existing numbers, the finding-309 rule). **THE LOCKOUT IT FIXES (finding 380):** the estimator passed the API ceiling (8192) as its output term, and output dominates cost — against the derived 0.20/day an Opus call estimated ~0.28 vs ~0.09 actual, so the guard refused BEFORE spending, the seat abstained daily, and the ledger showed zero spend while the budget sat unused; the estimator now reads the pre-registered value, the ceiling stays as the API hard cap only, and a fixture asserts the estimate uses the seed and NOT the ceiling. **STATED PRECONDITION:** the derived caps assume a calibrated estimator — until the seeds and `EstimatedArmCostUsd` (0.25 authored in code, finding 381, Phase 6) are recalibrated from actuals, the binding constraint is the estimator rather than the budget; without this sentence the caps read as money limits when they are not. **THE SHORTLIST (3-C):** the budget-affordable N at the derived daily budget is ~416 (MODELLED per-name tokens 72 in / 24 out from §23.2's layer table; opus rates, batched), so the STRUCTURAL D24 scope cap (25) binds, not the budget (it would bind only below ~$6/yr); the value is computed ONCE at candidate creation and FROZEN into that generation's `config_json` — a frozen param (D17/D127) that must never float daily with budget or rates (`FX-ShortlistSizeFrozenPerGeneration`, Phase 6). **BATCH (3-F):** a cost lever, not a cap input — the contestant and researcher paths are latency-insensitive and already batched (D46), which DOUBLES effective headroom under every cap rather than changing any formula. **OUT OF SCOPE, stated:** `Research.ForkBudgetPerYear` stays AUTHORED — finding 309's recorded resolution binds (a change needs a decision amending D82, and the trials tax is per-arena); `Research.MaxConcurrentCandidates` is a statistical-honesty rail, not a spend cap | A budget an operator can state ("$100 a year") now derives every cap beneath it; a derivation is auditable where eight authored numbers were not, and the lockout fix makes the caps bind on money rather than on the estimator's fiction | active |
| **D131** | **THE GATE’S OPPONENT IS THE CAP-WEIGHT BENCHMARK, NEVER LIVE — the code was right and the prose is corrected (v1.9.94; P19 / finding 373; amends D31).** **THE REASON, and it is a correctness reason rather than a convention:** §20.2 shrinks each strategy’s α̂ᵢ toward ᾱ, the CROSS-SECTIONAL MEAN over the roster — and a mean over strategies is meaningful only if every α̂ was measured against the SAME opponent. Live changes over time and differs by period, so alphas measured against it are NOT COMMENSURABLE and the shrinkage breaks silently, producing a number that looks like a roster average and is not one. The cap-weight account is the common yardstick that makes §20.2 well-defined. (Hard rule 6 also names the cap-weight account as THE alpha benchmark; that is the convention, this is the reason, and a future reader needs the second.) **THE CODE FACT:** `EvaluationStep.DefaultBenchmarkStrategyId = "buyhold:cw"`, bound once and never overridden — `DailyPipeline` calls `.Run(asOf, runKind:)` without the benchmark parameter — so every `power_reports` row carries `StrategyB = buyhold:cw`. **CORRECTED IN THE SAME COMMIT, five pre-D51 legacy sentences:** MASTER §8’s “promotes a Candidate over Live”, §20.2’s Inputs line and its clamp clause 3, and DESIGN_IMPROVEMENTS §3.5’s Inputs line and Step 3 clause 2. The allocator’s `TooEarly` clamp (`AllocationStep.cs:59`) reads the benchmark-paired verdict and is CONFIRMED CORRECT; its documentation is corrected to match. **THE `Revert` TOKEN, RULED (finding 384):** it is NOT an orphan. `OverfittingMonitor.cs:250` writes it on the RETIRE path and generation 1 holds exactly one such row; `PromotionGate.Decide` returns only Promoted/Refused/TooEarly and must never produce it. `Revert` is therefore RETAINED, and SCHEMA’s `go_live_log` comment — which enumerates four tokens as though all four were gate verdicts, and omits the FIFTH, `WouldRevert`, that the D100 two-pass writes at `OverfittingMonitor.cs:280` — is corrected to name each token’s PRODUCER. **THE vs-LIVE QUESTION IS DEFERRED, NOT DELETED:** the candidate-vs-Live paired difference may later be published as a DESCRIPTIVE-ONLY instrument in the D125 shape — its own Newey–West-corrected MDE and track length beside it, never a gate, monitor-verdict or allocator input. **ITS LIMIT, stated so it is not oversold:** unlike the contestant/twin pair, a candidate and Live are ACTIVELY PUSHED APART by the §10 anti-clone rule, so the quieting is modest and this will not be a fast channel. **TRIGGER TO BUILD IT:** an operator promotion decision in which the benchmark verdict and the vs-Live question would point differently. | A roster-mean shrinkage needs a common yardstick and Live is not one; correcting five sentences costs nothing and keeps §20.2 well-defined, while deleting the vs-Live question outright would discard a reading the operator may one day need | active |
| **D132** | **NOTHING WRITES TO `features` IN PHASE 6: every feature read goes through `IFeatureView` at (asOf, watermark) (v1.9.94; closes PROGRESS P3 for this phase).** The `features` table carries no `observed_at`, `version` or `watermark` column, so a value persisted at watermark W and re-read at W′ is a rule-4 leak that **F-LEAK STRUCTURALLY CANNOT CATCH** — the test compares two reads, and both would return the same poisoned row. Phase 6 therefore builds NO feature cache: `IFeatureView` over the versioned bar reader is leak-proof by construction, and every strategy, the covariance service, the attribution regression and the D127 pre-filter read through it. Recorded as a row rather than left as an absence so that nothing writes there by accident. A future cache is a SCHEMA change (a PIT column) taking its own decision — this row does not unlock it. | An empty table with no PIT column is bait; naming the prohibition costs one row and removes a leak the leakage test could not have caught | active |
| **D133** | **`strategies.config_json` BECOMES READABLE: a typed, ADDITIVE, TOLERANT and BYTE-STABLE frozen-parameter shape (v1.9.95; Phase 6 checkpoint 6.2).** **THE DEFECT:** the column is **WRITE-ONLY** — `DummyRoster` and `ReplayRunner` write it, `CandidateFactory` passes an opaque string through, and **nothing in `src/` ever deserializes `StrategyConfig`**. So D17’s "frozen params" is DOCUMENTARY, not enforced: no code can read back what was frozen, and no fixture can assert a fork where an edit should have forked. **THE SHAPE CANNOT HOLD WHAT THE CORPUS ALREADY ASSIGNS TO IT:** `StrategyConfig` has `Seed:int`, `Selection`, `Sizing`, `Params:IReadOnlyDictionary<string,double>` and `Unregistered:bool` — **no string member and no list member** — while D81/D85 place the instruction text, model id and twin scoring rule there, D127 places a `shortlist_recipe` id and a frozen signal-id LIST there, D80 places the pack `recipe_version` there, and D81 rule 5 places the memory option and lesson-set version there. A double-valued bag cannot carry any of them. **THE RULING, four properties that bind together:** (1) **TYPED** — the frozen set is a named shape with string and list members beside the numeric `Params` bag, not a magic-double encoding; (2) **ADDITIVE** — every existing frozen row is untouched and D17 forbids re-serializing over one, so new members are optional and absent from an old row rather than defaulted into it; (3) **TOLERANT** — `{}` and any historical payload must still READ (many fixtures and every `ReplayRunner` plant write `{}` or an anonymous object), because a reader that refuses an old row would make the column unreadable exactly where D17 guarantees it cannot be rewritten; (4) **BYTE-STABLE** — keys ordered and `AlphaLabJson.Options` on EVERY write, on `SignalRegistrar`’s recorded precedent that *a provenance record whose bytes wobble cannot back a determinism claim*. Today two writers bypass those options (`ReplayRunner`’s anonymous object and `CandidateFactory`’s `JsonNode` unregistered marker), so one column holds two conventions. **TWO HOLES THIS ROW CLOSES BY NAMING THEM:** (a) `Unregistered` has **two writers and one key** — the typed `bool` and the `JsonNode` marker `CandidateFactory` stamps — and the typed member is authoritative, the marker becoming a write THROUGH the shape; (b) **`HoldingHorizon` is not persisted at all**: only its `Days_` projection reaches `strategies.holding_horizon_days`, so `ToRankExit` and `ToNextRebalance` both store NULL and **are not recoverable from the store** — the horizon SHAPE joins the frozen set, serialized by DECLARED type so its `kind` discriminator is written (`DummyRoster`’s `ExitPolicy` precedent), and nested so its discriminator cannot collide with `ExitPolicy`’s, which uses the same property name. **SCOPE LIMIT:** this row changes what the column CAN express and who may read it. It does NOT re-freeze, re-serialize or migrate any existing row, and it adds no CHECK to `strategies.status` (SCHEMA declares none, deliberately). Fixture: `D133_ConfigJson_RoundTripsEveryFrozenRow` — every row written before this row deserializes and re-serializes **byte-identically**, the polymorphic discriminators included | A frozen parameter nobody can read back is a promise with no witness: the fork rule, the twin’s scorer and the shortlist recipe all depend on comparing what WAS frozen against what is being proposed, and none of those comparisons can be written against a write-only column | active |
| **D134** | **THE STAGE-3 TIE-BREAK IS A SEEDED STABLE ORDER OVER (`Config.Seed`, `security_id`) (v1.9.96; Phase 6 checkpoint 6.3).** Equal scores at a selection boundary break by a stable 64-bit mix of the strategy's frozen seed and the security id, then by `security_id` so the order is TOTAL by construction rather than by the improbability of a hash collision. Applied in the SHARED Stage-3 code and in `FunnelRunner.RanksOf` **together** — the two orders must agree or a name can sit inside the top N by one rule and past its `RankBuffer` exit by the other, and that agreement is now a fixture rather than a sentence. **THE DEFECT:** STRATEGY_CATALOG §3 has required exactly this since v1.9.23 and the shipped code broke ties by ascending `security_id` — which IS ingestion order, one of the three the rule names as forbidden — under a comment reading as though determinism were the whole requirement. It was necessary and not sufficient. **THE CATALOG'S CITATION WAS DANGLING and is corrected here:** §3 cited **(D40)**, which is *versioned append-only bars* and says nothing about ties; `check-register` cannot catch a right-number/wrong-subject citation, which is why it survived three sweeps. **SCOPE, stated so silence does not decide it:** `ConstructionStudyEngine.SelectFrom`'s own `security_id` tie-break (named by D127) is OUT — it is an offline construction study, not Stage-3 selection, its determinism requirement is already met, and changing it would cost a second archived artefact's reproducibility for nothing. | Ties are not a boundary detail: `BuyAndHoldModel` scores a literal 1.0 for every eligible name and `ThresholdModel` emits only 1.0/0.0, so the tie-break IS the selection rule whenever the qualifying count exceeds the cap. Measured in the frozen store, `threshold:sma50`'s 60-name cap cut into a block of exact 1.0 ties on **4,789 of 5,031** replay sessions, and its truncation count EQUALS its tie-cut count — every truncation it ever made was a tie cut, so its 33,332 replay trades are an artefact of ascending integer id rather than of any signal, and two siblings differing only in `Config.Seed` drew identically. Nor does it dissolve when the real families land: `brk:L252` scores exactly 1.0 for every name at its trailing high. **LANDED NOW, BEFORE THE FIRST REGISTRATION, and that is the decisive reason:** no forward day has ever run in this arena, so nothing live is disturbed — whereas registering Momentum and MeanReversion at 6.10 under the superseded order would make the correction a FORK under rule 8, spending a trial and raising the D89 floor (N′ = live trials + 1) for every candidate after it. Forward-only scoping was rejected for a second reason: `SeedingBacktestEngine` produces S1's backtest reference under `run_kind='replay'`, so a forward/replay split would corrupt S1 by construction. **THE COST, bounded and accepted rather than discovered later:** generation 2's three replay accounts' funnel outputs cease to be RE-DERIVABLE (the generation is not re-run — replay is skip-if-committed, and only an explicit `--reset` would), and the `allocator_value_add` line in the 2026-08-03 sign-off report stops being reproducible because its roster reads `threshold:sma50`'s curve. Nothing frozen loses INTEGRITY: the four calibrated rows are built from D64 plants, which bypass the funnel entirely, and the report's sha256 still matches on disk. `FX-RecomputeParity` is not the tripwire and never was — it seeds equity curves directly and never reaches the funnel. Scoring this change falls OUTSIDE the D106/D117 harness by that decision's stated rationale — it changes the trades every later number is computed from — not by its parenthetical list | active |
| **D135** | **A STRATEGY DECLARES ITS MATCHED POPULATION'S CADENCE FAMILY AS A FROZEN PARAM; NOTHING DERIVES IT (v1.9.96; Phase 6 checkpoint 6.3; implements the D36 mapping).** `config_json` carries `frozen.population_family` — one of `daily|banded|monthly|quarterly`, the SCHEMA `control_populations.family` tokens — and `CandidateFactory` STAMPS it at creation, which is where STRATEGY_CATALOG §5.2 puts the hookup. ONE resolver (`PopulationMatcher`) answers for every consumer, carrying the `costs_on` predicate with it (hard rule 9: two rows share the token `daily` — the cost-on band and its display-only cost-free twin, same name AND same seed — so a name-only match returns either). **The population and the D56 TRAJECTORY CURVES key off the same resolved family and move together**, because the curve states what a no-edge percentile looks like within THAT family's null. **FAIL CLOSED, in the catalog's own words:** a declared family with no spawned population yields no population and S3 renders `undefined` (§5.2: *“until it exists, S3 is undefined for those strategies”) — never a fall back to the daily null, which is the mismatch this row exists to remove. **COMPATIBILITY, with its reason:** rows frozen before the key existed (the three Phase-2 dummies, the D64 plants) resolve to `daily`, because D17 forbids re-serializing over a frozen row to add it — the same carve-out `StrategyRegistry` makes under D133, and it keeps the frozen generation judged exactly as it was. `event` is deliberately ABSENT from the vocabulary until Breakout spawns it at 6.13. | Hard rule 9 requires every strategy to be ranked against its TURNOVER-MATCHED population, and D36 makes the cadence family the matching unit — but nothing in the repo mapped a strategy to a family: `strategies.family` is the STRATEGY family (momentum|meanrev|lowvol), a different namespace, and the pipeline hardcoded the daily population for EVERY promotable strategy while `BandInputs` kept a second copy of that predicate under a comment asserting the two must agree — citing a line number that had already moved, which is precisely the relational defect rule 25 exists to stop. DERIVATION was rejected on the evidence rather than on taste: only `ScheduledRebalance` carries an interval, so `Never`, `TargetOrTimeStop` and `ChannelExit` would have no answer, and §6.4 resolves Breakout by SPAWNING a family, which no derivation rule can express — a heuristic silently wrong for three of five exit shapes is worse than a declaration. FROZEN is the point rather than a side effect: a strategy is judged against the null it was ADMITTED against, so moving that null later re-scores its whole history against a different distribution, and the Phase-6 rails already list the population family among the params a change to which is a fork (rule 8) | active |
| **D136** | **THE FORWARD MEMBERSHIP RECONCILE IS TAKEN AGAINST THE FORWARD ROSTER, AND RUNS ONCE PER LAUNCH (v1.9.97; Phase 6 checkpoint 6.4; discharges finding 197).** Three rulings in one row because they are one mechanism. **(1) SCOPE.** The diff is computed against `IIndexMembershipRead.MembersAsOf` — the same resolution the FUNNEL trades, slice-scoped once a snapshot exists (rule 22) and raw as-of before that — never against `index_membership WHERE removed_on IS NULL`. **(2) CADENCE.** The refresh runs ONCE PER LAUNCH, before the catch-up loop, stamped with the CURRENT session, in its own small transaction; never inside a daily write transaction and never under replay. **(3) FAILURE.** A provider outage writes nothing, leaves the stored roster standing and lets the freshness reading go stale; it does not fail the trading day. Two corollaries fall out of (1) and are part of the ruling: an entrant that already holds an open interval gains NO second row (it re-enters through the slice snapshot), and a drop closes the CURRENT spell — the open row with the latest `added_on` — not an arbitrary one. | **The blind read was correct until the Phase-4 historical seed (2026-07-23) co-mingled the full S&P 500 as-of history into the same table; from that day it named a set the forward reconcile is not authoritative for, and ONE cause produced TWO live defects with opposite symptoms.** *Mass eviction:* every open name outside the slice classified as a drop — measured at **402** in the live arena (604 open rows, 503 distinct open ids, a 101-id slice) — so an ordinary quiet refresh would stamp `removed_on = today` across the S&P 500 roster. The count-sanity gate cannot catch it: it bounds the two SOURCES, never the store. *Invisible adds,* and the worse of the two because it is silent and re-seeding does NOT repair it: a genuine new S&P 100 entrant is ALREADY open as an S&P 500 member, so it never appeared in the add set, never reached `MaintainSliceSnapshot`, and was filtered out of the forward universe permanently — the exact “removals applied, adds vanished” asymmetry that helper exists to prevent, reintroduced beneath it. **THE RECORDED RATIONALE FOR THE TRAP WAS ALSO WRONG AND IS CORRECTED HERE:** it does NOT corrupt replay (reads are half-open `[added_on, removed_on)` and the replay window ends 2025-12-31, so the as-of roster is identical before and after) and it is NOT permanent (`HistoricalMembershipIngestion` is an idempotent upsert that re-opens every stamped row on a re-seed). The real harm is FUTURE-facing: `MembersAsOf` collapses 503 → 101 for every date at or after the stamp, destroying the rule-22 WIDENING TARGET and writing 402 false universe-exit events into as-of state. Cadence is forced by the providers, not chosen: INTEGRATIONS §2/§2b drop `asOfDate` deliberately (*“the same freeze trap”), so a roster answers for NOW and a per-day refresh would stamp each REPLAYED session with today's names and fabricate as-of membership. Stamped with today instead, the write is invisible to every recovered day BY CONSTRUCTION — the same half-open property that made the trap harmless to replay. **AMENDS D47 AND D53 (naming added v1.9.107, finding 411 — it should have been here at v1.9.97).** D47's per-day chain carried "membership refresh (as-of, reconstructed from EODHD historical constituents)" and D53's Stage-2 chain listed `membership` inside the atomic transaction; this row replaced both and named neither, so two live specifications for the same mechanism sat in the register and either could be quoted in good faith. That is precisely the D87/D109 shape rule 25 was written after, and `check-register`'s relational checks CANNOT see it — 3b/3c validate only rows that DECLARE a relationship, so an undeclared amendment is structurally invisible to them. Both clauses are struck and both Status cells updated in the same commit as this naming | active |
| **D137** | **`index_membership_log` CARRIES PER-ROW PROVENANCE (`observed_at`, `source`), AND THE MEMBERSHIP FEED'S HEALTH IS A THREE-STATE READ-MODEL (v1.9.97; Phase 6 checkpoint 6.4; completes finding 197).** Two nullable columns by ALTER ADD COLUMN, **no CHECK on either** — `source` is constrained by convention and a test, so registering a third primary later is an INSERT and not a table rebuild (finding 324's lesson applied FORWARD). **PRE-MIGRATION ROWS ARE NOT BACKFILLED** and that is the load-bearing half: the only value available is `as_of`, and NULL means UNKNOWN. The read-model resolves **three** states — fresh-and-agreeing / stale-or-diverging / **unknown** — with freshness and last-validation as SEPARATE fields (UX-11(a)) and a reason string on every state including the absences (UX-17's rule). **IT IS A VISIBLE STATE, NOT AN ALARM THAT FIRES, and the passivity is recorded rather than implied:** nothing polls it and nothing pages; the producer's fail-closed hold is what stops a bad roster being applied, and this is what makes the resulting state legible. Something that actively fires would be new work with its own row. | A fetch instant and a source are PER-ROW PROVENANCE, not configuration: a config row cannot say when THIS row arrived and cannot carry a source per row — and the source is precisely the fact that changes at the rule-22 widen, when `oef_csv` flips to `ivv_csv` and both write into one table, leaving RUNBOOK's *“inspect index_membership_log diffs”* with no way to tell the two apart. Rule 24's append-only-versioned config pattern fits global operational state; this is not that, and the shape has two precedents here already (`bars.observed_at`/D40, `corporate_actions.observed_at`/D76). Backfilling would MANUFACTURE PROVENANCE that looks authoritative and is false, on rows nobody would re-examine — `as_of` is the run date, the exact number finding 197 exists to distinguish a fetch instant from. The third state follows directly: with NULL rows in the store a two-state model renders them STALE, and an operator opens the lab on day one and chases a divergence that does not exist. Unprovenanced ≠ old. Also closes a doc-vs-doc conflict — MASTER §14 has listed `index_membership_log (source, ...)` since v6 while SCHEMA's DDL had no such column; resolved in SCHEMA's favour by adding what MASTER already promised | active |
| **D138** | **S6 JUDGES AGAINST THE POPULATION BAND, NOT AGAINST ZERO: the anti-predictive arm requires the strategy to be BELOW its cost-matched band (v1.9.98; Phase 6 checkpoint 6.5; the finding-280 remedy).** The band is consulted BEFORE the negative-t arm, and that ordering is the whole change: the threshold and the sustain rule are untouched, and the arm gains the precondition the specification always stated. `insideCentralBand` becomes a three-way `BandPosition` (below/inside/above) because a bool cannot express the distinction the remedy turns on. **The harness now DRIVES the real rule** — `MonitorRecompute.RecomputeS6` calls `MonitorSignals.S6` instead of keeping a second copy of its branch order, and `MonitorSignals.S6` takes its threshold and sustain bar as overridable arguments so a change can be scored THROUGH the rule rather than around it. **CONSEQUENCE for finding 340, one level deeper:** the rule now needs band POSITION, not membership, so the contribution-token recovery path can no longer serve an S6 rule change at all — a stored `elevated_neg_alpha` row was written by a rule that returned before consulting the band and records nothing about which side it was on. Refused out loud, not guessed. | **THE DEFECT, in one sentence:** on the large majority of evaluations a D64 no-edge plant is simultaneously INSIDE its own cost-matched population's central 50 % band — the band is percentiles of the SAME cost-on family, so the plant sits at its median by construction — and BELOW ZERO on the t-stat; the early return made the second win before the first was evaluated, and the D63-conformant verdict was computed on the same call and discarded. It is not an edge case: a plant is a 40-name daily-churn COST-PAYING population member measured against a near-cost-free buy-and-hold benchmark, and that family's documented drag is 21.9 %/yr, which over 63 sessions is a mean shift of roughly —1.7 to —2.7 t-units — so P(t < —1) per evaluation is about 0.76—0.96, not the ~0.16 the code comment claimed. Everything fired at the arithmetic minimum BECAUSE that probability is near one; the fixed window and the 21-session grid are second-order symptoms. **THE REMEDY IS CONFORMANCE, NOT INVENTION** — §3 already required *“net alpha trend vs the strategy's own history AND VS ITS POPULATION BAND”* and D63 already stated *“an edgeless strategy sits at the MEDIAN of its band — an edgeless strategy is never *below* a cost-matched band”*. Below the band is what anti-predictive MEANS; below zero is what paying costs means, and the two were conflated. **WHY NO THRESHOLD COULD HAVE WORKED, and this argument cites no metric moving (C1):** the anti plant's PLANTED signal is —2 %/yr against a common-mode drag of —21.9 %/yr, a ratio of about 1:11, giving ~0.19 of expected t-separation against a noise SD of 1.0. No cut point on a statistic whose signal is a tenth of its shared offset can separate anti-predictive from no-edge — which is why the v1.9.73/75 measurements moved 15,379 status rows and ZERO no-edge outcomes, and why all three recompute reports record a separation of 0.00. That is the established sense in which BUILD_AND_PROMPTS' *“the remedy is behavioural, not a threshold”* is a result rather than an opinion | active |
| **D139** | **A CALIBRATION RUN THAT SIMULATES ZERO SESSIONS IS REFUSED, AND A CONFIRMATION SLICE RUNS IN A DESTROYED SCRATCH ARENA (v1.9.98; Phase 6 checkpoint 6.5).** `replay-calibrate` fails closed when every session skipped as already-committed, archiving nothing; the documented RESUME path (verification + freeze on a finished replay) is OPT-IN and must say so. D117 clause 2's confirmation slice runs against a BYTE COPY of the arena under a throwaway id, `--reset --report-only`, destroyed at the end, and **the scratch id never appears in any committed artefact**. | **The refusal:** a run that simulated nothing is not a fast success. Continuing archives a report built entirely from the previous generation's rows under the CURRENT rules — it reads exactly like a confirmation slice and confirms nothing, which is a false negative WITH A FILENAME, and worse than no report because it is citable. This is the trap the slice itself walks into: the obvious command against the arena holding the generation skips every session, and the only flag forcing re-simulation is `--reset`, which deletes the generation — so the operator's two obvious moves were a fake pass and a destroyed sign-off. **The scratch arena and rule 23/D71:** D71 forbids calibration being COPIED BETWEEN ARENAS because a curve is a property of its pool, horizon and noise, so a curve that travels acquires false provenance. A scratch arena that is a byte copy, read from and then DESTROYED, is not a second arena acquiring a curve — NOTHING SURVIVES TO BE MISTAKEN FOR CALIBRATION. That distinction is the permission; without it recorded, the next reader takes “we copied calibration once” as precedent. Scoped accordingly: read-only against a throwaway id, report-only, destroyed, and never named in a committed file | amended-by D141 |
| **D140** | **AN ARTEFACT IS REPRODUCED UNDER THE ARITHMETIC IT WAS PRODUCED WITH, AND THE HARNESS RESOLVES THAT ITSELF (v1.9.99; Phase 6 checkpoint 6.5; P23).** The effect definition is a PROPERTY OF THE STORED GENERATION, not a specification parameter: `RecomputeHarness` tries each known definition and keeps the one that REPRODUCES the stored promotion set. A spec that supplies one is **REFUSED** (the D139 fail-closed shape) — free, because an unclassifiable parameter was already refused, so deleting the key from the tier map IS the refusal. **AND THE DESCRIPTION BECOMES THE CHECK'S RESULT:** the run's self-description no longer states the arithmetic independently of the comparison; it reports the outcome (*“reproduces 144/144 stored promotions”), and when nothing reproduces it says **NOT RESOLVED** and disclaims the run as a parity claim rather than rounding to the nearest candidate. **NO MIGRATION:** nothing persists a generation's definition and nothing needs to — the arithmetic an artefact was produced with is the one that reproduces it, so it is DERIVABLE rather than stored, on the same principle that keeps `index_membership_log.observed_at` NULL rather than back-filled (D137): never manufacture provenance. Strictly better than a column, because a column can be wrong whereas a definition that reproduces 144/144 has demonstrated itself. | **THE GENERAL RULE, and it is the reason rather than the mechanism: A LINE MAY STATE A FACT IT VERIFIES, OR STATE THAT IT CANNOT VERIFY IT; WHAT IT MAY NOT DO IS STATE A FACT WHOSE TRUTH IT NEVER EXAMINES.** `GateRecompute` read the definition from the spec and defaulted to `raw_gap` — generation 1's arithmetic — while its own comment said so plainly, and `RecomputeSpec.Describe()` rendered *“generation 1's rules”* from the ABSENCE OF OVERRIDES ALONE, having never looked at the generation being reproduced. So the label could not be wrong in a way it could detect. Run against generation 2 it reported **94 differing promotions** and alpha*(H) **6.56 %/yr against a frozen 6.95 %/yr**, and archived a report that read exactly like evidence — a confident wrong answer with a filename, which is the same failure mode D139 refuses for zero-session calibrations. One flag made all 94 vanish. **THIRD INSTANCE OF ONE CLASS IN ONE CHECKPOINT**, which is what raises it from a patch to a rule: (i) `MonitorRecompute` held its own copy of the S6 rule it exists to score (finding 397); (ii) this; (iii) the harness LABELLED itself *“generation 1's rules”* while pointed at generation 2 — it knew, it said so in its own log line, and nothing acted on it. A harness that describes itself more accurately than it enforces itself is one whose reports cannot be read as evidence | active |
| **D141** | **A CONFIG ROW READ ON A REPRODUCIBLE PATH RESOLVES AS-OF THE WATERMARK — AND "REPLAY" IS NOT SYNONYMOUS WITH "CONVERT IT" (v1.9.100; Phase 6 checkpoint 6.5; P24).** NFR-1 says the same inputs + watermark + seeds produce identical outputs; a config row resolved at RUN TIME is an input that is not under the watermark, so this row changes what reproducibility MEANS. D40 gave read-at-watermark to `bars` and D76 extended it to `corporate_actions`; **D141 extends it IN KIND to `config`.** The classification is **PER CALL PATH, not per site** — a site is on a replay path if its result CAN DIFFER between the original run and a reproduction of it, and `OverfittingMonitor` is reached from both. **THE REMEDY IS PER SITE AND IS NOT ALWAYS A CONVERSION.** At `OverfittingMonitor.Run` the watermark becomes REQUIRED rather than the read converted: the sole production caller (`DailyPipeline.cs:648`) already passes non-null, so making it required DELETES the `ResolveCurrent` branch at compile time — the D139 fail-closed shape, costing nothing, and forcing `FX-RecomputeParity`'s seed to state an instant instead of building its ground truth on the branch being removed. At `DummyRoster.ResolveStartingCash` the read is watermark-bound. **TWO CARVE-OUTS, each with the reason that makes it one rather than an exception.** **CARVE-OUT 1 — PRESENCE-ONLY GUARDS** (`ProposalScoreKeys.cs:52`, `SignalBackfillRunner.cs:216-217`): the returned value is `is null`-tested and DISCARDED, and under rule 24 config is append-only so presence is MONOTONE in version — once pinned, non-null at every later watermark. Resolution mode cannot change the answer, so converting would be motion without meaning. **CARVE-OUT 2 — ROWS THE WRITING CHAIN STAMPS AT WALL-CLOCK TIME** (`CalibrationOrchestrator.cs:194`): as-of resolution CANNOT EXPRESS them. The chain stamps its own rows `ChangedOn = DateTime.UtcNow` (`:175-176`), always LATER than the frozen data watermark, so `ResolveAsOf` would return null on every run, flip `patienceAlreadySet` to false, re-stamp the Appendix-A default 4 over an operator's raise, and break D98's seed-once. The site is REPLAY by the criterion AND MUST STAY LATEST-WINS — **enforced by a test, never by a comment, because a comment saying "conversion forbidden" is exactly the unverified self-description D140 forbids.** **KNOWN LIMIT, STATED RATHER THAN HIDDEN (the D140 second state):** as-of NARROWS the hole and does not close it — a live day's watermark is the 22:00Z fiction while config writes stamp a true instant, so a same-day write landing after the run but before 22:00Z is visible to a reproduction and was not visible to the original. | **THE ENUMERATION IS THE RESULT, and it corrected the list that commissioned it in THREE ways.** P24 named six `ResolveCurrent` callers; two were PHANTOMS matched only in doc comments (`AsOfDetectabilityFloor.cs:22`, `DetectionCurves.cs:11` — both actually call `ResolveAsOf` or nothing), two REAL sites went unnamed (`SignalBackfillRunner`, `CalibrationOrchestrator` — the six named were all in `AlphaLab.Evaluation`, which is the shape of the omission), and the site that MATTERS MOST was structurally invisible to the exercise: **`DummyRoster.cs:68-72` is `ResolveCurrent`'s body written out BY HAND against `db.Config`** — latest-wins, no watermark, no `ConfigReadService` — so a grep for a method name could never find it. That one resolves `Accounts.StartingCash`, the accounts' OPENING CAPITAL: a simulation input upstream of every equity curve, every population comparison and therefore every S6 band. It is normally masked because the accounts already exist, and **`--reset` — which D139's own slice procedure MANDATES — deletes them (`ReplayRunner.cs:361`) and re-opens them through this read**, so the slice's own required flag is what arms it. Measured before the fix rather than assumed: the live `sp500` store holds **exactly ONE version** of that key (v1, `changed_on` 2006-01-03, 100000), so as-of and latest-wins agree TODAY and the binding is behaviourally free — which is why it lands here instead of blocking the slice. **FIFTH INSTANCE OF ONE CLASS**, after 6.4's membership co-mingling, finding 397, D140, and this — but the fifth site is the COUNTER-EXAMPLE and is cited as the pattern to copy: `ScratchStore.cs:37-43` documents this very gap under "KNOWN AS-OF GAPS (documented, not silently accepted)", states what it cannot verify, and is therefore NOT a defect — it is D140's permitted second state, the `RecomputeOrchestrator.cs:258` shape. **Consequences (rule 26): AMENDS D139** — its confirmation-slice procedure gains a MANDATORY THREE-PART PRE-FLIGHT, recorded as a decision rather than as remembered operator care, because each part is a silent failure. **(1) `--watermark` PINNED to generation 2's `2026-07-24T22:00:00Z`.** `ReplayRunner.cs:119` otherwise falls back to `MAX(observed_at)` OF THE COPY, and after `--reset` `GuardSingleGeneration` returns early (its `runs` rows were just deleted), so nothing forces the slice's watermark to equal the generation's. A copy taken after further ingest resolves a LATER watermark, and as-of resolution then lets the monitor see D98 curve rows generation 2's replay could not — S3 switches from the flat anchors to the trajectory and nothing announces it. **(2) The byte copy verified COMPLETE before it is read** — a truncated copy of a ~3.9 GB store produces a plausible report from partial data, the same failure shape D139 exists to refuse. **(3) `Accounts.StartingCash`'s version confirmed unchanged** since the generation opened its accounts — the `--reset` re-open reads it, so a version appended since would re-simulate at different capital. D40/D76 extended in kind; NFR-1's meaning changed. **Also recorded, because measuring it is how it was found:** a `--report-only` run writes EXACTLY ONE config row on a store that has never opened accounts (`Accounts.StartingCash@v1`, the roster bootstrap that finding K exists to make auditable) — the blanket claim "report-only writes no config rows" was false and is retired for the precise one, which `FX_Calibration_ReportOnly_WritesNoConfigRows` now enforces against the WHOLE table rather than three keys | amended-by D144 |
| **D142** | **A CORPORATE ACTION THAT RESTATES A POSITION ALSO RESTATES THAT SECURITY'S PENDING ORDERS, AND A SALE THAT STILL EXCEEDS THE HOLDING IS REFUSED (v1.9.101; Phase 6 checkpoint 6.5a, review remediation PR 1; findings 400-402).** The daily pipeline applies corporate actions and THEN fills the prior session's stored orders (D53 ordering), but `FillPriorOrders` passed `order.Shares` to the broker unchanged — so between the decision at close T and the fill at open T+1 the book could be re-denominated while the order was still written in the old units. **THE DIRECTION MATTERS AND IS THE REASON THIS IS NOT CRITICAL, so a reader who sees only the headline does not re-raise it:** the split itself is EQUITY-NEUTRAL in the position — §13.6's "equity unchanged" holds — and a FORWARD split leaves equity arithmetically correct while silently leaving part of an ExitPolicy close open (a rule-7 exposure defect, invisible, uncatchable by any guard). It stops being equity-neutral only on a REVERSE split, where the restated book falls below the stale sell and `PostFill`'s `newShares <= 1e-9` branch absorbs the negative remainder as a clean close: the row is deleted and the full over-large proceeds are credited, so the ledger gains cash that never existed. **THE FIX IS A UNIT CONVERSION, NOT A RECOMPUTE**, and that distinction is the whole justification against `OrderFill`'s stored-snapshot doctrine: that doctrine protects the DECISION (which name, which side, how much VALUE), and a split changes none of those — it changes the name of the unit the quantity is written in while the exchange divides the quote. `100 sh at P` and `100·r sh at P/r` are the same order. A DELTA order rescales by the same factor, provably: `targetShares = targetNotional / price`, the book restates by `r` and the quote by `1/r`, and `targetNotional` is MONEY, which a split leaves alone — so no `TradeReason` switch, which would be dead code that mishandles a reason added later. **RATIOS ARE RESOLVED OVER THE ORDER SET, NOT THE BOOK,** because `CorporateActionApplier` iterates HELD securities: a pending BUY into a name not yet held is invisible to it, and would fill at the restated price for the ordered count, spending `1/r` of the intended notional in breach of D84 at the fill. A ratio is a property of the SECURITY and the window; only a termination is a property of the book. **NOTHING IS WRITTEN BACK:** the restated order is a local copy and the prior session's `stage_json` stays byte-identical, so `reproduce-day` still reproduces (`FR25_D142_ASplitDayWithAPendingOrder_ReproducesByteIdentically`). **TERMINAL EVENTS ARE OUT OF SCOPE BY CONSTRUCTION** — `UnitRestatementRatio` reports no factor for a merger or delist, because no number converts an order into a line that no longer exists; that is finding 402's remedy, not this one. **CONSEQUENCES.** Generation 2 (watermark `2026-07-24T22:00:00Z`) was produced under pre-D142 arithmetic on TEN stored orders across NINE of its 5,031 sessions. **THE EXPOSURE IS STATED IN TWO PARTS, IN THIS ORDER, BECAUSE EITHER ALONE MISLEADS.** (1) The contamination is CONFINED TO THE FUNNEL PATH: exactly THREE of the arena's 403 replay accounts write `decisions` rows at all — `buyhold:cw`, `buyhold:ew`, `threshold:sma50`. (2) WITHIN that path it touched **TWO OF THE THREE EXPOSED ACCOUNTS** — `buyhold:ew` ×8, `threshold:sma50` ×2, `buyhold:cw` (D131's gate opponent) ×0. "Two of 403" is arithmetically true and reads as half a percent; an operator deciding whether to regenerate would conclude immaterial from it, which is the exact class of statement this remediation exists to remove. **PLANT IMMUNITY IS STRUCTURAL, NOT OBSERVED:** the 400 D64 plants are equity-only fixtures — `DailyPipeline` skips them before the funnel (`PlantCohorts.IsPlantId`), so they hold no book, carry no pending order, and **cannot be reached by this defect by construction**, at any watermark and in any window. That is a property a reader can verify without re-running the probe, and it is what licenses the frozen D56 curves, the D121/D122 detectable band, P23's recompute parity and the D117 confirmation slice all standing unchanged. All ten hits are FORWARD splits, so no cash was fabricated and the oversell guard is provably inert on every stored day — which is what licensed shipping the guard in the same PR. **THE DIVERGENCE IS NOT CONFINED TO THE HIT SESSION — MEASURED, NOT REASONED (D142-C3).** Nine of the ten are deltas/trims/opens; ONE is a genuine whole-line close (`threshold:sma50`, security 481, 2018-11-07) where a 2-for-1 left **exactly 50 % of a line the ExitPolicy had closed still on the book**. In ALL TEN cases the affected name is not traded again for **+21 sessions**, so the book deviation persists across 21 sessions rather than ending with the fill. Whether the next rebalance fully re-converges is **NOT ESTABLISHED** and is not claimed. **The generation STANDS, and the constraint that follows is: regenerate the window WHOLE or not at all** — a partial re-run after this row is what would create the mixed-arithmetic generation D95 forbids. **That constraint is PROSE THAT NOTHING ENFORCES, and the dangerous path is the DEFAULT one** (`ReplayRunner` skips same-watermark committed days, so `--reset` is the opt-in): recorded as **finding 403** with its own named PR in the 6.5a queue, deliberately NOT built here. `reproduce-day` on any of those nine dates will legitimately DIVERGE from here; that is the defect's fingerprint, not an NFR-1 failure. Evidence and method: `docs/calibration/2026-08-11-d142-pending-order-corporate-action-probe.md`. Amends nothing; extends §13.6. No schema change, no migration, no config key | A ledger that credits cash which never existed is not a ledger, and forward paper P&L is the only evidence that judges strategies (rule 1). Rule 10 requires an impossible book state to stop the run rather than pass through it; absorbing a negative remainder as a clean close defaults the impossible into the plausible, and in the flattering direction | active |
| **D143** | **A §13.6 EVENT THAT ENDS A LINE CANCELS THAT SECURITY'S PENDING ORDERS; IT DOES NOT LEAVE THEM TO FILL INTO A POSITION THAT NO LONGER EXISTS (v1.9.102; Phase 6 checkpoint 6.5a, review remediation PR 2; findings 404-405; the terminal-event half of D142).** D142 restates a pending order when a corporate action RE-DENOMINATES the position, and deliberately reports NO factor for a termination — there is no number that converts an order into a line that has ceased to exist. This row supplies the other half. A delist force-exit, a cash merger, or a conversion into the acquirer resolves the line in step (a); the stored order for that security is then **CANCELLED with a logged reason** in step (b), because **the §13.6 event is itself the disposal** and the funnel's order is void rather than merely unfillable. **BOTH ARMS WERE DEFECTS AND THE QUIETER ONE WAS WORSE.** A stale **SELL** reached `PostFill`'s `existing is null` throw and rolled the WHOLE day back — every account, every population row, the regime label — for an ordinary corporate event, with a message blaming the funnel for something the funnel did correctly. A stale **BUY** had no existence check at all, legitimately so (buying into an unheld name is how a position opens), and therefore **FABRICATED a position in a delisted or merged-out security**, which then froze forever on the next session's stoppage check: silent, permanent, and indistinguishable from a real holding. A cash merger failed only ACCIDENTALLY safe, because a merged-out target usually has no bar and the broker rejects on a missing price — `FR9_D143_ACashMergerOnADayWithAPendingSell_CancelsTheOrder` gives it a bar precisely to show that the safety was the absence of data rather than a rule. **THE CLASSIFIER IS KEYED ON THE EFFECT, AND ITS FALSE CASES ARE LISTED EXPLICITLY.** `CorporateActionLedger.TerminatesPosition` switches over the closed `CorporateActionEffect` hierarchy with no discard arm, so a new effect fails to compile until someone decides whether it ends a line, rather than inheriting "no" and letting a stale order fill into nothing. It is asked of the EFFECT, not the action, because a termination is a property of what happened to THIS account's book — the mirror of D142's ratio, which is a property of the security and is resolved over the day's orders instead. **A SPIN-OFF IS NOT A TERMINATION**, and it is the case most likely to be flipped by someone reasoning from "a new security appeared": the parent keeps its share count, only basis moves, so its pending orders stand — the same distinction `StoppageFreezeReason` already draws when it refuses a spin-off as an explanation for a missing bar. **WHAT THIS DOES NOT DO, STATED RATHER THAN IMPLIED:** the cancellation is surfaced as a log line and asserted by fixtures; it writes NO durable row. `capacity_rejections` does not fit (its PK is one row per account-security-day and its three columns carry D43 participation semantics, not a reason), and inventing a table is a schema change this remediation does not need. The Phase-7 Risk screen is where a durable surface belongs, and it is recorded there rather than claimed here. **CONSEQUENCES.** Step 0's probe for D142 reported **class B = 0** at generation 2's watermark, so no stored replay day contains this geometry: the frozen generation is unaffected, the D56 curves and the D117 slice are untouched, and no regeneration question arises. Forward, this converts a day-killing throw into a committed day and removes a silent fabrication. Amends nothing; extends §13.6 alongside D142. No schema change, no migration, no config key | A corporate event that ends a position is the most ordinary thing that can happen to a holding, and the two ways this went wrong are the two ways rule 10 says a system must not fail: one stopped the entire day for an event it should absorb, and the other silently defaulted an impossible state — a holding in a security that no longer trades — into a plausible-looking row that would have sat in the book indefinitely | active |
| **D144** | **A REPLAY GENERATION RECORDS THE LEDGER ARITHMETIC THAT PRODUCED IT, AND A BUILD WITH DIFFERENT ARITHMETIC REFUSES TO ADD SESSIONS TO IT WITHOUT `--reset` (v1.9.103; Phase 6 checkpoint 6.5a, review remediation PR 3; finding 406; enforces D142's Consequences).** D142 recorded that generation 2 must be regenerated **whole or not at all**, because a partial re-run mixes pre- and post-D142 arithmetic inside one generation — what D95's one-generation-per-arena rule exists to prevent. **THAT CONSTRAINT WAS PROSE THAT NOTHING EXAMINED, AND THE DANGEROUS PATH WAS THE DEFAULT.** `ReplayRunner` skips same-watermark committed days — which is exactly what makes a run resumable — so a plain `replay-calibrate` against this arena IS the partial re-run, while `--reset`, the safe path, is the opt-in. A safeguard recorded where nothing can check it is the defect class D140 names, and it was sitting inside the decision written to apply D140; filing it as finding 403 the moment it was noticed, and building it here, is the correction. **THE MECHANISM.** `LedgerArithmetic.Version` is a code-side constant (currently `la-2`) identifying the arithmetic that turns a day's decisions into stored `trades`, `positions`, `cash_events` and `equity_curve` rows; `ReplayRunner` stamps it into the append-only versioned config row `Replay.LedgerArithmeticVersion` at the moment a generation is created, and refuses before simulating anything when a COMMITTED generation's stamp differs from the current constant. **WHAT COUNTS AS A BUMP, stated so the constant does not become decorative through overuse or uselessly stale through underuse:** bump it when a decision changes the VALUES a day writes given identical inputs; do NOT bump it for reporting, read-models, monitor thresholds, or anything recomputed from stored rows — those are re-derivable, and a mixed generation of them is not a mixed generation of evidence. **AN UNSTAMPED GENERATION IS REFUSED, NOT ASSUMED COMPATIBLE**, and that is the case that matters: sp500 generation 2 predates the marker entirely, so its rows carry none. "No marker" means "produced under arithmetic this build cannot identify", and treating it as a match would silently permit the exact mix — fail closed, D139's pattern. **`--reset` IS EXEMPT AND MUST BE:** it deletes the generation first, so there is nothing to mix with, and re-simulating the whole window is the sanctioned remedy rather than the hazard; a guard that also blocked `--reset` would convert a mixing hazard into a dead end, which `D144_ResetIsExempt_AndRestampsTheFreshGeneration` exists to catch. The vintage read is `ResolveCurrent`-shaped and that is CORRECT rather than a D141 breach: it is an operator pre-flight about the store's present state, not an input to any number a reproduction must reproduce — the same reasoning as D141's presence-only carve-out. **CONSEQUENCES.** The practical effect on sp500 today: generation 2 can be regenerated with `--reset` or left alone, and nothing else. That is D142's constraint made operative rather than advisory, and it is a deliberate narrowing of what the operator can do by accident. Every future arithmetic change inherits the protection instead of needing its own remembered caveat. One new config key (`Replay.LedgerArithmeticVersion`, DB-row only, never in appsettings), recorded in CONFIG_REFERENCE. No schema change and no migration — `config` is an existing table and the row is an ordinary append under rule 24. **AMENDS D141's REPORT-ONLY COUNT.** D141 narrowed "a `--report-only` run writes no config rows" to the precise "EXACTLY ONE — `Accounts.StartingCash@v1`", enforced against the whole table. The vintage stamp makes it TWO, and it is admitted on D141's own stated reasoning rather than by exception: that clause permits the roster bootstrap because the write is PROVENANCE the store needs to be auditable, and explicitly rejects "suppressing it under a flag the roster cannot see" — `ReplayRunner` likewise cannot see `reportOnly`, and the stamp likewise records what produced the rows rather than governing a verdict. `FX_Calibration_ReportOnly_WritesNoConfigRows` becomes a two-key ALLOW-LIST rather than a count, so a third key still fails, and D141's Status is updated in this commit (rule 25) instead of the count being quietly outgrown | A generation is ~5,000 committed sessions that took hours to produce and cannot be reconstructed from anything else in the store, and the failure it is exposed to is silent: nothing about a mixed generation looks wrong, the curves still build, the report still renders, and the arithmetic behind any given row is unknowable after the fact. The asymmetry decides it — refusing costs one flag on the rare legitimate regeneration, and not refusing costs the evidence base with no signal that anything happened | active |
| **D145** | **STAGE 1's ALREADY-GATED FILTER IS SEVERITY-AWARE: A WARNING ON A RE-GATED DATE IS STILL DROPPED, A REJECT SURVIVES WHEN THE FETCHED BAR IS A CORRECTION (v1.9.104; Phase 6 checkpoint 6.5a, review remediation PR 4; finding 407; scopes P7's rationale rather than overturning it).** `Stage1Fetch.Gate` filtered quality flags by DATE ONLY — anything at or before `LastStoredDate` was dropped — while the unfiltered bars were ingested regardless. So a corrupt vendor CORRECTION of an already-stored date had its REJECT discarded, the day did not fail closed, and the bar was appended as a new version that **rule 3 then makes permanent**: unremovable, latest-visible forever, and feeding ADV, the participation cap, realized vol and the population band every verdict is judged against. **P7'S RATIONALE IS PRESERVED, NOT OVERTURNED, AND THAT IS THE POINT OF THE SPLIT.** The fetch window carries a 40-session context tail, so ~40 stored dates are re-gated every day; re-emitting their WARNINGS is duplicate-flag spam and re-fetching 20 years to gate the backlog would hammer the API budget. Both concerns are about warnings and cost. Neither is an argument for suppressing a reject, which means the bar cannot be trusted or priced and must stop the day (rule 10) — the filter simply never distinguished them. **WHY THE RULE IS NOT "KEEP EVERY REJECT", and this is the clause that keeps the arena runnable:** P7's own open backlog is ~488k backfilled bars the gate never vetted, so a STORED bar can itself be gate-rejectable. An unchanged re-fetch of one would abort the day, and abort it again on every subsequent run — a permanent wedge with no operator remedy. The distinguishing question is the one the WRITER asks: does the fetched bar DIFFER from the stored latest-visible one? An identical re-fetch is an idempotent no-op with nothing new to refuse; a differing one is a correction about to be appended, and that is exactly what must fail closed. A reject on a date with NO stored bar also survives — a gap being filled is new data, and the fail-closed direction is to refuse it rather than drop a corrupt bar into a hole. **ONE DEFINITION OF "DIFFERS".** The comparison is `BarIngestionService.Differs`, promoted from private to public for this purpose rather than restated: if the gate's notion of a correction drifted from the writer's, the two would disagree in the worst way — the gate passing a bar ingestion then appends, or aborting a day over a value nothing would have written. **THE STORED SERIES IS RESOLVED BY THE ORCHESTRATOR**, exactly as `PriorActions` and `LastStoredDate` already are, because `Stage1Fetch` carries no `DbContext` (finding BB) and must not acquire one — "Stage 1 wrote nothing" is checkable only while it structurally cannot write. **CONSEQUENCES.** No stored row changes and no generation is affected: this alters which DAYS ABORT going forward, never any value already written. Forward, a corrupt correction now stops the run before the run row is opened (FR-29) instead of entering the corpus permanently. P7 remains open for its actual subject — the one-time gate sweep of the never-gated backfill, which still needs a `gated_through` marker and a D-number of its own; D145 narrows what P7's daily-filter clause was ever claiming, and does not discharge it. No schema change, no migration, no config key | Hard rule 3 makes a bar unremovable, so the ONLY moment a corrupt value can be refused is before it is written; a filter that drops the refusal at exactly that moment converts the append-only guarantee from a protection into a trap. The asymmetry decides the shape: refusing a differing bar costs a stopped day the operator can investigate, while dropping it costs a permanently wrong series nothing will ever flag | active |
| **D146** | **A GAP EQUAL TO ITS MDE IS INSIDE IT, AND AN UNESTIMABLE MDE ADJUDICATES NOTHING: THE RULE-6 TEST IS `<=` AND GUARDS `NaN` ON BOTH SIDES (v1.9.105; Phase 6 checkpoint 6.5a, review remediation PR 5; finding 408).** `PromotionGate.Decide` is the ONE place hard rule 6's "inside the MDE ⇒ TooEarly" lives, and it had two defects on the same line pair. **(1) THE BOUNDARY.** The test was a strict `<`, so a gap EXACTLY equal to the smallest detectable effect fell through to the sign test. The case that makes this more than a rounding nicety is the DEGENERATE PAIR: a strategy that has never traded has a flat equity curve, so the paired estimator returns gap 0 AND MDE 0 — `0 < 0` is false, `0 > 0` is false, and the verdict was **Refused**. An absence of evidence rendered as a directional finding that the strategy is WORSE than the benchmark, which is the exact inversion the MDE rail exists to prevent. **(2) THE ASYMMETRIC GUARD.** The pre-flight checked `IsNaN` on the GAP and `IsInfinity` on the MDE, never `IsNaN` on the MDE; a NaN MDE therefore fell through (`Math.Abs(gap) <= NaN` is false) and the sign test decided a verdict off an unestimable bound. An MDE that is not a number is not a threshold. **WHY THIS REACHED FURTHER THAN THE GATE.** `SeparationState` treats ANY non-TooEarly verdict as decisive, so the spurious `Refused` also un-dimmed the alpha cell (UX-1), sorted the row into below-or-flagged, and **suppressed the `IndistinguishableFromRandom` chip for precisely the strategy most indistinguishable from random** — a rule-21 inversion produced by a comparison operator. `D146_ATooEarlyVerdictKeepsTheChip_WhileARefusedOneSuppressesIt` pins that mechanism from both sides, so the fix is shown to restore the chip rather than merely relabel a verdict nobody reads. **WHAT IS DELIBERATELY NOT CHANGED, and it is pinned by a fixture so the next reader does not "finish the job":** a REAL gap against an MDE of exactly 0 still decides. That combination arises from a degenerate BENCHMARK (the raw-gap fallback in `PairedEffect`), and whether an MDE of 0 should be read as infinite precision or as an unusable estimate is a separate design question about what the estimator MEANS — recorded as a proposal, not settled here. D146 corrects the boundary and the guard; it does not redefine MDE 0. **CONSEQUENCES.** No stored row changes: `power_reports.verdict` values already written are untouched, and the frozen generation is unaffected — this changes what the gate DECIDES from here. The affected population is narrow and, at this build state, empty in the forward arena (no forward day has run); it becomes reachable the moment a registered strategy holds no position on its evaluation cadence. Amends nothing — D31/D48 state the rule this implements, and the code was wrong about the rule rather than the rule being wrong. No schema change, no migration, no config key | Rule 6 exists so that "we cannot tell" never renders as "we can", and the failure it was written against is exactly the one the strict `<` produced: not a wrong number, but a CONFIDENT one where there was no evidence at all. A gate that converts silence into a directional verdict is worse than a gate that says nothing, because the verdict is citable and the silence is not | active |
| **D147** | **A FROZEN POSITION IS NOT SIZED, NOT TRADED, AND A FILL NEVER CLEARS THE FREEZE: "HELD UNTOUCHED" BECOMES TRUE RATHER THAN INTENDED (v1.9.106; Phase 6 checkpoint 6.5a, review remediation PR 6; findings 409-410; bumps the D144 arithmetic to `la-3`).** §13.6 / rule 10 freeze a position when an unmapped corporate action or a bar stoppage makes it unpriceable; its valuation is pinned and the ExitPolicy is never consulted on it. The rail was defeated in two independent places, and **neither looked wrong on its own** — which is why both survived. **(1) STAGE 4 PUT A FROZEN NAME IN `Holds`.** `PortfolioPlan.ToSize` unions `Holds` with `Opens` on a WHOLE-BOOK rebalance, so a frozen position was re-sized and TRADED on every rebalance day — priced at the very number the freeze had declared untrustworthy — directly beneath a comment promising it was "held untouched". It now has its OWN list: being a separate collection is what makes the promise structural rather than documentary, and `ToSize` cannot reach it by construction. **(2) THE BUY LEG SILENTLY UNFROZE IT.** `PostFill` rebuilt the row from four fields on a buy, dropping `Frozen` and `FrozenReason`, so an ordinary fill performed **D55's audited unfreeze with no admin action, no `admin_actions` row and no operator**. The sell leg had always used `existing with { … }` and preserved them, so the asymmetry was plainly unintentional; both legs now go through `PositionMath`, expressed the same way, which is what stops it recurring. A fill is not an operator decision and cannot resolve a freeze. **WHY NOTHING CAUGHT IT: there were NO `PortfolioPlanner` tests at all.** Not thin ones — none. The comment and the behaviour drifted apart with no failing test between them, and the defect was invisible on ordinary days because `OpensOnly` sizes only `Opens`; it needed a 21st session to appear. **SNAPSHOT COMPATIBILITY IS DELIBERATE.** `Stage4Snapshot` gains `Frozen` as an OPTIONAL, defaulted member rather than a required one, and `snapshot_version` is NOT bumped: `DecisionSnapshot.FromJson` refuses any version but the current, so a bump would make every stored `ds-1.0` row unreadable at the moment of deploy — including the row a run reads to fill YESTERDAY's orders, and every row `reproduce-day` replays. That failure would be far larger than the one being fixed. Additive-and-tolerant is the same discipline D133 applied to `config_json`. **CONSEQUENCES.** This is a **LEDGER ARITHMETIC CHANGE**: a rebalance day's stored trades differ given identical inputs, because a frozen name that used to be sized no longer is. `LedgerArithmetic.Version` therefore bumps **`la-2` → `la-3`**, which is the first bump driven by a decision that is not about corporate actions and is exactly the case D144's rule was written for. Generation 2 is unaffected in its stored rows and remains subject to D144's whole-or-nothing constraint; a partial re-run against it was already refused (it is unstamped) and now would be refused on the version too. No schema change, no migration, no config key | A freeze exists to say "we do not trust this price", so continuing to trade the position at that price is not a lesser version of the rule — it is the rule inverted, and it happens on a schedule. The unfreeze half is worse in kind: D55 makes manual intervention typed, confirmed and audited precisely so that state changes of this weight leave a record, and a fill that clears the flag produces the same state change with no record at all | active |
| **D148** | **"SUSTAINED" MEANS SUSTAINED ON BOTH ARMS AND IN BOTH PLACES: THE SEPARATION STATE READS THE MONITOR'S OWN EDGE BAR, AND S3's HEALTHY ARM REQUIRES A STREAK (v1.9.107; Phase 6 checkpoint 6.5a, review remediation PR 7; findings 411—413).** MASTER §20.8 defines the D63 separation state in three clauses — `distinguishable`: "the percentile path has been **SUSTAINED** above `P_edge(t)` (**mirroring S3 Healthy**), or a gate verdict other than TooEarly has been earned"; `emerging`: "the path is **SUSTAINED** outside the population's central band"; `none`: "the path remains inside". `SeparationState` implemented all three as SINGLE-POINT tests on `s3Path[^1]`, and compared against a literal `95.0`. **THE LITERAL WAS NOT A ROUNDING OF THE CURVE, IT WAS A DIFFERENT RULE — AND THE CONSERVATIVE ONE.** `P_edge(t)` is calibrated and track-length-dependent; sp500 generation 2's frozen curve runs **71.0 at t=252** rising to ~83, so the doc-conformant bar ADMITS MORE paths than 95 did. Fixing the bar alone would therefore have RAISED the false-positive rate, which is why the sustain requirement is not a separable improvement but the other half of the same correction. **THE BAR IS READ FROM THE ROW THE MONITOR WROTE** (`threshold_json.p_edge_at`, or `healthy_anchor` pre-calibration) rather than re-resolved from config: the monitor and the read-model previously judged the same strategy on the same day against different numbers — the monitor against the frozen `Monitor.S3.PEdgeCurve.{family}`, the read-model against 95 — and reading the persisted bar makes disagreement impossible rather than unlikely. §20.8's "mirroring S3 Healthy" also settles WHICH sustain applies: the monitor's own `sustain_evals` from the same row, deliberately not a second knob that could drift from the signal it is defined to mirror. **THE SECOND ARM WAS FOUND BY THE ENUMERATION, NOT BY THE FINDING.** The review named only the `distinguishable` arm; `emerging` was a single-point test too. It surfaced only because this PR was required to enumerate every path that can set or clear the chip before claiming the rail settled — the enumeration is committed at `docs/calibration/2026-08-11-separation-state-path-enumeration.md`, states its own closure argument, and records what it CANNOT close (the consuming side has no D91-style reflection closure; a proposal, not a claim). **S3's HEALTHY ARM (the mirrored signal) GETS ITS STREAK TOO.** OVERFITTING_MONITOR §3 states the band as "Suspect below `P_noise(t)` **sustained** · Healthy above `P_edge(t)` **sustained** · Warning between"; only the Suspect arm had it. The asymmetry ran the flattering way — one lucky print above the edge curve read Healthy, one unlucky print below the noise curve correctly read only Warning — and a signal whose adverse arm demands persistence while its favourable arm does not is biased by construction. New contribution token `approaching_edge` (above the curve, not yet sustained) with `ContinuesAboveEdgeStreak`; `MonitorRecompute` threads the streak in lockstep, which the compiler enforced rather than a reviewer noticing. **CONSEQUENCES.** **NO ledger-arithmetic bump (D144), and the reason is D144's own rule:** this changes monitor thresholds and a read-model, never the values a day writes to `trades`, `positions`, `cash_events` or `equity_curve`. `LedgerArithmetic.Version` stays `la-3`. **THE PARITY DIVERGENCE IS ZERO, MEASURED — AND THE FIRST DRAFT OF THIS ROW CLAIMED OTHERWISE.** It asserted that `replay-recompute --verify-parity` "will now report differences" on sessions whose stored contribution was an unsustained `above_edge`. That was inferred from the mechanism and never checked, which is the D140 shape — in the row written to close a D140-shaped defect. Bounded rather than declared, at watermark `2026-07-24T22:00:00Z`: **generation 2 holds 95,769 replay S3 rows and ALL 95,769 are FLAT-ANCHOR form** (`healthy_anchor`); **zero** carry `p_edge_at`, and **zero** carry the `above_edge` token at all. `S3Trajectory` — the only method this row changes on the monitor side — **never ran on this generation**, and the reason is structural rather than incidental: generation 2 IS the calibration run, and under Change 1's two-pass design the D56 curves do not exist while it runs (they are its OUTPUT), so the monitor was on flat anchors throughout. The `SeparationState` half is inert for a second, independent reason: **zero forward S3 rows exist** because no forward day has run. Confirmed empirically as well as structurally — a bare parity run reproduces exactly, **0 differing of 95,600 `overfitting_status`, 0 of 144 promotions, 0 of 18,533 would-reverts** (`docs/calibration/sp500/2026-08-11-recompute-parity.md`). **THE EXPECTED COUNT IS THEREFORE 0, AND IT IS STATED SO THAT A FUTURE NON-ZERO IS A REAL FAILURE** rather than something a reader must decide is "probably D148". **BUT THE ZERO IS A PROPERTY OF GENERATION 2, NOT OF D148, AND THE CONDITION TRAVELS WITH THE NUMBER.** It holds *because* the flat-anchor arm ran on all 95,769 rows and the trajectory arm on none — which is true of a generation that IS the calibration run and produces the curves, and false of any generation produced WITH the curves already present. **A future generation will carry trajectory rows, and a later S3 rule change checked against it WILL diverge.** Read without the condition, "parity impact: 0" becomes "D148 is parity-neutral", and the next person to change S3 and see divergence against generation 3 has no way to separate expected from real — the same diluted signal, deferred one generation rather than avoided. **AND PARITY'S ZERO LICENSES LESS THAN IT APPEARS TO. BOTH HALVES OF THIS ROW MODIFY CODE THAT HAS NEVER EXECUTED:** the trajectory arm has zero rows in replay and there are zero forward S3 rows, so it has never run anywhere; `SeparationState` is inert for the independent reason that no forward day has run. So the zero establishes exactly one thing — **the change perturbs nothing already stored** — and it is **NOT evidence that the new logic is correct**, because the path it changes is dormant. The fixtures are the only evidence of correctness this row has. First real execution: the **first forward day** for the read-model half, and the **next calibration generation** for the monitor half. **HARNESS POSITION, STATED BECAUSE THIS IS THE SECOND PARAMETER TO REACH IT.** `MonitorRecompute` defaults every monitor parameter to the CURRENT compile-time constant rather than resolving what the generation was produced under — and for monitor rules that is DELIBERATE AND CORRECT: the harness exists to answer "what would this generation look like under a candidate rule?", so re-deriving under current code IS the feature, and `--verify-parity` is the special case where the candidate rule equals current code. P23 reached the opposite conclusion for the ALPHA DEFINITION because that is a property of the stored generation, not a candidate knob — the two are different questions and the harness is right to treat them differently. **THE GAP, NAMED:** a parity result is therefore only interpretable against a stated code version, and nothing in the artefact records which one. The count above is the first such statement. Same shape as P25 from the calibrated-row side. **A SECOND GAP, RECORDED WITH ITS TRIGGER RATHER THAN BUILT:** `RecomputeParameters.S3SustainEvals` is ONE value passed to BOTH of `S3Trajectory`'s arms, so pinning it to reproduce the pre-D148 Healthy behaviour also collapses the Suspect sustain — the harness **cannot isolate a change to one arm of S3**, and an attempted pinned reproduction would deviate in a second, unrelated way. A per-arm override is **deliberately NOT added now**: it exists to attribute a divergence, there is no divergence to attribute (see the zero above), and building an instrument with nothing to measure is how a knob arrives untested and then gets trusted. **IT BECOMES BLOCKING the first time an S3 rule change must be attributed against a generation whose trajectory arm actually ran** — which is the same generation the condition above says will diverge, so the two land together. Tripwire recorded as **P26**, the P25 shape. No stored row is altered. No schema change, no migration, no config key | A rail that says "sustained" in the specification and tests one point in the code is not a strict implementation of a loose rule — it is a different rule wearing the same name, and here it was different in the direction that claims separation on a single lucky print. The chip it governs is D63's flagship product: the lab's honest, fast, most common answer. An arm of it that fires on one excursion turns "indistinguishable from luck" into a statement luck itself can switch off | active |
| **D149** | **FORWARD VISIBILITY IS ONE SEAM, NOT A PREDICATE EACH READ-MODEL RE-EXPRESSES (v1.9.108; Phase 6 checkpoint 6.5a, review remediation PR 8; finding 415).** Hard rule 1 quarantines replay from every forward view and D64 plants are replay-only fixtures (FR-36), so no forward screen lists one. The rule was hand-rolled per call site, and one site forgot it: `StrategiesReadModelBuilder.BuildDetail` served a plant as a full forward strategy card while its sibling `Build` — **eight lines above** — carried the filter with the rule cited in its own comment. **THE FIX IS THE SEAM, AND THE REASON IS THE FAILURE MODE.** A copied predicate fails by SILENT OMISSION: nothing throws, no test goes red, a screen simply shows a row it should not, and the next builder omits it the same way. `ForwardVisibility` now owns `IsForwardVisible` / `ForwardStrategies`, and all three forward readers route through it (`Build`, `BuildDetail`, `CohortMaturationBuilder`) — a fourth reader has to go out of its way to be wrong. **THE INVERSE IS NAMED, NOT EXCEPTED:** `ReplayReadModelBuilder` SELECTS plants because the replay screen's subject IS the quarantined generation, so it calls `IsReplayFixture` rather than re-testing the raw `plant:` prefix — the other side of the rule, visibly so. **LATENT, NOT LIVE, AND THE ROW SAYS WHICH:** `BuildDetail` resolves the forward stamp first and returns `NoRunYet` when there is none, and no forward day has run, so every id returns not-found today — plant or not. It ARMS at go-live, which is why it is fixed now rather than filed as urgent. Both the original review and its two verifiers called this live; the store said otherwise, and the correction is recorded here so the severity is not re-inflated by a future reader. **CONSEQUENCES.** No stored row changes; no generation is affected; nothing is recomputed. The behaviour change is confined to a forward endpoint that cannot currently return anything, so this is the rare fix whose entire value is prospective. **THERE WERE NO `BuildDetail` TESTS AT ALL** — the same absence that let the §13.6 freeze rail drift (D147) and, before it, the funnel's planner. `D149_APlantIsAbsentFromTheLeaderboard_AND_NotFoundOnItsDetailCard` asserts BOTH call sites in one fixture, because the defect was exactly that one of two adjacent sites was checked and the other was not, and it seeds a committed run precisely so the assertion is not satisfied by the short-circuit. No schema change, no migration, no config key | A quarantine that is re-implemented per reader is not a quarantine, it is a habit — and habits are kept by whoever remembers them. The rule this protects is the first hard rule in the project: forward paper P&L judges strategies, and replay judges only the machinery. A calibration fixture rendered as a forward strategy card is that boundary failing in the direction that shows an operator a number the design says does not exist | active |
